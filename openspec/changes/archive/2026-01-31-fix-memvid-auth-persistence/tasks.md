## 1. Project Initialization

- [x] 1.1 Install `dotenv` in the root project directory
- [ ] 1.2 Add `npm install` instruction to `README.md` if root usage increases

## 2. Script Updates

- [x] 2.1 Update `scripts/mem-recall.js` to load `.env` using `dotenv`
- [x] 2.2 Update `scripts/mem-log.js` to load `.env` using `dotenv`

## 3. Configuration & Verification

- [x] 3.1 Add `GEMINI_API_KEY=` placeholder to `.env`
- [x] 3.2 Verify that `mem-recall.js` runs without error when key is in `.env`
- [x] 3.3 Verify that `mem-log.js` runs without error when key is in `.env`
