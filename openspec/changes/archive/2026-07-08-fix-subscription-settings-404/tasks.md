## Tasks

- [x] Add Worker route-table and build-artifact smoke coverage for subscription settings endpoints.
- [x] Add Worker handler tests for authenticated subscription/current and organization/usage responses.
- [x] Add Worker unauthenticated smoke tests proving both endpoints are registered instead of route-not-found 404s.
- [x] Add backend route tests for `GET /subscription/current` and `GET /organization/usage`.
- [x] Implement Worker `GET /api/subscription/current` and `GET /api/organization/usage`.
- [x] Implement Express parity through existing subscription and organization route/controller surfaces.
- [x] Run focused verification commands and record memory for the completed fix.
