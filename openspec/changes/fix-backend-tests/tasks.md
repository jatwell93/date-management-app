# Tasks

- [ ] Fix JWT type error in `src/middleware/auth.middleware.ts`
- [ ] Fix `getDb` mock issues in `src/tests/unit/database.test.ts` and related files
- [ ] Update `backend/jest.config.js` with `isolatedModules: true` for performance
- [ ] Configure `setupFilesAfterEnv` or `globalSetup` to handle Test DB migrations
- [ ] Verify `npm run test:backend:coverage` passes (all green)
