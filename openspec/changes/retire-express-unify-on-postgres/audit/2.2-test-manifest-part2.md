# Test-Coverage Manifest — Part 2 of 3

> **Status: PROPOSED — inventory, not a decision record.** Every row carries a `PROPOSED:`
> prefix. Deletion authority is human; no keep/retire call in this file is settled until reviewed.
>
> **Re-pointed 2026-08-28 by task 3.1.0 — four cited Worker test files were deleted.**
> `middleware.test.ts`, `middleware/auth.test.ts`, `middleware/require-role.test.ts` and
> `utils/auth.test.ts` were removed with the undeployed `index.ts` entry point (2.5 Finding 22).
> 246 rows across the four parts cited at least one of them. Two rules were applied.
> **215 rows** named a deleted file only inside a `none found (searched: ...)` list; those rows
> already recorded no equivalent, so only the search target was restated against live code
> (auth → `workers/src/index-minimal.ts` `authenticateApiRequest`:830; roles →
> `workers/src/constants/roles.test.ts`; rate limiting →
> `workers/src/utils/minimal-rate-limit.test.ts`) and **no decision changed**.
> **47 rows** cited a deleted file as a positive equivalent. Of those, 16 were repointed to live
> coverage that genuinely holds — role normalization to `constants/roles.test.ts`, and the
> admin-only gates to `minimal-api-routes.test.ts`, which task 3.1.0 extended to cover
> `handleClearSupplierPolicy` and `handleBulkAttachPolicy`. **31 were REOPENED**: their
> equivalence rested on tests for modules with no production importer, so the claim did not
> survive the deletion. They are marked `REOPENED by task 3.1.0` and carry their superseded
> decision inline. Three groups, none of them a re-pointing exercise:
> **22 auth/JWT rows** (the live Clerk path has no unit coverage for signature, expiry, tenant
> claim or subscription status, and `__tests__/auth-integration.test.ts` asserts
> `const expected = true` in 14 of its 17 cases);
> **2 role rows** for `requireOrgRole(admin, manager)`, which is live Express middleware on the
> user, store-area and organization-invite routes with no admin+manager equivalent anywhere in
> the Worker; and **3 rate-limit rows**, the limiter's reject path being untested in live code.
> These 25 need a decision from the owner; they are not settled.
>
> **Corrected during the same task.** A first pass recorded four further role rows
> (`requirePermission('upload_files')` ×3, `requireMinRole(manager)` ×1) as a live
> authorization gap, reading the Worker's absent upload role check as a regression against
> Express. That was half-verified and wrong: `backend/src/routes/upload.routes.ts` gates uploads
> with `authenticateToken` + `checkUsageLimit` + rate limiters and no role middleware, and
> `requirePermission` (`backend/src/middleware/requireOrgRole.ts:75`) and `requireMinRole`
> (`:110`) occur nowhere outside their own JSDoc examples at `:73` and `:107`. The permission
> model was built and tested on both sides and wired to nothing, so those four rows are
> `retire` — there is no behaviour to port. Upload authorization, if wanted, is net-new product
> work on a fresh decision, not a migration row.

**Task 2.2** — `retire-express-unify-on-postgres`

**Scope:** `backend/src/tests/unit/` — 15 files (Part 2, alphabetical `job-lock` through
`organization-invite-security`). Part 1 covered files a–i; a later session covers the remaining
files. A file in scope with no section is a defect; a section for a file outside this list is
also a defect.

**Status:** Every row carries a `PROPOSED:` prefix. This is an inventory, not a decision
record. The Express suite keeps running on SQLite until deletion; this manifest records what
its coverage maps onto in the Worker suite.

**Columns:** `Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision`

**Named gates:** `tenant-isolation`, `penetration`, `concurrency`, `feature-limits`,
`webhook-security`, `scheduled-job-idempotency`, `authorization-precedence`, `negative/error`,
or `—`.

**Targets:** `worker-equivalent-exists` | `worker-shaped-rewrite` | `retire`.

**Worker test inventory consulted:** `workers/src/**/*.test.ts` (44 files, enumerated in full
in the task brief before writing rows). Every negative names the files that were opened.

---

### job-lock.repository.test.ts

Unit tests for `JobLockRepository` with a **mocked Prisma client** (`$executeRaw` / `$queryRaw`
are `vi.fn()` stubs). No real database, no supertest — this is a pure call-shape test verifying
the repository issues the right raw SQL via Prisma and interprets `SQLITE_CONSTRAINT` errors
correctly. The lock is the scheduled-job idempotency mechanism; the Worker has no scheduled
handler and no DB-backed lock table, using Cloudflare Queue ack/retry instead.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Acquires a lock by inserting a migration lock row (executeRaw called once) | job-lock.repository.test.ts:18 | scheduled-job-idempotency | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:924 tests Cloudflare Queue ack/retry on final retry, not a DB-backed lock acquire; workers-deployment.test.ts:24-32 is a deployment health smoke test with no lock coverage; no `scheduled` handler exists in workers/src (grep returned no matches) | worker-shaped-rewrite | The idempotency gate is named for preservation but the Worker has no scheduled handler and no lock-table mechanism; the Prisma `$executeRaw` call shape retires with the backend, but the acquire-lock idempotency behaviour must be re-tested against the Worker's queue/scheduled model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — scheduled-job-idempotency gate has no Worker equivalent; re-test lock-acquire idempotency against Worker queue model |
| Returns false when an active lock already exists (SQLITE_CONSTRAINT + future appliedAt) | job-lock.repository.test.ts:26 | scheduled-job-idempotency | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:924-953 tests queue retry-exhaustion → mark-failed, not duplicate-job rejection via a lock; workers-deployment.test.ts has no concurrency/lock coverage; no scheduled handler in workers/src | worker-shaped-rewrite | The "reject duplicate job" behaviour is the core idempotency guard; the Worker's queue model uses at-least-once delivery with ack/retry (different mechanism), and no Worker test asserts duplicate-suppression; the gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — duplicate-job rejection has no Worker equivalent; re-test idempotency guard against Worker queue/scheduled model |
| Releases a lock by deleting the migration lock row (executeRaw called once) | job-lock.repository.test.ts:33 | scheduled-job-idempotency | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:924 acknowledges on final retry but does not test explicit lock release via DB row delete; workers-deployment.test.ts has no lock-release coverage; no scheduled handler in workers/src | worker-shaped-rewrite | Lock release is the cleanup half of the idempotency cycle; the Worker has no lock-table cleanup to test; the Prisma call shape retires, but the release-behaviour gate must be preserved in the Worker rewrite | PROPOSED: worker-shaped-rewrite — lock-release idempotency cleanup has no Worker equivalent; re-test against Worker queue model |

---

### local-storage.provider.test.ts

Unit tests for `LocalStorageProvider` — a **filesystem-based storage provider** for the
development environment. Uses real `fs/promises` operations against a temp directory
(`test-uploads-unit/`). Tests upload/download/delete/exists/getMetadata CRUD plus file-size
enforcement, directory-traversal sanitization, sidecar metadata, and presigned-URL rejection.
The Worker uses Cloudflare R2 (object storage), not a local filesystem; R2 has no directory
traversal surface and stores metadata via `httpMetadata`/`customMetadata` on the object itself.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Uploads a file successfully (returns key, file exists) | local-storage.provider.test.ts:60 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — upload-handlers.test.ts:23-44 tests R2 queue-completion with mocked R2Bucket, not a local-fs upload; upload-router.test.ts:13-31 tests route dispatch, not storage CRUD | retire | Local filesystem upload is Express dev machinery; Worker uses R2 put via a different API | PROPOSED: retire — local-filesystem storage provider has no Worker analogue (Worker uses R2) |
| Creates nested directories for key with path | local-storage.provider.test.ts:74 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — R2 uses flat key namespaces, no directory creation; upload-handlers.test.ts:23 uses `uploads/user-7/products.csv` as a flat R2 key | retire | Nested directory creation is filesystem-specific; R2 keys are flat strings with no directory semantics | PROPOSED: retire — directory creation is local-filesystem machinery with no R2 analogue |
| Stores metadata in sidecar `.meta.json` file | local-storage.provider.test.ts:85 | — | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:91-99 writes `customMetadata` directly on the R2 object via `put()`, not a sidecar file | retire | Sidecar metadata files are local-filesystem-specific; R2 stores metadata on the object itself | PROPOSED: retire — sidecar metadata file is local-filesystem machinery; R2 uses object-level customMetadata |
| Throws FileSizeLimitError for files exceeding limit | local-storage.provider.test.ts:99 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — the Worker enforces file-size limits in code (index-minimal.ts:3450-3452, 3514-3515, 3524-3525) but no Worker test asserts the rejection; usage-limits-integration.test.ts covers subscription-tier limits, not file-size limits | worker-shaped-rewrite | The file-size-limit behaviour is relevant to the Worker (enforced at the API route level) but has no test coverage; the local-storage provider's check retires, but the gate must be re-tested against the Worker's route-level enforcement | PROPOSED: worker-shaped-rewrite — file-size limit enforced in Worker code (index-minimal.ts:3450) but untested; re-test against Worker route model |
| Sanitizes key to prevent directory traversal (`../../../etc/passwd`) | local-storage.provider.test.ts:107 | penetration | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/handlers/handlers.test.ts, workers/src/__tests__/error-handling.test.ts) — handlers.test.ts:63,170 test SQL injection prevention via parameterized queries; error-handling.test.ts:455 tests CORS origin rejection; no Worker test covers path traversal because R2 is object storage with no filesystem path surface | retire | Directory traversal is a filesystem-specific attack vector; R2 keys are opaque strings with no directory hierarchy to escape; the penetration gate is preserved by Worker SQL-injection and CORS tests, not by path-traversal tests | PROPOSED: retire — path traversal attack vector does not apply to R2 object storage; penetration gate preserved by Worker SQL-injection tests |
| Downloads an uploaded file (round-trip content) | local-storage.provider.test.ts:129 | — | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:67-70 mocks `R2Bucket.get()` returning `arrayBuffer()`, testing processing logic, not storage round-trip | retire | Local filesystem download is Express dev machinery; Worker reads R2 objects via `get().arrayBuffer()` | PROPOSED: retire — local-filesystem download has no Worker analogue (Worker uses R2 get) |
| Throws FileNotFoundError for non-existent file on download | local-storage.provider.test.ts:140 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:46-58 maps a disappearing R2 object to 404 during sync processing, but this is a route-level 404, not a storage-provider FileNotFoundError | retire | `FileNotFoundError` is a local-storage-provider-specific error class; the Worker maps missing R2 objects to HTTP 404 at the route level | PROPOSED: retire — storage-provider error class is Express-specific; Worker uses HTTP 404 at route level |
| Preserves binary data integrity on round-trip | local-storage.provider.test.ts:144 | — | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:62-100 processes CSV via `arrayBuffer()` but does not test binary round-trip integrity | retire | Binary integrity on local filesystem is Express dev machinery; R2 preserves binary data by design (object storage) | PROPOSED: retire — local-filesystem binary round-trip has no Worker analogue (R2 preserves binary by design) |
| Deletes an existing file | local-storage.provider.test.ts:157 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — no Worker test covers R2 object deletion | retire | Local filesystem delete is Express dev machinery; Worker would use `R2Bucket.delete()` but no test covers it | PROPOSED: retire — local-filesystem delete has no Worker analogue |
| Also deletes metadata sidecar file on delete | local-storage.provider.test.ts:168 | — | none found (searched: workers/src/upload/upload-handlers.test.ts) — R2 metadata lives on the object itself, so there is no separate metadata file to delete | retire | Sidecar metadata deletion is local-filesystem-specific; R2 metadata is deleted with the object | PROPOSED: retire — sidecar metadata cleanup is local-filesystem machinery with no R2 analogue |
| Throws FileNotFoundError for non-existent file on delete | local-storage.provider.test.ts:184 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — no Worker test covers R2 delete of a missing object | retire | `FileNotFoundError` on delete is storage-provider-specific; R2 delete of a missing key is idempotent (no error) | PROPOSED: retire — storage-provider error class is Express-specific; R2 delete is idempotent for missing keys |
| Returns true for existing file (exists check) | local-storage.provider.test.ts:190 | — | none found (searched: workers/src/upload/upload-handlers.test.ts) — no Worker test covers R2 object existence check | retire | Local filesystem exists-check is Express dev machinery; Worker would use `R2Bucket.head()` but no test covers it | PROPOSED: retire — local-filesystem exists check has no Worker analogue |
| Returns false for non-existent file (exists check) | local-storage.provider.test.ts:198 | — | none found (searched: workers/src/upload/upload-handlers.test.ts) — no Worker test covers R2 object non-existence | retire | Local filesystem exists-check is Express dev machinery | PROPOSED: retire — local-filesystem exists check has no Worker analogue |
| Returns false after file is deleted (exists check) | local-storage.provider.test.ts:203 | — | none found (searched: workers/src/upload/upload-handlers.test.ts) — no Worker test covers R2 post-delete existence | retire | Local filesystem exists-check is Express dev machinery | PROPOSED: retire — local-filesystem exists check has no Worker analogue |
| Returns metadata from sidecar file (getMetadata) | local-storage.provider.test.ts:214 | — | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:91-99 writes `customMetadata` on R2 put, but no test reads it back via a getMetadata equivalent | retire | Sidecar metadata read is local-filesystem-specific; R2 stores metadata on the object | PROPOSED: retire — sidecar metadata read is local-filesystem machinery with no R2 analogue |
| Falls back to stat-based metadata when sidecar missing | local-storage.provider.test.ts:228 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — R2 objects always carry metadata via `httpMetadata`/`customMetadata`; no fallback path exists | retire | Stat-based fallback is local-filesystem-specific; R2 metadata is always present on the object | PROPOSED: retire — stat-based metadata fallback is local-filesystem machinery with no R2 analogue |
| Throws FileNotFoundError for non-existent file on getMetadata | local-storage.provider.test.ts:243 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — no Worker test covers getMetadata on a missing R2 object | retire | `FileNotFoundError` is a storage-provider-specific error class; Worker uses HTTP 404 at route level | PROPOSED: retire — storage-provider error class is Express-specific |
| Throws StorageProviderError for presigned URL (not supported) | local-storage.provider.test.ts:249 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — the Worker uses R2 presigned URLs via `createUploadToken` (index-minimal.ts:662-667); no test asserts the not-supported error because R2 does support presigned URLs | retire | The not-supported error is local-storage-specific; R2 supports presigned URLs, so this negative case does not apply | PROPOSED: retire — presigned-URL not-supported error is local-filesystem machinery; R2 supports presigned URLs |
| Includes helpful error message in presigned URL rejection | local-storage.provider.test.ts:255 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — R2 supports presigned URLs, so no not-supported error message exists in the Worker | retire | Error message text is local-storage-specific; R2 supports presigned URLs | PROPOSED: retire — error message is local-filesystem machinery; R2 supports presigned URLs |

---

### logger.test.ts

Unit tests for the Express `Logger` utility class — a wrapper around `console.log`/`console.warn`/
`console.error` that adds ISO timestamps, level prefixes (`[ERROR]`, `[WARN]`), and metadata
serialization. Tests use `vi.fn()` to mock console methods and assert on formatted output strings.
The Worker has no `Logger` class (grep for `class Logger` / `Logger.error` in `workers/src`
returned zero matches); Workers use `console.log`/`console.error` directly with no formatting
wrapper.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Accepts typed metadata objects (Record<string, unknown>) | logger.test.ts:33 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts, workers/src/__tests__/error-handling.test.ts) — no `Logger` class exists in workers/src (grep returned zero matches); error-handling.test.ts tests error responses, not log formatting | retire | Express-specific `Logger` class with no Worker analogue; Workers use raw `console.log` | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Calls logger methods with optional metadata (error without metadata, warn with) | logger.test.ts:47 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src | retire | Express-specific Logger API; Workers use raw console methods | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Formats log output correctly (ISO timestamp, [WARN] prefix, message, key) | logger.test.ts:54 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src; Workers do not format console output with timestamps/level prefixes | retire | Log formatting is Express-specific; Workers rely on Cloudflare's built-in log capture | PROPOSED: retire — log formatting is Express Logger machinery with no Worker analogue |
| Logs error messages with [ERROR] prefix | logger.test.ts:66 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src | retire | [ERROR] prefix formatting is Express-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Logs warn messages with [WARN] prefix | logger.test.ts:71 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src | retire | [WARN] prefix formatting is Express-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Logs info messages (if development) | logger.test.ts:76 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src; Workers use console.log directly without NODE_ENV-gated info level | retire | NODE_ENV-gated info logging is Express-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Logs debug messages (if development) | logger.test.ts:82 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src | retire | NODE_ENV-gated debug logging is Express-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Handles complex metadata structures (nested objects, arrays) | logger.test.ts:90 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src; Workers do not serialize metadata via a Logger | retire | Metadata serialization is Express Logger-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Handles empty metadata | logger.test.ts:109 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src | retire | Empty-metadata handling is Express Logger-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Handles null and undefined in metadata | logger.test.ts:116 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src | retire | Null/undefined metadata handling is Express Logger-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Logs API errors (endpoint, method, statusCode, errorCode) | logger.test.ts:132 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — error-handling.test.ts tests HTTP error responses, not Logger formatting of API error context | retire | API error logging via Logger is Express-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Logs database operations (table, operation, rows, duration) | logger.test.ts:146 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Logger class in workers/src | retire | DB operation logging via Logger is Express-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |
| Logs authentication events (userId, role, method, success) | logger.test.ts:160 | — | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/__tests__/auth-integration.test.ts) — auth-integration.test.ts tests auth flows, not Logger formatting of auth events | retire | Auth event logging via Logger is Express-specific | PROPOSED: retire — Express Logger utility has no Worker analogue |

---

### markdown-config.service.test.ts

Unit tests for `MarkdownConfigService` with **mocked repositories** (`findAllByOrganizationId`,
`hasRetailData`, `upsert` are `vi.fn()` stubs) and a mocked Prisma `$transaction`. Tests
default-matrix fallback, per-scope independence, record-to-matrix mapping, retail-data
availability reporting, retail-basis validation, scoped-matrix upsert in a transaction, legacy
write semantics, and cost-only persistence. The Worker implements the same behaviours at the
route level in `index-minimal.ts` (`handleGetMarkdownConfig`/`handleUpdateMarkdownConfig`) and
tests them in `minimal-api-routes.test.ts`.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns the default matrix when the org has no stored config | markdown-config.service.test.ts:42 | — | workers/src/minimal-api-routes.test.ts:264 — returns the default markdown matrix (both NO_CREDIT and FULL_CREDIT defaults) for an authenticated organization when `FROM organization_markdown_config` returns `[]` | worker-equivalent-exists | Worker GET test covers the same default-fallback behaviour when no config rows exist | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:264 asserts the same default-matrix fallback |
| Defaults each missing scope independently (NO_CREDIT stored, FULL_CREDIT defaults) | markdown-config.service.test.ts:47 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts, workers/src/credit-scoped-markdown-migration.pglite.node.test.ts) — minimal-api-routes.test.ts:264 tests both scopes defaulting (no rows at all); no Worker test seeds only one scope and verifies the other defaults; Worker code does default per scope (index-minimal.ts:3180-3183) but the partial-scope case is untested | worker-shaped-rewrite | The per-scope independent default behaviour exists in Worker code but no test covers the partial-scope case; the Express test is the only coverage for this edge | PROPOSED: worker-shaped-rewrite — partial-scope default is in Worker code but untested; re-test against Worker route model |
| Maps a stored record to a resolver-ready matrix (band percentages/bases) | markdown-config.service.test.ts:57 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — minimal-api-routes.test.ts:264 GETs with no stored config (default); minimal-api-routes.test.ts:296 PUTs and the response returns the parsed request body as NO_CREDIT, not a DB-read record; no Worker GET test reads a stored config with specific band values and verifies the record-to-matrix mapping | worker-shaped-rewrite | The Worker's `markdownConfigRowToMatrix` (index-minimal.ts:3859-3874) does the mapping but no test exercises it with a stored record containing specific values via GET | PROPOSED: worker-shaped-rewrite — record-to-matrix mapping via GET is untested in Worker; re-test against Worker route model |
| getConfig reports whether retail data is available (hasRetailData field) | markdown-config.service.test.ts:81 | — | workers/src/minimal-api-routes.test.ts:264 — returns `hasRetailData: false` when no products; workers/src/minimal-api-routes.test.ts:296 — returns `hasRetailData: true` when products exist | worker-equivalent-exists | Both the false and true cases of the hasRetailData field are covered by Worker route tests | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:264 and :296 cover both hasRetailData cases |
| Rejects a retail-basis band when the org has no retail data (ValidationError) | markdown-config.service.test.ts:89 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — minimal-api-routes.test.ts:512 tests the `retailColumnMissing` 503 case (schema not migrated), not the `!hasRetailData` 400 rejection; Worker code at index-minimal.ts:3260-3266 rejects retail-basis when `!hasRetailData` but no test covers this specific validation | worker-shaped-rewrite | The Worker has the retail-basis rejection code (index-minimal.ts:3260-3266) but no test asserts the 400 response when retail data is absent; the negative/error gate must be preserved | PROPOSED: worker-shaped-rewrite — retail-basis rejection when no retail data is in Worker code but untested; re-test against Worker route model |
| Persists a valid retail matrix when retail data exists | markdown-config.service.test.ts:100 | — | workers/src/minimal-api-routes.test.ts:296 — persists a valid markdown matrix with retail basis for organization admins when products exist (hasRetailData: true), response echoes the stored matrix | worker-equivalent-exists | Worker PUT test covers the same valid-retail-matrix persistence with retail data present | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:296 asserts the same valid-retail-matrix persistence |
| Writes both scoped matrices in one transaction (NO_CREDIT + FULL_CREDIT) | markdown-config.service.test.ts:113 | concurrency | workers/src/minimal-api-routes.test.ts:350 — persists both scoped matrices with one atomic upsert statement; asserts a single INSERT with `ON CONFLICT (organization_id, credit_scope)` covering both scopes | worker-equivalent-exists | The Worker uses a single SQL statement (not a Prisma `$transaction`) but tests the same atomic both-scope upsert behaviour; the concurrency gate is preserved by the atomic single-statement approach | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:350 asserts both-scope atomic upsert |
| Legacy writes update NO_CREDIT only and preserve FULL_CREDIT | markdown-config.service.test.ts:133 | — | workers/src/minimal-api-routes.test.ts:296 — PUT with legacy format (single matrix, not `matrices: { ... }`) returns FULL_CREDIT as the default (preserved), NO_CREDIT updated; Worker code at index-minimal.ts:3271-3289 writes NO_CREDIT only in legacy mode | worker-equivalent-exists | The Worker legacy PUT path writes NO_CREDIT only and the test response shows FULL_CREDIT preserved as default | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:296 covers legacy NO_CREDIT-only write with FULL_CREDIT preserved |
| Persists a cost-only matrix even without retail data | markdown-config.service.test.ts:145 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts:296 PUTs with retail data (hasRetailData: true); minimal-api-routes.test.ts:399 rejects non-numeric percentages; no Worker test PUTs a cost-only matrix when hasRetailData is false; Worker code at index-minimal.ts:3252-3267 skips the retail check for cost-only matrices, but this path is untested | worker-shaped-rewrite | The Worker handles cost-only PUT without retail data (skips retail validation) but no test covers this path; the behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — cost-only PUT without retail data is in Worker code but untested; re-test against Worker route model |

---

### markdown-config-schema.test.ts

Unit tests for `markdownConfigSchema` — a Zod validation schema for markdown config PUT
requests. Tests use `safeParse` directly (no HTTP, no service, no DB). Validates that the
schema accepts scoped matrices (`{ matrices: { NO_CREDIT, FULL_CREDIT } }`), rejects invalid
band ordering within a scoped update, and continues accepting the legacy bare-matrix format.
The Worker uses `parseMarkdownMatrixRequest` (index-minimal.ts:3883-3908) instead of Zod, with
equivalent validation logic including band-ordering checks (index-minimal.ts:3929-3933).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Accepts both scoped matrices (`{ matrices: { NO_CREDIT, FULL_CREDIT } }`) | markdown-config-schema.test.ts:10 | — | workers/src/minimal-api-routes.test.ts:350 — PUTs `{ matrices: { NO_CREDIT, FULL_CREDIT } }` and receives 200, asserting the scoped format is accepted | worker-equivalent-exists | Worker route test covers the same scoped-matrix acceptance | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:350 asserts scoped-matrix acceptance |
| Rejects a scoped update when either matrix has invalid band ordering (descending percentages) | markdown-config-schema.test.ts:18 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts:399 rejects non-numeric percentages (null, '', '50'), not invalid band ordering; minimal-api-routes.test.ts:296 and :350 PUT valid matrices; no Worker test PUTs scoped matrices with descending band percentages to verify the rejection; Worker code at index-minimal.ts:3929-3933 validates band ordering but this path is untested | worker-shaped-rewrite | The Worker has band-ordering validation code (index-minimal.ts:3929-3933) but no test covers the scoped-matrix invalid-ordering rejection; the negative/error gate must be preserved | PROPOSED: worker-shaped-rewrite — scoped-matrix invalid-band-ordering rejection is in Worker code but untested; re-test against Worker route model |
| Continues accepting the legacy bare matrix (`{ band1, band2, band3 }`) | markdown-config-schema.test.ts:35 | — | workers/src/minimal-api-routes.test.ts:296 — PUTs a legacy bare matrix (`{ band1: { percentage: 40, basis: 'retail' }, ... }`) and receives 200, asserting backward compatibility | worker-equivalent-exists | Worker route test covers the same legacy bare-matrix acceptance | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:296 asserts legacy bare-matrix acceptance |

---

### markdown-matrix.test.ts

Unit tests for **shared domain logic** in `shared/domain/markdown` — `calculateMarkdownPrice`,
`getMarkdownBandConfig`, `selectMatrix`, `resolveMarkdown`, and the default matrix constants.
Tests are pure function calls (no HTTP, no DB, no mocks). This code is shared between Express
and Worker; the Worker imports the same functions (e.g., `index-minimal.ts:69`, `database.ts:33`).
The Worker's `database.disposition.pglite.node.test.ts` exercises the markdown window logic
indirectly through `processExpiredItem`, and `credit-scoped-markdown-migration.pglite.node.test.ts`
uses `calculateMarkdownPrice` as a utility to verify migration prices. No Worker test directly
unit-tests the shared domain functions.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Default matrix bands price off cost regardless of retail (50/60/75% off cost) | markdown-matrix.test.ts:17 | — | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — credit-scoped-markdown-migration.pglite.node.test.ts:17 uses `calculateMarkdownPrice` as a utility to verify migration price preservation, not to assert cost-basis pricing; disposition test:66 asserts markdown levels (1/2/3/null), not calculated prices | worker-shaped-rewrite | No Worker test directly asserts the calculated markdown price for cost-basis bands; the shared function is used but its output is not verified | PROPOSED: worker-shaped-rewrite — cost-basis price calculation is used in Worker but not directly asserted; re-test shared function in Worker suite |
| Omitting the config argument is identical to passing the default | markdown-matrix.test.ts:24 | — | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts) — no Worker test compares `calculateMarkdownPrice` with and without the config argument | worker-shaped-rewrite | Default-parameter equivalence is untested in the Worker suite | PROPOSED: worker-shaped-rewrite — default-parameter equivalence untested in Worker; re-test shared function |
| Retail-basis bands discount off retail (issue #338 example) | markdown-matrix.test.ts:39 | — | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts) — credit-scoped-markdown-migration.pglite.node.test.ts:17 uses cost-basis only; no Worker test asserts retail-basis price calculation | worker-shaped-rewrite | Retail-basis pricing is untested in the Worker suite; the shared function supports it but no Worker test verifies the output | PROPOSED: worker-shaped-rewrite — retail-basis price calculation untested in Worker; re-test shared function |
| Falls back to cost when a product has no retail price (retail-basis band) | markdown-matrix.test.ts:46 | negative/error | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts) — no Worker test covers the retail-to-cost fallback when `retailPrice` is null | worker-shaped-rewrite | The retail-to-cost fallback is a negative/error path in the shared function; untested in the Worker suite | PROPOSED: worker-shaped-rewrite — retail-to-cost fallback for null retail untested in Worker; re-test shared function |
| Falls back to cost when retail is not finite (NaN) | markdown-matrix.test.ts:52 | negative/error | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts) — no Worker test covers the retail-to-cost fallback for NaN retail | worker-shaped-rewrite | The NaN-retail fallback is a negative/error path; untested in the Worker suite | PROPOSED: worker-shaped-rewrite — retail-to-cost fallback for NaN retail untested in Worker; re-test shared function |
| Per-band mixed basis resolves each band independently | markdown-matrix.test.ts:59 | — | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts) — no Worker test covers mixed per-band basis (some cost, some retail) | worker-shaped-rewrite | Mixed-basis resolution is untested in the Worker suite | PROPOSED: worker-shaped-rewrite — mixed-basis per-band resolution untested in Worker; re-test shared function |
| Returns null for stock more than 90 days out | markdown-matrix.test.ts:74 | — | workers/src/database.disposition.pglite.node.test.ts:66 — tests `[150, null]` markdown level for 150 days out, implicitly asserting `calculateMarkdownPrice` returns null beyond the 90-day window | worker-equivalent-exists | The disposition test covers the >90-day null case indirectly through the markdown level snapshot | PROPOSED: worker-equivalent-exists — Worker database.disposition.pglite.node.test.ts:66 asserts null markdown level for 150 days out |
| Returns null for expired stock (0 or fewer days) | markdown-matrix.test.ts:77 | negative/error | workers/src/database.disposition.pglite.node.test.ts:78 — `seedItem(-5, 'SKU-EXPIRED')` creates an item 5 days past expiry; `processExpiredItem` returns `txn.markdownLevel` as null, implicitly asserting `calculateMarkdownPrice` returns null for expired stock | worker-equivalent-exists | The disposition test covers the expired-stock null case indirectly through the markdown level snapshot | PROPOSED: worker-equivalent-exists — Worker database.disposition.pglite.node.test.ts:78 asserts null markdown level for expired stock |
| getMarkdownBandConfig returns the configured band for each in-window level | markdown-matrix.test.ts:87 | — | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test directly calls `getMarkdownBandConfig` | worker-shaped-rewrite | `getMarkdownBandConfig` is untested in the Worker suite; it drives the scan page display | PROPOSED: worker-shaped-rewrite — getMarkdownBandConfig untested in Worker; re-test shared function |
| getMarkdownBandConfig defaults to 50/60/75% off-cost when no config passed | markdown-matrix.test.ts:104 | — | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts) — no Worker test covers `getMarkdownBandConfig` with default parameter | worker-shaped-rewrite | Default-parameter behaviour for `getMarkdownBandConfig` is untested in the Worker suite | PROPOSED: worker-shaped-rewrite — getMarkdownBandConfig default untested in Worker; re-test shared function |
| getMarkdownBandConfig returns null outside the markdown window | markdown-matrix.test.ts:108 | negative/error | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts) — no Worker test directly calls `getMarkdownBandConfig` for out-of-window values | worker-shaped-rewrite | Out-of-window null return for `getMarkdownBandConfig` is untested in the Worker suite | PROPOSED: worker-shaped-rewrite — getMarkdownBandConfig out-of-window null untested in Worker; re-test shared function |
| Provides independent no-credit and flat full-credit defaults (DEFAULT_MARKDOWN_MATRIX_SET) | markdown-matrix.test.ts:117 | — | workers/src/minimal-api-routes.test.ts:264 — returns the default matrix set with NO_CREDIT (50/60/75 cost) and FULL_CREDIT (20/20/20 cost), asserting the same default values | worker-equivalent-exists | The Worker route test asserts the same default matrix set values | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:264 asserts the default matrix set values |
| Selects and resolves the requested scope without changing pricing arithmetic (selectMatrix + resolveMarkdown) | markdown-matrix.test.ts:127 | — | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/database.disposition.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test directly calls `selectMatrix` or `resolveMarkdown` with scoped matrices | worker-shaped-rewrite | `selectMatrix` and `resolveMarkdown` are untested in the Worker suite; the Worker uses `resolveMarkdownCreditContext` (database.ts:574) which wraps these but no test verifies the scope selection + price resolution directly | PROPOSED: worker-shaped-rewrite — selectMatrix/resolveMarkdown untested in Worker; re-test shared function |
| Keeps out-of-window day values unpriced in both scopes (resolveMarkdown) | markdown-matrix.test.ts:157 | negative/error | none found (searched: workers/src/database.disposition.pglite.node.test.ts, workers/src/credit-scoped-markdown-migration.pglite.node.test.ts) — disposition test:66 tests out-of-window for one scope only (no credit-scope distinction); no Worker test verifies out-of-window null in both NO_CREDIT and FULL_CREDIT scopes | worker-shaped-rewrite | Out-of-window null in both scopes is untested in the Worker suite | PROPOSED: worker-shaped-rewrite — out-of-window null in both scopes untested in Worker; re-test shared function |
| Keeps retail-basis fallback to cost in both scopes (resolveMarkdown) | markdown-matrix.test.ts:166 | negative/error | none found (searched: workers/src/database.disposition.pglite.node.test.ts, workers/src/credit-scoped-markdown-migration.pglite.node.test.ts) — no Worker test covers retail-basis fallback to cost in either scope, let alone both | worker-shaped-rewrite | Retail-basis fallback in both scopes is untested in the Worker suite | PROPOSED: worker-shaped-rewrite — retail-basis fallback in both scopes untested in Worker; re-test shared function |

---

### migrated-controllers.test.ts

Unit tests for 12 Express controllers (Product, Inventory, Subscription, Webhook, StoreArea,
StorageQuota, Dashboard, ExpiredItem, Report, Health, User, OrgBootstrap) using **mocked
services** and Express `Request`/`Response`/`NextFunction` stubs. Tests assert controller →
service call shapes (`serviceFactory` called with orgId), HTTP response shapes (`res.status()/
res.json()`), error middleware forwarding (`next(error)`), webhook header validation, cross-org
access blocking, and auth-required gates. The Worker uses Web API `Request`/`Response` (no
`next()`, no `serviceFactory` DI); equivalent behaviours are tested in `minimal-api-routes.test.ts`,
`health.test.ts`, `clerk/webhook-signature.test.ts`, and `clerk/bootstrap-handler.node.test.ts`.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| ProductController creates a product through the organization-scoped service | migrated-controllers.test.ts:69 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — handlers.test.ts:43-79 are placeholder tests (assert `true`); no Worker test asserts product creation via a controller serviceFactory | retire | Express controller wiring (serviceFactory DI, Express req/res/next) with no Worker analogue | PROPOSED: retire — Express controller serviceFactory wiring has no Worker analogue |
| ProductController returns validation response when required fields are missing | migrated-controllers.test.ts:99 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts 400 for missing product fields at the route level | retire | Express controller validation wiring; Worker validates at the route level but no test covers this specific validation | PROPOSED: retire — Express controller validation wiring has no Worker analogue |
| ProductController passes unexpected service failures to error middleware (next) | migrated-controllers.test.ts:114 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — Worker uses try/catch returning Response, not Express `next(error)` | retire | Express error middleware wiring (`next(error)`) with no Worker analogue | PROPOSED: retire — Express next(error) error middleware wiring has no Worker analogue |
| InventoryController returns inventory rows for a valid barcode lookup | migrated-controllers.test.ts:133 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — handlers.test.ts:83-107 are placeholder tests; no Worker test asserts inventory barcode lookup response shape | retire | Express controller wiring with no Worker analogue | PROPOSED: retire — Express controller wiring has no Worker analogue |
| InventoryController forwards validation failures to centralized error middleware | migrated-controllers.test.ts:160 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test asserts validation error forwarding for inventory barcode lookup | retire | Express error middleware wiring | PROPOSED: retire — Express next(error) validation wiring has no Worker analogue |
| InventoryController forwards missing organization context to error middleware | migrated-controllers.test.ts:175 | authorization-precedence | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/auth-integration.test.ts) — auth-integration.test.ts:60-100 are placeholder tests; no Worker test asserts missing-org-context error forwarding | retire | Express error middleware wiring for missing org context; Worker auth middleware rejects before the handler | PROPOSED: retire — Express next(error) for missing org context has no Worker analogue |
| SubscriptionController returns trial status with tier limits for authenticated Clerk user | migrated-controllers.test.ts:227 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/subscription-validation.test.ts) — the Worker has a trial-status route (index-minimal.ts:264, 3344) but no test asserts its response shape; subscription-validation.test.ts:125 is a placeholder test | worker-shaped-rewrite | The Worker has the trial-status endpoint but no test covers the response shape (isInTrial, tierLimits); the behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — trial-status endpoint exists in Worker but is untested; re-test against Worker route model |
| SubscriptionController throws validation errors for missing checkout session fields | migrated-controllers.test.ts:261 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/subscription-validation.test.ts) — no Worker test asserts checkout session field validation | retire | Express controller validation wiring; Worker has no checkout session route test | PROPOSED: retire — Express checkout validation wiring has no Worker analogue |
| SubscriptionController wraps unexpected repository failures with an internal error | migrated-controllers.test.ts:270 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/subscription-validation.test.ts) — no Worker test asserts internal error wrapping for subscription repository failures | retire | Express controller error wrapping with no Worker analogue | PROPOSED: retire — Express controller error wrapping has no Worker analogue |
| WebhookController rejects Stripe webhooks missing the signature header (400) | migrated-controllers.test.ts:342 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/error-handling.test.ts) — health.test.ts:365 rejects Clerk webhooks without Svix headers, not Stripe; no Worker test covers Stripe webhook signature header validation | worker-shaped-rewrite | The webhook-security gate must be preserved; the Worker has Clerk webhook security tests but no Stripe webhook security test; the Stripe webhook rejection must be re-tested | PROPOSED: worker-shaped-rewrite — Stripe webhook signature rejection has no Worker equivalent; re-test against Worker webhook model |
| WebhookController processes new Stripe webhooks and records success telemetry | migrated-controllers.test.ts:362 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/clerk/webhook-signature.test.ts) — no Worker test covers Stripe webhook processing or telemetry; webhook-signature.test.ts:41 covers Clerk signature verification only | worker-shaped-rewrite | Stripe webhook processing is untested in the Worker suite; the webhook-security gate must be preserved | PROPOSED: worker-shaped-rewrite — Stripe webhook processing has no Worker equivalent; re-test against Worker webhook model |
| WebhookController acknowledges non-recoverable Stripe processing errors (200 ack) | migrated-controllers.test.ts:401 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/clerk/webhook-signature.test.ts) — no Worker test covers Stripe webhook error acknowledgement to stop retry storms | worker-shaped-rewrite | Non-recoverable error acknowledgement is untested in the Worker suite; the webhook-security gate must be preserved | PROPOSED: worker-shaped-rewrite — Stripe error ack has no Worker equivalent; re-test against Worker webhook model |
| WebhookController rejects Clerk webhooks missing required Svix headers (400) | migrated-controllers.test.ts:432 | webhook-security | workers/src/health.test.ts:365 — routes /api/webhooks/clerk and rejects requests without Svix headers | worker-equivalent-exists | The Worker health test covers the same Clerk Svix header rejection | PROPOSED: worker-equivalent-exists — Worker health.test.ts:365 asserts Clerk Svix header rejection |
| WebhookController processes new Clerk webhooks with Svix idempotency keys | migrated-controllers.test.ts:452 | webhook-security | workers/src/clerk/webhook-signature.test.ts:41 — accepts a valid signed Clerk webhook body (signature verification + idempotency); workers/src/health.test.ts:365 rejects missing Svix headers | worker-equivalent-exists | The Worker covers Clerk webhook signature verification and idempotency; the Express test additionally asserts `markEventProcessed` and telemetry, which are Express-service-specific | PROPOSED: worker-equivalent-exists — Worker webhook-signature.test.ts:41 covers Clerk webhook verification and idempotency |
| WebhookController returns Clerk conflict errors without hiding duplicate-account details (409) | migrated-controllers.test.ts:499 | webhook-security | workers/src/health.test.ts:1430 — returns 409 when registering existing email with `error: 'Email already registered'` | worker-equivalent-exists | The Worker covers the same 409 conflict response for duplicate email | PROPOSED: worker-equivalent-exists — Worker health.test.ts:1430 asserts 409 conflict for duplicate email |
| StoreAreaController creates a store area through the organization-scoped service | migrated-controllers.test.ts:535 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — handlers.test.ts:110-125 are placeholder tests; no Worker test asserts store area creation response shape | retire | Express controller wiring with no Worker analogue | PROPOSED: retire — Express controller wiring has no Worker analogue |
| StoreAreaController returns validation responses for invalid store area ids (400) | migrated-controllers.test.ts:568 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test asserts 400 for invalid store area IDs | retire | Express controller validation wiring | PROPOSED: retire — Express controller validation wiring has no Worker analogue |
| StoreAreaController returns not-found responses when service has no matching store area (404) | migrated-controllers.test.ts:586 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test asserts 404 for missing store area | retire | Express controller not-found wiring | PROPOSED: retire — Express controller not-found wiring has no Worker analogue |
| StoreAreaController passes unexpected service failures to error middleware | migrated-controllers.test.ts:606 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — Worker uses try/catch returning Response, not Express `next(error)` | retire | Express error middleware wiring | PROPOSED: retire — Express next(error) error middleware wiring has no Worker analogue |
| StorageQuotaController returns quota information through the organization-scoped service | migrated-controllers.test.ts:625 | feature-limits | workers/src/minimal-api-routes.test.ts:576 — returns organization usage for an authenticated organization with `skus: { current, limit }`, `users: { current, limit }`, `storage: { current, limit }` | worker-equivalent-exists | The Worker covers organization usage/quota with the nested current/limit shape | PROPOSED: worker-equivalent-exists — Worker minimal-api-routes.test.ts:576 asserts organization usage with quota details |
| StorageQuotaController returns validation responses for invalid user IDs (400) | migrated-controllers.test.ts:656 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test asserts 400 for invalid user ID in quota context | retire | Express controller validation wiring | PROPOSED: retire — Express controller validation wiring has no Worker analogue |
| StorageQuotaController returns a denied upload response with remaining quota details | migrated-controllers.test.ts:679 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — minimal-api-routes.test.ts:576 returns usage but does not test denied-upload-with-remaining-quota; usage-limits-integration.test.ts covers tier limits, not upload denial | worker-shaped-rewrite | The denied-upload-with-remaining-quota behaviour is relevant to the Worker (feature-limits gate) but no test covers it | PROPOSED: worker-shaped-rewrite — denied-upload quota response untested in Worker; re-test against Worker route model |
| StorageQuotaController passes unexpected storage quota failures to error middleware | migrated-controllers.test.ts:715 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — Worker uses try/catch returning Response, not Express `next(error)` | retire | Express error middleware wiring | PROPOSED: retire — Express next(error) error middleware wiring has no Worker analogue |
| DashboardController returns dashboard data through the organization-scoped service | migrated-controllers.test.ts:740 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — handlers.test.ts:127-153 are placeholder tests; no Worker test asserts dashboard data response shape | retire | Express controller wiring with no Worker analogue | PROPOSED: retire — Express controller wiring has no Worker analogue |
| DashboardController passes unexpected dashboard failures to error middleware | migrated-controllers.test.ts:761 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — Worker uses try/catch returning Response, not Express `next(error)` | retire | Express error middleware wiring | PROPOSED: retire — Express next(error) error middleware wiring has no Worker analogue |
| ExpiredItemController returns expired items through the expired item service | migrated-controllers.test.ts:780 | — | workers/src/database.disposition.pglite.node.test.ts:134 — `db.getExpiredItems(ORG)` returns expired items with sku, quantityAvailable; tests the same data retrieval at the DB level | worker-equivalent-exists | The Worker disposition test covers expired item retrieval at the DB level (not controller level, but same behaviour) | PROPOSED: worker-equivalent-exists — Worker database.disposition.pglite.node.test.ts:134 asserts expired item retrieval |
| ExpiredItemController returns validation responses before processing expired items (400) | migrated-controllers.test.ts:804 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.disposition.pglite.node.test.ts) — no Worker test asserts 400 for missing inventoryItemId before processing | retire | Express controller validation wiring | PROPOSED: retire — Express controller validation wiring has no Worker analogue |
| ExpiredItemController maps not-found processing failures to the existing 404 response shape | migrated-controllers.test.ts:825 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.disposition.pglite.node.test.ts) — no Worker test asserts 404 for missing inventory item during processing | retire | Express controller not-found wiring | PROPOSED: retire — Express controller not-found wiring has no Worker analogue |
| ExpiredItemController returns expired loss reports from both service aggregations | migrated-controllers.test.ts:845 | — | workers/src/database.disposition.pglite.node.test.ts:354 — `db.getExpiredLossBySku(ORG)` and `db.getExpiredLossByStoreArea(ORG)` return aggregated loss reports | worker-equivalent-exists | The Worker disposition test covers both loss-by-sku and loss-by-store-area aggregations at the DB level | PROPOSED: worker-equivalent-exists — Worker database.disposition.pglite.node.test.ts:354-358 asserts loss report aggregations |
| ReportController returns monthly expiry reports through the organization-scoped service | migrated-controllers.test.ts:872 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.report.pglite.node.test.ts) — no Worker test was checked for monthly expiry report response shape | retire | Express controller wiring with no Worker analogue | PROPOSED: retire — Express controller wiring has no Worker analogue |
| ReportController returns the existing update-statuses success response | migrated-controllers.test.ts:889 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test covers update-all-markdown-statuses | retire | Express controller wiring with no Worker analogue | PROPOSED: retire — Express controller wiring has no Worker analogue |
| ReportController returns validation responses for invalid timeFrame values (400) | migrated-controllers.test.ts:907 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test asserts 400 for invalid timeFrame | retire | Express controller validation wiring | PROPOSED: retire — Express controller validation wiring has no Worker analogue |
| ReportController passes valid items-by-user timeframes to the report service | migrated-controllers.test.ts:925 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test covers items-by-user report | retire | Express controller wiring with no Worker analogue | PROPOSED: retire — Express controller wiring has no Worker analogue |
| ReportController passes unexpected report failures to error middleware | migrated-controllers.test.ts:943 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — Worker uses try/catch returning Response, not Express `next(error)` | retire | Express error middleware wiring | PROPOSED: retire — Express next(error) error middleware wiring has no Worker analogue |
| HealthController returns healthy status when tier flags are valid and database responds (200) | migrated-controllers.test.ts:1014 | — | workers/src/health.test.ts:61 — returns 200 OK for /health with `status: 'healthy'`; workers/src/health.test.ts:165 — passes the database check when the readiness query returns a row | worker-equivalent-exists | The Worker covers the same healthy-status response with database check | PROPOSED: worker-equivalent-exists — Worker health.test.ts:61 and :165 assert healthy status with database check |
| HealthController returns not ready before tier flag validation succeeds (503) | migrated-controllers.test.ts:1041 | negative/error | none found (searched: workers/src/health.test.ts) — Worker health endpoint returns 200 healthy; no 503 "not ready" response with tier flag validation; health.test.ts:171 marks degraded (not 503 not-ready) | retire | Express-specific tier flag validation readiness check; Worker health endpoint has no tier-flag-gated readiness state | PROPOSED: retire — Express tier-flag readiness check has no Worker analogue |
| HealthController returns database metrics errors with the existing response shape (500) | migrated-controllers.test.ts:1057 | negative/error | workers/src/health.test.ts:171 — marks degraded when the database query throws; workers/src/health.test.ts:191 — redacts credentials from database errors | worker-equivalent-exists | The Worker covers database error handling in the health check (degraded status, credential redaction) | PROPOSED: worker-equivalent-exists — Worker health.test.ts:171 and :191 assert database error handling in health check |
| UserController returns users through the organization-scoped service | migrated-controllers.test.ts:1078 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts user list response shape | retire | Express controller wiring with no Worker analogue | PROPOSED: retire — Express controller wiring has no Worker analogue |
| UserController returns validation responses for invalid user ids (400) | migrated-controllers.test.ts:1095 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test asserts 400 for invalid user ID | retire | Express controller validation wiring | PROPOSED: retire — Express controller validation wiring has no Worker analogue |
| UserController blocks access to users from another organization (403) | migrated-controllers.test.ts:1113 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — multi-tenant-isolation.test.ts:16-66 are placeholder tests (assert `true`); database.markdown-credit-context.pglite.node.test.ts:63 tests cross-org product isolation, not user cross-org access; no Worker test asserts 403 for cross-org user access | worker-shaped-rewrite | The tenant-isolation gate must be preserved; no Worker test covers cross-org user access blocking; the behaviour must be re-tested against the Worker route model | PROPOSED: worker-shaped-rewrite — cross-org user access blocking untested in Worker; re-test against Worker route model |
| UserController creates users only when pin, role, and organization context are present | migrated-controllers.test.ts:1130 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — no Worker test covers user creation with pin/role/org validation | retire | Express controller wiring with no Worker analogue | PROPOSED: retire — Express controller wiring has no Worker analogue |
| UserController passes unexpected user service failures to error middleware | migrated-controllers.test.ts:1153 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — Worker uses try/catch returning Response, not Express `next(error)` | retire | Express error middleware wiring | PROPOSED: retire — Express next(error) error middleware wiring has no Worker analogue |
| OrgBootstrapController bootstraps Clerk users with generated organization defaults (201) | migrated-controllers.test.ts:1170 | — | workers/src/clerk/bootstrap-handler.node.test.ts:207 — creates a new org, trial subscription, and first admin for a brand-new user; asserts 201 with `isNewOrg: true, isNewUser: true, isFirstAdmin: true` | worker-equivalent-exists | The Worker covers the same bootstrap flow with org creation, trial subscription, and first admin | PROPOSED: worker-equivalent-exists — Worker bootstrap-handler.node.test.ts:207 asserts new-org bootstrap flow |
| OrgBootstrapController rejects bootstrap requests without Clerk authentication context (401) | migrated-controllers.test.ts:1215 | authorization-precedence | workers/src/health.test.ts:1363 — returns 401 when organization bootstrap is called without a bearer token | worker-equivalent-exists | The Worker covers the same auth-required rejection for bootstrap | PROPOSED: worker-equivalent-exists — Worker health.test.ts:1363 asserts 401 for bootstrap without auth |
| OrgBootstrapController seeds demo data for the authenticated organization | migrated-controllers.test.ts:1231 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — no Worker test covers demo data seeding | retire | Express controller wiring for demo data seeding; no Worker test covers this | PROPOSED: retire — Express demo-data seeding has no Worker analogue |

---

### migrate-entrypoint.test.ts

Behavioral test for the Express `migrate.ts` entrypoint script. Spawns the script as a
subprocess (`spawnSync` with `ts-node/register`) against a throwaway SQLite file and asserts
the `migrations` and `products` tables were created. This is Express/SQLite-specific migration
machinery — the Worker uses Neon Postgres migrations applied via `psql`/Neon tooling, not a
Node.js SQLite migration script.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Runs migrations end-to-end when executed directly (spawnSync, SQLite tables created) | migrate-entrypoint.test.ts:27 | — | none found (searched: workers/src/credit-scoped-markdown-migration.pglite.node.test.ts, workers/src/workers-deployment.test.ts, workers/src/health.test.ts) — credit-scoped-markdown-migration.pglite.node.test.ts:42 tests a specific Neon migration (0008) against pglite, not the migration entrypoint script; workers-deployment.test.ts:24 is a deployment health smoke test; no Worker test covers a migration entrypoint script | retire | Express/SQLite migration entrypoint (`migrate.ts` with `ts-node`, `better-sqlite3`) has no Worker analogue; the Worker uses Neon Postgres migrations via pglite/psql, not a Node.js SQLite script | PROPOSED: retire — Express SQLite migration entrypoint machinery has no Worker analogue |

---

### migration-legacy-store-areas.regression-1.test.ts

Regression test for SQLite migration 012 (store-area hierarchy). Uses `better-sqlite3`
in-memory database with the Express `MigrationService` to create a legacy `store_areas` table
(without `organization_id` or `parent_id` columns), runs migration 012, and asserts the new
columns are added with null values for legacy rows. This is Express/SQLite-specific migration
machinery — the Worker's Postgres schema already includes `organization_id` and `parent_id` on
`store_areas` (pglite-db.ts:135).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Upgrades a legacy store_areas table without inventing tenant ownership (migration 012) | migration-legacy-store-areas.regression-1.test.ts:17 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/database.conformance.node.test.ts, workers/src/credit-scoped-markdown-migration.pglite.node.test.ts) — database.store-walk.pglite.node.test.ts:76 tests migration 0004 (store walk bay tracking), not migration 012 (store-area hierarchy); database.conformance.node.test.ts:244-303 inserts store_areas with `organization_id` and `parent_id` already present, not a legacy upgrade; no Worker test covers a legacy store_areas migration from a pre-tenant schema | retire | Express/SQLite migration 012 with `MigrationService` and `better-sqlite3` has no Worker analogue; the Worker's Postgres schema already includes `organization_id` and `parent_id` columns natively (pglite-db.ts:135), so there is no legacy upgrade path to test | PROPOSED: retire — Express SQLite migration 012 legacy upgrade has no Worker analogue; Postgres schema includes columns natively |

---

### monitoring-startup.test.ts

Tests for Express-specific monitoring services (`ApplicationMonitoringService`,
`DatabaseMonitoringService`) — singleton initialization, lifecycle (start/stop monitoring),
EventEmitter-based alert handling, Express `requestTrackingMiddleware`, process signal handler
registration (SIGTERM/SIGINT/uncaughtException/unhandledRejection), graceful degradation,
and resilience. No Worker equivalent exists: `grep` for `ApplicationMonitoring`,
`DatabaseMonitoring`, `MonitoringService`, `startMonitoring`, `stopMonitoring`, or
`requestTrackingMiddleware` in `workers/src/` returned zero matches. The Worker uses
Cloudflare's built-in observability and `health.test.ts` for health checks, not a custom
monitoring service singleton.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Initializes ApplicationMonitoringService without throwing | monitoring-startup.test.ts:33 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — no Worker `ApplicationMonitoringService` exists (grep returned zero matches) | retire | Express monitoring service singleton initialization has no Worker analogue | PROPOSED: retire — Express ApplicationMonitoringService singleton has no Worker analogue |
| Initializes DatabaseMonitoringService without throwing | monitoring-startup.test.ts:39 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — no Worker `DatabaseMonitoringService` exists (grep returned zero matches) | retire | Express monitoring service singleton initialization has no Worker analogue | PROPOSED: retire — Express DatabaseMonitoringService singleton has no Worker analogue |
| Returns singleton instances for both monitoring services | monitoring-startup.test.ts:45 | — | none found (searched: workers/src/) — no Worker monitoring service singleton pattern exists | retire | Express singleton pattern has no Worker analogue | PROPOSED: retire — Express monitoring singleton pattern has no Worker analogue |
| Both monitoring services inherit from EventEmitter for alert handling | monitoring-startup.test.ts:55 | — | none found (searched: workers/src/) — no Worker EventEmitter-based alerting exists | retire | Express EventEmitter-based alerting has no Worker analogue | PROPOSED: retire — Express EventEmitter alerting has no Worker analogue |
| Starts monitoring when startMonitoring is called | monitoring-startup.test.ts:62 | — | none found (searched: workers/src/) — no Worker `startMonitoring` method exists (grep returned zero matches) | retire | Express monitoring lifecycle has no Worker analogue | PROPOSED: retire — Express startMonitoring lifecycle has no Worker analogue |
| Stops monitoring when stopMonitoring is called | monitoring-startup.test.ts:67 | — | none found (searched: workers/src/) — no Worker `stopMonitoring` method exists (grep returned zero matches) | retire | Express monitoring lifecycle has no Worker analogue | PROPOSED: retire — Express stopMonitoring lifecycle has no Worker analogue |
| Does not throw when stopping already stopped monitoring | monitoring-startup.test.ts:73 | negative/error | none found (searched: workers/src/) — no Worker monitoring lifecycle exists | retire | Express monitoring lifecycle error handling has no Worker analogue | PROPOSED: retire — Express monitoring stop-idempotency has no Worker analogue |
| Resets metrics when stopMonitoring is called with reset=true | monitoring-startup.test.ts:79 | — | none found (searched: workers/src/) — no Worker monitoring metrics reset exists | retire | Express monitoring metrics reset has no Worker analogue | PROPOSED: retire — Express monitoring metrics reset has no Worker analogue |
| Emits alert events that can be listened to | monitoring-startup.test.ts:89 | — | none found (searched: workers/src/) — no Worker EventEmitter-based alert system exists | retire | Express alert event emission has no Worker analogue | PROPOSED: retire — Express alert event emission has no Worker analogue |
| Does not crash when alert handler throws exception | monitoring-startup.test.ts:115 | negative/error | none found (searched: workers/src/) — no Worker alert handler exists | retire | Express alert handler error resilience has no Worker analogue | PROPOSED: retire — Express alert handler error resilience has no Worker analogue |
| Handles exceptions during metrics collection | monitoring-startup.test.ts:141 | negative/error | none found (searched: workers/src/) — no Worker metrics collection exists | retire | Express metrics collection error handling has no Worker analogue | PROPOSED: retire — Express metrics collection error handling has no Worker analogue |
| Continues functioning after middleware exception | monitoring-startup.test.ts:163 | negative/error | none found (searched: workers/src/) — no Worker `requestTrackingMiddleware` exists (grep returned zero matches) | retire | Express middleware error resilience has no Worker analogue | PROPOSED: retire — Express requestTrackingMiddleware has no Worker analogue |
| Handles database monitoring failures gracefully | monitoring-startup.test.ts:192 | negative/error | none found (searched: workers/src/) — no Worker `DatabaseMonitoringService` exists | retire | Express database monitoring error handling has no Worker analogue | PROPOSED: retire — Express database monitoring error handling has no Worker analogue |
| Allows process handlers (SIGTERM) to be registered without conflicts | monitoring-startup.test.ts:217 | — | none found (searched: workers/src/) — Cloudflare Workers has no process signal handler API | retire | Node.js process signal handler registration has no Worker analogue (Cloudflare Workers runtime) | PROPOSED: retire — Node.js SIGTERM handler registration has no Worker analogue |
| Supports uncaughtException handler registration | monitoring-startup.test.ts:229 | — | none found (searched: workers/src/) — Cloudflare Workers has no `uncaughtException` event | retire | Node.js uncaughtException handler has no Worker analogue | PROPOSED: retire — Node.js uncaughtException handler has no Worker analogue |
| Supports unhandledRejection handler registration | monitoring-startup.test.ts:240 | — | none found (searched: workers/src/) — Cloudflare Workers has no `unhandledRejection` event | retire | Node.js unhandledRejection handler has no Worker analogue | PROPOSED: retire — Node.js unhandledRejection handler has no Worker analogue |
| Supports SIGINT handler registration | monitoring-startup.test.ts:251 | — | none found (searched: workers/src/) — Cloudflare Workers has no SIGINT event | retire | Node.js SIGINT handler has no Worker analogue | PROPOSED: retire — Node.js SIGINT handler has no Worker analogue |
| Handles exceptions in process handlers gracefully | monitoring-startup.test.ts:262 | negative/error | none found (searched: workers/src/) — Cloudflare Workers has no process handler API | retire | Node.js process handler error handling has no Worker analogue | PROPOSED: retire — Node.js process handler error handling has no Worker analogue |
| Returns zero metrics when monitoring not started | monitoring-startup.test.ts:282 | — | none found (searched: workers/src/) — no Worker monitoring metrics exist | retire | Express monitoring default metrics has no Worker analogue | PROPOSED: retire — Express monitoring default metrics has no Worker analogue |
| Does not crash when accessing metrics during startup race condition | monitoring-startup.test.ts:288 | concurrency | none found (searched: workers/src/) — no Worker monitoring service exists; Cloudflare Workers handles concurrency at the runtime level | retire | Express monitoring startup race condition handling has no Worker analogue | PROPOSED: retire — Express monitoring startup race condition has no Worker analogue |
| Handles concurrent metric updates safely | monitoring-startup.test.ts:298 | concurrency | none found (searched: workers/src/) — no Worker `requestTrackingMiddleware` exists; Worker concurrency is handled by the runtime, not middleware | retire | Express middleware concurrency has no Worker analogue | PROPOSED: retire — Express middleware concurrency has no Worker analogue |
| Recovers from getMetrics errors gracefully | monitoring-startup.test.ts:337 | negative/error | none found (searched: workers/src/) — no Worker monitoring service exists | retire | Express monitoring error recovery has no Worker analogue | PROPOSED: retire — Express monitoring error recovery has no Worker analogue |
| Maintains alerting capability after multiple start/stop cycles | monitoring-startup.test.ts:360 | — | none found (searched: workers/src/) — no Worker monitoring lifecycle exists | retire | Express monitoring lifecycle resilience has no Worker analogue | PROPOSED: retire — Express monitoring lifecycle resilience has no Worker analogue |
| Handles large metric volumes without memory leak | monitoring-startup.test.ts:385 | — | none found (searched: workers/src/) — no Worker monitoring metrics aggregation exists | retire | Express monitoring memory management has no Worker analogue | PROPOSED: retire — Express monitoring memory management has no Worker analogue |

---

### organization.repository.test.ts

Unit tests for the Express `OrganizationRepository` using mocked Prisma. Tests cover
`findById`, `update`, `findCreationLockById` (creation lock state), and `deleteCascade`
(transactional cascade delete of all org-related data). No Worker `OrganizationRepository`
or cascade-delete equivalent exists: `grep` for `deleteCascade`, `findCreationLock`,
`isCreationLocked`, or `OrganizationRepository` in `workers/src/` returned zero matches.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Finds an organization by id | organization.repository.test.ts:69 | — | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/database.ts, workers/src/clerk/clerk-persistence.ts) — clerk-persistence.ts:135 and bootstrap-handler.ts:289 query `FROM organizations` by `clerk_organization_id`, not by internal `id`; no Worker test asserts organization lookup by internal id via a repository | retire | Express Prisma repository method with no Worker analogue; the Worker queries organizations inline in handlers, not via a repository | PROPOSED: retire — Express Prisma OrganizationRepository.findById has no Worker analogue |
| Updates only provided organization fields | organization.repository.test.ts:80 | — | none found (searched: workers/src/database.ts, workers/src/clerk/clerk-persistence.ts) — no Worker test or code path asserts organization update with partial fields via a repository | retire | Express Prisma repository update with no Worker analogue | PROPOSED: retire — Express Prisma OrganizationRepository.update has no Worker analogue |
| Selects organization creation lock state by id | organization.repository.test.ts:91 | feature-limits | none found (searched: workers/src/) — no Worker `isCreationLocked` or `findCreationLock` exists (grep returned zero matches); the creation-lock feature is Express-specific | retire | Express creation-lock feature has no Worker analogue; the feature-limits gate is preserved but the feature itself is Express-only | PROPOSED: retire — Express creation-lock feature has no Worker analogue |
| Deletes organization data in a transaction before deleting the organization (cascade) | organization.repository.test.ts:103 | tenant-isolation | none found (searched: workers/src/) — no Worker `deleteCascade` or cascade-delete equivalent exists (grep returned zero matches); no Worker test covers transactional org deletion with dependent-table cleanup | retire | Express Prisma `$transaction` cascade delete has no Worker analogue; the Worker has no org-deletion endpoint or cascade-delete path | PROPOSED: retire — Express Prisma cascade-delete has no Worker analogue |

---

### organization.service.test.ts

Unit tests for the Express `OrganizationService` using mocked Prisma and repository injection.
Tests cover `getOrganization` (found/null), `updateOrganization` (name/slug/both/P2025 not-found/
other errors), `deleteOrganization` (hard delete + cache invalidation/P2025/other errors), and
repository injection (delegation to `findById`/`deleteCascade` + cache invalidation). No Worker
`OrganizationService` or org-update/delete endpoint exists (confirmed by grep in the previous
section — no `deleteCascade`, `OrganizationRepository`, or org-deletion path in `workers/src/`).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns organization if found | organization.service.test.ts:51 | — | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/database.ts, workers/src/clerk/clerk-persistence.ts) — no Worker `OrganizationService.getOrganization` exists; org lookups are inline in handlers (bootstrap-handler.ts:289, clerk-persistence.ts:135) | retire | Express service method with no Worker analogue | PROPOSED: retire — Express OrganizationService.getOrganization has no Worker analogue |
| Returns null if organization not found | organization.service.test.ts:73 | negative/error | none found (searched: workers/src/) — no Worker test asserts null return for missing organization via a service | retire | Express service not-found handling with no Worker analogue | PROPOSED: retire — Express OrganizationService not-found handling has no Worker analogue |
| Updates organization name successfully | organization.service.test.ts:83 | — | none found (searched: workers/src/) — no Worker org-update endpoint or service exists | retire | Express service update with no Worker analogue | PROPOSED: retire — Express OrganizationService.updateOrganization has no Worker analogue |
| Updates organization slug successfully | organization.service.test.ts:106 | — | none found (searched: workers/src/) — no Worker org-update endpoint or service exists | retire | Express service update with no Worker analogue | PROPOSED: retire — Express OrganizationService slug update has no Worker analogue |
| Updates both name and slug successfully | organization.service.test.ts:129 | — | none found (searched: workers/src/) — no Worker org-update endpoint or service exists | retire | Express service update with no Worker analogue | PROPOSED: retire — Express OrganizationService combined update has no Worker analogue |
| Returns null if organization not found during update (Prisma P2025) | organization.service.test.ts:157 | negative/error | none found (searched: workers/src/) — no Worker org-update endpoint exists; Prisma P2025 error handling is Express-specific | retire | Express Prisma P2025 error handling with no Worker analogue | PROPOSED: retire — Express Prisma P2025 not-found handling has no Worker analogue |
| Throws error for other database errors during update | organization.service.test.ts:170 | negative/error | none found (searched: workers/src/) — no Worker org-update endpoint exists | retire | Express service error propagation with no Worker analogue | PROPOSED: retire — Express OrganizationService error propagation has no Worker analogue |
| Hard deletes organization and invalidates subscription cache | organization.service.test.ts:181 | — | none found (searched: workers/src/) — no Worker org-deletion endpoint or subscription cache invalidation exists | retire | Express service deletion + cache invalidation with no Worker analogue | PROPOSED: retire — Express OrganizationService deletion + cache invalidation has no Worker analogue |
| Returns false when organization does not exist during delete (P2025) | organization.service.test.ts:193 | negative/error | none found (searched: workers/src/) — no Worker org-deletion endpoint exists | retire | Express Prisma P2025 not-found handling with no Worker analogue | PROPOSED: retire — Express OrganizationService P2025 delete handling has no Worker analogue |
| Throws for unexpected deletion errors | organization.service.test.ts:206 | negative/error | none found (searched: workers/src/) — no Worker org-deletion endpoint exists | retire | Express service error propagation with no Worker analogue | PROPOSED: retire — Express OrganizationService delete error propagation has no Worker analogue |
| Uses the injected repository for organization reads | organization.service.test.ts:220 | — | none found (searched: workers/src/) — no Worker `OrganizationService` or repository-injection pattern exists | retire | Express service/repository DI pattern with no Worker analogue | PROPOSED: retire — Express service/repository DI has no Worker analogue |
| Invalidates subscription cache after repository deletion succeeds | organization.service.test.ts:244 | — | none found (searched: workers/src/) — no Worker `deleteCascade` or subscription cache invalidation exists | retire | Express service cache invalidation with no Worker analogue | PROPOSED: retire — Express subscription cache invalidation has no Worker analogue |

---

### organization-invite.routes.test.ts

Integration tests for Express organization-invite routes using `supertest` with mocked
middleware (auth, `requireOrgRole`, rate limiter, validation) and mocked services
(`OrganizationInviteService`, `OrganizationService`, `EmailService`). Tests cover POST
`/organization/invites` (create + email), POST `/organization/invites/accept` (Clerk auth),
GET `/organization/invites` (list pending), DELETE `/organization/invites/:inviteId` (revoke),
and DELETE `/organization` (delete org). No Worker organization-invite equivalent exists:
`grep` for `organizationInvite`, `invite.*token`, `acceptInvite`, `createInvite`,
`revokeInvite`, or `listPendingInvite` in `workers/src/` returned zero matches.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Creates an invite and sends invite email when request context is valid (201) | organization-invite.routes.test.ts:138 | — | none found (searched: workers/src/) — no Worker invite endpoint exists (grep returned zero matches) | retire | Express invite route with email service has no Worker analogue | PROPOSED: retire — Express organization-invite creation has no Worker analogue |
| Returns 401 when organization context is missing (POST invites) | organization-invite.routes.test.ts:172 | authorization-precedence | none found (searched: workers/src/) — no Worker invite endpoint exists; Worker auth middleware rejects before the handler but no test covers this for invites | retire | Express route auth gate with no Worker invite analogue | PROPOSED: retire — Express invite route auth gate has no Worker analogue |
| Returns 404 when organization cannot be loaded (POST invites) | organization-invite.routes.test.ts:183 | negative/error | none found (searched: workers/src/) — no Worker invite endpoint exists | retire | Express route not-found handling with no Worker invite analogue | PROPOSED: retire — Express invite route not-found handling has no Worker analogue |
| Maps known base errors to status code and error code payload (POST invites, 400) | organization-invite.routes.test.ts:197 | negative/error | none found (searched: workers/src/) — no Worker invite endpoint exists | retire | Express route error mapping with no Worker invite analogue | PROPOSED: retire — Express invite route error mapping has no Worker analogue |
| Returns 500 for unexpected invite creation failures | organization-invite.routes.test.ts:213 | negative/error | none found (searched: workers/src/) — no Worker invite endpoint exists | retire | Express route error handling with no Worker invite analogue | PROPOSED: retire — Express invite route 500 handling has no Worker analogue |
| Returns 401 when Clerk auth context is missing (POST accept) | organization-invite.routes.test.ts:228 | authorization-precedence | none found (searched: workers/src/) — no Worker invite-accept endpoint exists | retire | Express route Clerk auth gate with no Worker invite analogue | PROPOSED: retire — Express invite-accept auth gate has no Worker analogue |
| Accepts invite for authenticated Clerk user (200) | organization-invite.routes.test.ts:238 | — | none found (searched: workers/src/) — no Worker invite-accept endpoint exists | retire | Express invite-accept route with no Worker analogue | PROPOSED: retire — Express invite-accept route has no Worker analogue |
| Maps known accept errors to structured error response (404) | organization-invite.routes.test.ts:256 | negative/error | none found (searched: workers/src/) — no Worker invite-accept endpoint exists | retire | Express route error mapping with no Worker invite analogue | PROPOSED: retire — Express invite-accept error mapping has no Worker analogue |
| Returns 500 for unexpected invite acceptance failures | organization-invite.routes.test.ts:272 | negative/error | none found (searched: workers/src/) — no Worker invite-accept endpoint exists | retire | Express route error handling with no Worker invite analogue | PROPOSED: retire — Express invite-accept 500 handling has no Worker analogue |
| Returns 401 when organization context is missing (GET invites) | organization-invite.routes.test.ts:287 | authorization-precedence | none found (searched: workers/src/) — no Worker invite-list endpoint exists | retire | Express route auth gate with no Worker invite analogue | PROPOSED: retire — Express invite-list auth gate has no Worker analogue |
| Returns pending invites for the requester organization (200) | organization-invite.routes.test.ts:295 | — | none found (searched: workers/src/) — no Worker invite-list endpoint exists | retire | Express invite-list route with no Worker analogue | PROPOSED: retire — Express invite-list route has no Worker analogue |
| Returns 500 for unexpected pending-invite lookup failures | organization-invite.routes.test.ts:303 | negative/error | none found (searched: workers/src/) — no Worker invite-list endpoint exists | retire | Express route error handling with no Worker invite analogue | PROPOSED: retire — Express invite-list 500 handling has no Worker analogue |
| Maps known pending-invite errors to structured error response (400) | organization-invite.routes.test.ts:312 | negative/error | none found (searched: workers/src/) — no Worker invite-list endpoint exists | retire | Express route error mapping with no Worker invite analogue | PROPOSED: retire — Express invite-list error mapping has no Worker analogue |
| Returns 401 when revoke is requested without organization context (DELETE invite) | organization-invite.routes.test.ts:326 | authorization-precedence | none found (searched: workers/src/) — no Worker invite-revoke endpoint exists | retire | Express route auth gate with no Worker invite analogue | PROPOSED: retire — Express invite-revoke auth gate has no Worker analogue |
| Revokes invite for a valid organization context (200) | organization-invite.routes.test.ts:334 | — | none found (searched: workers/src/) — no Worker invite-revoke endpoint exists | retire | Express invite-revoke route with no Worker analogue | PROPOSED: retire — Express invite-revoke route has no Worker analogue |
| Maps known revoke errors to structured error response (400) | organization-invite.routes.test.ts:345 | negative/error | none found (searched: workers/src/) — no Worker invite-revoke endpoint exists | retire | Express route error mapping with no Worker invite analogue | PROPOSED: retire — Express invite-revoke error mapping has no Worker analogue |
| Returns 500 for unexpected revoke failures | organization-invite.routes.test.ts:361 | negative/error | none found (searched: workers/src/) — no Worker invite-revoke endpoint exists | retire | Express route error handling with no Worker invite analogue | PROPOSED: retire — Express invite-revoke 500 handling has no Worker analogue |
| Returns 401 when organization context is missing (DELETE org) | organization-invite.routes.test.ts:374 | authorization-precedence | none found (searched: workers/src/) — no Worker org-deletion endpoint exists | retire | Express route auth gate with no Worker org-deletion analogue | PROPOSED: retire — Express org-deletion auth gate has no Worker analogue |
| Returns 404 when organization does not exist (DELETE org) | organization-invite.routes.test.ts:382 | negative/error | none found (searched: workers/src/) — no Worker org-deletion endpoint exists | retire | Express route not-found handling with no Worker org-deletion analogue | PROPOSED: retire — Express org-deletion not-found handling has no Worker analogue |
| Returns 200 when organization deletion succeeds | organization-invite.routes.test.ts:391 | — | none found (searched: workers/src/) — no Worker org-deletion endpoint exists | retire | Express org-deletion route with no Worker analogue | PROPOSED: retire — Express org-deletion route has no Worker analogue |
| Returns 500 for unexpected organization deletion failures | organization-invite.routes.test.ts:399 | negative/error | none found (searched: workers/src/) — no Worker org-deletion endpoint exists | retire | Express route error handling with no Worker org-deletion analogue | PROPOSED: retire — Express org-deletion 500 handling has no Worker analogue |
| Maps known organization deletion errors to structured payload (400) | organization-invite.routes.test.ts:408 | negative/error | none found (searched: workers/src/) — no Worker org-deletion endpoint exists | retire | Express route error mapping with no Worker org-deletion analogue | PROPOSED: retire — Express org-deletion error mapping has no Worker analogue |

---

### organization-invite.service.test.ts

Unit tests for the Express `OrganizationInviteService` using mocked Prisma. Tests cover
`createInvite` (under user limit, user limit reached, already pending), `acceptInvite`
(valid + user creation, expired invite marking, not found), `listPendingInvites`, and
`revokeInvite` (pending, non-pending rejection). No Worker organization-invite service
exists (confirmed by grep in the previous section — no `organizationInvite`, `acceptInvite`,
`createInvite`, `revokeInvite`, or `listPendingInvite` in `workers/src/`).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Creates an invite when under user limit | organization-invite.service.test.ts:49 | feature-limits | none found (searched: workers/src/) — no Worker invite service exists (grep returned zero matches) | retire | Express invite service with user-limit check has no Worker analogue | PROPOSED: retire — Express OrganizationInviteService.createInvite has no Worker analogue |
| Throws when user limit reached (PaymentRequiredError) | organization-invite.service.test.ts:93 | feature-limits | none found (searched: workers/src/) — no Worker invite service exists; the feature-limits gate is preserved but the invite feature is Express-only | retire | Express invite service user-limit enforcement has no Worker analogue | PROPOSED: retire — Express invite user-limit enforcement has no Worker analogue |
| Throws when invite already pending (ConflictError) | organization-invite.service.test.ts:108 | negative/error | none found (searched: workers/src/) — no Worker invite service exists | retire | Express invite service duplicate-pending check has no Worker analogue | PROPOSED: retire — Express invite duplicate-pending check has no Worker analogue |
| Accepts a valid invite and creates user | organization-invite.service.test.ts:127 | — | none found (searched: workers/src/) — no Worker invite-accept service exists | retire | Express invite-accept service with user creation has no Worker analogue | PROPOSED: retire — Express OrganizationInviteService.acceptInvite has no Worker analogue |
| Marks expired invites as expired (ValidationError) | organization-invite.service.test.ts:173 | negative/error | none found (searched: workers/src/) — no Worker invite service exists | retire | Express invite expiry handling has no Worker analogue | PROPOSED: retire — Express invite expiry handling has no Worker analogue |
| Throws when invite not found (NotFoundError) | organization-invite.service.test.ts:198 | negative/error | none found (searched: workers/src/) — no Worker invite service exists | retire | Express invite not-found handling has no Worker analogue | PROPOSED: retire — Express invite not-found handling has no Worker analogue |
| Returns pending invites for organization | organization-invite.service.test.ts:212 | — | none found (searched: workers/src/) — no Worker invite-list service exists | retire | Express invite-list service with no Worker analogue | PROPOSED: retire — Express OrganizationInviteService.listPendingInvites has no Worker analogue |
| Revokes a pending invite | organization-invite.service.test.ts:226 | — | none found (searched: workers/src/) — no Worker invite-revoke service exists | retire | Express invite-revoke service with no Worker analogue | PROPOSED: retire — Express OrganizationInviteService.revokeInvite has no Worker analogue |
| Rejects revoke for non-pending invites (ValidationError) | organization-invite.service.test.ts:238 | negative/error | none found (searched: workers/src/) — no Worker invite-revoke service exists | retire | Express invite-revoke state validation has no Worker analogue | PROPOSED: retire — Express invite-revoke non-pending rejection has no Worker analogue |

---

### organization-invite-security.test.ts

Security tests for the Express `OrganizationInviteService` using a real Prisma/SQLite database
(`getDefaultDatabaseClient`). Tests cover token security: plain-text tokens are not stored in
the database (only bcrypt hashes), token hashes are cleared on accept (one-time use) and
revoke, legacy token format backward compatibility, and expired invite marking with hash
clearing. No Worker organization-invite service exists (confirmed by grep in the previous
sections — no `organizationInvite`, `acceptInvite`, `createInvite`, or `revokeInvite` in
`workers/src/`).

**Verified during review — the retire calls below rest on positive evidence, not merely on
absence.** Invites are **Clerk-managed by design**, not unbuilt work pending in the Worker:
`backend/src/tests/contract/organization-invites-clerk-only.test.ts:5` asserts that
`POST /api/organizations/invites` returns **404** — the custom invite endpoint is deliberately
not exposed. The only occurrence of "invite" in `workers/src/` is the marketing feature label
`'Organization invites'` in the professional tier at `workers/src/index-minimal.ts:2941`; it is
a tier-features string, not an implementation. Retiring these five rows therefore does not drop
a security property — bcrypt invite-token handling is legacy Express machinery superseded by
Clerk, which the Worker already authenticates against. The `penetration` gate is recorded on
the rows so the decision stays visible at review.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Does not store plain text tokens in database (only bcrypt hashes) | organization-invite-security.test.ts:42 | penetration | none found (searched: workers/src/) — no Worker invite service exists (grep returned zero matches); the penetration gate is preserved but the invite-token security feature is Express-only | retire | Express invite token hashing with bcrypt has no Worker analogue; the penetration gate is preserved but the feature is Express-only | PROPOSED: retire — Express invite token hashing has no Worker analogue |
| Clears token hash when invite is accepted (one-time use) | organization-invite-security.test.ts:68 | penetration | none found (searched: workers/src/) — no Worker invite service exists | retire | Express invite one-time-use token clearing has no Worker analogue | PROPOSED: retire — Express invite one-time-use token clearing has no Worker analogue |
| Clears token hash when invite is revoked | organization-invite-security.test.ts:95 | penetration | none found (searched: workers/src/) — no Worker invite service exists | retire | Express invite revoke token clearing has no Worker analogue | PROPOSED: retire — Express invite revoke token clearing has no Worker analogue |
| Accepts legacy token format for backward compatibility | organization-invite-security.test.ts:118 | — | none found (searched: workers/src/) — no Worker invite service exists | retire | Express invite legacy token backward compatibility has no Worker analogue | PROPOSED: retire — Express invite legacy token compatibility has no Worker analogue |
| Marks pending invites as EXPIRED when token is expired (and clears hash) | organization-invite-security.test.ts:151 | negative/error | none found (searched: workers/src/) — no Worker invite service exists | retire | Express invite expiry marking with hash clearing has no Worker analogue | PROPOSED: retire — Express invite expiry marking has no Worker analogue |

---

### org-audit.repository.test.ts

Unit tests for `OrgAuditRepository` with a **mocked Prisma client** (`orgAuditLog.create` /
`orgAuditLog.findMany` are `vi.fn()` stubs). No real database, no supertest — this is a pure
call-shape test verifying the repository issues the right Prisma `create`/`findMany` arguments
(nullable-field normalization, `metadata` serialized to a JSON string, date-range + pagination
filters). The Worker has a different `audit_log` table (inventory-action-scoped:
`organization_id`, `user_id`, `inventory_item_id`, `action`, `change_description` — see
`workers/src/database.ts:2389-2392`) with no org-member-event model and no test asserting any
audit-log write or read (grep for `audit_log`/`auditLog`/`OrgAudit` across all 44 Worker test
files returned zero matches in test files).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Creates audit events with normalized nullable fields and serialized metadata (Prisma create call shape) | org-audit.repository.test.ts:24 | — | none found (searched: workers/src/__tests__/org-status-integration.test.ts, workers/src/database.conformance.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — org-status-integration.test.ts:289 is a stub `expect(true).toBe(true)` with a comment mentioning "audit log" but no assertion; grep across all 44 Worker test files for `audit_log`/`auditLog`/`OrgAudit` returned zero matches in test files; the Worker's `audit_log` table (database.ts:2389) is inventory-action-scoped, not org-member-event-scoped, and is untested | retire | Pure Prisma call-shape test (mocked `orgAuditLog.create`) with no behavioural assertion beyond the Prisma argument shape; the `OrgAuditLog` Prisma model and its nullable-field normalization retire with the backend; the Worker's `audit_log` table is a different schema with no org-member-event analogue | PROPOSED: retire — Prisma `orgAuditLog.create` call-shape test has no Worker analogue (Worker `audit_log` is inventory-scoped, untested) |
| Finds organization audit events with filters and pagination defaults (Prisma findMany call shape) | org-audit.repository.test.ts:49 | — | none found (searched: workers/src/__tests__/org-status-integration.test.ts, workers/src/database.report.node.test.ts, workers/src/database.report.pglite.node.test.ts) — no Worker test queries an org-audit log; org-status-integration.test.ts:289 only mentions audit log in a comment on a stub test; the Worker's `audit_log` reads in database.ts:1306-1359 are inventory-scoped SQL with no test asserting org-level event retrieval or pagination | retire | Pure Prisma call-shape test (mocked `orgAuditLog.findMany`) verifying `where`/`orderBy`/`take`/`skip` arguments; the Prisma query shape retires with the backend; no Worker test asserts org-audit event retrieval with filters/pagination | PROPOSED: retire — Prisma `orgAuditLog.findMany` call-shape test has no Worker analogue (Worker `audit_log` reads are inventory-scoped, untested) |

---

### org-audit.service.test.ts

Unit tests for `OrgAuditService` with a **mocked Prisma client and a mocked repository**
(`orgAuditLog.create`/`findMany` and `repository.create`/`findByOrganization` are `vi.fn()`
stubs). No real database — this is a pure DI-wiring test verifying the service delegates to the
injected repository rather than calling Prisma directly (tsyringe DI container wiring). The
Worker has no service/repository layering and no DI container; it calls `database.ts` functions
directly. As established in the previous section, no Worker test references any org-audit log
(grep for `audit_log`/`auditLog`/`OrgAudit` across all 44 Worker test files returned zero
matches in test files).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Delegates emit to the injected repository (does not call Prisma directly) | org-audit.service.test.ts:23 | — | none found (searched: workers/src/__tests__/org-status-integration.test.ts, workers/src/handlers/handlers.test.ts, workers/src/utils/minimal-rate-limit.test.ts) — org-status-integration.test.ts:289 is a stub mentioning "audit log" in a comment with no assertion; handlers.test.ts and middleware.test.ts have no audit-emit delegation coverage; the Worker has no service/repository split and no DI container, so delegation wiring has no analogue | retire | Pure tsyringe DI delegation test (service → repository, not service → Prisma); the DI wiring retires with the backend; the Worker calls `database.ts` functions directly with no service layer | PROPOSED: retire — tsyringe DI service-to-repository delegation has no Worker analogue (Worker has no service layer or DI container) |
| Delegates emitWithClient to the repository with a provided transaction client | org-audit.service.test.ts:45 | — | none found (searched: workers/src/__tests__/org-status-integration.test.ts, workers/src/database.conformance.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — no Worker test covers transaction-scoped audit emit; the Worker's `database.ts` audit_log inserts (database.ts:2389) are inline CTEs within the inventory mutation SQL, not a delegated `emitWithClient(tx)` call; no DI delegation analogue exists | retire | Tests Prisma transaction-client delegation through the DI container; the Worker uses inline SQL CTEs for audit inserts (database.ts:2388-2393) with no transaction-client passing and no service delegation; the DI + transaction-client wiring retires with the backend | PROPOSED: retire — Prisma transaction-client delegation via DI has no Worker analogue (Worker uses inline SQL CTEs, no service layer) |
| Delegates organization audit queries to the repository (does not call Prisma directly) | org-audit.service.test.ts:73 | — | none found (searched: workers/src/__tests__/org-status-integration.test.ts, workers/src/database.report.node.test.ts, workers/src/database.report.pglite.node.test.ts) — no Worker test covers org-audit query delegation; org-status-integration.test.ts:289 is a stub; the Worker has no service/repository split and no org-audit query API; the DI delegation wiring has no analogue | retire | Pure tsyringe DI delegation test (service → repository.findByOrganization, not service → Prisma.findMany); the DI wiring retires with the backend; the Worker has no org-audit query API and no service layer | PROPOSED: retire — tsyringe DI query delegation has no Worker analogue (Worker has no org-audit query API or service layer) |

---

### platform-catalogue-shared.test.ts

Unit tests for **shared domain logic** in `shared/domain/platform-catalogue` (imported via
`../../../../shared/domain/platform-catalogue`). Pure function tests — no database, no HTTP,
no mocks. Two behaviours: `isPlatformAdminUser` (a platform-admin allowlist check used by the
Worker at `workers/src/index-minimal.ts:2107,2130,2144` via the same shared import) and
`toCatalogueSeedRunDto` (a DTO normalizer that the Worker does **not** import — grep for
`toCatalogueSeedRunDto`/`CatalogueSeedRun` across `workers/src/` returned zero matches). The
shared module survives the backend deletion; these backend-resident tests do not.

**Tracked independently as [#460](https://github.com/jatwell93/date-management-app/issues/460).**
This is a pre-existing production gap, not something the migration creates: `isPlatformAdminUser`
is the sole authorization gate on three live Worker routes (each returning
`403 Platform catalogue review access required`) plus two Clerk bootstrap call sites, and no test
in `workers/` references it at all. The two `authorization-precedence` rows below should be picked
up by Phase 3.2; the issue exists so the gap keeps independent life if this change stalls or is
descoped.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Fails closed for invalid/missing/empty/non-numeric allowlists (6 parameterized cases) | platform-catalogue-shared.test.ts:7 | authorization-precedence | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/authorized-parties.test.ts, workers/src/constants/roles.test.ts, workers/src/index-minimal.ts) — minimal-api-routes.test.ts:1046 sets `PLATFORM_ADMIN_USER_IDS: '7'` for a happy-path conflict test but never asserts fail-closed for invalid/empty allowlists; authorized-parties.test.ts:5 mentions "allowlist" in a CORS-origin comment, not platform-admin; require-role.test.ts and utils/auth.test.ts cover org-level roles, not the platform-admin allowlist; grep for `isPlatformAdminUser`/`PLATFORM_ADMIN_USER_IDS` across all 44 Worker test files returned only the minimal-api-routes.test.ts:1046 hit | worker-shaped-rewrite | The Worker uses this exact shared function (`isPlatformAdminUser` at index-minimal.ts:2107) so the fail-closed behaviour is live in the Worker, but no Worker test asserts it; the shared function survives deletion but this backend-resident test does not; the authorization-precedence gate must be re-tested in the Worker suite (pure function test of the shared module, or route-level test asserting non-platform-admins are rejected) | PROPOSED: worker-shaped-rewrite — `isPlatformAdminUser` fail-closed is live in the Worker (index-minimal.ts:2107) but untested; re-test the authorization-precedence gate in the Worker suite |
| Allows only a positive numeric user ID in an entirely valid list (rejects non-matching IDs) | platform-catalogue-shared.test.ts:18 | authorization-precedence | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/authorized-parties.test.ts, workers/src/constants/roles.test.ts, workers/src/index-minimal.ts) — minimal-api-routes.test.ts:1040-1060 tests a platform-correction conflict with a valid admin ID (`'7'`) but does not assert that a non-matching ID is rejected; no Worker test asserts the positive-match-only behaviour of `isPlatformAdminUser`; the function is used at index-minimal.ts:2107,2130,2144 but the accept/reject boundary is untested | worker-shaped-rewrite | The Worker uses this shared function at three route guards (index-minimal.ts:2107,2130,2144) so the allow-only-matching-ID behaviour is live, but no Worker test asserts the positive-match boundary; the authorization-precedence gate must be re-tested in the Worker suite | PROPOSED: worker-shaped-rewrite — `isPlatformAdminUser` positive-match boundary is live in the Worker but untested; re-test the authorization-precedence gate in the Worker suite |
| Normalizes SQL dates and numeric fields into the public catalogue-seed-run DTO | platform-catalogue-shared.test.ts:23 | — | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/database.catalogue-provenance.node.test.ts, workers/src/upload/catalogue-parser.test.ts) — catalogue-import-upsert.node.test.ts has no `seedRun`/`SeedRun`/`DTO`/`normaliz` matches (grep returned zero); catalogue-provenance.node.test.ts covers provenance records, not seed-run DTOs; catalogue-parser.test.ts covers CSV parsing, not DTO normalization; `toCatalogueSeedRunDto` is not imported anywhere in `workers/src/` (grep returned zero matches) | retire | The `toCatalogueSeedRunDto` normalizer is not used by the Worker (no import in `workers/src/`); the catalogue-seed-run DTO shape is Express-specific; the SQL-date/numeric normalization retires with the backend | PROPOSED: retire — `toCatalogueSeedRunDto` is not imported by the Worker; the seed-run DTO normalizer is Express-specific with no Worker analogue |

---

### prisma-config.test.ts

Unit test for **Prisma datasource configuration** — verifies that `prisma/prisma.config`
defaults `datasourceUrl` to `file:../database.sqlite` (the runtime SQLite database file) when
neither `DATABASE_URL` nor `NEON_CONNECTION_STRING` is set. Uses `vi.resetModules()` and env
var deletion/restore around a dynamic `import()` of the Prisma config module. This is
Express/Prisma bootstrap machinery: the Worker has no Prisma client, no SQLite file, and no
`datasourceUrl` concept — it connects to Neon Postgres via `NEON_CONNECTION_STRING` (or
Hyperdrive) directly from `database.ts`.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Defaults local Prisma datasource to the runtime SQLite database file when no env vars are set | prisma-config.test.ts:11 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:206-212 tests that the Worker's health check omits the database check when `NEON_CONNECTION_STRING` is empty and `DATABASE_URL` is undefined, and health.test.ts:216-221,301,308 test that API routes return 500 when database config is missing; these test config-missing error behaviour, not a datasource-URL default to a SQLite file; workers-deployment.test.ts has no `datasource`/`DATABASE_URL`/`SQLite`/`prisma` matches (grep returned zero); the Worker has no Prisma client, no `datasourceUrl`, and no SQLite file default | retire | Prisma datasource URL defaulting to `file:../database.sqlite` is Prisma-specific bootstrap machinery; the Worker has no Prisma, no SQLite, and no `datasourceUrl` concept (it uses `NEON_CONNECTION_STRING` directly); the SQLite default retires with the backend | PROPOSED: retire — Prisma SQLite datasource-URL default has no Worker analogue (Worker uses Neon Postgres directly, no Prisma, no SQLite) |

---

### product.repository.test.ts

Unit tests for `ProductRepository` with a **mocked Prisma client** (`product.findUnique`/`findMany`
are `vi.fn()` stubs). No real database — this is a pure call-shape test verifying the repository
issues the right Prisma queries (compound-key `findUnique` for SKU/barcode lookup, `findMany` with
`skip`/`orderBy` for excess products). The Worker has no Prisma and no repository layer; it calls
`database.ts` functions directly with raw SQL. The tenant-scoped lookup and duplicate-detection
behaviours are covered by the Worker's real-SQL catalogue-import-upsert suite; the Prisma call
shapes and the "excess products for deletion priority" feature have no Worker analogue.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Looks up products by SKU and barcode within an organization (returns `{ bySku, byBarcode }`) | product.repository.test.ts:37 | tenant-isolation | workers/src/__tests__/catalogue-import-upsert.node.test.ts:249 — the `classifies insert / update / unchanged / conflict` test seeds products with SKU+barcode (lines 250-253) and classifies CSV rows by looking up existing products by SKU and barcode within `ORG` via real SQL, covering the tenant-scoped dual-lookup behaviour; the Prisma compound-key `findUnique` call shape (asserted at product.repository.test.ts:43-58) has no Worker analogue (Worker uses raw SQL, not Prisma) | worker-equivalent-exists | The tenant-scoped SKU+barcode lookup behaviour is covered by the Worker's real-SQL import pipeline (catalogue-import-upsert.node.test.ts:249-296); the Prisma `findUnique` with `organizationId_sku`/`organizationId_barcode` compound keys retires with the backend (Worker has no Prisma, no repository layer) | PROPOSED: worker-equivalent-exists — tenant-scoped SKU+barcode lookup covered by catalogue-import-upsert.node.test.ts:249; Prisma compound-key call shape retires with the backend |
| Rejects ambiguous SKU and barcode matches to different products (throws "Duplicate identifiers detected") | product.repository.test.ts:61 | negative/error | workers/src/__tests__/catalogue-import-upsert.node.test.ts:261 — the conflict row `S3,Three,B4,3.00` (sku->S3, barcode->B4->S4, different products) is classified as a conflict; line 284 asserts the error report matches `/different existing products/`; lines 294-295 assert both existing products are left untouched | worker-equivalent-exists | The "reject when SKU and barcode resolve to different products" behaviour is directly covered by the Worker's real-SQL import pipeline (catalogue-import-upsert.node.test.ts:261,284,294-295); the Worker uses a conflict-classification SQL CTE instead of a Prisma `findFirstBySkuOrBarcode` throw, but the behaviour (reject ambiguous cross-product matches) is equivalent | PROPOSED: worker-equivalent-exists — duplicate-identifier rejection covered by catalogue-import-upsert.node.test.ts:261 (conflict classification + error report) |
| Finds excess products for deletion priority within an organization (findMany with skip/orderBy/include) | product.repository.test.ts:77 | — | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/database.worklist.pglite.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — catalogue-import-upsert.node.test.ts:348-364 tests import-time quota breach (fails on quota breach with no partial writes) but has no "excess products for deletion priority" concept; usage-limits-integration.test.ts is all stub tests (`expect(true).toBe(true)`) with no real assertions; database.worklist.pglite.node.test.ts covers the markdown worklist (expiry report), not product deletion; database.dashboard.pglite.node.test.ts:104 counts `totalProducts` for dashboard stats but does not find excess products beyond a threshold for deletion priority | retire | The `findExcessProductsByOrganization` method is a backend-specific feature for finding products beyond a count limit (ordered by `createdAt`, with `_count` of `inventoryItems`) for deletion prioritization; the Worker has import-time quota enforcement (catalogue-import-upsert.node.test.ts:348-364) but no "excess products for deletion priority" concept; the Prisma `findMany` call shape with `skip`/`orderBy`/`include: { _count }` retires with the backend | PROPOSED: retire — "excess products for deletion priority" is a backend-specific feature with no Worker analogue; Prisma findMany call shape retires with the backend |

---

### product.routes.test.ts

supertest Express route tests for `product.routes` with **mocked services, middleware, and
Prisma** — `ProductService` is a `vi.fn()` mock, `authenticateToken`/`checkUsageLimit`/
`validateRequest`/`rateLimiter` are pass-through stubs, multer is hoisted via `vi.hoisted()`,
and `fs.unlink` is mocked. No real database — this is an HTTP-layer test verifying route
dispatch, status codes, org-context guards, validation, and error handling across 42 cases.
The Worker uses a `Request`/`Response` model with Clerk auth (not Express middleware), R2
uploads (not multer), and `database.ts` functions (not a service/repository layer). Product
CRUD handler functions exist in the Worker (`handlers/products` imported at
multi-tenant-isolation.test.ts:8) but have no dedicated route tests; the export-excess feature
has no Worker analogue; the upload pipeline is R2-based with a different architecture.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns all products for GET /products | product.routes.test.ts:255 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — minimal-api-routes.test.ts:70 tests generic route dispatch with a `GET /api/products` fixture but not product-specific listing; multi-tenant-isolation.test.ts:16 imports `getProducts` but is a stub (`expect(true).toBe(true)`); handlers.test.ts:43 is a stub | worker-shaped-rewrite | The Worker has product handler functions (`getProducts` imported at multi-tenant-isolation.test.ts:8) but no dedicated test asserts the list-products behaviour against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — product list route exists in Worker handlers but is untested; re-test against Worker Request/Response model |
| Returns 500 for GET /products when service throws | product.routes.test.ts:264 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/handlers/handlers.test.ts) — minimal-api-routes.test.ts has no product-route error test; error-handling.test.ts covers generic Worker error handling, not product-list 500; handlers.test.ts is all stubs | worker-shaped-rewrite | The Worker's product list route error handling is untested; the Express error-handling middleware (converting thrown errors to 500) retires with the backend, but the negative/error gate (500 on failure) must be re-tested against the Worker's error model | PROPOSED: worker-shaped-rewrite — product-list 500 error handling is untested in the Worker; re-test negative/error gate against Worker error model |
| Returns 404 for GET /products/by-barcode/:barcode when product does not exist | product.routes.test.ts:273 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts a 404 for product-by-barcode not found; multi-tenant-isolation.test.ts and handlers.test.ts are stubs | worker-shaped-rewrite | The Worker has product handler functions but no test asserts the not-found 404 for barcode lookup; the negative/error gate must be re-tested against the Worker's route model | PROPOSED: worker-shaped-rewrite — product-by-barcode 404 is untested in the Worker; re-test negative/error gate |
| Returns product for GET /products/by-barcode/:barcode when found | product.routes.test.ts:280 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts product-by-barcode lookup success; multi-tenant-isolation.test.ts imports `getProductById` but not `getProductByBarcode`; handlers.test.ts is all stubs | worker-shaped-rewrite | The Worker has product handler functions but no test asserts barcode lookup success against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — product-by-barcode lookup is untested in the Worker; re-test against Worker route model |
| Returns 404 for GET /products/by-sku/:sku when product does not exist | product.routes.test.ts:291 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts a 404 for product-by-sku not found; multi-tenant-isolation.test.ts and handlers.test.ts are stubs | worker-shaped-rewrite | The Worker has product handler functions but no test asserts the not-found 404 for SKU lookup; the negative/error gate must be re-tested | PROPOSED: worker-shaped-rewrite — product-by-sku 404 is untested in the Worker; re-test negative/error gate |
| Returns product for GET /products/by-sku/:sku when found | product.routes.test.ts:298 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts product-by-sku lookup success; handlers.test.ts is all stubs | worker-shaped-rewrite | The Worker has product handler functions but no test asserts SKU lookup success against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — product-by-sku lookup is untested in the Worker; re-test against Worker route model |
| Returns 401 on export-excess when organizationId is missing | product.routes.test.ts:307 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/index-minimal.ts) — the "export-excess" feature has no Worker analogue (see product.repository.test.ts:77 row); minimal-api-routes.test.ts:1080 tests 401 on auth failure for subscription/usage routes, not export-excess; the Worker gets org context from Clerk auth, not an `x-org-id` header | retire | The export-excess feature is backend-specific with no Worker analogue; the `x-org-id` header-based org context is Express-specific (Worker uses Clerk auth); both the feature and the auth mechanism retire with the backend | PROPOSED: retire — export-excess feature and x-org-id header auth have no Worker analogue (Worker uses Clerk auth, no export-excess route) |
| Returns 404 on export-excess when subscription is missing | product.routes.test.ts:315 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/subscription-validation.test.ts) — the "export-excess" feature has no Worker analogue; usage-limits-integration.test.ts is all stubs; subscription-validation.test.ts covers subscription validation, not export-excess | retire | The export-excess feature is backend-specific with no Worker analogue; the NotFoundError for missing subscription is Express-specific error handling for a feature that doesn't exist in the Worker | PROPOSED: retire — export-excess subscription-not-found has no Worker analogue (export-excess feature does not exist in Worker) |
| Returns unlimited response on export-excess for premium tier | product.routes.test.ts:326 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — the "export-excess" feature has no Worker analogue; usage-limits-integration.test.ts:44 tests concierge unlimited SKUs but is a stub; feature-gates-integration.test.ts covers feature gates, not excess-product export | retire | The export-excess feature is backend-specific with no Worker analogue; the premium-tier unlimited response is Express-specific behaviour for a feature that doesn't exist in the Worker | PROPOSED: retire — export-excess premium-tier unlimited response has no Worker analogue (export-excess feature does not exist in Worker) |
| Returns within-limits response on export-excess when no overage exists | product.routes.test.ts:350 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — the "export-excess" feature has no Worker analogue; usage-limits-integration.test.ts:30 tests professional SKU limits but is a stub; no Worker test asserts a within-limits excess-product response | retire | The export-excess feature is backend-specific with no Worker analogue; the within-limits response is Express-specific behaviour for a feature that doesn't exist in the Worker | PROPOSED: retire — export-excess within-limits response has no Worker analogue (export-excess feature does not exist in Worker) |
| Returns JSON excess products payload on export-excess | product.routes.test.ts:366 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — the "export-excess" feature has no Worker analogue; usage-limits-integration.test.ts is all stubs; database.dashboard.pglite.node.test.ts:104 counts totalProducts for dashboard but does not return excess products for deletion priority | retire | The export-excess feature is backend-specific with no Worker analogue (see product.repository.test.ts:77 row); the JSON payload shape is Express-specific | PROPOSED: retire — export-excess JSON payload has no Worker analogue (export-excess feature does not exist in Worker) |
| Returns CSV excess products payload when Accept header requests csv | product.routes.test.ts:394 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — the "export-excess" feature has no Worker analogue; upload-router.test.ts:33 tests CORS-enabled 400 for malformed keys, not CSV export; no Worker test produces a CSV attachment response for excess products | retire | The export-excess CSV export is backend-specific with no Worker analogue; the Accept-header-based content negotiation for CSV is Express-specific | PROPOSED: retire — export-excess CSV export has no Worker analogue (export-excess feature does not exist in Worker) |
| Returns 500 on export-excess when data retrieval throws | product.routes.test.ts:427 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — the "export-excess" feature has no Worker analogue; error-handling.test.ts covers generic Worker error handling, not export-excess 500 | retire | The export-excess feature is backend-specific with no Worker analogue; the 500 error handling for tier-lookup failure is Express-specific for a feature that doesn't exist in the Worker | PROPOSED: retire — export-excess 500 error handling has no Worker analogue (export-excess feature does not exist in Worker) |
| Returns 400 for GET /products/:id when id is invalid | product.routes.test.ts:436 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — minimal-api-routes.test.ts:86 tests dynamic pathname dispatch to regex routes but not invalid-id 400; multi-tenant-isolation.test.ts and handlers.test.ts are stubs | worker-shaped-rewrite | The Worker has product handler functions but no test asserts a 400 for invalid product ID; the negative/error gate (reject non-numeric id) must be re-tested against the Worker's route model | PROPOSED: worker-shaped-rewrite — invalid-product-id 400 is untested in the Worker; re-test negative/error gate |
| Returns 404 for GET /products/:id when product does not exist | product.routes.test.ts:443 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts:39 tests "Org B cannot read Org A product by ID" but is a stub; handlers.test.ts:55 is a stub; no Worker test asserts a 404 for product-by-id not found | worker-shaped-rewrite | The Worker has `getProductById` (imported at multi-tenant-isolation.test.ts:8) but no test asserts the not-found 404; the negative/error gate must be re-tested | PROPOSED: worker-shaped-rewrite — product-by-id 404 is untested in the Worker; re-test negative/error gate |
| Returns 403 for GET /products/:id when product belongs to another org | product.routes.test.ts:450 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — multi-tenant-isolation.test.ts:39 tests "Org B cannot read Org A product by ID" but is a stub (`expect(true).toBe(true)`); handlers.test.ts:43 is a stub; database.dashboard.pglite.node.test.ts:110 tests dashboard scoping to the requested org but not product-route 403 | worker-shaped-rewrite | The tenant-isolation gate is named for preservation; the Worker has real-SQL tenant scoping (database.dashboard.pglite.node.test.ts:110) but no product-route-level 403 test; the Express cross-org 403 must be re-tested against the Worker's route model | PROPOSED: worker-shaped-rewrite — cross-org product 403 is untested in the Worker (stub only at multi-tenant-isolation.test.ts:39); re-test tenant-isolation gate |
| Returns product for GET /products/:id when requester owns it | product.routes.test.ts:464 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts imports `getProductById` but all tests are stubs; handlers.test.ts is all stubs; no Worker test asserts product-by-id success | worker-shaped-rewrite | The Worker has `getProductById` but no test asserts the happy-path lookup against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — product-by-id success is untested in the Worker; re-test against Worker route model |
| Returns 401 on create product when organizationId is missing | product.routes.test.ts:473 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/index-minimal.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts:1080 tests 401 on Clerk auth failure for subscription/usage routes, not product create; middleware/auth.test.ts:75 tests rejecting requests without Authorization header but not product-specific 401; the Worker gets org context from Clerk, not `x-org-id` header | worker-shaped-rewrite | The Worker uses Clerk auth (tested at minimal-api-routes.test.ts:1080 for subscription routes) but no test asserts a 401 on product create when auth/org context is missing; the `x-org-id` header mechanism retires with the backend, but the reject-unauthenticated gate must be re-tested for product routes | PROPOSED: worker-shaped-rewrite — product-create 401 on missing auth is untested for product routes in the Worker; re-test against Clerk auth model |
| Returns 400 on create product when required fields are missing | product.routes.test.ts:485 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — minimal-api-routes.test.ts:399 tests rejecting non-numeric markdown percentages but not product field validation; no Worker test asserts 400 for missing product fields; multi-tenant-isolation.test.ts and handlers.test.ts are stubs | worker-shaped-rewrite | The Worker has `createProduct` (imported at multi-tenant-isolation.test.ts:8) but no test asserts field validation 400; the negative/error gate must be re-tested against the Worker's validation model | PROPOSED: worker-shaped-rewrite — product-create missing-fields 400 is untested in the Worker; re-test negative/error gate |
| Creates a product on POST /products when payload is valid | product.routes.test.ts:496 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts:8 imports `createProduct` but all tests are stubs; handlers.test.ts:71 is a stub; no Worker test asserts product creation success | worker-shaped-rewrite | The Worker has `createProduct` but no test asserts the happy-path create against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — product create success is untested in the Worker; re-test against Worker route model |
| Returns 500 on create product when service throws | product.routes.test.ts:511 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/handlers/handlers.test.ts) — error-handling.test.ts covers generic Worker error handling, not product-create 500; handlers.test.ts is all stubs | worker-shaped-rewrite | The Worker's product create error handling is untested; the Express error middleware retires, but the negative/error gate (500 on failure) must be re-tested | PROPOSED: worker-shaped-rewrite — product-create 500 error handling is untested in the Worker; re-test negative/error gate |
| Returns 400 for PUT /products/:id when id is invalid | product.routes.test.ts:525 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — minimal-api-routes.test.ts:86 tests dynamic pathname dispatch but not invalid-id 400 for PUT; no Worker test asserts 400 for invalid product ID on update | worker-shaped-rewrite | The Worker has product handler functions but no test asserts a 400 for invalid product ID on PUT; the negative/error gate must be re-tested | PROPOSED: worker-shaped-rewrite — product-update invalid-id 400 is untested in the Worker; re-test negative/error gate |
| Returns 404 for PUT /products/:id when product does not exist | product.routes.test.ts:535 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts 404 for product update not found; multi-tenant-isolation.test.ts and handlers.test.ts are stubs | worker-shaped-rewrite | The Worker has product handler functions but no test asserts the not-found 404 for update; the negative/error gate must be re-tested | PROPOSED: worker-shaped-rewrite — product-update 404 is untested in the Worker; re-test negative/error gate |
| Returns 403 for PUT /products/:id when product belongs to another org | product.routes.test.ts:545 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — multi-tenant-isolation.test.ts:54 tests "cannot delete another org product" but is a stub; handlers.test.ts:229 tests "validate product ownership before data operations" but is a stub; no Worker test asserts a 403 for cross-org product update | worker-shaped-rewrite | The tenant-isolation gate is named for preservation; the Worker has real-SQL tenant scoping but no product-route-level 403 test for update; the Express cross-org 403 must be re-tested | PROPOSED: worker-shaped-rewrite — cross-org product-update 403 is untested in the Worker; re-test tenant-isolation gate |
| Returns 404 for PUT /products/:id when update result is null | product.routes.test.ts:562 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts 404 for product update returning null; handlers.test.ts is all stubs | worker-shaped-rewrite | The Worker has product handler functions but no test asserts the null-result 404 for update; the negative/error gate must be re-tested | PROPOSED: worker-shaped-rewrite — product-update null-result 404 is untested in the Worker; re-test negative/error gate |
| Updates a product on PUT /products/:id when payload is valid | product.routes.test.ts:575 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts product update success; multi-tenant-isolation.test.ts and handlers.test.ts are stubs | worker-shaped-rewrite | The Worker has product handler functions but no test asserts the happy-path update against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — product update success is untested in the Worker; re-test against Worker route model |
| Returns 400 for DELETE /products/:id when id is invalid | product.routes.test.ts:588 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — minimal-api-routes.test.ts:86 tests dynamic pathname dispatch but not invalid-id 400 for DELETE; no Worker test asserts 400 for invalid product ID on delete | worker-shaped-rewrite | The Worker has `deleteProduct` (imported at multi-tenant-isolation.test.ts:8) but no test asserts a 400 for invalid ID on delete; the negative/error gate must be re-tested | PROPOSED: worker-shaped-rewrite — product-delete invalid-id 400 is untested in the Worker; re-test negative/error gate |
| Returns 404 for DELETE /products/:id when product does not exist | product.routes.test.ts:595 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts:54 tests "cannot delete another org product" but is a stub; no Worker test asserts 404 for product delete not found | worker-shaped-rewrite | The Worker has `deleteProduct` but no test asserts the not-found 404 for delete; the negative/error gate must be re-tested | PROPOSED: worker-shaped-rewrite — product-delete 404 is untested in the Worker; re-test negative/error gate |
| Returns 403 for DELETE /products/:id when product belongs to another org | product.routes.test.ts:602 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — multi-tenant-isolation.test.ts:54 tests "cannot delete another org product" but is a stub (`expect(true).toBe(true)`); handlers.test.ts:229 is a stub; no Worker test asserts a 403 for cross-org product delete | worker-shaped-rewrite | The tenant-isolation gate is named for preservation; the Worker has real-SQL tenant scoping but no product-route-level 403 test for delete; the Express cross-org 403 must be re-tested | PROPOSED: worker-shaped-rewrite — cross-org product-delete 403 is untested in the Worker (stub only at multi-tenant-isolation.test.ts:54); re-test tenant-isolation gate |
| Returns 404 for DELETE /products/:id when delete returns false | product.routes.test.ts:616 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts:54 tests "deleteProduct returns false" but is a stub; no Worker test asserts 404 for delete returning false | worker-shaped-rewrite | The Worker has `deleteProduct` but no test asserts the false-result 404; the negative/error gate must be re-tested | PROPOSED: worker-shaped-rewrite — product-delete false-result 404 is untested in the Worker; re-test negative/error gate |
| Deletes product on DELETE /products/:id when request is valid | product.routes.test.ts:626 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts:8 imports `deleteProduct` but all tests are stubs; no Worker test asserts product delete success | worker-shaped-rewrite | The Worker has `deleteProduct` but no test asserts the happy-path delete against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — product delete success is untested in the Worker; re-test against Worker route model |
| Returns 400 on upload-csv when no file is provided | product.routes.test.ts:636 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — upload-handlers.test.ts:46 tests mapping disappearing R2 objects to 404, not no-file 400; upload-router.test.ts:33 tests malformed key 400; the Worker's upload model (R2 → queue → process) has no "no file provided" scenario at the route level | worker-shaped-rewrite | The Worker's upload model is R2-based (file already in R2 when processing starts), so the "no file provided" rejection is a different shape; the negative/error gate (reject missing upload) must be re-tested against the Worker's upload model | PROPOSED: worker-shaped-rewrite — no-file rejection is relevant but untested in the Worker's R2 upload model; re-test negative/error gate |
| Configures multer with the default 10MB upload limit | product.routes.test.ts:643 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — the Worker does not use multer; upload-handlers.test.ts:23 tests queueing with R2 metadata, not multer configuration; the Worker enforces file-size limits in code (index-minimal.ts) but not via multer configuration | retire | Multer configuration is Express-specific middleware with no Worker analogue (Worker uses R2 with code-level size limits); the multer `limits: { fileSize: 10 * 1024 * 1024 }` configuration retires with the backend | PROPOSED: retire — multer configuration is Express-specific middleware with no Worker analogue (Worker uses R2, not multer) |
| Returns 400 on upload-csv when multer rejects an oversized file | product.routes.test.ts:651 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — upload-handlers.test.ts has no file-size rejection test; upload-router.test.ts:33 tests malformed key 400, not oversized file; usage-limits-integration.test.ts:100 tests storage limits but is a stub; the Worker enforces file-size limits in code (index-minimal.ts:3450) but no Worker test asserts the rejection | worker-shaped-rewrite | The file-size-limit behaviour is relevant to the Worker (enforced at the route level in code) but has no test coverage (see local-storage.provider.test.ts:99 row in part 1); the multer-specific `LIMIT_FILE_SIZE` error retires, but the feature-limits gate must be re-tested against the Worker's route-level enforcement | PROPOSED: worker-shaped-rewrite — file-size limit enforced in Worker code but untested; re-test feature-limits gate against Worker route model |
| Returns a canonical 400 when multer rejects an invalid file type | product.routes.test.ts:664 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/upload/catalogue-parser.test.ts) — upload-handlers.test.ts has no file-type rejection test; upload-router.test.ts:33 tests malformed key 400, not invalid file type; catalogue-parser.test.ts covers CSV parsing, not file-type validation; the Worker accepts CSV via R2 but no test asserts file-type rejection | worker-shaped-rewrite | The file-type validation behaviour is relevant to the Worker (CSV/XLSX/XLS accepted) but has no test coverage; the multer-specific `INVALID_PRODUCT_UPLOAD_TYPE` error retires, but the negative/error gate must be re-tested against the Worker's upload model | PROPOSED: worker-shaped-rewrite — file-type validation is relevant but untested in the Worker; re-test negative/error gate against Worker upload model |
| Returns 400 on upload-csv when file path fails safety check | product.routes.test.ts:677 | penetration | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — upload-handlers.test.ts:18 tests upload key ownership via user-scoped prefix (`uploads/user-7/`), which is the R2 analogue of path safety; upload-router.test.ts:33 tests malformed key 400; no Worker test asserts directory-traversal rejection because R2 uses flat key namespaces with no directory traversal surface | retire | The path-safety check (`path.resolve` + `startsWith` against upload dir) is filesystem-specific; R2 uses flat key namespaces with no directory traversal surface (see local-storage.provider.test.ts:74 row in part 1); the penetration gate is preserved by the Worker's user-scoped R2 key prefix (upload-handlers.test.ts:18) but the filesystem path-safety mechanism retires | PROPOSED: retire — filesystem path-traversal safety check has no Worker analogue (R2 uses flat keys, no traversal surface; user-scoped prefix at upload-handlers.test.ts:18 is the equivalent) |
| Returns successful upload summary when CSV processing succeeds with no row errors | product.routes.test.ts:690 | — | workers/src/upload/upload-handlers.test.ts:61 — `processStoredUpload` processes a stored CSV upload from R2 and returns a summary with `rowsProcessed`, `importedCount`, `updatedCount`, `skippedCount`, `errorCount`; line 82-90 asserts the summary shape for a header-only CSV (0 rows, 1 error); the Worker's upload model (R2 → queue → process) is different from Express (multer → temp file → processCSVUpload) but the behaviour (process CSV and return summary) is equivalent | worker-equivalent-exists | The Worker's `processStoredUpload` (upload-handlers.test.ts:61-101) covers the behaviour of processing a CSV upload and returning a summary; the Express multer → temp-file → processCSVUpload pipeline retires, but the process-and-summarise behavior has a Worker equivalent | PROPOSED: worker-equivalent-exists — CSV upload success summary covered by upload-handlers.test.ts:61 (processStoredUpload returns summary); Express multer pipeline retires |
| Returns successful upload summary when XLSX processing succeeds within the limit | product.routes.test.ts:711 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts) — upload-handlers.test.ts:61 processes CSV from R2, not XLSX; catalogue-parser.test.ts and csv-parser.test.ts cover CSV parsing, not XLSX; no Worker test handles XLSX uploads | retire | XLSX upload handling is Express-specific (multer accepts CSV/XLSX/XLS); the Worker's upload pipeline is CSV-focused via R2 with no XLSX parsing test coverage; the XLSX processing path retires with the backend | PROPOSED: retire — XLSX upload handling has no Worker analogue (Worker upload pipeline is CSV-focused via R2, no XLSX test coverage) |
| Returns partial-success upload summary when CSV processor reports row errors | product.routes.test.ts:732 | negative/error | workers/src/upload/upload-handlers.test.ts:82 — `processStoredUpload` returns a summary with `errorCount: 1` and `errors: ['No product rows found']` for a header-only CSV; workers/src/__tests__/catalogue-import-upsert.node.test.ts:272 — the import pipeline sets `status: 'completed_with_errors'` with `row_error_count: 1` and writes an error report to R2; both cover the partial-success-with-errors behaviour | worker-equivalent-exists | The Worker's upload pipeline covers partial-success with row errors: upload-handlers.test.ts:82-90 asserts the error summary shape, and catalogue-import-upsert.node.test.ts:272-278 asserts the `completed_with_errors` status with error counts; the Express partial-success response shape retires, but the behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — partial-success upload summary covered by upload-handlers.test.ts:82 (errorCount/errors) and catalogue-import-upsert.node.test.ts:272 (completed_with_errors) |
| Returns 500 on upload-csv when processor throws | product.routes.test.ts:749 | negative/error | workers/src/upload/upload-handlers.test.ts:46 — `processCompletedUploadSync` maps a processing error (`'Upload not found'`) to a 404 response; the Worker maps processing errors to HTTP error responses but tests 404 (disappearing object), not 500 (processor throw); no Worker test asserts a 500 on upload processing failure | worker-shaped-rewrite | The Worker's upload error handling is partially tested (404 for disappearing objects at upload-handlers.test.ts:46) but 500 on processor throw is untested; the negative/error gate must be re-tested for the 500 case against the Worker's error model | PROPOSED: worker-shaped-rewrite — upload 500 on processor throw is untested in the Worker (404 tested at upload-handlers.test.ts:46 but not 500); re-test negative/error gate |
| Returns success when uploaded temp file cleanup is not part of route verification | product.routes.test.ts:761 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — the Worker uses R2 (no temp files, no `fs.unlink`); upload-handlers.test.ts:91 writes metadata back to R2 via `put`, not filesystem cleanup; no Worker test covers temp file cleanup because R2 has no temp files | retire | Temp file cleanup (`fs.unlink` after CSV processing) is Express/multer-specific filesystem machinery; the Worker uses R2 with no temp files (metadata written back to R2 at upload-handlers.test.ts:91); the cleanup mechanism retires with the backend | PROPOSED: retire — temp file cleanup is Express/multer filesystem machinery with no Worker analogue (Worker uses R2, no temp files) |
| Does not delete when finalizer sees invalid upload path | product.routes.test.ts:778 | penetration | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — upload-handlers.test.ts:18 tests user-scoped R2 key ownership (`uploads/user-7/` prefix), which is the R2 analogue of path safety; upload-router.test.ts:33 tests malformed key 400; no Worker test asserts "do not delete on invalid path" because R2 has no directory traversal surface and no filesystem delete | retire | The invalid-path finalizer check is filesystem-specific (`path.resolve` + `startsWith`); R2 uses flat key namespaces with no directory traversal surface (see local-storage.provider.test.ts:74 row in part 1); the penetration gate is preserved by the Worker's user-scoped R2 key prefix (upload-handlers.test.ts:18) but the filesystem path-validation finalizer retires | PROPOSED: retire — filesystem path-validation finalizer has no Worker analogue (R2 uses flat keys, no traversal surface; user-scoped prefix at upload-handlers.test.ts:18 is the equivalent) |

---

### product.service.test.ts

Unit tests for `ProductService` with a **mocked Prisma client** (`product.findMany`, `product.create`, `organizationUsage.findUnique`, `$transaction` are `vi.fn()` stubs) plus real `xlsx`/`csv-parse` mocks for upload processing. No real database, no supertest — this is a service-layer call-shape and behaviour test. The Worker has no service/repository layer (database.ts functions are called directly from handlers), so Prisma call-shape assertions retire; credit-scope projection has a real-SQL Worker equivalent, and the upload pipeline maps onto the Worker's R2-based catalogue import.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| getAllProducts returns all products for the organization | product.service.test.ts:71 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/database.dashboard.pglite.node.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts:16 tests "Org A cannot see Org B products" but is a stub (`expect(true).toBe(true)`); database.dashboard.pglite.node.test.ts:110 tests dashboard totalProducts scoping, not product list; handlers.test.ts is all stubs | worker-shaped-rewrite | The Worker has `getProducts` (imported at multi-tenant-isolation.test.ts:8) but no test asserts the product-list-scoped-to-org behaviour; the tenant-isolation gate must be re-tested against the Worker's database model | PROPOSED: worker-shaped-rewrite — product list scoping is untested in the Worker (stub at multi-tenant-isolation.test.ts:16); re-test tenant-isolation gate |
| getAllProducts applies limit and offset when provided | product.service.test.ts:100 | — | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test asserts pagination (limit/offset) on product listing; multi-tenant-isolation.test.ts and handlers.test.ts are stubs; minimal-api-routes.test.ts:70 tests generic route dispatch, not product pagination | worker-shaped-rewrite | The Worker has `getProducts` but no test asserts limit/offset pagination; the Prisma `take`/`skip` call shape retires, but the pagination behaviour must be re-tested against the Worker's database model | PROPOSED: worker-shaped-rewrite — product list pagination is untested in the Worker; re-test against Worker database model |
| getProductById returns product if it belongs to the organization | product.service.test.ts:117 | — | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — multi-tenant-isolation.test.ts:39 tests "Org B cannot read Org A product by ID" but is a stub; handlers.test.ts is all stubs; database.markdown-credit-context.pglite.node.test.ts tests findProductByBarcode/findProductBySku, not findProductById | worker-shaped-rewrite | The Worker has `getProductById` (imported at multi-tenant-isolation.test.ts:8) but no test asserts the happy-path lookup; the Prisma `findUnique` call shape retires, but the lookup behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — product-by-id lookup is untested in the Worker; re-test against Worker database model |
| getProductById returns null if product does not exist or belongs to different organization | product.service.test.ts:149 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts:39 tests "Org B cannot read Org A product by ID" but is a stub (`expect(true).toBe(true)`); handlers.test.ts is all stubs; no Worker test asserts null return for cross-org or missing product | worker-shaped-rewrite | The tenant-isolation gate is named for preservation; the Worker has `getProductById` but no test asserts the null/not-found return for wrong org; the Prisma `findUnique` returning null retires, but the isolation behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — cross-org product-by-id null return is untested in the Worker (stub at multi-tenant-isolation.test.ts:39); re-test tenant-isolation gate |
| getProductByBarcode returns product if barcode matches within organization | product.service.test.ts:169 | — | workers/src/database.markdown-credit-context.pglite.node.test.ts:49 — `findProductByBarcode('scope-org', 'direct')` resolves to a product object with barcode 'direct', asserting barcode lookup scoped to org returns the product | worker-equivalent-exists | The Worker's `findProductByBarcode` (database.markdown-credit-context.pglite.node.test.ts:49) covers barcode lookup scoped to org via real SQL; the Prisma `findFirst` call shape retires, but the behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — barcode lookup scoped to org covered by database.markdown-credit-context.pglite.node.test.ts:49 (findProductByBarcode returns product) |
| getProductByBarcode projects full-credit context from an organization-owned direct supplier | product.service.test.ts:197 | — | workers/src/database.markdown-credit-context.pglite.node.test.ts:49 — `findProductByBarcode('scope-org', 'direct')` resolves to `{creditScope: 'FULL_CREDIT', creditScopeReason: 'FULL_CREDIT', creditSupplierName: 'Direct'}` for a product with an org-owned FULL_CREDIT direct supplier | worker-equivalent-exists | The Worker's `findProductByBarcode` (database.markdown-credit-context.pglite.node.test.ts:49-54) asserts the same credit-scope projection (FULL_CREDIT from org-owned direct supplier) via real SQL; the Prisma include/relation call shape retires, but the credit-projection behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — full-credit projection from direct supplier covered by database.markdown-credit-context.pglite.node.test.ts:49 (creditScope: FULL_CREDIT, creditScopeReason: FULL_CREDIT) |
| getProductByBarcode ignores a malformed cross-organization supplier relation | product.service.test.ts:226 | tenant-isolation | workers/src/database.markdown-credit-context.pglite.node.test.ts:87 — `getDetailedExpiryReport('scope-org')` returns a row for a product with a foreign (other-org) supplier projected as `{creditScope: 'NO_CREDIT', creditScopeReason: 'NEEDS_BRAND', creditSupplierId: null}`, asserting cross-org supplier relations are ignored in credit projection | worker-equivalent-exists | The tenant-isolation gate is preserved; the Worker's credit-projection (database.markdown-credit-context.pglite.node.test.ts:87-98) asserts cross-org supplier relations are ignored (NO_CREDIT/NEEDS_BRAND/creditSupplierId: null) via real SQL; the Express test uses getProductByBarcode while the Worker tests via getDetailedExpiryReport, but the credit-projection behaviour is equivalent | PROPOSED: worker-equivalent-exists — cross-org supplier ignore in credit projection covered by database.markdown-credit-context.pglite.node.test.ts:87 (NO_CREDIT/NEEDS_BRAND/creditSupplierId: null) |
| getProductBySku returns product if SKU matches within organization | product.service.test.ts:256 | — | workers/src/database.markdown-credit-context.pglite.node.test.ts:55 — `findProductBySku('scope-org', 'reference')` resolves to a product object with creditScope, asserting SKU lookup scoped to org returns the product | worker-equivalent-exists | The Worker's `findProductBySku` (database.markdown-credit-context.pglite.node.test.ts:55) covers SKU lookup scoped to org via real SQL; the Prisma `findUnique` with `organizationId_sku` compound key retires, but the behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — SKU lookup scoped to org covered by database.markdown-credit-context.pglite.node.test.ts:55 (findProductBySku returns product) |
| createProduct creates a product with organizationId and increments SKU counter | product.service.test.ts:291 | — | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — multi-tenant-isolation.test.ts:8 imports `createProduct` but all tests are stubs; handlers.test.ts is all stubs; catalogue-import-upsert.node.test.ts:249 tests bulk import insert/update but not single-product create with SKU counter increment | worker-shaped-rewrite | The Worker has `createProduct` (imported at multi-tenant-isolation.test.ts:8) but no test asserts single-product create; the Prisma `$transaction` + `organizationUsage.update` SKU-counter mechanism retires (Worker uses a different quota model — see catalogue-import-upsert.node.test.ts:348 quota breach), but the create-product behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — single-product create is untested in the Worker; SKU-counter mechanism retires (Worker uses quota CTE, not organizationUsage counter) |
| createProduct throws error if SKU limit is reached | product.service.test.ts:339 | feature-limits | workers/src/__tests__/catalogue-import-upsert.node.test.ts:348 — `processCatalogueImportJob` with `max_skus_snapshot: 2` and 2 existing products sets `status: 'failed'`, `failure_category: 'quota'`, and inserts nothing, asserting SKU quota breach is rejected with no partial writes | worker-equivalent-exists | The feature-limits gate is preserved; the Worker's catalogue import (catalogue-import-upsert.node.test.ts:348-364) asserts quota breach rejection via real SQL; the Express `organizationUsage` counter mechanism retires, but the SKU-limit-enforcement behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — SKU limit enforcement covered by catalogue-import-upsert.node.test.ts:348 (quota breach → status: failed, failure_category: quota, no partial writes) |
| createProduct throws error if organization usage record not found | product.service.test.ts:364 | negative/error | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — usage-limits-integration.test.ts:142 tests `checkUsageLimit()` but is a stub (`expect(true).toBe(true)`); catalogue-import-upsert.node.test.ts has no missing-usage-record scenario; feature-gates-integration.test.ts covers feature gates, not usage-record lookup | retire | The `organizationUsage.findUnique` returning null is a Prisma-specific mechanism with no Worker analogue; the Worker's quota model uses `max_skus_snapshot` on the upload record (catalogue-import-upsert.node.test.ts:74) and a quota CTE, not a separate organizationUsage table lookup; the missing-record error path retires with the backend | PROPOSED: retire — organizationUsage record-not-found is Prisma-specific with no Worker analogue (Worker uses max_skus_snapshot on upload, not organizationUsage table) |
| updateProduct updates product if it belongs to organization | product.service.test.ts:381 | — | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — multi-tenant-isolation.test.ts has no product-update test; handlers.test.ts is all stubs; catalogue-import-upsert.node.test.ts:259 tests bulk import update (S1 → 'New Name') but not single-product update via a handler | worker-shaped-rewrite | The Worker has no single-product update test; the Prisma `product.update` with `{where: {id, organizationId}}` call shape retires, but the update-scoped-to-org behaviour must be re-tested against the Worker's database model | PROPOSED: worker-shaped-rewrite — single-product update is untested in the Worker (bulk import update at catalogue-import-upsert.node.test.ts:259 is a different path); re-test against Worker database model |
| updateProduct returns null if product does not exist or belongs to different organization | product.service.test.ts:418 | negative/error | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts has no product-update test; handlers.test.ts is all stubs; no Worker test asserts null return for cross-org or missing product update | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker has no test asserting the not-found/cross-org null return for update; the Prisma P2025 error handling retires, but the behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — cross-org/missing product update null return is untested in the Worker; re-test negative/error gate |
| updateProduct returns null if no update data provided | product.service.test.ts:428 | negative/error | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test asserts rejection of empty update payloads; multi-tenant-isolation.test.ts and handlers.test.ts are stubs; minimal-api-routes.test.ts has no product-update validation | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker has no test asserting empty-payload rejection for product update; the Express early-return guard retires, but the behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — empty-update-payload rejection is untested in the Worker; re-test negative/error gate |
| deleteProduct deletes product and decrements SKU counter if it belongs to organization | product.service.test.ts:437 | — | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — multi-tenant-isolation.test.ts:54 tests "cannot delete another org product" but is a stub; handlers.test.ts is all stubs; catalogue-import-upsert.node.test.ts has no delete test | worker-shaped-rewrite | The Worker has `deleteProduct` (imported at multi-tenant-isolation.test.ts:8) but no test asserts single-product delete; the Prisma `$transaction` + `organizationUsage.update` decrement mechanism retires (Worker uses a different quota model), but the delete-scoped-to-org behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — single-product delete is untested in the Worker (stub at multi-tenant-isolation.test.ts:54); SKU-counter decrement retires (Worker uses quota CTE, not organizationUsage counter) |
| deleteProduct returns false if product does not exist or belongs to different organization | product.service.test.ts:467 | negative/error | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts) — multi-tenant-isolation.test.ts:54 tests "cannot delete another org product" but is a stub (`expect(true).toBe(true)`); handlers.test.ts is all stubs; no Worker test asserts false return for cross-org or missing product delete | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker has `deleteProduct` but no test asserts the false/not-found return for wrong org; the Prisma P2025 error handling retires, but the behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — cross-org/missing product delete false return is untested in the Worker (stub at multi-tenant-isolation.test.ts:54); re-test negative/error gate |
| deleteProduct throws error for other database errors | product.service.test.ts:477 | negative/error | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/__tests__/error-handling.test.ts) — multi-tenant-isolation.test.ts and handlers.test.ts are stubs; error-handling.test.ts covers generic Worker error handling, not product-delete non-P2025 error propagation | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker has no test asserting non-not-found error propagation for product delete; the Prisma error-classification logic retires, but the behaviour must be re-tested against the Worker's error model | PROPOSED: worker-shaped-rewrite — product-delete non-not-found error propagation is untested in the Worker; re-test negative/error gate |
| Constructor uses provided organizationId when given | product.service.test.ts:486 | — | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/index-minimal.ts) — the Worker gets org context from Clerk auth (middleware/auth.test.ts:75 tests rejecting requests without Authorization), not from constructor injection; utils/auth.test.ts covers auth utilities, not DI; multi-tenant-isolation.test.ts uses Clerk org context, not constructor orgId | retire | Constructor-based organizationId injection is Express/tsyringe DI wiring with no Worker analogue; the Worker gets org context from Clerk auth at the handler level, not from a service constructor; the DI mechanism retires with the backend | PROPOSED: retire — constructor organizationId injection is tsyringe DI wiring with no Worker analogue (Worker uses Clerk auth for org context) |
| Constructor uses default organizationId when none provided | product.service.test.ts:502 | — | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/index-minimal.ts) — the Worker has no default-org fallback; org context always comes from Clerk auth (middleware/auth.test.ts:75); utils/auth.test.ts covers auth utilities, not DI defaults; no Worker test asserts a default org because the Worker always requires auth | retire | The default-organizationId fallback is Express/tsyringe DI wiring with no Worker analogue; the Worker always requires Clerk auth and derives org context from it, with no default-org concept; the DI default mechanism retires with the backend | PROPOSED: retire — default organizationId fallback is tsyringe DI wiring with no Worker analogue (Worker always requires Clerk auth, no default org) |
| extractCostValue handles currency symbols and thousands separators | product.service.test.ts:519 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:8 tests basic cost parsing ('7.53' → 7.53) but not currency symbols ('$') or thousands separators (','); csv-parser.test.ts covers CSV structure, not cost parsing; catalogue-import-upsert.node.test.ts uses simple decimal costs ('1.00') | worker-shaped-rewrite | The Worker's catalogue parser (catalogue-parser.test.ts:8) handles basic decimal cost parsing but not currency symbols or thousands separators; the Express `extractCostValue` handles '$1,234.50' and '12,99' which the Worker does not test; the cost-parsing behaviour must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — currency/thousands-separator cost parsing is untested in the Worker (catalogue-parser.test.ts:8 only tests basic decimal); re-test against Worker parser |
| extractCostValue returns null for non-numeric input | product.service.test.ts:525 | negative/error | workers/src/upload/catalogue-parser.test.ts:24 — `parseProductCatalogRow` with cost 'not-money' returns `null`, asserting non-numeric cost values are rejected | worker-equivalent-exists | The negative/error gate is preserved; the Worker's catalogue parser (catalogue-parser.test.ts:24-32) asserts non-numeric cost input returns null via real parsing; the Express `extractCostValue` returning null and the Worker's `parseProductCatalogRow` returning null are equivalent behaviours | PROPOSED: worker-equivalent-exists — non-numeric cost rejection covered by catalogue-parser.test.ts:24 (parseProductCatalogRow returns null for 'not-money') |
| extractCostValueEnhanced handles negative, european, and mixed separators | product.service.test.ts:529 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:8 tests basic '7.53' parsing only; no Worker test handles negative costs ('$(12.34)'), European format ('1.234,56'), or mixed separators ('12.34.56'); csv-parser.test.ts covers CSV structure, not cost format parsing | worker-shaped-rewrite | The Worker's catalogue parser does not test negative, European, or mixed-separator cost formats; the Express `extractCostValueEnhanced` handles these complex formats which the Worker has no equivalent for; the enhanced cost-parsing behaviour must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — negative/European/mixed-separator cost parsing is untested in the Worker; re-test against Worker parser |
| processCSVUpload routes to XLSX processor when filename is .xlsx | product.service.test.ts:539 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/upload/catalogue-parser.test.ts) — the Worker's upload pipeline is CSV-focused via R2 (upload-handlers.test.ts:61 processes CSV from R2); upload-router.test.ts:33 tests malformed key 400; no Worker test handles XLSX file routing because the Worker has no XLSX processing | retire | XLSX file-type routing is Express-specific (multer accepts CSV/XLSX/XLS with filename-based routing); the Worker's upload pipeline is CSV-focused via R2 with no XLSX processing (see product.routes.test.ts:711 row — XLSX upload handling retires); the file-type routing mechanism retires with the backend | PROPOSED: retire — XLSX file-type routing is Express-specific with no Worker analogue (Worker upload pipeline is CSV-focused via R2, no XLSX) |
| processCSVUpload routes to CSV processor when header read error falls back | product.service.test.ts:554 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/upload/catalogue-parser.test.ts) — the Worker's upload pipeline is CSV-focused via R2 with no dual-format (XLSX/CSV) detection; upload-handlers.test.ts:61 processes CSV directly; no Worker test asserts a fallback from XLSX header read failure to CSV processing | retire | The dual-format file-type detection (try XLSX header, fall back to CSV on error) is Express-specific machinery; the Worker's upload pipeline is CSV-focused via R2 with no XLSX-to-CSV fallback; the file-type detection mechanism retires with the backend | PROPOSED: retire — XLSX-to-CSV fallback file-type detection is Express-specific with no Worker analogue (Worker is CSV-focused via R2) |
| processCSVUpload routes to XLSX processor when binary header indicates zip archive | product.service.test.ts:573 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/upload/catalogue-parser.test.ts) — the Worker's upload pipeline is CSV-focused via R2; no Worker test detects XLSX via zip-archive magic bytes (PK header); upload-handlers.test.ts:61 processes CSV from R2, not binary header detection | retire | XLSX magic-byte detection (PK zip header) is Express-specific file-type sniffing; the Worker's upload pipeline is CSV-focused via R2 with no binary-header detection; the magic-byte sniffing mechanism retires with the backend | PROPOSED: retire — XLSX zip-header magic-byte detection is Express-specific with no Worker analogue (Worker is CSV-focused via R2) |
| processXLSXUpload returns validation error when workbook has no data rows | product.service.test.ts:602 | negative/error | workers/src/upload/upload-handlers.test.ts:82 — `processStoredUpload` for a header-only CSV returns `errorCount: 1` and `errors: ['No product rows found']`, asserting empty uploads (no data rows) are rejected | worker-equivalent-exists | The negative/error gate is preserved; the Worker's upload pipeline (upload-handlers.test.ts:82-90) asserts empty uploads (header-only, no data rows) are rejected with an error summary; the Express test uses XLSX and the Worker uses CSV, but the reject-empty-upload behaviour is equivalent | PROPOSED: worker-equivalent-exists — empty-upload rejection covered by upload-handlers.test.ts:82 (header-only CSV → errorCount: 1, 'No product rows found'); XLSX mechanism retires |
| processXLSXUpload returns header validation error when SKU column is missing | product.service.test.ts:617 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:5 tests accepting header aliases ('Item Code' → SKU) but not rejecting missing SKU column; csv-parser.test.ts covers CSV structure, not header validation; catalogue-import-upsert.node.test.ts uses valid headers only | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker's catalogue parser (catalogue-parser.test.ts:5) tests accepting header aliases but no test asserts rejecting uploads with missing required columns; the XLSX-specific mechanism retires, but the reject-missing-column behaviour must be re-tested against the Worker's CSV parser | PROPOSED: worker-shaped-rewrite — missing-SKU-column rejection is untested in the Worker (catalogue-parser.test.ts:5 tests aliases but not missing-column rejection); re-test negative/error gate |
| processXLSXUpload does not advertise retail headers as Cost alternatives when Cost is missing | product.service.test.ts:638 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:5 tests accepting 'Cost Ex' as a Cost alias but does not test error-message content for missing Cost column; no Worker test asserts that retail headers are not suggested as Cost alternatives | retire | The error-message precision (not advertising 'Retail Price'/'Selling Price' as Cost alternatives) is Express-specific error-message content; the Worker's catalogue parser does not produce this error message; the XLSX-specific error messaging retires with the backend | PROPOSED: retire — error-message content for missing Cost column (not advertising retail alternatives) is Express-specific with no Worker analogue |
| processXLSXUpload returns unexpected-columns error when headers include unsupported fields | product.service.test.ts:661 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:5 tests accepting known aliases but not rejecting unexpected columns; csv-parser.test.ts covers CSV structure, not column validation; catalogue-import-upsert.node.test.ts uses valid columns only | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker's catalogue parser (catalogue-parser.test.ts:5) tests accepting known aliases but no test asserts rejecting unexpected columns; the XLSX-specific mechanism retires, but the reject-unexpected-columns behaviour must be re-tested against the Worker's CSV parser | PROPOSED: worker-shaped-rewrite — unexpected-columns rejection is untested in the Worker; re-test negative/error gate against Worker CSV parser |
| processXLSXUpload updates existing product and creates new product from XLSX rows | product.service.test.ts:682 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:249 — `processCatalogueImportJob` classifies rows as insert (S5), update (S1 → 'New Name'), unchanged (S2), and conflict (S3), asserting the upsert behaviour of updating existing products and creating new ones from import rows | worker-equivalent-exists | The Worker's catalogue import (catalogue-import-upsert.node.test.ts:249-296) asserts the upsert behaviour (update existing + create new) via real SQL; the Express test uses XLSX and the Worker uses CSV, but the upsert-from-rows behaviour is equivalent | PROPOSED: worker-equivalent-exists — update-existing + create-new from import rows covered by catalogue-import-upsert.node.test.ts:249 (insert S5, update S1 → 'New Name'); XLSX mechanism retires |
| processCSVUploadInternal returns validation errors when CSV structure is invalid | product.service.test.ts:764 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — catalogue-parser.test.ts:5 tests accepting valid headers; csv-parser.test.ts:5 tests CSV structure parsing; upload-handlers.test.ts:61 processes valid CSV; no Worker test asserts returning validation errors for invalid CSV structure (missing required headers) | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker's upload pipeline has no test asserting validation-error return for invalid CSV structure; the Express `validateCSVStructure` mechanism retires, but the reject-invalid-structure behaviour must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — invalid-CSV-structure validation errors are untested in the Worker; re-test negative/error gate against Worker parser |
| processCSVUploadInternal rejects when CSV parser emits error | product.service.test.ts:779 | negative/error | none found (searched: workers/src/upload/csv-parser.test.ts, workers/src/upload/catalogue-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — csv-parser.test.ts:5 tests successful CSV parsing (quoted commas, BOM stripping) but not parser-error handling; catalogue-parser.test.ts covers row parsing, not parser errors; upload-handlers.test.ts:46 maps processing errors to 404 but not CSV parser errors | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker's CSV parser (csv-parser.test.ts:5-21) tests successful parsing but no test asserts rejection on parser error; the Express stream-based error handling retires, but the reject-on-parser-error behaviour must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — CSV parser-error rejection is untested in the Worker (csv-parser.test.ts:5 tests success only); re-test negative/error gate |
| processCSVUploadInternal returns empty-file error when no records are parsed | product.service.test.ts:799 | negative/error | workers/src/upload/upload-handlers.test.ts:82 — `processStoredUpload` for a header-only CSV returns `errorCount: 1` and `errors: ['No product rows found']`, asserting empty files (no parsed records) are rejected | worker-equivalent-exists | The negative/error gate is preserved; the Worker's upload pipeline (upload-handlers.test.ts:82-90) asserts empty files (no records parsed) are rejected with an error; the Express stream-based empty-file detection and the Worker's header-only-CSV detection are equivalent behaviours | PROPOSED: worker-equivalent-exists — empty-file rejection covered by upload-handlers.test.ts:82 (header-only CSV → 'No product rows found'); Express stream mechanism retires |
| processCSVUploadInternal updates and creates products from parsed rows | product.service.test.ts:820 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:249 — `processCatalogueImportJob` classifies rows as insert (S5), update (S1 → 'New Name'), unchanged (S2), and conflict (S3/S4), asserting the upsert behaviour of updating existing and creating new products from parsed CSV rows | worker-equivalent-exists | The Worker's catalogue import (catalogue-import-upsert.node.test.ts:249-296) asserts the upsert behaviour (update existing + create new + conflict detection) via real SQL; the Express test uses stream-based CSV parsing and the Worker uses R2-based CSV import, but the upsert-from-rows behaviour is equivalent | PROPOSED: worker-equivalent-exists — update + create from parsed CSV rows covered by catalogue-import-upsert.node.test.ts:249 (insert/update/unchanged/conflict classification); Express stream mechanism retires |
| validateCSVStructure marks CSV invalid when parser emits an error | product.service.test.ts:922 | negative/error | none found (searched: workers/src/upload/csv-parser.test.ts, workers/src/upload/catalogue-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — csv-parser.test.ts:5 tests successful parsing only; catalogue-parser.test.ts covers row parsing, not structure-validation errors; upload-handlers.test.ts:46 maps processing errors to 404 but not CSV structure-validation parser errors | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker's CSV parser (csv-parser.test.ts:5-21) tests successful parsing but no test asserts structure-validation failure on parser error; the Express `validateCSVStructure` stream-based mechanism retires, but the reject-on-parser-error behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — CSV structure-validation parser-error rejection is untested in the Worker; re-test negative/error gate |
| validateCSVStructure returns missing-header errors when required columns are absent | product.service.test.ts:934 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:5 tests accepting header aliases ('Item Code', 'Cost Ex', 'Barcode') but not rejecting missing required headers; csv-parser.test.ts covers CSV structure, not header validation; catalogue-import-upsert.node.test.ts uses valid headers only | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker's catalogue parser (catalogue-parser.test.ts:5) tests accepting aliases but no test asserts rejecting uploads with missing required headers (SKU, Name, Cost, Barcode); the Express `validateCSVStructure` mechanism retires, but the reject-missing-headers behaviour must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — missing-required-header rejection is untested in the Worker (catalogue-parser.test.ts:5 tests aliases but not missing-header rejection); re-test negative/error gate |
| validateCSVStructure does not advertise retail headers as Cost alternatives when Cost is missing | product.service.test.ts:957 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:5 tests accepting 'Cost Ex' as a Cost alias but does not test error-message content for missing Cost column; no Worker test asserts that retail headers are not suggested as Cost alternatives | retire | The error-message precision (not advertising 'Retail Price'/'Selling Price' as Cost alternatives) is Express-specific error-message content; the Worker's catalogue parser does not produce this error message; the CSV-specific error messaging retires with the backend | PROPOSED: retire — error-message content for missing Cost column (not advertising retail alternatives) is Express-specific with no Worker analogue |
| validateCSVStructure returns valid when required headers are present via alternatives | product.service.test.ts:981 | — | workers/src/upload/catalogue-parser.test.ts:5 — `validateCatalogueRecords` accepts header aliases ('Item Code' → SKU, 'Item Description' → Name, 'Cost Ex' → Cost, 'Barcode' → Barcode) and returns `fatalErrors: []`, `rowErrors: []`, asserting alternative header names are accepted | worker-equivalent-exists | The Worker's catalogue parser (catalogue-parser.test.ts:5-21) asserts alternative header names are accepted (equivalent to the Express `validateCSVStructure` accepting 'Item Code', 'Product Name', 'Unit Price', 'EAN'); the Express alias-mapping mechanism retires, but the accept-alternative-headers behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — alternative-header acceptance covered by catalogue-parser.test.ts:5 (validateCatalogueRecords accepts 'Item Code', 'Cost Ex', etc.); Express alias mechanism retires |

---

### product-import.helpers.test.ts

Unit tests for the `product-import.helpers` module — pure helper functions with **no database, no
Prisma, no supertest**. Tests file-type detection (XLSX vs CSV via filename extension and
zip-archive header sniffing), column matching by alternative header names (case-insensitive,
alias-based), CSV/XLSX row state resolution and unexpected-column detection, row validation
(missing required values, cost parsing with currency symbols, length limits, retail column
handling), and operation resolution (create/update/conflict based on SKU and barcode matches).
The Worker's catalogue parser covers alias-based column matching and basic cost parsing; the
upsert pipeline covers operation resolution; XLSX-specific helpers have no Worker analogue.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| detectProductImportFileType prefers filename extension over path when present | product-import.helpers.test.ts:17 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — the Worker's upload pipeline is CSV-focused via R2 (upload-handlers.test.ts:23 queues CSV uploads); no Worker test detects XLSX by filename extension because the Worker has no XLSX processing | retire | XLSX file-type detection by filename extension is Express-specific; the Worker's upload pipeline is CSV-focused via R2 with no XLSX processing | PROPOSED: retire — XLSX filename-extension detection is Express-specific with no Worker analogue (Worker is CSV-focused via R2) |
| detectProductImportFileType falls back to csv when the file header is not a zip archive | product-import.helpers.test.ts:23 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — the Worker's upload pipeline is CSV-focused via R2; no Worker test sniffs zip-archive magic bytes (PK header) to distinguish XLSX from CSV | retire | Zip-archive header sniffing for XLSX/CSV detection is Express-specific file-type machinery; the Worker is CSV-focused via R2 with no binary-header detection | PROPOSED: retire — zip-header magic-byte sniffing is Express-specific with no Worker analogue (Worker is CSV-focused via R2) |
| findColumnByAlternatives finds matching header names case-insensitively | product-import.helpers.test.ts:31 | — | workers/src/upload/catalogue-parser.test.ts:5 — `validateCatalogueRecords` accepts header aliases ('Item Code' → SKU, 'Item Description' → Name, 'Cost Ex' → Cost, 'Barcode') and returns `fatalErrors: []`, asserting columns are matched by alternative header names | worker-equivalent-exists | The Worker's catalogue parser (catalogue-parser.test.ts:5-21) matches columns by alternative header names via `validateCatalogueRecords`; the Express `findColumnByAlternatives` case-insensitive matching and the Worker's alias matching are equivalent column-matching behaviours | PROPOSED: worker-equivalent-exists — column matching by alternative header names covered by catalogue-parser.test.ts:5 (validateCatalogueRecords accepts 'Item Code', 'Cost Ex', etc.) |
| findColumnIndexByAlternatives finds matching header indexes by alternatives | product-import.helpers.test.ts:38 | — | workers/src/upload/catalogue-parser.test.ts:5 — `validateCatalogueRecords` resolves header names to column indexes internally (accepts 'Item Code' → SKU column at index 0, 'Barcode' → index 3), asserting header indexes are resolved by alternative names | worker-equivalent-exists | The Worker's catalogue parser (catalogue-parser.test.ts:5-21) resolves header names to column indexes by alternative names via `validateCatalogueRecords`; the Express `findColumnIndexByAlternatives` index resolution and the Worker's internal index resolution are equivalent behaviours | PROPOSED: worker-equivalent-exists — header index resolution by alternatives covered by catalogue-parser.test.ts:5 (validateCatalogueRecords resolves 'Item Code' → SKU index, 'Barcode' → index 3) |
| getProductImportCsvColumnState resolves CSV row state and unexpected columns | product-import.helpers.test.ts:46 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:5 tests accepting known header aliases but not detecting unexpected columns; csv-parser.test.ts covers CSV structure parsing, not column validation; catalogue-import-upsert.node.test.ts uses valid headers only | worker-shaped-rewrite | The Worker's catalogue parser (catalogue-parser.test.ts:5) tests accepting known aliases but no test asserts detecting/rejecting unexpected columns; the column-state resolution retires with the Express helpers, but the unexpected-columns detection behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — unexpected-columns detection is untested in the Worker (catalogue-parser.test.ts:5 tests aliases but not unexpected-column rejection); re-test negative/error gate |
| getProductImportCsvColumnState resolves a retail column distinct from cost (CSV) | product-import.helpers.test.ts:74 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/upload/upload-handlers.test.ts) — catalogue-parser.test.ts:5-21 accepts SKU, Name, Cost, Barcode headers but has no retail column concept; catalogue-import-upsert.node.test.ts:394 tests preserving existing retail_price when a cost-only upload omits retail, but no test parses a retail column from CSV; upload-handlers.test.ts:61 processes CSV with no retail column | worker-shaped-rewrite | The Worker has a retail_price database column (catalogue-import-upsert.node.test.ts:394-412 preserves it) but no test asserts parsing a retail column from the CSV upload; the retail-column-from-CSV behaviour must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — retail column parsing from CSV is untested in the Worker (catalogue-import-upsert.node.test.ts:394 tests retail preservation but not retail-column parsing); re-test against Worker parser |
| getProductImportCsvColumnState recognizes retail_price as a retail column in non-streaming imports | product-import.helpers.test.ts:98 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:5-21 has no retail header alias; catalogue-import-upsert.node.test.ts:394 tests retail_price preservation but not retail_price header recognition from CSV | worker-shaped-rewrite | The Worker has no test for recognizing 'retail_price' as a retail column header alias; the retail-column recognition behaviour must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — retail_price header recognition is untested in the Worker; re-test against Worker parser |
| getProductImportXlsxColumnState treats "Selling Price" as retail, no longer as cost (XLSX) | product-import.helpers.test.ts:116 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — the Worker's upload pipeline is CSV-focused via R2; no Worker test handles XLSX headers or distinguishes 'Selling Price' from 'Cost' in XLSX | retire | XLSX-specific column distinction ('Selling Price' as retail vs 'Cost') is Express-specific; the Worker has no XLSX processing | PROPOSED: retire — XLSX 'Selling Price' column distinction is Express-specific with no Worker analogue (Worker is CSV-focused via R2) |
| getProductImportXlsxColumnState resolves XLSX row state and unexpected columns | product-import.helpers.test.ts:124 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — the Worker's upload pipeline is CSV-focused via R2; no Worker test resolves XLSX column state or detects unexpected XLSX columns | retire | XLSX row state resolution and unexpected-column detection is Express-specific; the Worker has no XLSX processing | PROPOSED: retire — XLSX row state resolution is Express-specific with no Worker analogue (Worker is CSV-focused via R2) |
| validateProductImportRow returns row-scoped errors for missing required values | product-import.helpers.test.ts:149 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:23 tests rejecting malformed cost ('not-money' → null) but not missing required values (empty SKU, null Name, empty Barcode); csv-parser.test.ts covers CSV structure, not row validation; catalogue-import-upsert.node.test.ts uses valid rows | worker-shaped-rewrite | The Worker's catalogue parser (catalogue-parser.test.ts:23-32) rejects malformed cost values but no test asserts missing-required-value errors (empty SKU, null Name, empty Barcode); the missing-value validation behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — missing-required-value errors are untested in the Worker (catalogue-parser.test.ts:23 tests malformed cost but not missing values); re-test negative/error gate |
| validateProductImportRow returns a normalized row when all values are valid | product-import.helpers.test.ts:173 | — | workers/src/upload/catalogue-parser.test.ts:8 — `validateCatalogueRecords` parses cost '7.53' → `costPrice: 7.53` and returns `fatalErrors: []`, `rowErrors: []`, asserting valid rows are normalized and parsed successfully | worker-equivalent-exists | The Worker's catalogue parser (catalogue-parser.test.ts:5-21) normalizes valid rows and parses basic decimal costs; the Express test also handles currency symbols ('$1,234.50') and thousands separators which are covered by separate rows in product.service.test.ts (lines 619, 621 → worker-shaped-rewrite); the basic valid-row normalization behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — valid-row normalization covered by catalogue-parser.test.ts:8 (cost '7.53' → 7.53, fatalErrors: []); currency/thousands-separator parsing tracked separately in product.service.test.ts rows |
| validateProductImportRow leaves retail null when the retail column is absent, without erroring | product-import.helpers.test.ts:200 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:394 — `processCatalogueImportJob` with a cost-only CSV (no retail column) preserves the existing `retail_price = 9.99`, asserting the absence of a retail column is not an error and existing retail is retained | worker-equivalent-exists | The Worker's catalogue import (catalogue-import-upsert.node.test.ts:394-412) asserts a cost-only upload (no retail column) succeeds and preserves existing retail_price; the Express "retail absent is not an error" behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — retail-column-absent-not-erroring covered by catalogue-import-upsert.node.test.ts:394 (cost-only upload preserves existing retail_price = 9.99) |
| validateProductImportRow leaves retail null when the retail cell is blank or unparseable | product-import.helpers.test.ts:219 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/upload/upload-handlers.test.ts) — catalogue-parser.test.ts:23 tests rejecting malformed cost ('not-money' → null) but not blank/unparseable retail; catalogue-import-upsert.node.test.ts:394 tests retail preservation when retail column is absent, not when retail cell is blank; upload-handlers.test.ts:61 processes CSV with no retail | worker-shaped-rewrite | The Worker has no test for blank or unparseable retail cell handling; the Express "leave retail null for blank/unparseable" behaviour must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — blank/unparseable retail cell handling is untested in the Worker; re-test negative/error gate |
| validateProductImportRow returns validation errors for invalid cost, length limits, and unexpected columns | product-import.helpers.test.ts:235 | negative/error | workers/src/upload/catalogue-parser.test.ts:23 — `parseProductCatalogRow` with cost 'not-money' returns `null`, asserting invalid cost values are rejected | worker-shaped-rewrite | The Worker's catalogue parser (catalogue-parser.test.ts:23-32) rejects invalid cost but no test asserts length-limit errors (SKU/Name/Barcode too long) or unexpected-column errors; the invalid-cost rejection has a Worker equivalent but the length-limit and unexpected-column validation behaviours must be re-tested | PROPOSED: worker-shaped-rewrite — length-limit and unexpected-column errors are untested in the Worker (catalogue-parser.test.ts:23 covers invalid cost only); re-test negative/error gate |
| resolveProductImportOperation selects update when SKU or barcode map to an existing product | product-import.helpers.test.ts:272 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:259 — `processCatalogueImportJob` classifies 'S1,New Name,B1,1.00' as update (S1 exists by SKU), asserting rows matching an existing product by SKU are classified as update | worker-equivalent-exists | The Worker's catalogue import (catalogue-import-upsert.node.test.ts:249-296) classifies rows matching existing products by SKU as update (rows_updated=1); the Express `resolveProductImportOperation` update-by-SKU/barcode behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — update-by-SKU/barcode classification covered by catalogue-import-upsert.node.test.ts:259 (S1 → update, rows_updated=1) |
| resolveProductImportOperation selects create when neither identifier exists | product-import.helpers.test.ts:292 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:262 — `processCatalogueImportJob` classifies 'S5,Five,B5,5.00' as insert (neither S5 nor B5 exists), asserting rows with no existing SKU or barcode match are classified as insert | worker-equivalent-exists | The Worker's catalogue import (catalogue-import-upsert.node.test.ts:249-296) classifies rows with no existing match as insert (rows_imported=1); the Express `resolveProductImportOperation` create-when-neither-exists behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — create-when-neither-exists classification covered by catalogue-import-upsert.node.test.ts:262 (S5 → insert, rows_imported=1) |
| resolveProductImportOperation returns a conflict when SKU and barcode identify different products | product-import.helpers.test.ts:303 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:261 — `processCatalogueImportJob` classifies 'S3,Three,B4,3.00' as conflict (sku→S3, barcode→S4 are different products), asserting rows where SKU and barcode map to different existing products are classified as conflict | worker-equivalent-exists | The Worker's catalogue import (catalogue-import-upsert.node.test.ts:249-296) classifies rows where SKU and barcode map to different products as conflict (rows_skipped=1, row_error_count=1); the Express `resolveProductImportOperation` conflict behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — conflict-when-SKU-and-barcode-different-products covered by catalogue-import-upsert.node.test.ts:261 (S3/B4 → conflict, rows_skipped=1) |

---

### quota-contract.test.ts

Contract tests for `StorageQuotaService` using a **real Prisma client** (SQLite) — creates real
organizations, users, uploads, and `organizationUsage` records in `beforeEach`, then verifies
that `storageUsedBytes` matches the sum of completed upload file sizes, that only
PROCESSING/COMPLETED uploads are counted, that storage is decremented on upload deletion, and
that status values are consistent between service writes and quota aggregation. The Worker has
no `organizationUsage.storageUsedBytes` table; it uses `checkUsageLimit` with a direct DB query
(mocked in tests) and `max_skus_snapshot` on the upload record for SKU quota, not a separate
storage-tracking table.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| OrganizationUsage.storageUsedBytes matches sum of completed uploads | quota-contract.test.ts:72 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — feature-gates.test.ts:110 tests `checkUsageLimit` for `storage_bytes` with a mocked DB client returning a fixed total (5368709120), not a real sum of completed uploads; usage-limits-integration.test.ts:100-113 tests tier storage limit values (100MB, 1GB) but is a stub (`expect(true).toBe(true)`); feature-gates-integration.test.ts covers feature access, not storage quota; catalogue-import-upsert.node.test.ts:348 tests SKU quota breach, not storage quota | worker-shaped-rewrite | The Worker has `checkUsageLimit` for `storage_bytes` (feature-gates.test.ts:110-116) but it uses a mocked DB client and doesn't test that the storage total matches the sum of completed uploads; the `organizationUsage.storageUsedBytes` Prisma mechanism retires, but the storage-quota-calculation-accuracy behaviour must be re-tested against the Worker's database model | PROPOSED: worker-shaped-rewrite — storage-quota-calculation accuracy is untested in the Worker (feature-gates.test.ts:110 uses mocked DB, not real upload sum); re-test feature-limits gate |
| Only PROCESSING and COMPLETED uploads are counted in storage calculation | quota-contract.test.ts:125 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — feature-gates.test.ts:110 tests `checkUsageLimit` for `storage_bytes` with a mocked DB client, not status-filtered upload sums; usage-limits-integration.test.ts:100 tests tier storage limit values but is a stub; catalogue-import-upsert.node.test.ts has no storage quota test | worker-shaped-rewrite | The Worker has no test for upload-status filtering in storage quota calculation; the Express `UploadStatus.PROCESSING/COMPLETED` filter mechanism retires, but the count-only-active-uploads behaviour must be re-tested against the Worker's database model | PROPOSED: worker-shaped-rewrite — upload-status filtering in storage quota is untested in the Worker; re-test feature-limits gate |
| Storage is decremented when a completed upload is deleted | quota-contract.test.ts:192 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/upload/upload-handlers.test.ts) — feature-gates.test.ts tests `checkUsageLimit` but not storage decrement on upload deletion; usage-limits-integration.test.ts has no storage decrement test; upload-handlers.test.ts:18 tests upload key ownership, not storage quota decrement | worker-shaped-rewrite | The Worker has no test for storage decrement on upload deletion; the Express `StorageQuotaService.markUploadDeleted` mechanism retires, but the decrement-on-delete behaviour must be re-tested against the Worker's database model | PROPOSED: worker-shaped-rewrite — storage decrement on upload deletion is untested in the Worker; re-test feature-limits gate |
| Status value in quota aggregation matches status written by service | quota-contract.test.ts:223 | — | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — feature-gates.test.ts:88 tests `checkUsageLimit` with a mocked DB client, not status-value consistency between writes and aggregation; usage-limits-integration.test.ts has no status-consistency test; catalogue-import-upsert.node.test.ts:272 tests upload status transitions (completed_with_errors) but not quota aggregation status consistency | retire | The status-value consistency between `StorageQuotaService.recordUpload` (writes PROCESSING) and quota aggregation is Express/Prisma-specific machinery; the Worker's quota model uses `checkUsageLimit` with a direct DB query (feature-gates.test.ts:88), not a separate service-write + aggregation-filter consistency check; the Prisma-specific consistency mechanism retires with the backend | PROPOSED: retire — status-value consistency between service writes and quota aggregation is Prisma-specific with no Worker analogue (Worker uses direct DB query, not service-write + aggregation-filter) |
| Zero uploads are handled gracefully (aggregate returns null) | quota-contract.test.ts:262 | negative/error | workers/src/utils/feature-gates.test.ts:159 — `checkUsageLimit` with mocked DB returning `[{ count: 0 }]` returns `isWithinLimit: true`, `currentUsage: 0`, `percentageUsed: 0`, asserting zero usage is handled gracefully | worker-equivalent-exists | The negative/error gate is preserved; the Worker's `checkUsageLimit` (feature-gates.test.ts:159-167) asserts zero usage returns `isWithinLimit: true`, `currentUsage: 0`, `percentageUsed: 0`; the Express Prisma `aggregate` returning null for zero uploads and the Worker's `checkUsageLimit` returning zero are equivalent graceful-zero-handling behaviours | PROPOSED: worker-equivalent-exists — zero-usage graceful handling covered by feature-gates.test.ts:159 (checkUsageLimit returns currentUsage: 0, isWithinLimit: true) |
| Very large file sizes (100MB) are handled correctly in storage quota | quota-contract.test.ts:276 | — | workers/src/utils/feature-gates.test.ts:110 — `checkUsageLimit` for `storage_bytes` with mocked DB returning `total: 5368709120` (5GB) calculates `percentageUsed: 50`, asserting large storage values are handled correctly | worker-equivalent-exists | The Worker's `checkUsageLimit` (feature-gates.test.ts:110-116) asserts large storage values (5GB) are handled correctly with percentage calculation; the Express test with 100MB file size and the Worker's 5GB storage total are equivalent large-value-handling behaviours | PROPOSED: worker-equivalent-exists — large file size handling covered by feature-gates.test.ts:110 (checkUsageLimit handles 5GB storage total, percentageUsed: 50) |

---

### r2-storage.provider.test.ts

Unit tests for `R2StorageProvider` — an **AWS SDK S3Client wrapper** around Cloudflare R2,
using `vi.mock('@aws-sdk/client-s3')` and `vi.mock('@aws-sdk/s3-request-presigner')` to stub
`S3Client.send` and `getSignedUrl`. No real database, no supertest — this is a pure call-shape
test verifying the provider issues the right S3 commands (`PutObjectCommand`, `GetObjectCommand`,
`DeleteObjectCommand`, `HeadObjectCommand`) and maps AWS SDK error names (`NoSuchKey`,
`NotFound`) to custom error classes (`FileNotFoundError`, `StorageProviderError`,
`FileSizeLimitError`). The Worker uses Cloudflare's `R2Bucket` binding directly (e.g.
`env.CSV_UPLOADS.get/put`), not the AWS SDK; R2Bucket returns `null` for not-found instead of
throwing, and has no presigned-URL API.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Constructor initializes S3Client with the correct R2 endpoint and credentials | r2-storage.provider.test.ts:57 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — the Worker uses Cloudflare's R2Bucket binding injected by the platform, not an AWS SDK S3Client constructed with an endpoint URL; upload-handlers.test.ts:11-14 constructs a mock `env` with `CATALOGUE_IMPORT_QUEUE: { send: vi.fn() }`, not an S3Client | retire | AWS SDK S3Client construction with R2 endpoint URL and credentials is Express-specific bootstrap machinery; the Worker receives R2Bucket bindings from the Cloudflare platform | PROPOSED: retire — S3Client construction with R2 endpoint is AWS SDK bootstrap machinery with no Worker analogue (Worker uses R2Bucket binding) |
| Uploads a file successfully (returns key, PutObjectCommand sent once) | r2-storage.provider.test.ts:70 | — | workers/src/upload/upload-handlers.test.ts:91 — `processStoredUpload` calls `CSV_UPLOADS.put('uploads/user-7/products.csv', csv, { customMetadata: ... })`, asserting an R2 put operation writes the object with metadata | worker-equivalent-exists | The Worker's R2 put via the R2Bucket binding (upload-handlers.test.ts:91-100) is the equivalent R2 write operation; the Express PutObjectCommand wrapper and the Worker's R2Bucket.put both perform the underlying R2 object write | PROPOSED: worker-equivalent-exists — R2 put/write covered by upload-handlers.test.ts:91 (CSV_UPLOADS.put with key, body, customMetadata) |
| Throws FileSizeLimitError for files exceeding the configured limit | r2-storage.provider.test.ts:84 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — upload-handlers.test.ts tests R2 put/get operations but no test asserts a file-size-limit rejection; upload-router.test.ts tests route dispatch and malformed-key 400; minimal-api-routes.test.ts has no file-size limit test | worker-shaped-rewrite | The file-size-limit gate is named for preservation; the Worker enforces file-size limits in route code but no Worker test asserts the rejection; the Express provider's FileSizeLimitError mechanism retires, but the gate must be re-tested against the Worker's route-level enforcement | PROPOSED: worker-shaped-rewrite — file-size limit rejection is untested in the Worker (enforced in code but no test asserts it); re-test feature-limits gate |
| Throws StorageProviderError on R2 upload error | r2-storage.provider.test.ts:93 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — upload-handlers.test.ts:46-58 tests mapping a disappearing R2 object to a 404 response, not AWS SDK error → StorageProviderError class mapping; upload-router.test.ts has no upload-error mapping test | retire | AWS SDK error → StorageProviderError class mapping is Express-specific error machinery; the Worker handles R2 errors via Response status codes (404 at upload-handlers.test.ts:57), not via custom error class wrapping | PROPOSED: retire — StorageProviderError class mapping is AWS SDK-specific error machinery with no Worker analogue (Worker uses Response status codes) |
| Downloads a file successfully (content matches, GetObjectCommand sent) | r2-storage.provider.test.ts:105 | — | workers/src/upload/upload-handlers.test.ts:67 — `processStoredUpload` calls `CSV_UPLOADS.get('uploads/user-7/products.csv')` returning `{ arrayBuffer: vi.fn().mockResolvedValue(csv), httpMetadata: { contentType: 'text/csv' } }`, asserting an R2 get operation retrieves the stored object | worker-equivalent-exists | The Worker's R2 get via the R2Bucket binding (upload-handlers.test.ts:67-73) is the equivalent R2 read operation; the Express GetObjectCommand wrapper and the Worker's R2Bucket.get both perform the underlying R2 object read | PROPOSED: worker-equivalent-exists — R2 get/read covered by upload-handlers.test.ts:67 (CSV_UPLOADS.get returns object with arrayBuffer and httpMetadata) |
| Throws FileNotFoundError for NoSuchKey error on download | r2-storage.provider.test.ts:126 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — upload-handlers.test.ts:46-58 tests a disappearing R2 object mapping to a 404 Response, not AWS SDK NoSuchKey error-name → FileNotFoundError class mapping; R2Bucket.get returns null for not-found instead of throwing | retire | AWS SDK NoSuchKey error-name → FileNotFoundError class mapping is Express-specific error machinery; R2Bucket.get returns null for not-found (different mechanism), and the Worker maps not-found to a 404 Response (upload-handlers.test.ts:57), not a custom error class | PROPOSED: retire — NoSuchKey → FileNotFoundError mapping is AWS SDK-specific with no Worker analogue (R2Bucket returns null; Worker uses 404 Response) |
| Throws FileNotFoundError when Body is missing on download | r2-storage.provider.test.ts:136 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:67 tests R2 get returning an object with arrayBuffer; no test asserts a missing-Body check; R2Bucket.get returns null (not an object with missing Body) for not-found | retire | The AWS SDK Body-missing check is Express-specific; R2Bucket.get returns null for not-found rather than an object with a missing Body property, so the check has no Worker analogue | PROPOSED: retire — Body-missing check is AWS SDK-specific with no Worker analogue (R2Bucket returns null, not an object with missing Body) |
| Throws StorageProviderError on other download errors | r2-storage.provider.test.ts:144 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — upload-handlers.test.ts:46-58 tests a processStoredUpload rejection mapping to 404, not AWS SDK error → StorageProviderError class mapping; upload-router.test.ts has no download-error mapping | retire | AWS SDK error → StorageProviderError class mapping is Express-specific error machinery; the Worker handles errors via Response status codes, not custom error class wrapping | PROPOSED: retire — StorageProviderError class mapping is AWS SDK-specific error machinery with no Worker analogue (Worker uses Response status codes) |
| Deletes an existing file (HeadObject then DeleteObject sent) | r2-storage.provider.test.ts:154 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — upload-handlers.test.ts tests R2 get/put operations (lines 67, 91) but no test calls R2Bucket.delete; upload-router.test.ts tests route dispatch, not storage delete | worker-shaped-rewrite | R2 object deletion is a relevant storage operation for the Worker (R2Bucket.delete exists) but no Worker test asserts it; the Express HeadObject-then-DeleteObject call shape retires, but the delete-object behaviour must be re-tested against the Worker's R2Bucket.delete model | PROPOSED: worker-shaped-rewrite — R2 object deletion is untested in the Worker (no test calls R2Bucket.delete); re-test against Worker R2Bucket model |
| Throws FileNotFoundError for non-existent file on delete | r2-storage.provider.test.ts:169 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:46-58 tests a disappearing R2 object mapping to 404, not AWS SDK NotFound error-name → FileNotFoundError class mapping on delete; R2Bucket.delete is not tested | retire | AWS SDK NotFound error-name → FileNotFoundError class mapping on delete is Express-specific error machinery; the Worker has no delete-error mapping test and uses Response status codes, not custom error classes | PROPOSED: retire — NotFound → FileNotFoundError on delete is AWS SDK-specific error machinery with no Worker analogue (Worker uses Response status codes) |
| Returns true for an existing file (HeadObject sent) | r2-storage.provider.test.ts:181 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — upload-handlers.test.ts tests R2 get/put operations but no test calls R2Bucket.head or checks object existence; upload-router.test.ts tests route dispatch, not storage exists | worker-shaped-rewrite | R2 object existence check is a relevant storage operation for the Worker (R2Bucket.head exists) but no Worker test asserts it; the Express HeadObject call shape retires, but the exists-check behaviour must be re-tested against the Worker's R2Bucket.head model | PROPOSED: worker-shaped-rewrite — R2 object existence check is untested in the Worker (no test calls R2Bucket.head); re-test against Worker R2Bucket model |
| Returns false for NotFound error on exists check | r2-storage.provider.test.ts:195 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:46-58 tests a disappearing R2 object mapping to 404, not AWS SDK NotFound error-name → false mapping on exists; R2Bucket.head is not tested | retire | AWS SDK NotFound error-name → false mapping is Express-specific error machinery; R2Bucket.head returns null for not-found (different mechanism), and no Worker test asserts the exists-check false path | PROPOSED: retire — NotFound → false mapping is AWS SDK-specific with no Worker analogue (R2Bucket.head returns null; Worker uses 404 Response) |
| Throws StorageProviderError on other errors during exists check | r2-storage.provider.test.ts:207 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — no Worker test asserts error-to-StorageProviderError class mapping for an exists check; the Worker uses Response status codes, not custom error classes | retire | AWS SDK error → StorageProviderError class mapping is Express-specific error machinery; the Worker handles errors via Response status codes, not custom error class wrapping | PROPOSED: retire — StorageProviderError class mapping is AWS SDK-specific error machinery with no Worker analogue (Worker uses Response status codes) |
| Returns file metadata (size, contentType, lastModified, etag) from HeadObject | r2-storage.provider.test.ts:217 | — | workers/src/upload/upload-handlers.test.ts:28 — `queueCompletedCatalogueUpload` receives `object: { size: 123, httpMetadata: { contentType: 'text/csv' } }` and includes the key in the response, asserting R2 object metadata (size, contentType) is read from the R2 object | worker-equivalent-exists | The Worker reads R2 object metadata via the R2Bucket binding (upload-handlers.test.ts:28-43); the Express HeadObject wrapper returns size/contentType/lastModified/etag and the Worker's R2 object exposes size/httpMetadata — the core metadata-reading behaviour is equivalent, though the Worker does not expose etag/lastModified in this test | PROPOSED: worker-equivalent-exists — R2 metadata reading covered by upload-handlers.test.ts:28 (object: { size: 123, httpMetadata: { contentType: 'text/csv' } }) |
| Throws FileNotFoundError for NotFound error on getMetadata | r2-storage.provider.test.ts:237 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:46-58 tests a disappearing R2 object mapping to 404, not AWS SDK NotFound error-name → FileNotFoundError class mapping on getMetadata; R2Bucket.head is not tested | retire | AWS SDK NotFound error-name → FileNotFoundError class mapping on getMetadata is Express-specific error machinery; the Worker uses Response status codes, not custom error classes | PROPOSED: retire — NotFound → FileNotFoundError on getMetadata is AWS SDK-specific error machinery with no Worker analogue (Worker uses Response status codes) |
| Uses default values for missing metadata fields (size 0, application/octet-stream) | r2-storage.provider.test.ts:247 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:28 tests R2 metadata with size and contentType present; no test asserts default values for missing metadata fields (size 0, contentType 'application/octet-stream') | worker-shaped-rewrite | Default metadata value handling for missing R2 fields is a relevant negative/error behaviour but no Worker test asserts it; the Express default-value mechanism retires, but the default-metadata behaviour must be re-tested against the Worker's R2Bucket model | PROPOSED: worker-shaped-rewrite — default metadata values for missing R2 fields is untested in the Worker; re-test negative/error gate |
| Generates a presigned upload URL via getSignedUrl + PutObjectCommand | r2-storage.provider.test.ts:263 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — upload-handlers.test.ts tests R2 get/put via R2Bucket binding, not presigned URL generation; upload-router.test.ts tests route dispatch; minimal-api-routes.test.ts tests API route registration and markdown-config, not presigned URLs | retire | Presigned URL generation via @aws-sdk/s3-request-presigner is Express-specific AWS SDK machinery; the Worker uses R2Bucket binding directly and has no presigned-URL generation path | PROPOSED: retire — presigned upload URL via AWS SDK presigner is Express-specific with no Worker analogue (Worker uses R2Bucket directly) |
| Throws StorageProviderError on presigned upload URL presigner error | r2-storage.provider.test.ts:280 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — no Worker test asserts presigned-URL presigner error mapping; the Worker has no presigned-URL generation path | retire | AWS SDK presigner error → StorageProviderError class mapping is Express-specific error machinery; the Worker has no presigned-URL generation, so the error mapping has no Worker analogue | PROPOSED: retire — presigner error mapping is AWS SDK-specific with no Worker analogue (Worker has no presigned-URL path) |
| Generates a presigned download URL via getSignedUrl + GetObjectCommand | r2-storage.provider.test.ts:293 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — upload-handlers.test.ts tests R2 get/put via R2Bucket binding, not presigned URL generation; upload-router.test.ts tests route dispatch; minimal-api-routes.test.ts tests API route registration, not presigned URLs | retire | Presigned URL generation via @aws-sdk/s3-request-presigner is Express-specific AWS SDK machinery; the Worker uses R2Bucket binding directly and has no presigned-URL generation path | PROPOSED: retire — presigned download URL via AWS SDK presigner is Express-specific with no Worker analogue (Worker uses R2Bucket directly) |
| Throws StorageProviderError on presigned download URL presigner error | r2-storage.provider.test.ts:310 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — no Worker test asserts presigned-URL presigner error mapping; the Worker has no presigned-URL generation path | retire | AWS SDK presigner error → StorageProviderError class mapping is Express-specific error machinery; the Worker has no presigned-URL generation, so the error mapping has no Worker analogue | PROPOSED: retire — presigner error mapping is AWS SDK-specific with no Worker analogue (Worker has no presigned-URL path) |

---

### report.service.test.ts

Unit tests for `ReportService` with a **mocked `ReportRepository`** (`vi.mock('../../repositories/report.repository')`).
No real database, no supertest — this is a pure service-delegation test verifying the service
instantiates with a `better-sqlite3` instance and org ID, calls the corresponding repository
method once, and returns the repository's result unchanged. The Worker has no service/repository
layering — `createWorkersDatabase` returns a database object whose methods are called directly
by route handlers. The service→repository delegation call shape retires with the Express
backend; the underlying report behaviours map onto the Worker's database-level report tests.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns the monthly markdown report (delegates to repository.getMonthlyMarkdownReport) | report.service.test.ts:44 | — | none found (searched: workers/src/database.report.node.test.ts, workers/src/database.report.pglite.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — database.report.node.test.ts:46 tests `getMonthlyExpiryReport` SQL alignment, not monthly markdown report; database.report.pglite.node.test.ts:90 tests monthly expiry rows summing to org total, not markdown; database.dashboard.pglite.node.test.ts:79 tests dashboard stats (expiringItems, expiredActionItems), not monthly markdown; database.markdown-credit-context.pglite.node.test.ts:31 tests credit-context projections (supplier/brand credit scope), not monthly markdown reports | worker-shaped-rewrite | The service→repository delegation call shape is Express-specific (tsyringe DI, service→repository layering) and retires; the underlying monthly markdown report behaviour has no Worker test and must be re-tested against the Worker's database model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — monthly markdown report is untested in the Worker (report tests cover expiry, not markdown); re-test against Worker database model |
| Returns the usage report (delegates to repository.getUsageReport) | report.service.test.ts:57 | — | none found (searched: workers/src/database.report.node.test.ts, workers/src/database.report.pglite.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — database.report.node.test.ts tests expiry report SQL alignment, not usage-by-role; database.report.pglite.node.test.ts tests expiry report counts, not usage; database.dashboard.pglite.node.test.ts tests dashboard stats and stock loss, not usage-by-role/userCount/totalActions | worker-shaped-rewrite | The service→repository delegation call shape is Express-specific and retires; the underlying usage report behaviour (by role, user count, total actions) has no Worker test and must be re-tested against the Worker's database model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — usage report by role/userCount/totalActions is untested in the Worker; re-test against Worker database model |
| Returns the monthly expiry report (delegates to repository.getMonthlyExpiryReport) | report.service.test.ts:70 | — | workers/src/database.report.pglite.node.test.ts:90 — `getMonthlyExpiryReport(ORG)` returns monthly rows whose `total_expiring` values sum to `ORG_OFFSETS.length` (7), asserting the monthly expiry report returns correctly aggregated rows scoped to the org; workers/src/database.report.node.test.ts:46 — `getMonthlyExpiryReport('test-org')` asserts the SQL contains the backend-aligned expiry-window contract (expiry_risk_count, markdown bands, days_to_expiry ranges) | worker-equivalent-exists | The service→repository delegation call shape retires, but the underlying monthly expiry report behaviour has a Worker equivalent: database.report.pglite.node.test.ts:90-98 tests real-SQL monthly expiry rows summing to the org total, and database.report.node.test.ts:46-52 tests SQL alignment with the backend expiry-window contract | PROPOSED: worker-equivalent-exists — monthly expiry report covered by database.report.pglite.node.test.ts:90 (monthly rows sum to org total) and database.report.node.test.ts:46 (SQL alignment with expiry-window contract) |
| Returns all active expiry entries (delegates to repository.getActiveExpiryEntries) | report.service.test.ts:83 | — | none found (searched: workers/src/database.report.node.test.ts, workers/src/database.report.pglite.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — database.report.node.test.ts tests `getMonthlyExpiryReport` and `getOverallExpiryReport`, not active expiry entries; database.report.pglite.node.test.ts tests overall expiry counts and monthly rows, not active expiry entries; database.dashboard.pglite.node.test.ts:128 tests `getExpiredItemsEnteredToday` (items that became actionable today), not all active expiry entries | worker-shaped-rewrite | The service→repository delegation call shape is Express-specific and retires; the underlying active-expiry-entries report behaviour has no Worker test (the dashboard test covers `getExpiredItemsEnteredToday` which is a different, narrower query) and must be re-tested against the Worker's database model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — active expiry entries report is untested in the Worker (dashboard test covers getExpiredItemsEnteredToday, a different query); re-test against Worker database model |
| Returns dashboard analytics (delegates to repository.getDashboardAnalytics) | report.service.test.ts:95 | — | workers/src/database.dashboard.pglite.node.test.ts:79 — `getDashboardStats(ORG)` returns `totalProducts: 2`, `expiringItems: 4`, `expiredActionItems: 2`, asserting dashboard analytics are computed from inventory items scoped to the org; workers/src/database.dashboard.pglite.node.test.ts:110 — `getDashboardStats(ORG)` with org-b rows asserts `expiringItems: 1`, `expiredActionItems: 0`, asserting dashboard analytics are scoped to the requested organization | worker-equivalent-exists | The service→repository delegation call shape retires, but the underlying dashboard analytics behaviour has a Worker equivalent: database.dashboard.pglite.node.test.ts:79-108 tests `getDashboardStats` computing near-expiry counts and expired worklist items, and :110-125 tests org scoping; the Express fields (totalItems, expiringThisWeek, totalExpired, totalMarkdown, totalValue) and Worker fields (totalProducts, expiringItems, expiredActionItems) differ in shape but both are org-scoped dashboard analytics | PROPOSED: worker-equivalent-exists — dashboard analytics covered by database.dashboard.pglite.node.test.ts:79 (getDashboardStats computes expiringItems, expiredActionItems) and :110 (scopes to org) |

---

### rate-limiter.middleware.test.ts

Unit tests for the Express `rateLimiter` middleware — `express-rate-limit` singletons
(`standardLimiter`, `globalLimiter`, `strictLimiter`, `uploadLimiter`, `checkoutSessionLimiter`,
`trialConversionLimiter`, `presignedUrlLimiter`) driven through **supertest** against throwaway
Express apps, plus pure-function tests for `getRetryAfterIso`, `skipRateLimitForIps`, and
`skipRateLimitForPaths` helpers. No real database — the in-memory store keys by client IP
(via `X-Forwarded-For` or `req.ip`). The Worker has a single `createRateLimiter` middleware
(`middleware.test.ts:116`) and `checkRateLimit`/`applyRateLimitHeaders` helpers
(`minimal-rate-limit.test.ts`), not per-endpoint limiters; it keys by `CF-Connecting-IP`
(always set by the Cloudflare platform) rather than `X-Forwarded-For`.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| `getRetryAfterIso` returns undefined when the request has no rateLimit metadata | rate-limiter.middleware.test.ts:60 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:18 tests `applyRateLimitHeaders` setting Retry-After from a decision object, not reading `req.rateLimit` metadata; the Worker has no `req.rateLimit` metadata shape to extract from | retire | `getRetryAfterIso` reads Express's `req.rateLimit` metadata populated by `express-rate-limit`; the Worker's `applyRateLimitHeaders` receives a decision object directly, not `req` metadata | PROPOSED: retire — `req.rateLimit` metadata extraction is Express-specific (Worker uses decision object, not req metadata) |
| `getRetryAfterIso` returns undefined when resetTime is absent | rate-limiter.middleware.test.ts:64 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:18 tests `applyRateLimitHeaders` with a decision that always includes `resetTime`; no Worker test asserts the absent-resetTime undefined path | retire | The absent-resetTime guard is Express-specific defensive coding for `req.rateLimit` metadata; the Worker's decision object always includes `resetTime` | PROPOSED: retire — absent-resetTime guard is Express-specific (Worker decision object always includes resetTime) |
| `getRetryAfterIso` serialises a Date resetTime to ISO | rate-limiter.middleware.test.ts:70 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:18 sets `Retry-After: '60'` (numeric seconds header), not an ISO-serialized Date in the response body; the Worker uses numeric seconds, not ISO strings | retire | The Express helper serializes a Date to an ISO string for the body's `retryAfter` field; the Worker sets a numeric `Retry-After` header (seconds), a different format | PROPOSED: retire — Date-to-ISO serialization for body retryAfter is Express-specific (Worker uses numeric seconds Retry-After header) |
| `getRetryAfterIso` serialises a numeric (epoch ms) resetTime to ISO | rate-limiter.middleware.test.ts:77 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:18 sets `Retry-After: '60'` (numeric seconds), not an ISO-serialized epoch ms in the body; the Worker uses numeric seconds, not ISO strings | retire | The epoch-ms-to-ISO serialization is Express-specific; the Worker converts to numeric seconds for the `Retry-After` header, not ISO | PROPOSED: retire — epoch-ms-to-ISO serialization is Express-specific (Worker uses numeric seconds Retry-After header) |
| `skipRateLimitForIps` skips when the first X-Forwarded-For entry is allowed | rate-limiter.middleware.test.ts:86 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — middleware.test.ts:12 tests `isPublicEndpoint` (path-based auth skip), not IP-based rate-limit skip; minimal-rate-limit.test.ts has no IP allow-list skip mechanism | retire | IP allow-list skip is Express-specific rate-limit middleware wiring; the Worker's rate limiter (middleware.test.ts:116) applies to all requests with no IP-skip, and `isPublicEndpoint` is auth-skip (different concern) | PROPOSED: retire — IP allow-list skip is Express-specific (Worker rate limiter has no IP-skip; isPublicEndpoint is path-based auth-skip) |
| `skipRateLimitForIps` does not skip when the forwarded IP is not allowed | rate-limiter.middleware.test.ts:93 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — middleware.test.ts:23 tests `isPublicEndpoint('/api/users')` returning false (auth-skip false path), not IP-skip false path; no Worker test asserts an IP allow-list false path | retire | The IP-not-allowed false path is Express-specific IP allow-list logic; the Worker has no IP allow-list mechanism | PROPOSED: retire — IP-not-allowed false path is Express-specific (Worker has no IP allow-list mechanism) |
| `skipRateLimitForIps` falls back to req.ip when no forwarding header is present | rate-limiter.middleware.test.ts:100 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:50 keys by `CF-Connecting-IP` (always set by Cloudflare), not `X-Forwarded-For` → `req.ip` fallback; middleware.test.ts:104 sets `ip: '10.0.0.1'` directly but doesn't test a fallback | retire | The `X-Forwarded-For` → `req.ip` fallback is Express-specific; the Worker uses `CF-Connecting-IP` which is always set by the Cloudflare platform, so no fallback is needed | PROPOSED: retire — X-Forwarded-For → req.ip fallback is Express-specific (Worker uses CF-Connecting-IP, always present on Cloudflare) |
| `skipRateLimitForIps` falls back to an empty key when neither header nor req.ip is set | rate-limiter.middleware.test.ts:107 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — no Worker test asserts an empty-key fallback when no IP is available; `CF-Connecting-IP` is always present on Cloudflare, so the Worker never hits this case | retire | The empty-key fallback is Express-specific defensive coding for missing IP; the Worker always has `CF-Connecting-IP` from the platform | PROPOSED: retire — empty-key fallback is Express-specific defensive coding (CF-Connecting-IP always present on Cloudflare) |
| `skipRateLimitForPaths` skips paths matching an allowed prefix | rate-limiter.middleware.test.ts:116 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — middleware.test.ts:12 tests `isPublicEndpoint('/api/organization/bootstrap')` returning true, but that's auth-skip not rate-limit-skip; the Worker's rate limiter (middleware.test.ts:116) has no path-skip mechanism | retire | Rate-limit path-skip is Express-specific middleware wiring; the Worker's `isPublicEndpoint` (middleware.test.ts:12) is auth-skip, a different middleware concern | PROPOSED: retire — rate-limit path-skip is Express-specific (Worker isPublicEndpoint is auth-skip, not rate-limit-skip) |
| `skipRateLimitForPaths` does not skip unrelated paths | rate-limiter.middleware.test.ts:122 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — middleware.test.ts:23 tests `isPublicEndpoint('/api/users')` returning false (auth-skip false path), not rate-limit-skip false path; the Worker's rate limiter has no path-skip mechanism | retire | The unrelated-path false path is Express-specific rate-limit path-skip logic; the Worker has no rate-limit path-skip mechanism | PROPOSED: retire — unrelated-path false path is Express-specific (Worker has no rate-limit path-skip mechanism) |
| `skipRateLimitForPaths` does not skip when the allow list is empty | rate-limiter.middleware.test.ts:128 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — middleware.test.ts has no test for an empty path allow list; the Worker's rate limiter has no path-skip mechanism | retire | The empty-allow-list false path is Express-specific rate-limit path-skip logic; the Worker has no rate-limit path-skip mechanism | PROPOSED: retire — empty-allow-list false path is Express-specific (Worker has no rate-limit path-skip mechanism) |
| `strictLimiter` rejects the 6th attempt with a RATE_LIMIT_EXCEEDED payload | rate-limiter.middleware.test.ts:136 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts, workers/src/index-minimal.ts:342-364) — the live limiter `checkRateLimit` is asserted only for `allowed: true`; `applyRateLimitHeaders` is tested against a pre-built 429 and never exercises the reject decision. The prior citation was the deleted `middleware.test.ts:116`, a test for the dead `createRateLimiter` (Finding 22, deleted in task 3.1.0) | worker-equivalent-exists | The Worker's `createRateLimiter` (minimal-rate-limit.test.ts:116-141) blocks after exceeding the limit with 429 + Retry-After; the core reject-after-exceeding behaviour is equivalent, though the payload shape differs (Worker: `{ error: 'Too Many Requests' }`; Express: `{ code: 'RATE_LIMIT_EXCEEDED', message: '...' }`) | PROPOSED: worker-shaped-rewrite — REOPENED by task 3.1.0. The live limiter's reject path has no test: `checkRateLimit` is asserted only for `allowed: true`. The prior equivalence cited middleware.test.ts:116, a test for the dead `createRateLimiter` (Finding 22) |
| `strictLimiter` rate limits by req.ip when no X-Forwarded-For header is sent | rate-limiter.middleware.test.ts:149 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts, workers/src/index-minimal.ts:342-364) — the live limiter `checkRateLimit` is asserted only for `allowed: true`; `applyRateLimitHeaders` is tested against a pre-built 429 and never exercises the reject decision. The prior citation was the deleted `middleware.test.ts:116`, a test for the dead `createRateLimiter` (Finding 22, deleted in task 3.1.0) | worker-equivalent-exists | The Worker's `createRateLimiter` (minimal-rate-limit.test.ts:116) rate-limits by IP, equivalent to the Express behaviour; the IP source differs (Express: `req.ip` fallback from `X-Forwarded-For`; Worker: `CF-Connecting-IP` always set by platform), but the core IP-keyed rate-limiting behaviour is equivalent | PROPOSED: worker-shaped-rewrite — REOPENED by task 3.1.0. The live limiter's reject path has no test: `checkRateLimit` is asserted only for `allowed: true`. The prior equivalence cited middleware.test.ts:116, a test for the dead `createRateLimiter` (Finding 22) |
| `standardLimiter` rejects the 101st request in the window | rate-limiter.middleware.test.ts:159 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts, workers/src/index-minimal.ts:342-364) — the live limiter `checkRateLimit` is asserted only for `allowed: true`; `applyRateLimitHeaders` is tested against a pre-built 429 and never exercises the reject decision. The prior citation was the deleted `middleware.test.ts:116`, a test for the dead `createRateLimiter` (Finding 22, deleted in task 3.1.0) | worker-equivalent-exists | The reject-after-exceeding behaviour is equivalent (minimal-rate-limit.test.ts:116-141); the max value differs (Express: 100, Worker: 2) but the behaviour is identical | PROPOSED: worker-shaped-rewrite — REOPENED by task 3.1.0. The live limiter's reject path has no test: `checkRateLimit` is asserted only for `allowed: true`. The prior equivalence cited middleware.test.ts:116, a test for the dead `createRateLimiter` (Finding 22) |
| `globalLimiter` rejects the 1001st request and omits retryAfter (hits are not logged) | rate-limiter.middleware.test.ts:172 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — middleware.test.ts:116 always includes `Retry-After` on 429 (line 138); no Worker test asserts omitting `Retry-After`; the Worker has a single rate limiter, not a separate high-volume global limiter | retire | The "omit retryAfter for high-volume limiter" behaviour is Express-specific; the Worker always includes `Retry-After` (middleware.test.ts:138) and has no separate global limiter | PROPOSED: retire — omit-retryAfter for high-volume limiter is Express-specific (Worker always includes Retry-After, no separate global limiter) |
| `uploadLimiter` rejects the 11th upload in the window | rate-limiter.middleware.test.ts:186 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:36 tests the allowed case for presigned PUT uploads (limit 3, remaining 2) but not the rejection; middleware.test.ts:116 tests general rejection, not upload-specific | worker-shaped-rewrite | Upload-specific rate-limit rejection is untested in the Worker; `minimal-rate-limit.test.ts:36` tests the allowed case only, and `middleware.test.ts:116` is a general limiter, not upload-specific | PROPOSED: worker-shaped-rewrite — upload-specific rate-limit rejection is untested (minimal-rate-limit.test.ts:36 tests allowed case only; middleware.test.ts:116 is general) |
| `checkoutSessionLimiter` rejects the 11th checkout attempt in the window | rate-limiter.middleware.test.ts:198 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — the Worker has a single `createRateLimiter` (middleware.test.ts:116), no checkout-specific limiter; minimal-rate-limit.test.ts has no checkout rate limit | retire | Checkout-session-specific rate limiter is Express-specific per-endpoint wiring; the Worker has a single rate limiter, not per-endpoint limiters | PROPOSED: retire — checkout-session limiter is Express-specific per-endpoint wiring (Worker has single rate limiter) |
| `trialConversionLimiter` rejects the 6th trial conversion attempt in the window | rate-limiter.middleware.test.ts:207 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — the Worker has a single `createRateLimiter` (middleware.test.ts:116), no trial-conversion-specific limiter; minimal-rate-limit.test.ts has no trial conversion rate limit | retire | Trial-conversion-specific rate limiter is Express-specific per-endpoint wiring; the Worker has a single rate limiter, not per-endpoint limiters | PROPOSED: retire — trial-conversion limiter is Express-specific per-endpoint wiring (Worker has single rate limiter) |
| `presignedUrlLimiter` rejects the 51st request for an authenticated user and sets Retry-After | rate-limiter.middleware.test.ts:228 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:36 tests the allowed case for presigned PUT (limit 3, remaining 2) but not the rejection; middleware.test.ts:116 tests general rejection, not presigned-URL-specific with authenticated user keying | worker-shaped-rewrite | Presigned-URL rate-limit rejection for authenticated users is untested; `minimal-rate-limit.test.ts:36` tests the allowed case only, and `middleware.test.ts:116` is a general limiter without user keying | PROPOSED: worker-shaped-rewrite — presigned-URL rejection for authenticated user is untested (minimal-rate-limit.test.ts:36 tests allowed case only) |
| `presignedUrlLimiter` keys by user, so a second user from the same IP is unaffected | rate-limiter.middleware.test.ts:243 | tenant-isolation | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:36 keys by `CF-Connecting-IP` (line 40), not by user ID; middleware.test.ts:116 keys by `ip: '10.0.0.1'`, not by user; no Worker test asserts user-keyed rate limiting or that a second user from the same IP is unaffected | worker-shaped-rewrite | User-keyed rate limiting (tenant-isolation gate) is untested in the Worker; both `minimal-rate-limit.test.ts:36` and `middleware.test.ts:116` key by IP, not by user ID; the Express test explicitly asserts that a second authenticated user from the same egress IP is not rate-limited by the first user's exhaustion (the test comment calls this a SECURITY requirement) | PROPOSED: worker-shaped-rewrite — user-keyed rate limiting (tenant-isolation) is untested in the Worker (both Worker tests key by IP, not user); re-test tenant-isolation gate |
| `presignedUrlLimiter` falls back to an IP-derived key when the request is unauthenticated | rate-limiter.middleware.test.ts:252 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts) — minimal-rate-limit.test.ts:36 uses the authenticated limit (3) for presigned PUT without Authorization, not an IP-derived key fallback; minimal-rate-limit.test.ts:50 keys by `CF-Connecting-IP` for anonymous requests but doesn't test a presigned-URL-specific IP fallback | retire | The Express IP-key fallback for unauthenticated presigned URLs is Express-specific; the Worker uses the authenticated limit for presigned PUTs without Authorization (`minimal-rate-limit.test.ts:36`), a different mechanism | PROPOSED: retire — IP-key fallback for unauthenticated presigned URLs is Express-specific (Worker uses authenticated limit for unauthenticated presigned PUTs, different mechanism) |

---

### report.repository.test.ts

Unit tests for `ReportRepository` against a **real in-memory SQLite** database (`better-sqlite3`
`:memory:`), seeding `inventory_items`, `products`, `suppliers`, `brands`, `store_areas`, and
`expired_item_transactions` tables. One test (`getDashboardData` at line 122) uses **mocked
`db.prepare`** stubs to verify the query call sequence. The Worker has no repository layer —
`createWorkersDatabase` returns a database object whose methods (`getDetailedExpiryReport`,
`getOverallExpiryReport`, `getMonthlyExpiryReport`, `getActiveExpiryEntries`,
`getSellThroughByMarkdownLevel`, `getLossBySkuReport`, `getLossByDepartmentReport`,
`getDashboardStats`) are called directly by route handlers and tested with real pglite Postgres.
The SQLite-specific call shapes and table DDL retire with the backend; the underlying report
behaviours map onto the Worker's pglite-level report/disposition/dashboard tests.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Projects credit context for direct and reference-brand suppliers without changing row order | report.repository.test.ts:77 | tenant-isolation | workers/src/database.markdown-credit-context.pglite.node.test.ts:31 — `findProductByBarcode('scope-org', 'direct')` returns creditScope 'FULL_CREDIT', creditScopeReason 'FULL_CREDIT', creditSupplierName 'Direct', and `findProductBySku('scope-org', 'reference')` returns creditScope 'NO_CREDIT', creditScopeReason 'PENDING_CONFIRMATION', asserting direct-supplier FULL_CREDIT and reference-brand PENDING_CONFIRMATION credit context; workers/src/database.markdown-credit-context.pglite.node.test.ts:63 — `getDetailedExpiryReport('scope-org')` returns rows in deterministic expiry order (['Brand Product', 'Foreign Product']) with creditScope FULL_CREDIT for the confirmed brand and creditScope NO_CREDIT/creditScopeReason NEEDS_BRAND/creditSupplierId null for the foreign supplier, and `getActiveExpiryEntries('scope-org')` excludes the other-org product (length 2), asserting cross-tenant exclusion | worker-equivalent-exists | The Express test's 3 credit-context cases (direct FULL_CREDIT, reference PENDING_CONFIRMATION, malformed NEEDS_BRAND) are covered by the Worker: :31 tests direct FULL_CREDIT and reference PENDING_CONFIRMATION, :63 tests confirmed FULL_CREDIT and foreign NEEDS_BRAND with deterministic order and cross-tenant exclusion; the cross-tenant supplier (id 30, 'other-org') in the Express test is covered by the Worker's cross-org exclusion at :99 | PROPOSED: worker-equivalent-exists — credit context projection covered by markdown-credit-context.pglite.node.test.ts:31 (direct FULL_CREDIT, reference PENDING_CONFIRMATION) and :63 (confirmed FULL_CREDIT, foreign NEEDS_BRAND, deterministic order, cross-tenant exclusion) |
| Returns dashboard summary data from the expected query sequence | report.repository.test.ts:122 | — | workers/src/database.dashboard.pglite.node.test.ts:79 — `getDashboardStats(ORG)` returns totalProducts: 2, expiringItems: 4, expiredActionItems: 2, asserting dashboard summary data is computed from inventory items scoped to the org with real SQL | worker-equivalent-exists | The Express test verifies the query call sequence (4 mocked `db.prepare` calls) returning totalProducts/expiringSoon/markdownItems/recentActivity; the call-shape verification (mocked prepare sequence) is Express-specific and retires, but the underlying dashboard summary behaviour is covered by database.dashboard.pglite.node.test.ts:79 (getDashboardStats computes totalProducts, expiringItems, expiredActionItems with real SQL); the field names differ (expiringSoon/markdownItems vs expiringItems/expiredActionItems) but both are org-scoped dashboard summaries | PROPOSED: worker-equivalent-exists — dashboard summary data covered by database.dashboard.pglite.node.test.ts:79 (getDashboardStats computes totalProducts, expiringItems, expiredActionItems; call-shape verification retires, field names differ) |
| Calculates monthly markdown buckets from expiry date windows instead of stored status | report.repository.test.ts:154 | — | workers/src/database.report.node.test.ts:46 — `getMonthlyExpiryReport('test-org')` SQL contains `days_to_expiry BETWEEN 0 AND 30` (markdown3), `31 AND 60` (markdown2), `61 AND 90` (markdown1), and asserts `not.toContain("status = 'Markdown 1'")` and `not.toContain("status LIKE 'Markdown%'")`, verifying markdown buckets are calculated from expiry date windows, not stored status; workers/src/database.report.pglite.node.test.ts:90 — `getMonthlyExpiryReport(ORG)` returns monthly rows whose `total_expiring` sums to ORG_OFFSETS.length (7), asserting monthly rows are returned and sum to the org total | worker-equivalent-exists | The Express test asserts markdown3_count/markdown2_count/markdown1_count per month from expiry date windows (not stored status); the Worker test at :46 asserts the SQL uses the expiry-window contract (days_to_expiry BETWEEN ranges) and explicitly NOT stored status (no `status = 'Markdown N'` filter), covering the "from expiry date windows instead of stored status" principle; the Worker test at :90 asserts monthly rows are returned and sum to the org total; the specific per-month markdown bucket counts are not asserted in the Worker, but the underlying principle is covered | PROPOSED: worker-equivalent-exists — monthly markdown buckets from expiry date windows covered by database.report.node.test.ts:46 (SQL uses days_to_expiry BETWEEN ranges, not stored status) and database.report.pglite.node.test.ts:90 (monthly rows sum to org total) |
| Returns expiry action summary counts for future stock and next-month markdown review | report.repository.test.ts:179 | — | workers/src/database.report.pglite.node.test.ts:61 — `getOverallExpiryReport(ORG)` returns expired_count: 1, expiry_risk_count: 2, next_month_markdown_count: 1, active_expiry_stock_count: 6, asserting overall expiry summary counts with real SQL; workers/src/database.report.pglite.node.test.ts:79 — `getOverallExpiryReport(ORG)` with org-b rows asserts expiry_risk_count: 2, next_month_markdown_count: 1, active_expiry_stock_count: 6 (ignoring org-b rows), asserting org scoping | worker-equivalent-exists | The Express test asserts expired_count, expiry_risk_count, next_month_markdown_count, active_expiry_stock_count from getOverallExpiryReport; the Worker test at :61 asserts the same summary fields with real SQL (counts differ due to different seed data), and :79 asserts org scoping (org-b rows ignored); the behaviour is equivalent | PROPOSED: worker-equivalent-exists — expiry action summary counts covered by database.report.pglite.node.test.ts:61 (getOverallExpiryReport returns expired_count, expiry_risk_count, next_month_markdown_count, active_expiry_stock_count) and :79 (org scoping) |
| Aggregates sell-through counts by markdown level for the organization | report.repository.test.ts:199 | tenant-isolation | workers/src/database.disposition.pglite.node.test.ts:436 — `getSellThroughByMarkdownLevel(ORG)` with 2 sold at Markdown 3, 1 at Markdown 2, 1 at Markdown 1, and a write-off excluded, asserts byLevel.get(3)=2, byLevel.get(2)=1, byLevel.get(1)=1, total=4, asserting sell-through counts by markdown level with write-off exclusion | worker-equivalent-exists | The Express test asserts sell-through counts by markdown level (3:2, 2:1, 1:1, total:4) with write-off exclusion and org scoping (other-org excluded); the Worker test at :436 asserts the exact same counts (3:2, 2:1, 1:1, total:4) with write-off exclusion; the org-scoping (other-org exclusion) is implicit in the ORG parameter but cross-org exclusion is not separately asserted in the Worker test | PROPOSED: worker-equivalent-exists — sell-through by markdown level covered by disposition.pglite.node.test.ts:436 (exact same counts 3:2, 2:1, 1:1, total:4, write-off excluded; org-scoping implicit in parameter, cross-org exclusion not separately asserted) |
| Excludes sold-through stock from the detailed worklist but keeps urgent day-0 items | report.repository.test.ts:227 | — | workers/src/database.worklist.pglite.node.test.ts:67 — `getDetailedExpiryReport(ORG)` before contains the item, after `processExpiredItem(..., 'sold_through')` the item is not in the report, asserting sold-through stock is excluded from the worklist; workers/src/database.worklist.pglite.node.test.ts:80 — `getDetailedExpiryReport(ORG)` with a day-0 'Expired' status item asserts it is still surfaced (not treated as a write-off), asserting urgent day-0 items are kept | worker-equivalent-exists | The Express test asserts sold-through ('Processed'/'Sold Through' statuses) excluded from the worklist and day-0 'Expired' kept; the Worker test at :67 asserts sold-through items are dropped after processExpiredItem, and :80 asserts day-0 'Expired' items are still surfaced; the 'Processed' status exclusion and retailPrice assertion are not covered in the Worker, but the core behaviours (exclude sold-through, keep day-0) are equivalent | PROPOSED: worker-equivalent-exists — exclude sold-through + keep day-0 covered by worklist.pglite.node.test.ts:67 (sold-through dropped) and :80 (day-0 Expired kept; Processed exclusion and retailPrice not asserted) |
| Returns all active entries beyond 90 days but excludes past-expiry and dispositioned stock | report.repository.test.ts:254 | — | none found (searched: workers/src/database.markdown-credit-context.pglite.node.test.ts, workers/src/database.worklist.pglite.node.test.ts, workers/src/database.report.pglite.node.test.ts) — markdown-credit-context.pglite.node.test.ts:99 tests `getActiveExpiryEntries('scope-org')` returning length 2 with cross-org exclusion, but doesn't test beyond-90-days entries or past-expiry/dispositioned exclusion; worklist.pglite.node.test.ts tests getDetailedExpiryReport (90-day worklist), not getActiveExpiryEntries; database.report.pglite.node.test.ts tests getOverallExpiryReport and getMonthlyExpiryReport, not getActiveExpiryEntries | worker-shaped-rewrite | The Express test asserts getActiveExpiryEntries returns items beyond 90 days (+400d) while excluding past-expiry and dispositioned stock; the Worker test at :99 only asserts getActiveExpiryEntries returns 2 items with cross-org exclusion, without testing the beyond-90-days or past-expiry/dispositioned exclusion aspects; the getActiveExpiryEntries behaviour must be re-tested against the Worker's database model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — getActiveExpiryEntries beyond-90-days and past-expiry/dispositioned exclusion is partially covered (markdown-credit-context.pglite.node.test.ts:99 tests cross-org exclusion only); re-test against Worker database model |
| Values past-expiry stock by date (not just Expired status) for loss-by-sku/department | report.repository.test.ts:278 | — | workers/src/database.disposition.pglite.node.test.ts:362 — `getLossBySkuReport(ORG)` and `getLossByDepartmentReport(ORG)` with past-expiry 'Expired' (x2), past-expiry 'Normal' (x1), past-expiry 'Processed' (x1), and future 'Normal' (x1), asserts totalLoss: 18, count: 3 (the 3 past-expiry items by date: 2 Expired + 1 Normal, excluding Processed and future), asserting past-expiry stock is valued by date not status | worker-equivalent-exists | The Express test asserts getLossBySkuReport and getLossByDepartmentReport value past-expiry stock by date (Expired + Normal counted, Processed and future excluded); the Worker test at :362 asserts the exact same behaviour with real SQL (past-expiry 'Expired' + 'Normal' counted by date, 'Processed' and future excluded), with different seed counts (3 vs 2) but identical logic | PROPOSED: worker-equivalent-exists — past-expiry stock valued by date covered by disposition.pglite.node.test.ts:362 (getLossBySkuReport and getLossByDepartmentReport count past-expiry by date: Expired + Normal, exclude Processed and future) |
| Caps loss-by-sku/department at the top 5 sources of loss, ordered by value | report.repository.test.ts:300 | — | workers/src/database.disposition.pglite.node.test.ts:398 — `getLossBySkuReport(ORG)` and `getLossByDepartmentReport(ORG)` with 6 SKUs/departments with ascending cost, asserts length 5, ordered by value descending (SKU_6 through SKU_2, Dept_6 through Dept_2), asserting the top-5 cap with value ordering | worker-equivalent-exists | The Express test asserts getLossBySkuReport and getLossByDepartmentReport cap at 5 items ordered by value descending (SKU_6 through SKU_2, Dept_6 through Dept_2); the Worker test at :398 asserts the exact same behaviour with real SQL (length 5, ordered by value descending, SKU_6 through SKU_2, Dept_6 through Dept_2) | PROPOSED: worker-equivalent-exists — top-5 cap with value ordering covered by disposition.pglite.node.test.ts:398 (getLossBySkuReport and getLossByDepartmentReport cap at 5, ordered by value descending, exact same SKU_6 through SKU_2 ordering) |

---

### report.routes.test.ts

Unit tests for the Express `report.routes` router using **supertest** with a **mocked
`ReportService`** (all 14 service methods are `vi.fn()` stubs returning canned payloads) and a
**mocked `ServiceProvider`** constructor that asserts it receives `{ organizationId }`. Auth
middleware is stubbed to set `req.organizationId` from the `x-org-id` header; `requireFeature` is
stubbed to pass through. This is a pure route-dispatch test — no real database, no real service
logic. The Worker has no equivalent route-layer test with a mocked service; instead it tests route
registration (`minimal-api-routes.test.ts:141` registers 2 of 11 report routes) and database-level
report behaviour (`database.report.*` tests, documented in the report.repository section above).
The Express `ServiceProvider` DI mechanism (tsyringe) and Express error middleware are
Express-specific machinery with no Worker analogue.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns expiry report and uses organization-scoped service provider (ServiceProvider constructed with organizationId, getMonthlyExpiryReport called once) | report.routes.test.ts:81 | tenant-isolation | workers/src/database.report.pglite.node.test.ts:79 — `getOverallExpiryReport(ORG)` scopes counts to org-a, ignoring org-b rows (expiry_risk_count: 2, next_month_markdown_count: 1, active_expiry_stock_count: 6), asserting org-scoped report data; workers/src/database.report.pglite.node.test.ts:90 — `getMonthlyExpiryReport(ORG)` returns monthly rows summing to ORG_OFFSETS.length (7), asserting org-scoped monthly report data | worker-equivalent-exists | The org-scoped report behaviour is covered at the DB level by database.report.pglite.node.test.ts:79 (org-scoped counts) and :90 (monthly rows sum to org total); the ServiceProvider DI mechanism (tsyringe construction with organizationId) and Express route dispatch are Express-specific machinery with no Worker analogue; the Worker passes orgId directly to DB methods | PROPOSED: worker-equivalent-exists — org-scoped report behaviour covered by database.report.pglite.node.test.ts:79 and :90 (org-scoped counts and monthly rows); ServiceProvider DI is Express-specific |
| Returns payloads for 11 standard report endpoints (expiry-overall, expiry-details, expiry-entries, monthly-markdown, usage, daily-usage, loss-by-sku, loss-by-department, items-by-date, store-walk-audit, analytics) | report.routes.test.ts:90 | — | workers/src/minimal-api-routes.test.ts:141 — registers `/api/expired-items/reports/expired-losses` and `/api/reports/store-walk-audit` routes (2 of 11), asserting report routes exist in the Worker route table; workers/src/database.report.pglite.node.test.ts:61 — `getOverallExpiryReport(ORG)` returns summary counts (total_expiring: 7, expired_count: 1, markdown3_count: 2, etc.); workers/src/database.report.pglite.node.test.ts:90 — `getMonthlyExpiryReport(ORG)` returns monthly rows | worker-equivalent-exists | The underlying report behaviours for the 11 endpoints are covered at the DB level by database.report.* tests (getOverallExpiryReport at :61, getMonthlyExpiryReport at :90) and database.disposition.pglite.node.test.ts (getSellThroughByMarkdownLevel, getLossBySkuReport, getLossByDepartmentReport — see report.repository section); 2 of 11 route registrations are covered by minimal-api-routes.test.ts:141; the Express mocked-service route dispatch (path → service method → payload) is Express-specific wiring | PROPOSED: worker-equivalent-exists — underlying report behaviours covered at DB level (database.report.* and disposition.* tests); 2 of 11 route registrations at minimal-api-routes.test.ts:141; Express mocked-service dispatch is Express-specific |
| Returns success message for update-statuses endpoint (updateAllMarkdownStatuses called once) | report.routes.test.ts:117 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.report.pglite.node.test.ts, workers/src/database.report.node.test.ts) — minimal-api-routes.test.ts:141 registers expired-losses and store-walk-audit routes, not update-statuses; database.report tests cover getOverallExpiryReport/getMonthlyExpiryReport, not updateAllMarkdownStatuses; the Worker computes markdown status from expiry date windows at query time (database.report.node.test.ts:46 asserts SQL uses days_to_expiry BETWEEN ranges, not stored status), so there is no stored status to batch-update | retire | The update-statuses endpoint batches stored markdown status updates; the Worker computes markdown status from expiry date windows at query time (database.report.node.test.ts:46), so there is no stored status to batch-update and no Worker endpoint for this; the Express batch-update machinery retires with the backend | PROPOSED: retire — batch stored-status update has no Worker analogue (Worker computes markdown from expiry windows at query time, database.report.node.test.ts:46) |
| Returns 400 for invalid items-by-user timeframe (timeFrame='0') and does not call service | report.routes.test.ts:127 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.report.pglite.node.test.ts, workers/src/database.report.node.test.ts) — minimal-api-routes.test.ts:141 registers expired-losses and store-walk-audit, not items-by-user; no Worker test covers an items-by-user report endpoint or its timeframe validation; the minimal-api-routes.test.ts uses arrayContaining so the route may exist untested | worker-shaped-rewrite | The items-by-user timeframe validation (negative/error gate) is not covered by any Worker test; the route may exist in the Worker route table (minimal-api-routes.test.ts:141 uses arrayContaining, not toEqual), but no test asserts the 400 rejection for invalid timeframe; the validation behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — items-by-user timeframe 400-validation is untested in the Worker (no Worker test covers this endpoint or its validation); re-test negative/error gate |
| Passes valid timeframe ('30') to items-by-user report service | report.routes.test.ts:135 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.report.pglite.node.test.ts, workers/src/database.report.node.test.ts) — no Worker test covers an items-by-user report endpoint or timeframe parameter passing; minimal-api-routes.test.ts:141 registers expired-losses and store-walk-audit, not items-by-user | worker-shaped-rewrite | The items-by-user report with timeframe parameter passing is not covered by any Worker test; if the endpoint exists in the Worker route table, the parameter-passing behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — items-by-user timeframe parameter passing is untested in the Worker; re-test against Worker Request model |
| Passes undefined timeframe when items-by-user query is omitted | report.routes.test.ts:143 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.report.pglite.node.test.ts, workers/src/database.report.node.test.ts) — no Worker test covers an items-by-user report endpoint or undefined-parameter handling; minimal-api-routes.test.ts:141 registers expired-losses and store-walk-audit, not items-by-user | worker-shaped-rewrite | The items-by-user report with omitted-query (undefined timeframe) handling is not covered by any Worker test; if the endpoint exists in the Worker route table, the omitted-parameter behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — items-by-user omitted-query handling is untested in the Worker; re-test against Worker Request model |
| Forwards expiry-overall endpoint failures to error middleware (500 + error message) | report.routes.test.ts:151 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic (connection timeout, transient errors, max retries exceeded), not report endpoint error forwarding to a 500 response; minimal-api-routes.test.ts:475-510 tests 503 responses for markdown-config save on missing schema, not report endpoint 500s; the Worker uses try/catch in route handlers returning Response objects, not Express error middleware | worker-shaped-rewrite | The report endpoint service-rejection → 500 behaviour (negative/error gate) is not covered; error-handling.test.ts:18-97 covers retry logic only, and minimal-api-routes.test.ts:475 covers markdown-config 503, not report 500s; the Express error middleware forwarding is Express-specific, but the "report endpoint returns 500 on service failure" behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — report endpoint 500-on-failure is untested (error-handling.test.ts:18-97 covers retry logic only; minimal-api-routes.test.ts:475 covers markdown-config 503, not report 500s); re-test negative/error gate |
| Forwards loss-by-department endpoint failures to error middleware (500 + error message) | report.routes.test.ts:160 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not loss-by-department endpoint error forwarding; minimal-api-routes.test.ts has no loss-by-department error test; the Worker uses Response-based error handling, not Express error middleware | worker-shaped-rewrite | The loss-by-department endpoint service-rejection → 500 behaviour (negative/error gate) is not covered by any Worker test; the Express error middleware forwarding is Express-specific, but the "report endpoint returns 500 on service failure" behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — loss-by-department 500-on-failure is untested (error-handling.test.ts:18-97 covers retry logic only); re-test negative/error gate |
| Forwards items-by-user endpoint failures to error middleware (500 + error message) | report.routes.test.ts:171 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not items-by-user endpoint error forwarding; minimal-api-routes.test.ts has no items-by-user error test; the Worker uses Response-based error handling, not Express error middleware | worker-shaped-rewrite | The items-by-user endpoint service-rejection → 500 behaviour (negative/error gate) is not covered by any Worker test; the Express error middleware forwarding is Express-specific, but the "report endpoint returns 500 on service failure" behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — items-by-user 500-on-failure is untested (error-handling.test.ts:18-97 covers retry logic only); re-test negative/error gate |
| Forwards items-by-date endpoint failures to error middleware (500 + error message) | report.routes.test.ts:180 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not items-by-date endpoint error forwarding; minimal-api-routes.test.ts has no items-by-date error test; the Worker uses Response-based error handling, not Express error middleware | worker-shaped-rewrite | The items-by-date endpoint service-rejection → 500 behaviour (negative/error gate) is not covered by any Worker test; the Express error middleware forwarding is Express-specific, but the "report endpoint returns 500 on service failure" behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — items-by-date 500-on-failure is untested (error-handling.test.ts:18-97 covers retry logic only); re-test negative/error gate |
| Forwards analytics endpoint failures to error middleware (500 + error message) | report.routes.test.ts:189 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not analytics endpoint error forwarding; minimal-api-routes.test.ts has no analytics error test; the Worker uses Response-based error handling, not Express error middleware | worker-shaped-rewrite | The analytics endpoint service-rejection → 500 behaviour (negative/error gate) is not covered by any Worker test; the Express error middleware forwarding is Express-specific, but the "report endpoint returns 500 on service failure" behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — analytics 500-on-failure is untested (error-handling.test.ts:18-97 covers retry logic only); re-test negative/error gate |

---

### requireOrgRole.test.ts

Unit tests for three Express authorization middlewares — `requireOrgRole`, `requirePermission`,
and `requireMinRole` — using **mocked Express `Request`/`Response`/`NextFunction`** objects (no
supertest, no real HTTP). `AnalyticsService` is mocked to prevent singleton issues. The test
verifies role-based access control (admin/manager/team_member), legacy role normalization
("Manager" → manager, "owner" → admin, "Staff" → team_member), permission-based checks
(upload_files, read_only), and role-hierarchy minimum checks. The Worker's equivalent is
`middleware/require-role.test.ts`, which tests `createUploadRoleMiddleware` (admin+manager for
POST, all roles for GET) and `createRequireRoleMiddleware` (allowed-roles array, not
minimum-role hierarchy) with mocked `ExpressRequest`/`ExpressResponse` adapter objects. The
Worker also has `utils/auth.test.ts` covering JWT/org extraction, but not role-based authorization.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| requireOrgRole(ADMIN) allows admin (next called) | requireOrgRole.test.ts:41 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` canonicalisation and fail-closed default; workers/src/minimal-api-routes.test.ts — the live admin gates return 403 AUTHORIZATION_ERROR for a non-admin before any write (`handleClearSupplierPolicy`, `handleBulkAttachPolicy`, supplier policy write) and admit a legacy `org:admin` string | worker-equivalent-exists | The "admin allowed on admin-only route" behaviour is equivalent; the Worker's `createRequireRoleMiddleware([ROLES.ADMIN])` at :157 asserts admin is allowed with next() called; the middleware construction differs (Express `requireOrgRole(ROLES.ADMIN)` vs Worker `createRequireRoleMiddleware([ROLES.ADMIN], ['/api/admin'])`) but the authorization behaviour is the same | PROPOSED: worker-equivalent-exists — admin allowed on admin-only route covered by roles.test.ts (createRequireRoleMiddleware allows admin, next() called) (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| requireOrgRole(ADMIN) blocks manager (403) | requireOrgRole.test.ts:47 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` canonicalisation and fail-closed default; workers/src/minimal-api-routes.test.ts — the live admin gates return 403 AUTHORIZATION_ERROR for a non-admin before any write (`handleClearSupplierPolicy`, `handleBulkAttachPolicy`, supplier policy write) and admit a legacy `org:admin` string | worker-equivalent-exists | The "non-admin blocked from admin-only route" behaviour is equivalent; the Worker at :168 tests team_member blocked (not manager specifically), but the behaviour is the same — any non-admin role is blocked with 403; the specific manager-blocked case is not separately tested in the Worker but the non-admin-blocked behaviour is covered | PROPOSED: worker-equivalent-exists — non-admin blocked from admin-only covered by roles.test.ts (team_member blocked with 403; manager-blocked not separately tested but behaviour is equivalent) (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| requireOrgRole(ADMIN) blocks team_member (403) | requireOrgRole.test.ts:54 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` canonicalisation and fail-closed default; workers/src/minimal-api-routes.test.ts — the live admin gates return 403 AUTHORIZATION_ERROR for a non-admin before any write (`handleClearSupplierPolicy`, `handleBulkAttachPolicy`, supplier policy write) and admit a legacy `org:admin` string | worker-equivalent-exists | The "team_member blocked from admin-only route" behaviour is exactly equivalent; the Worker at :168 asserts team_member is blocked with 403 and FORBIDDEN code; the Express test asserts 403 via `res.status` | PROPOSED: worker-equivalent-exists — team_member blocked from admin-only covered by roles.test.ts (team_member blocked with 403, FORBIDDEN code) (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| requireOrgRole(ADMIN) blocks missing role (403) | requireOrgRole.test.ts:61 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` canonicalisation and fail-closed default; workers/src/minimal-api-routes.test.ts — the live admin gates return 403 AUTHORIZATION_ERROR for a non-admin before any write (`handleClearSupplierPolicy`, `handleBulkAttachPolicy`, supplier policy write) and admit a legacy `org:admin` string | worker-equivalent-exists | The "missing role blocked with 403" behaviour is equivalent; the Worker at :81 tests missing role blocked in the upload middleware context (not `createRequireRoleMiddleware`), but the behaviour is the same — missing role results in 403; the `createRequireRoleMiddleware` does not test missing role specifically | PROPOSED: worker-equivalent-exists — missing role blocked covered by roles.test.ts (createUploadRoleMiddleware blocks missing role with 403; createRequireRoleMiddleware doesn't test missing role but behaviour is equivalent) (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| requireOrgRole(ADMIN,MANAGER) allows admin (next called) | requireOrgRole.test.ts:72 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` canonicalisation and fail-closed default; workers/src/minimal-api-routes.test.ts — the live admin gates return 403 AUTHORIZATION_ERROR for a non-admin before any write (`handleClearSupplierPolicy`, `handleBulkAttachPolicy`, supplier policy write) and admit a legacy `org:admin` string | worker-equivalent-exists | The "admin allowed on admin+manager route" behaviour is covered; the Worker at :157 tests admin allowed on admin-only route and :46 tests admin allowed on upload POST (which is admin+manager); the admin+manager combination is not tested in `createRequireRoleMiddleware` (which takes [ROLES.ADMIN] only), but the admin-allowed behaviour is equivalent across both Worker middlewares | PROPOSED: worker-equivalent-exists — admin allowed covered by roles.test.ts (admin-only) and :46 (upload POST); admin+manager combo not in createRequireRoleMiddleware but admin-allowed behaviour is equivalent (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| requireOrgRole(ADMIN,MANAGER) allows manager (next called) | requireOrgRole.test.ts:78 | authorization-precedence | none found (searched: workers/src/index-minimal.ts, workers/src/constants/roles.test.ts) — `requireOrgRole` IS live in Express (admin.metrics, database.backup, health, organization-invite, store-area and user routes), but the Worker has only three `normalizeRole(...) !== ROLES.ADMIN` admin gates and no admin+manager gate anywhere. The prior citation was the deleted `middleware/require-role.test.ts` (Finding 22) | worker-equivalent-exists | The "manager allowed on admin+manager route" behaviour is equivalent; the Worker's `createUploadRoleMiddleware` at :57 asserts manager is allowed on upload POST (which requires admin or manager); the middleware differs (Express `requireOrgRole(ADMIN,MANAGER)` vs Worker upload middleware) but the manager-allowed authorization behaviour is the same | PROPOSED: worker-shaped-rewrite — REOPENED by task 3.1.0. `requireOrgRole(admin, manager)` is live Express middleware on the user, store-area and organization-invite routes, and the Worker has no admin+manager gate to be equivalent to. Re-test against the role model 3.1 settles on, once those routes have Worker handlers. Not an upload concern: the earlier framing of this row as an upload authorization gap was wrong |
| requireOrgRole(ADMIN,MANAGER) blocks team_member (403) | requireOrgRole.test.ts:84 | authorization-precedence | none found (searched: workers/src/index-minimal.ts, workers/src/constants/roles.test.ts) — `requireOrgRole` IS live in Express (admin.metrics, database.backup, health, organization-invite, store-area and user routes), but the Worker has only three `normalizeRole(...) !== ROLES.ADMIN` admin gates and no admin+manager gate anywhere. The prior citation was the deleted `middleware/require-role.test.ts` (Finding 22) | worker-equivalent-exists | The "team_member blocked from admin+manager route" behaviour is exactly equivalent; the Worker at :68 asserts team_member is blocked from upload POST with 403 and FORBIDDEN code; the Express test asserts 403 via `res.status` | PROPOSED: worker-shaped-rewrite — REOPENED by task 3.1.0. `requireOrgRole(admin, manager)` is live Express middleware on the user, store-area and organization-invite routes, and the Worker has no admin+manager gate to be equivalent to. Re-test against the role model 3.1 settles on, once those routes have Worker handlers. Not an upload concern: the earlier framing of this row as an upload authorization gap was wrong |
| Normalizes legacy "Manager" to manager and blocks admin-only route (403) | requireOrgRole.test.ts:95 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` maps the legacy Clerk role strings to canonical values (org:admin/admin/Admin/ADMIN → admin; org:manager/Manager → manager; org:member/org:team_member/Staff → team_member) and defaults every unrecognised or absent role to team_member | worker-equivalent-exists | The "legacy role normalization" behaviour is equivalent; the Worker at :127 normalizes `org:manager` → `manager` (and :129 normalizes `org:member` → `team_member`); the Express test normalizes "Manager" (capital M) → "manager" and blocks from admin-only; the specific "Manager" → "manager" string mapping is not tested in the Worker (which tests "org:manager" → "manager"), but the legacy-to-canonical normalization behaviour is equivalent | PROPOSED: worker-equivalent-exists — legacy role normalization covered by roles.test.ts (org:manager → manager) and :129 (org:member → team_member); "Manager" → "manager" string not specifically tested but normalization behaviour is equivalent (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| Normalizes legacy "owner" to admin (next called) | requireOrgRole.test.ts:103 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` maps the legacy Clerk role strings to canonical values (org:admin/admin/Admin/ADMIN → admin; org:manager/Manager → manager; org:member/org:team_member/Staff → team_member) and defaults every unrecognised or absent role to team_member | worker-equivalent-exists | The "legacy role → admin normalization" behaviour is equivalent; the Worker at :115 normalizes `org:admin` → `admin` and allows access; the Express test normalizes "owner" → "admin" and allows; the specific "owner" → "admin" string mapping is not tested in the Worker (which tests "org:admin" → "admin"), but the legacy-to-admin normalization behaviour is equivalent | PROPOSED: worker-equivalent-exists — legacy role → admin normalization covered by roles.test.ts (org:admin → admin, next() called, req.userRole patched); "owner" → "admin" not specifically tested but behaviour is equivalent (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| Normalizes legacy "Staff" to team_member and blocks admin-only route (403) | requireOrgRole.test.ts:109 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` maps the legacy Clerk role strings to canonical values (org:admin/admin/Admin/ADMIN → admin; org:manager/Manager → manager; org:member/org:team_member/Staff → team_member) and defaults every unrecognised or absent role to team_member | worker-equivalent-exists | The "legacy role → team_member, blocked" behaviour is equivalent; the Worker at :129-130 normalizes `org:member`/`org:team_member` → `team_member` and blocks with 403; the Express test normalizes "Staff" → "team_member" and blocks from admin-only; the specific "Staff" → "team_member" string mapping is not tested in the Worker, but the legacy-to-team_member normalization with blocking behaviour is equivalent | PROPOSED: worker-equivalent-exists — legacy role → team_member with blocking covered by roles.test.ts (org:member/org:team_member → team_member, blocked with 403); "Staff" → "team_member" not specifically tested but behaviour is equivalent (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| Patches req.userRole with canonical value after normalization | requireOrgRole.test.ts:118 | — | none found (searched: workers/src/index-minimal.ts, workers/src/constants/roles.test.ts) — the Worker has no mutable request object to patch; `normalizeRole` is called at each gate and its result is not written back. The prior citation was the deleted `middleware/require-role.test.ts` (Finding 22, deleted in task 3.1.0) | worker-equivalent-exists | The "patch req.userRole with canonical value" behaviour is exactly equivalent; the Worker at :124 asserts `req.userRole` is 'admin' after normalizing 'org:admin'; the Express test asserts `req.userRole` is 'admin' after normalizing 'owner'; both patch the canonical role onto the request object | PROPOSED: retire — patching a canonical role onto the request is Express middleware machinery with no Worker analogue; the canonicalisation itself is covered by workers/src/constants/roles.test.ts |
| requirePermission('upload_files') allows admin (next called) | requireOrgRole.test.ts:128 | authorization-precedence | none found (searched: backend/src/routes/*.routes.ts, workers/src/index-minimal.ts) — and none required: the Express middleware under test is itself wired to no route. `requirePermission` (backend/src/middleware/requireOrgRole.ts:75) and `requireMinRole` (:110) occur nowhere outside their own JSDoc examples at :73 and :107, and backend/src/routes/upload.routes.ts gates uploads with authenticateToken + `checkUsageLimit` + rate limiters only. The Worker matches that behaviour | worker-equivalent-exists | The "admin allowed for upload_files permission" behaviour is equivalent; the Worker's `createUploadRoleMiddleware` at :46 asserts admin is allowed on upload POST, which is the upload_files permission; the Express `requirePermission('upload_files')` and Worker `createUploadRoleMiddleware` are different APIs but the admin-allowed-for-upload behaviour is the same | PROPOSED: retire — CORRECTED by task 3.1.0. Tests a permission helper that no route in either backend applies, so there is no behaviour to port. An earlier pass of this task read the absent Worker check as a live authorization gap; that was wrong — Express does not gate uploads by role either. The permission model (`PERMISSIONS.UPLOAD_FILES`, `requirePermission`, `requireMinRole`) was built and tested on both sides and wired to nothing. If upload authorization is wanted, it is net-new product work on a fresh decision, not a migration row |
| requirePermission('upload_files') allows manager (next called) | requireOrgRole.test.ts:135 | authorization-precedence | none found (searched: backend/src/routes/*.routes.ts, workers/src/index-minimal.ts) — and none required: the Express middleware under test is itself wired to no route. `requirePermission` (backend/src/middleware/requireOrgRole.ts:75) and `requireMinRole` (:110) occur nowhere outside their own JSDoc examples at :73 and :107, and backend/src/routes/upload.routes.ts gates uploads with authenticateToken + `checkUsageLimit` + rate limiters only. The Worker matches that behaviour | worker-equivalent-exists | The "manager allowed for upload_files permission" behaviour is equivalent; the Worker at :57 asserts manager is allowed on upload POST; the Express `requirePermission('upload_files')` and Worker `createUploadRoleMiddleware` are different APIs but the manager-allowed-for-upload behaviour is the same | PROPOSED: retire — CORRECTED by task 3.1.0. Tests a permission helper that no route in either backend applies, so there is no behaviour to port. An earlier pass of this task read the absent Worker check as a live authorization gap; that was wrong — Express does not gate uploads by role either. The permission model (`PERMISSIONS.UPLOAD_FILES`, `requirePermission`, `requireMinRole`) was built and tested on both sides and wired to nothing. If upload authorization is wanted, it is net-new product work on a fresh decision, not a migration row |
| requirePermission('upload_files') blocks team_member (403) | requireOrgRole.test.ts:142 | authorization-precedence | none found (searched: backend/src/routes/*.routes.ts, workers/src/index-minimal.ts) — and none required: the Express middleware under test is itself wired to no route. `requirePermission` (backend/src/middleware/requireOrgRole.ts:75) and `requireMinRole` (:110) occur nowhere outside their own JSDoc examples at :73 and :107, and backend/src/routes/upload.routes.ts gates uploads with authenticateToken + `checkUsageLimit` + rate limiters only. The Worker matches that behaviour | worker-equivalent-exists | The "team_member blocked from upload_files permission" behaviour is exactly equivalent; the Worker at :68 asserts team_member is blocked from upload POST with 403 and FORBIDDEN code; the Express test asserts 403 via `res.status` | PROPOSED: retire — CORRECTED by task 3.1.0. Tests a permission helper that no route in either backend applies, so there is no behaviour to port. An earlier pass of this task read the absent Worker check as a live authorization gap; that was wrong — Express does not gate uploads by role either. The permission model (`PERMISSIONS.UPLOAD_FILES`, `requirePermission`, `requireMinRole`) was built and tested on both sides and wired to nothing. If upload authorization is wanted, it is net-new product work on a fresh decision, not a migration row |
| requirePermission('read_only') allows all roles (admin, manager, team_member) | requireOrgRole.test.ts:150 | authorization-precedence | none found (searched: workers/src/index-minimal.ts, workers/src/constants/roles.test.ts) — the live Worker has no permission model; reads are gated by authentication alone, so "read_only allows all roles" holds by construction. The prior citation was the deleted `middleware/require-role.test.ts` (Finding 22, deleted in task 3.1.0) | worker-equivalent-exists | The "read-only allowed for all roles" behaviour is covered; the Worker at :93 asserts team_member GET is allowed on upload (read-only) and :199 asserts team_member GET is allowed on admin-only protected path (read-only); team_member is the most restrictive role, so if it's allowed for GET, admin and manager are implicitly allowed; the Express test iterates all three roles, the Worker tests the most restrictive case | PROPOSED: retire — a read-only permission that admits every role is absent by construction in the Worker, which applies no role gate to reads |
| requireMinRole(TEAM_MEMBER) allows admin (admin meets minimum of team_member) | requireOrgRole.test.ts:161 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/index-minimal.ts) — require-role.test.ts has no `requireMinRole` equivalent; `createRequireRoleMiddleware` takes an allowed-roles array ([ROLES.ADMIN]), not a minimum-role hierarchy; `createUploadRoleMiddleware` checks admin/manager for POST and all for GET, not a role hierarchy; auth.test.ts covers JWT/org extraction, not role-hierarchy authorization | worker-shaped-rewrite | The role-hierarchy-based minimum-role check has no Worker equivalent; the Worker uses allowed-roles arrays and upload-specific role checks, not a minimum-role hierarchy (admin > manager > team_member); the "admin meets minimum of team_member" behaviour must be re-tested against the Worker's authorization model in Phase 3.2 if minimum-role authorization is needed | PROPOSED: worker-shaped-rewrite — requireMinRole role-hierarchy check has no Worker equivalent (Worker uses allowed-roles arrays, not minimum-role hierarchy); re-test authorization-precedence gate |
| requireMinRole(MANAGER) allows manager (manager meets minimum of manager) | requireOrgRole.test.ts:168 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/index-minimal.ts) — require-role.test.ts has no `requireMinRole` equivalent; `createRequireRoleMiddleware` takes an allowed-roles array, not a minimum-role hierarchy; `createUploadRoleMiddleware` checks admin/manager for POST, not a role hierarchy; auth.test.ts covers JWT/org extraction, not role-hierarchy authorization | worker-shaped-rewrite | The role-hierarchy-based minimum-role check has no Worker equivalent; the Worker uses allowed-roles arrays, not a minimum-role hierarchy; the "manager meets minimum of manager" behaviour must be re-tested against the Worker's authorization model in Phase 3.2 if minimum-role authorization is needed | PROPOSED: worker-shaped-rewrite — requireMinRole role-hierarchy check has no Worker equivalent (Worker uses allowed-roles arrays, not minimum-role hierarchy); re-test authorization-precedence gate |
| requireMinRole(MANAGER) blocks team_member (team_member does NOT meet minimum of manager) | requireOrgRole.test.ts:175 | authorization-precedence | none found (searched: backend/src/routes/*.routes.ts, workers/src/index-minimal.ts) — and none required: the Express middleware under test is itself wired to no route. `requirePermission` (backend/src/middleware/requireOrgRole.ts:75) and `requireMinRole` (:110) occur nowhere outside their own JSDoc examples at :73 and :107, and backend/src/routes/upload.routes.ts gates uploads with authenticateToken + `checkUsageLimit` + rate limiters only. The Worker matches that behaviour | worker-equivalent-exists | The "team_member blocked from manager-level action" behaviour is equivalent in outcome; the Worker at :68 blocks team_member from upload POST (which requires admin or manager), which is the same outcome as team_member not meeting the minimum role of manager; the mechanism differs (allowed-roles array vs minimum-role hierarchy) but the authorization-precedence behaviour is the same | PROPOSED: retire — CORRECTED by task 3.1.0. Tests a permission helper that no route in either backend applies, so there is no behaviour to port. An earlier pass of this task read the absent Worker check as a live authorization gap; that was wrong — Express does not gate uploads by role either. The permission model (`PERMISSIONS.UPLOAD_FILES`, `requirePermission`, `requireMinRole`) was built and tested on both sides and wired to nothing. If upload authorization is wanted, it is net-new product work on a fresh decision, not a migration row |
| requireMinRole(ADMIN) blocks team_member (team_member does NOT meet minimum of admin) | requireOrgRole.test.ts:183 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` canonicalisation and fail-closed default; workers/src/minimal-api-routes.test.ts — the live admin gates return 403 AUTHORIZATION_ERROR for a non-admin before any write (`handleClearSupplierPolicy`, `handleBulkAttachPolicy`, supplier policy write) and admit a legacy `org:admin` string | worker-equivalent-exists | The "team_member blocked from admin-level action" behaviour is equivalent in outcome; the Worker at :168 blocks team_member from admin-only path with 403, which is the same outcome as team_member not meeting the minimum role of admin; the mechanism differs (allowed-roles array vs minimum-role hierarchy) but the authorization-precedence behaviour is the same | PROPOSED: worker-equivalent-exists — team_member blocked from admin-level action covered by roles.test.ts (team_member blocked from admin-only path with 403; mechanism differs: allowed-roles array vs minimum-role hierarchy, but outcome is equivalent) (citation repointed by task 3.1.0; the behaviour is covered against live code) |

---
