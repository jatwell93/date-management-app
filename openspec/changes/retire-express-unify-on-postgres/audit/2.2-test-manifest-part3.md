# Test-Coverage Manifest — Part 3

> **Status: PROPOSED — inventory, not a decision record.** Every row carries a `PROPOSED:`
> prefix. Deletion authority is human; no keep/retire call in this file is settled until reviewed.
>
> **Re-pointed 2026-08-28 by task 3.1.0 — four cited Worker test files were deleted.**
> `middleware.test.ts`, `middleware/auth.test.ts`, `middleware/require-role.test.ts` and
> `utils/auth.test.ts` were removed with the undeployed `index.ts` entry point (2.5 Finding 22).
> 246 rows across the four parts cited at least one of them. Two rules were applied.
> **215 rows** named a deleted file only inside a `none found (searched: ...)` list; those rows
> already recorded no equivalent, so only the search target was restated against live code
> (auth → `workers/src/index-minimal.ts` `authenticateApiRequest`:830; roles →
> `workers/src/constants/roles.test.ts`; rate limiting →
> `workers/src/utils/minimal-rate-limit.test.ts`) and **no decision changed**.
> **47 rows** cited a deleted file as a positive equivalent. Of those, 16 were repointed to live
> coverage that genuinely holds — role normalization to `constants/roles.test.ts`, and the
> admin-only gates to `minimal-api-routes.test.ts`, which task 3.1.0 extended to cover
> `handleClearSupplierPolicy` and `handleBulkAttachPolicy`. **31 were REOPENED**: their
> equivalence rested on tests for modules with no production importer, so the claim did not
> survive the deletion. They are marked `REOPENED by task 3.1.0` and carry their superseded
> decision inline. Three groups, none of them a re-pointing exercise:
> **22 auth/JWT rows** (the live Clerk path has no unit coverage for signature, expiry, tenant
> claim or subscription status, and `__tests__/auth-integration.test.ts` asserts
> `const expected = true` in 14 of its 17 cases);
> **2 role rows** for `requireOrgRole(admin, manager)`, which is live Express middleware on the
> user, store-area and organization-invite routes with no admin+manager equivalent anywhere in
> the Worker; and **3 rate-limit rows**, the limiter's reject path being untested in live code.
> These 25 need a decision from the owner; they are not settled.
>
> **Corrected during the same task.** A first pass recorded four further role rows
> (`requirePermission('upload_files')` ×3, `requireMinRole(manager)` ×1) as a live
> authorization gap, reading the Worker's absent upload role check as a regression against
> Express. That was half-verified and wrong: `backend/src/routes/upload.routes.ts` gates uploads
> with `authenticateToken` + `checkUsageLimit` + rate limiters and no role middleware, and
> `requirePermission` (`backend/src/middleware/requireOrgRole.ts:75`) and `requireMinRole`
> (`:110`) occur nowhere outside their own JSDoc examples at `:73` and `:107`. The permission
> model was built and tested on both sides and wired to nothing, so those four rows are
> `retire` — there is no behaviour to port. Upload authorization, if wanted, is net-new product
> work on a fresh decision, not a migration row.

**Task 2.2** — `retire-express-unify-on-postgres`

**Scope:** `backend/src/tests/unit/` — the 50 files alphabetically from `saas-metrics.service`
through `xlsx.upload` (initials `s`–`x`), completing the `unit/` directory. Part 1 covered `a`–`i`,
Part 2 covered `j`–`r`. The non-unit suites (`integration/`, `performance/`, `contract/`,
`security/`, `regression/`) are Part 4. A file in scope with no section is a defect; a section for
a file outside this list is also a defect.

**Status:** Every row carries a `PROPOSED:` prefix. This is an inventory, not a decision
record. The Express suite keeps running on SQLite until deletion; this manifest records what
its coverage maps onto in the Worker suite.

**Columns:** `Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision`

**Named gates:** `tenant-isolation`, `penetration`, `concurrency`, `feature-limits`,
`webhook-security`, `scheduled-job-idempotency`, `authorization-precedence`, `negative/error`,
or `—`.

**Targets:** `worker-equivalent-exists` | `worker-shaped-rewrite` | `retire`.

**Worker test inventory consulted:** `workers/src/**/*.test.ts` (44 files, enumerated in full
in the task brief before writing rows; re-verified byte-identical to the Part 2 inventory at the
start of this part). Every negative names the files that were opened.

---

### saas-metrics.service.test.ts

Unit tests for `SaasMetricsService` with a **mocked analytics repository** (`vi.fn()` stubs on
repository methods — no real database, no supertest). The service is a platform-operator
analytics layer: it records Stripe webhook event metrics, computes trial-to-paid conversion
rate, average revenue per user, and subscription tier distribution. The Worker is the
application API for date-management operations and has no SaaS-platform analytics service; its
dashboard test covers operational stats (expiring items, stock loss) for end users, not
operator-facing SaaS metrics.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Records webhook metrics through the analytics repository (incrementWebhookMetrics called once with event name, success flag, timestamp) | saas-metrics.service.test.ts:4 | — | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/database.dashboard.pglite.node.test.ts, workers/src/clerk/webhook-signature.test.ts) — usage-limits-integration.test.ts:15-54 asserts tier SKU/seat/storage limit constants, not webhook event metrics; subscription-validation.test.ts:17-33 asserts subscription status access control, not webhook analytics; database.dashboard.pglite.node.test.ts:79-108 asserts operational dashboard counts (expiring/expired items), not webhook metrics; clerk/webhook-signature.test.ts verifies Clerk webhook signature validity, not Stripe webhook event analytics recording | retire | Webhook metrics recording is a platform-operator analytics behaviour backed by a Prisma analytics repository with no Worker analogue; the Worker handles Clerk webhooks for auth sync, not Stripe webhook event analytics | PROPOSED: retire — SaaS webhook-metrics analytics is Express/Prisma-specific operator machinery with no Worker analogue |
| Calculates trial conversion rate from analytics repository trials (50% when 1 of 2 ended trials converted) | saas-metrics.service.test.ts:20 | — | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — subscription-validation.test.ts:125-141 asserts that a trial subscription is created with status 'trial' and a 14-day end date, but does not compute trial-to-paid conversion rate; usage-limits-integration.test.ts covers tier limit constants, not conversion analytics; database.dashboard.pglite.node.test.ts covers operational item counts, not subscription conversion | retire | Trial conversion rate is a platform-operator SaaS metric computed from a Prisma analytics repository; the Worker has no SaaS analytics service and does not compute conversion rates | PROPOSED: retire — trial conversion analytics is Express/Prisma-specific operator machinery with no Worker analogue |
| Calculates average revenue per user from repository revenue inputs (premium + professional over user count) | saas-metrics.service.test.ts:38 | — | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — usage-limits-integration.test.ts:26-41 asserts SKU limit values per tier, not revenue-per-user; subscription-validation.test.ts:191-207 asserts tier_level determines feature access, not ARPU; database.dashboard.pglite.node.test.ts:161-171 sums stock-loss financial values, not subscription revenue | retire | ARPU is a platform-operator SaaS financial metric computed from a Prisma analytics repository; the Worker has no revenue analytics service | PROPOSED: retire — ARPU analytics is Express/Prisma-specific operator machinery with no Worker analogue |
| Gets tier distribution from the analytics repository (maps tier levels to counts) | saas-metrics.service.test.ts:55 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — usage-limits-integration.test.ts:15-54 asserts per-tier limit constants (max_skus, max_users, max_storage), not a distribution count across orgs; subscription-validation.test.ts:191-207 asserts tier_level determines features, not tier distribution counts; database.dashboard.pglite.node.test.ts covers item counts, not subscription tier distribution | retire | Tier distribution is a platform-operator SaaS reporting metric backed by a Prisma analytics repository; the Worker enforces tier limits (feature-limits gate) but does not report on tier distribution across orgs | PROPOSED: retire — tier distribution reporting is Express/Prisma-specific operator analytics; feature-limits gate preserved by Worker usage-limits tests |

---

### scheduler.service.test.ts

Unit tests for `SchedulerService` with **mocked `node-cron`** (`vi.mock('node-cron')`), mocked
job modules (stripe-sync, trialExpiration, dunning, credit-claim), a mocked `getDb` returning
`prepare()` stubs, and mocked `DatabaseBackupService` / `InventoryService`. No real database,
no supertest — this is a pure wiring test for the Express `node-cron` scheduler that registers
two cron schedules (markdown update at 0 2 * * *, backup at 0 1 * * *) and starts four
background jobs. The Worker has no `node-cron` scheduler; it uses Cloudflare Queue/Scheduled
handlers. The scheduled-job-idempotency gate is preserved by the job-lock rows in Part 2
(worker-shaped-rewrite), not by these cron-registration tests.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Registers two cron schedules and starts four background jobs (stripe-sync, trial-expiration, dunning, credit-claim) | scheduler.service.test.ts:86 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:870-903 tests Cloudflare Queue duplicate-delivery ack (catalogue import), not node-cron schedule registration; workers-deployment.test.ts:24-32 is a deployment health smoke test with no scheduler coverage; no `scheduled` handler or cron registration exists in the Worker test suite | retire | Cron schedule registration via `node-cron` is Express-specific scheduling machinery; the Worker uses Cloudflare Scheduled handlers configured in wrangler.toml, not runtime cron registration | PROPOSED: retire — node-cron schedule registration is Express-specific machinery with no Worker analogue |
| Wires cron callbacks to markdown-status update and database-backup operations | scheduler.service.test.ts:98 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — health.test.ts has no cron callback wiring; workers-deployment.test.ts has no scheduler coverage; database.markdown-credit-context.pglite.node.test.ts:31-61 tests credit-context projections per product, not a scheduled cron callback that triggers markdown updates | retire | Cron callback wiring is Express-specific `node-cron` machinery; the Worker's scheduled handler (if any) is configured declaratively, not via runtime callback registration | PROPOSED: retire — cron callback wiring is Express-specific node-cron machinery with no Worker analogue |
| Processes all organizations in bulk mode (iterates orgs, calls bulkUpdateMarkdownStatuses per org) | scheduler.service.test.ts:118 | — | none found (searched: workers/src/health.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — health.test.ts:870-984 tests queue-level idempotency for a single catalogue import job, not a scheduled bulk operation across all orgs; database.markdown-credit-context.pglite.node.test.ts:31-61 tests per-product credit context for a single org, not a scheduled bulk markdown update iterating all orgs | retire | The scheduled bulk-markdown-across-all-orgs behaviour is Express-specific `node-cron` job machinery; the Worker has no scheduled handler that iterates all orgs for markdown status updates | PROPOSED: retire — scheduled bulk markdown update across orgs is Express-specific cron job machinery with no Worker analogue |
| Falls back to per-item autoCalculateMarkdownStatus when bulk update fails | scheduler.service.test.ts:142 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — health.test.ts:924-953 tests queue retry-exhaustion → mark-failed, not a bulk-to-per-item fallback; database.markdown-credit-context.pglite.node.test.ts tests credit context projection, not a fallback from bulk to per-item markdown calculation | retire | The bulk-to-per-item fallback is Express-specific scheduler error-recovery logic; the Worker has no scheduled bulk markdown job and thus no fallback path to test | PROPOSED: retire — bulk-to-per-item fallback is Express-specific scheduler error-recovery machinery with no Worker analogue |
| Creates a database backup without throwing (DatabaseBackupService.createBackup) | scheduler.service.test.ts:172 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:140-212 tests health-check connectivity to R2 and Neon Postgres, not a database backup operation; workers-deployment.test.ts:24-32 is a health smoke test with no backup coverage; the Worker uses Neon Postgres (managed), not SQLite, and has no backup service | retire | SQLite database backup is Express-specific machinery; the Worker runs on Neon Postgres (managed service) with no application-level backup service | PROPOSED: retire — SQLite backup service is Express-specific machinery; Worker uses managed Neon Postgres with no backup service |
| Handles backup errors gracefully (createBackup rejection swallowed) | scheduler.service.test.ts:177 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:148-159 tests R2 health-check degradation on failure, not backup error handling; workers-deployment.test.ts has no backup coverage; the Worker has no backup service to error-handle | retire | Backup error handling is Express-specific SQLite backup machinery; the Worker has no backup service and thus no backup error path to test | PROPOSED: retire — backup error handling is Express-specific SQLite machinery with no Worker analogue |

---

### secure.pin.policies.test.ts

Unit tests for `AuthService` PIN validation and hashing — instantiates `AuthService` directly
(no mocks, no database, no supertest). Tests PIN length/character/predictable-pattern policies
and bcrypt-style hash/verify round-trips. The Worker uses Clerk JWT-based authentication
(verified in `utils/auth.test.ts` and `__tests__/auth-integration.test.ts`); it has no PIN-based
auth, no `validatePin` / `hashPin` / `verifyPin` methods, and no PIN policy enforcement.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Accepts valid PINs of 4-6 digits (non-sequential, non-repeating) | secure.pin.policies.test.ts:11 | penetration | none found (searched: workers/src/index-minimal.ts, workers/src/__tests__/auth-integration.test.ts) — utils/auth.test.ts:72-83 tests JWT verification with valid tokens, not PIN validation; auth-integration.test.ts:13-30 tests valid JWT acceptance, not PIN acceptance; no `validatePin` or PIN-related function exists in the Worker auth module | retire | PIN validation is Express-specific auth machinery; the Worker uses Clerk JWT auth with no PIN-based authentication | PROPOSED: retire — PIN validation policy is Express-specific auth machinery; Worker uses Clerk JWT; penetration gate preserved by Worker JWT signature/expiry tests |
| Rejects PINs that are too short (fewer than 4 digits) | secure.pin.policies.test.ts:21 | negative/error | none found (searched: workers/src/index-minimal.ts, workers/src/__tests__/auth-integration.test.ts) — utils/auth.test.ts:108-117 tests malformed JWT rejection, not short-PIN rejection; auth-integration.test.ts:122-134 tests malformed Authorization header rejection, not PIN length validation; no PIN length check exists in the Worker | retire | PIN length validation is Express-specific auth machinery; the Worker has no PIN input surface to validate | PROPOSED: retire — short-PIN rejection is Express-specific auth machinery with no Worker analogue |
| Rejects PINs that are too long (more than 6 digits) | secure.pin.policies.test.ts:31 | negative/error | none found (searched: workers/src/index-minimal.ts, workers/src/__tests__/auth-integration.test.ts) — utils/auth.test.ts:108-117 tests malformed JWT rejection, not long-PIN rejection; auth-integration.test.ts:122-134 tests malformed header rejection, not PIN length validation; no PIN length check exists in the Worker | retire | PIN length validation is Express-specific auth machinery; the Worker has no PIN input surface to validate | PROPOSED: retire — long-PIN rejection is Express-specific auth machinery with no Worker analogue |
| Rejects PINs with non-digit characters | secure.pin.policies.test.ts:41 | negative/error | none found (searched: workers/src/index-minimal.ts, workers/src/__tests__/auth-integration.test.ts) — utils/auth.test.ts:108-117 tests malformed JWT rejection (non-PIN string), not PIN character validation; auth-integration.test.ts:122-134 tests malformed Authorization header, not PIN character validation; no PIN character check exists in the Worker | retire | PIN character validation is Express-specific auth machinery; the Worker has no PIN input surface to validate | PROPOSED: retire — non-digit PIN rejection is Express-specific auth machinery with no Worker analogue |
| Rejects predictable PIN patterns (sequential, reverse, repeating, keypad) | secure.pin.policies.test.ts:51 | penetration | none found (searched: workers/src/index-minimal.ts, workers/src/__tests__/auth-integration.test.ts) — utils/auth.test.ts:96-106 tests JWT with wrong secret rejection, not predictable-PIN-pattern rejection; auth-integration.test.ts:136-148 tests invalid JWT signature rejection, not PIN pattern analysis; no PIN pattern detection exists in the Worker | retire | Predictable-PIN-pattern rejection is Express-specific penetration defence for PIN-based auth; the Worker uses Clerk JWT auth with no PIN surface; the penetration gate is preserved by Worker JWT signature/expiry/malformed-token tests | PROPOSED: retire — predictable-PIN rejection is Express-specific penetration defence; Worker uses Clerk JWT; penetration gate preserved by Worker JWT tests |
| Accepts non-predictable PINs (random digit combinations) | secure.pin.policies.test.ts:91 | penetration | none found (searched: workers/src/index-minimal.ts, workers/src/__tests__/auth-integration.test.ts) — utils/auth.test.ts:72-83 tests valid JWT acceptance, not non-predictable PIN acceptance; auth-integration.test.ts:13-30 tests valid JWT format, not PIN randomness; no PIN pattern analysis exists in the Worker | retire | Non-predictable PIN acceptance is Express-specific auth machinery; the Worker has no PIN-based auth | PROPOSED: retire — non-predictable PIN acceptance is Express-specific auth machinery with no Worker analogue |
| Hashes and verifies PINs correctly (hash differs from plaintext, verify succeeds) | secure.pin.policies.test.ts:102 | penetration | none found (searched: workers/src/index-minimal.ts, workers/src/__tests__/auth-integration.test.ts) — utils/auth.test.ts:72-83 tests JWT verification (signature-based), not PIN hashing/verification; auth-integration.test.ts:13-30 tests JWT token validation, not PIN hash round-trip; no `hashPin` / `verifyPin` function exists in the Worker auth module | retire | PIN hashing/verification is Express-specific auth machinery; the Worker uses Clerk JWT (signature-based auth, not PIN hashing); the penetration gate is preserved by Worker JWT signature verification tests | PROPOSED: retire — PIN hash/verify is Express-specific auth machinery; Worker uses Clerk JWT signatures; penetration gate preserved by Worker JWT tests |
| Does not verify incorrect PINs (wrong PIN fails verification) | secure.pin.policies.test.ts:114 | negative/error | none found (searched: workers/src/index-minimal.ts, workers/src/__tests__/auth-integration.test.ts) — utils/auth.test.ts:96-106 tests JWT with wrong secret returns null, not wrong-PIN verification failure; auth-integration.test.ts:136-148 tests invalid JWT signature rejection, not incorrect PIN rejection; no `verifyPin` function exists in the Worker | retire | Incorrect-PIN rejection is Express-specific auth machinery; the Worker uses Clerk JWT auth with no PIN verification surface | PROPOSED: retire — incorrect-PIN rejection is Express-specific auth machinery with no Worker analogue |

---

### seed-master-catalogue-cli.test.ts

Unit tests for the `scripts/seed-master-catalogue` CLI — a Node.js script that seeds a master
catalogue from an Excel workbook. Tests import the script's pure functions directly
(`parseSeedMasterCatalogueArgs`, `assertSafeSeedInvocation`, `serializeSeedMasterCatalogueError`)
with no mocks, no database, no supertest. The Worker is a Cloudflare Worker with no CLI tooling;
its catalogue import pipeline (`catalogue-import-upsert.node.test.ts`) processes per-org CSV
uploads via HTTP/queue, not platform-operator Excel workbook seeding via CLI.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Parses a workbook path and supported flags (--dry-run, --confirm-retirements) | seed-master-catalogue-cli.test.ts:10 | — | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/database.catalogue-provenance.node.test.ts) — catalogue-import-upsert.node.test.ts:107-148 tests processCatalogueImportJob with CSV input via R2/queue, not CLI argument parsing for an Excel workbook; database.catalogue-provenance.node.test.ts:33-57 tests getCatalogueProvenance() reading seed run history, not CLI argument parsing; no CLI script exists in the Worker | retire | CLI argument parsing is Express-specific Node.js script tooling; the Worker has no CLI entry point and no Excel workbook seeding | PROPOSED: retire — CLI argument parsing is Express-specific Node.js tooling with no Worker analogue |
| Rejects missing paths, unknown flags, and multiple workbook paths | seed-master-catalogue-cli.test.ts:24 | negative/error | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/database.catalogue-provenance.node.test.ts) — catalogue-import-upsert.node.test.ts:249-296 tests insert/update/conflict classification via CSV, not CLI argument validation; database.catalogue-provenance.node.test.ts:33-57 tests provenance read, not CLI argument rejection; no CLI argument validation exists in the Worker | retire | CLI argument validation is Express-specific Node.js script tooling; the Worker has no CLI entry point to validate arguments for | PROPOSED: retire — CLI argument validation is Express-specific Node.js tooling with no Worker analogue |
| Rejects a live production seed from the checked-in sample workbook (assertSafeSeedInvocation) | seed-master-catalogue-cli.test.ts:34 | penetration | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/database.catalogue-provenance.node.test.ts) — catalogue-import-upsert.node.test.ts:348-364 tests quota breach failure with no partial writes, not a safety guard against seeding from a sample workbook in production; database.catalogue-provenance.node.test.ts:33-57 tests provenance read, not production-seed safety; no sample-workbook safety guard exists in the Worker | retire | The production-seed safety guard is Express-specific CLI tooling that prevents accidental production seeding from a checked-in sample Excel workbook; the Worker has no CLI seeding and no sample workbook | PROPOSED: retire — production-seed safety guard is Express-specific CLI tooling with no Worker analogue |
| Preserves validation details and the prospective diff in CLI JSON output (serializeSeedMasterCatalogueError) | seed-master-catalogue-cli.test.ts:62 | negative/error | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/database.catalogue-provenance.node.test.ts) — catalogue-import-upsert.node.test.ts:249-296 writes an error report to R2 (upload-errors/{id}.json) with conflict details, not a CLI JSON error serialization with prospective diff; database.catalogue-provenance.node.test.ts:33-57 tests provenance read, not error serialization; the Worker's error reporting is R2-based, not CLI JSON output | retire | CLI JSON error serialization is Express-specific Node.js script tooling; the Worker reports import errors via R2 error-report keys, not CLI JSON output | PROPOSED: retire — CLI JSON error serialization is Express-specific Node.js tooling; Worker uses R2 error reports |

---

### service-provider-constructor.test.ts

Unit tests for `ServiceProvider` — a tsyringe-style dependency injection container that
constructs and caches services/repositories with a `PrismaClient`. Tests instantiate
`ServiceProvider` directly with config objects and mock Prisma clients (no real database, no
supertest). The Worker has no DI container, no `ServiceProvider`, and no `PrismaClient`; it
creates a database via `createWorkersDatabase(env)` (seen in `health.test.ts:1457-1497`) and
calls methods directly.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Uses default values when no config provided (constructor does not throw) | service-provider-constructor.test.ts:6 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:1457-1497 creates a database via `createWorkersDatabase(env)` with env config, not a ServiceProvider constructor with default config; workers-deployment.test.ts:24-32 is a health smoke test with no DI container; no ServiceProvider class exists in the Worker | retire | ServiceProvider default construction is Express-specific tsyringe DI wiring with PrismaClient; the Worker has no DI container | PROPOSED: retire — ServiceProvider default construction is Express-specific DI machinery with no Worker analogue |
| Accepts organizationId in config | service-provider-constructor.test.ts:12 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:460-469 creates a mock Database with organizationId in user rows, not a ServiceProvider config with organizationId; workers-deployment.test.ts has no DI container; no ServiceProvider config exists in the Worker | retire | ServiceProvider organizationId config is Express-specific DI wiring; the Worker passes organizationId per-request, not via a DI container config | PROPOSED: retire — ServiceProvider organizationId config is Express-specific DI machinery with no Worker analogue |
| Accepts custom prisma client in config | service-provider-constructor.test.ts:17 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:1457-1497 uses `createWorkersDatabase(env)` with Neon/Hyperdrive, not a ServiceProvider with a custom PrismaClient; workers-deployment.test.ts has no PrismaClient; the Worker uses @neondatabase/serverless, not Prisma | retire | Custom PrismaClient injection is Express-specific DI wiring; the Worker uses Neon serverless driver, not Prisma | PROPOSED: retire — PrismaClient injection is Express-specific DI machinery; Worker uses Neon serverless driver |
| Accepts custom storage provider in config | service-provider-constructor.test.ts:23 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:431-458 creates an upload env with R2Bucket mock (CSV_UPLOADS), not a ServiceProvider with a custom storage provider; workers-deployment.test.ts has no storage provider config; the Worker uses R2 directly via env binding, not a pluggable storage provider | retire | Custom storage provider injection is Express-specific DI wiring; the Worker binds R2 via env (CSV_UPLOADS), not via a DI container | PROPOSED: retire — storage provider injection is Express-specific DI machinery; Worker uses R2 via env binding |
| Accepts all config options (organizationId, prisma, storageProvider) | service-provider-constructor.test.ts:28 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:116-138 creates an env with all config fields (NEON_CONNECTION_STRING, R2 bindings, etc.), not a ServiceProvider with all config options; workers-deployment.test.ts has no DI container; no ServiceProvider config exists in the Worker | retire | Full config construction is Express-specific DI wiring; the Worker uses env-based configuration, not a DI container config object | PROPOSED: retire — full config construction is Express-specific DI machinery with no Worker analogue |
| Creates provider for organization (forOrganization factory) | service-provider-constructor.test.ts:40 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:102 calls `makeDb().getDashboardStats(ORG)` with org as a method argument, not a factory creating a provider for an org; workers-deployment.test.ts has no factory method; no forOrganization factory exists in the Worker | retire | The forOrganization factory is Express-specific DI wiring; the Worker passes organizationId per-request as a method argument, not via a factory | PROPOSED: retire — forOrganization factory is Express-specific DI machinery with no Worker analogue |
| Creates provider for testing (forTesting factory) | service-provider-constructor.test.ts:45 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:460-469 creates a mock Database directly for testing, not via a forTesting factory on a ServiceProvider; workers-deployment.test.ts has no testing factory; no forTesting factory exists in the Worker | retire | The forTesting factory is Express-specific DI wiring; the Worker creates mock databases directly in tests, not via a factory | PROPOSED: retire — forTesting factory is Express-specific DI machinery with no Worker analogue |
| Creates provider with custom clients (withClients factory) | service-provider-constructor.test.ts:50 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:460-469 creates a mock Database with vi.fn() stubs directly, not via a withClients factory; workers-deployment.test.ts has no client injection factory; no withClients factory exists in the Worker | retire | The withClients factory is Express-specific DI wiring; the Worker creates mock databases directly, not via a factory | PROPOSED: retire — withClients factory is Express-specific DI machinery with no Worker analogue |
| Combines factory methods with config (forOrganization with prisma override) | service-provider-constructor.test.ts:56 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:102 passes ORG as a method argument to getDashboardStats, not a factory+config combination; workers-deployment.test.ts has no factory+config pattern; no factory method with config override exists in the Worker | retire | Factory+config combination is Express-specific DI wiring; the Worker uses env config and per-request org arguments, not factory+config | PROPOSED: retire — factory+config combination is Express-specific DI machinery with no Worker analogue |
| Reuses repository-backed services and repositories per provider instance (caching: same instance returned) | service-provider-constructor.test.ts:66 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:27 creates `createWorkersDatabase()` once per test setup and calls methods on it, but no test asserts that repeated getter calls return the same cached instance; workers-deployment.test.ts has no service caching; the Worker has no ServiceProvider caching pattern | retire | Service/repository instance caching is Express-specific DI container behaviour; the Worker creates a single database instance and calls methods directly, with no getter-caching pattern to test | PROPOSED: retire — service instance caching is Express-specific DI container machinery with no Worker analogue |
| Prevents ambiguous parameter passing (type-safe constructor API) | service-provider-constructor.test.ts:80 | — | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — health.test.ts:116-138 creates env via a typed `createEnv()` helper, not a ServiceProvider type-safe constructor; workers-deployment.test.ts has no type-safety constructor; no ServiceProvider type-safety pattern exists in the Worker | retire | Type-safe constructor API is Express-specific DI wiring; the Worker uses env-based configuration with TypeScript types, not a DI container constructor | PROPOSED: retire — type-safe constructor API is Express-specific DI machinery with no Worker analogue |

---

### sku-generator.test.ts

Unit tests for `generateSKU` — a pure utility function that generates SKUs with a prefix and
crypto-based random suffix. Tests call `generateSKU()` directly with no mocks, no database, no
supertest. The Worker does not generate SKUs; it uses SKUs from user CSV input (verified in
`upload/catalogue-parser.test.ts` which parses SKUs from CSV rows, and
`catalogue-import-upsert.node.test.ts` which imports products with CSV-provided SKUs).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Creates uppercase SKUs with the requested prefix and random suffix (format: PREFIX-[A-Z0-9]+-[A-F0-9]{12}) | sku-generator.test.ts:4 | — | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/utils/worker-response.test.ts) — catalogue-parser.test.ts:5-21 parses SKU '619647' from a CSV row, not a generated SKU; catalogue-import-upsert.node.test.ts:107-148 imports products with CSV-provided SKUs (e.g. 'NO-SKU-MATCH', 'RETIRED-SKU'), not generated SKUs; worker-response.test.ts:18-68 tests CORS and gzip, not SKU generation; no `generateSKU` function exists in the Worker | retire | SKU generation is an Express-specific utility; the Worker uses SKUs from user CSV input and does not generate SKUs | PROPOSED: retire — SKU generation utility is Express-specific with no Worker analogue; Worker uses CSV-provided SKUs |
| Does not use Math.random for SKU entropy (uses crypto-based randomness) | sku-generator.test.ts:10 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/utils/worker-response.test.ts) — catalogue-parser.test.ts:5-21 parses SKUs from CSV, no random generation; catalogue-import-upsert.node.test.ts:107-148 uses CSV-provided SKUs, no entropy source; worker-response.test.ts:18-68 tests CORS/gzip, no random ID generation; no SKU entropy test exists in the Worker | retire | The crypto-entropy requirement is Express-specific SKU generator machinery; the Worker does not generate SKUs and has no entropy source to test; the penetration gate is preserved by Worker JWT signature and webhook signature tests | PROPOSED: retire — crypto-entropy SKU generation is Express-specific utility with no Worker analogue; penetration gate preserved by Worker JWT/webhook signature tests |

---

### unit/storage-factory.test.ts
Unit tests for `createStorageProvider` — a factory that selects `LocalStorageProvider` or
`R2StorageProvider` based on `NODE_ENV` and config. Tests manipulate `process.env` directly
and assert provider instance types (no real database, no supertest, no R2 calls). The Worker
binds R2 directly via env (`CSV_UPLOADS: R2Bucket`); it has no storage factory, no
`LocalStorageProvider`, and no runtime provider selection — R2 is always the storage backend.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Creates LocalStorageProvider with default test path (NODE_ENV=test) | storage-factory.test.ts:26 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts:11-14 uses `env` with `CATALOGUE_IMPORT_QUEUE` mock, no storage factory or LocalStorageProvider; upload-router.test.ts:6-8 uses `env` with NODE_ENV only, no storage provider selection; health.test.ts:116-138 creates env with `CSV_UPLOADS` R2Bucket mock, not a LocalStorageProvider; no storage factory exists in the Worker | retire | LocalStorageProvider creation is Express-specific dev/test machinery; the Worker always uses R2 via env binding, never local filesystem | PROPOSED: retire — LocalStorageProvider factory selection is Express-specific dev/test machinery with no Worker analogue |
| Creates LocalStorageProvider with explicit base path (NODE_ENV=development) | storage-factory.test.ts:35 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts:61-73 uses `CSV_UPLOADS` R2Bucket mock with `get`/`put`, not a LocalStorageProvider with a base path; upload-router.test.ts has no storage provider; health.test.ts:431-458 creates env with R2Bucket mock, not a local storage path; no local storage base path exists in the Worker | retire | LocalStorageProvider with custom base path is Express-specific dev machinery; the Worker uses R2 with flat key namespaces, no filesystem base path | PROPOSED: retire — LocalStorageProvider base path config is Express-specific dev machinery with no Worker analogue |
| Throws when production R2 config is missing (R2_STORAGE_CONFIG incomplete) | storage-factory.test.ts:44 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts:46-58 tests disappearing R2 objects → 404, not missing R2 config → throw; upload-router.test.ts:33-50 tests malformed key encoding → 400, not missing R2 config; health.test.ts:216-221 tests missing database config → 500, not missing R2 config; health.test.ts:315-326 tests upload route auth/config errors (401/429/500), not R2 config validation; no Worker test asserts R2 config missing → throw | retire | R2 config validation is Express-specific factory machinery; the Worker binds R2 via env at deploy time (wrangler.toml), not at runtime via a factory that validates config | PROPOSED: retire — R2 config validation is Express-specific factory machinery; Worker binds R2 via env at deploy time |
| Creates R2StorageProvider when production config provided (accountId, accessKey, secretKey, bucket) | storage-factory.test.ts:50 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts:23-44 uses `env.CSV_UPLOADS` R2Bucket mock directly, not an R2StorageProvider instance created from config; upload-router.test.ts has no R2StorageProvider; health.test.ts:431-458 creates env with R2Bucket mock, not an R2StorageProvider from accountId/accessKey/secretKey; no R2StorageProvider class exists in the Worker | retire | R2StorageProvider construction from config is Express-specific factory machinery; the Worker uses R2Bucket bound via env directly, not an R2StorageProvider wrapper | PROPOSED: retire — R2StorageProvider factory construction is Express-specific machinery; Worker uses R2Bucket via env binding directly |
| Returns provider type names for logging (LocalStorageProvider / R2StorageProvider) | storage-factory.test.ts:63 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts has no provider type name logging; upload-router.test.ts has no storage provider type; health.test.ts:116-138 creates env with `STORAGE_PROVIDER: 'r2'` string, not a `getStorageProviderType()` call returning a class name; no provider type name function exists in the Worker | retire | Provider type name reporting is Express-specific factory machinery; the Worker uses a `STORAGE_PROVIDER` env string, not a runtime factory type query | PROPOSED: retire — provider type name reporting is Express-specific factory machinery with no Worker analogue |

---

### storage-index.test.ts

Unit tests for the `storage/` barrel module — verifies that named exports (provider classes,
factory helpers, error types) are defined. Pure import-based tests with no mocks, no database,
no supertest. The Worker has no storage module barrel export; it uses R2 directly via env
binding (`CSV_UPLOADS: R2Bucket`) and has no `LocalStorageProvider`, `R2StorageProvider`,
`createStorageProvider`, or storage error classes.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Exports provider classes and factory helpers (LocalStorageProvider, R2StorageProvider, createStorageProvider, getStorageProviderType, getDefaultStorageProvider, resetDefaultStorageProvider) | storage-index.test.ts:8 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts:1-9 imports `processStoredUpload`, `processCompletedUploadSync`, `queueCompletedCatalogueUpload`, `userOwnsUploadKey` from `./upload-handlers`, not storage provider classes or factory helpers; upload-router.test.ts:1-4 imports `handleWorkerUploadRoute` from `./upload-router`, not storage exports; health.test.ts:1-23 imports `healthCheck`, `worker`, handler functions, and `createWorkersDatabase`, not storage module exports; no storage barrel module exists in the Worker | retire | Storage module barrel exports are Express-specific module structure; the Worker has no storage module — it uses R2 via env binding directly | PROPOSED: retire — storage module barrel exports are Express-specific module structure with no Worker analogue |
| Exports error types (FileNotFoundError, FileSizeLimitError, StorageProviderError) | storage-index.test.ts:17 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts:46-58 maps disappearing R2 objects to HTTP 404 via `new Response(..., { status: 404 })`, not a `FileNotFoundError` class; upload-router.test.ts:33-50 returns a CORS-enabled 400 for malformed keys, not a `StorageProviderError`; health.test.ts:216-221 returns 500 for missing config, not a storage error class; no `FileNotFoundError`, `FileSizeLimitError`, or `StorageProviderError` class exists in the Worker | retire | Storage error type exports are Express-specific module structure; the Worker maps storage errors to HTTP status codes at the route level, not via storage-specific error classes | PROPOSED: retire — storage error type exports are Express-specific module structure; Worker uses HTTP status codes at route level |

---

### storage-provider-errors.test.ts

Unit tests for storage provider error classes (`FileNotFoundError`, `FileSizeLimitError`,
`StorageProviderError`) — verifies `name`, `message`, and `originalError` properties on
constructed instances. Pure class instantiation tests with no mocks, no database, no supertest.
The Worker has no storage provider error classes; it maps storage failures to HTTP status
codes at the route level (e.g. 404 for missing R2 objects, 500 for config errors).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Sets FileNotFoundError name and message (name='FileNotFoundError', message='File not found: {key}') | storage-provider-errors.test.ts:12 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts:46-58 maps disappearing R2 objects to HTTP 404 via `new Response(JSON.stringify({ error: 'Upload not found' }), { status: 404 })`, not a `FileNotFoundError` class with name/message; upload-router.test.ts:33-50 returns 400 for malformed keys, not a FileNotFoundError; health.test.ts:1310-1341 returns 404 when a completed upload disappears, not a FileNotFoundError class; no `FileNotFoundError` class exists in the Worker | retire | `FileNotFoundError` is an Express-specific storage provider error class; the Worker maps missing R2 objects to HTTP 404 at the route level, not via a typed error class | PROPOSED: retire — FileNotFoundError class is Express-specific storage abstraction; Worker uses HTTP 404 at route level |
| Sets FileSizeLimitError name and message (name='FileSizeLimitError', message includes actual and limit bytes) | storage-provider-errors.test.ts:19 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts has no file-size limit error; upload-router.test.ts has no file-size check; health.test.ts:531-562 tests presigned strategy for files larger than 2MB (strategy selection, not rejection), not a `FileSizeLimitError` class; health.test.ts:1073-1075 tests `isCatalogueWithinLimit` for SKU count limits, not file-size limits; no `FileSizeLimitError` class exists in the Worker | retire | `FileSizeLimitError` is an Express-specific storage provider error class; the Worker enforces file-size limits in code (index-minimal.ts) but maps rejections to HTTP status codes, not a typed error class | PROPOSED: retire — FileSizeLimitError class is Express-specific storage abstraction; Worker uses HTTP status codes for file-size rejection |
| Sets StorageProviderError name and preserves original error (name='StorageProviderError', originalError chained) | storage-provider-errors.test.ts:26 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/health.test.ts) — upload-handlers.test.ts:46-58 wraps R2 errors in `new Response(..., { status: 404 })` with a plain error message, not a `StorageProviderError` with `originalError` chaining; upload-router.test.ts:33-50 returns a plain `{ error: 'Invalid key encoding' }` 400, not a wrapped storage error; health.test.ts:191-204 redacts credentials from database errors in the health check response, not a `StorageProviderError` with `originalError`; no `StorageProviderError` class exists in the Worker | retire | `StorageProviderError` with original-error chaining is an Express-specific storage provider error class; the Worker wraps errors in HTTP responses with plain messages, not typed error classes with `originalError` | PROPOSED: retire — StorageProviderError class is Express-specific storage abstraction; Worker uses HTTP responses with plain error messages |

---

### storage-quota.repository.test.ts

Unit tests for `StorageQuotaRepository` with a **mocked Prisma client** (`upload.aggregate`,
`upload.create`, `upload.findUnique`, `upload.update`, `organizationUsage.upsert`,
`organizationUsage.update`, `$transaction` are all `vi.fn()` stubs). No real database, no
supertest — this is a pure call-shape test verifying the repository issues the right Prisma
calls and that `recordUpload` wraps upload-create + usage-increment in a single
`$transaction`. The Worker uses Neon via tagged-template `sql` (no Prisma, no `$transaction`,
no `organizationUsage.upsert` pattern) and enforces storage limits via tier thresholds
(`usage-limits-integration.test.ts:100-138`), but no Worker test asserts the byte-summing,
increment-on-record, or decrement-on-delete behaviour at the DB level.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Sums processing + completed upload bytes for an organization (aggregate with org-scoped where + status in [PROCESSING, COMPLETED]) | storage-quota.repository.test.ts:40 | tenant-isolation | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — usage-limits-integration.test.ts:100-138 asserts tier storage thresholds (100MB/1GB/Infinity) via stub `expect(expected).toBe(true)`, not an org-scoped byte-sum query; upload-handlers.test.ts:23-44 tests R2 queue-completion with mocked R2Bucket, not storage byte aggregation; database.dashboard.pglite.node.test.ts:179-226 tests getLastCatalogueUpload (most-recent completed upload per org), not storage byte sums | worker-shaped-rewrite | The org-scoped active-upload byte sum is the data behind storage-limit enforcement (a tenant-isolation gate: each org's usage counted against its own limit only), but no Worker test asserts the byte-summing query; the Prisma `aggregate` call shape retires with the backend, but the org-scoped storage-sum behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — org-scoped storage byte sum backs storage-limit enforcement but is untested in the Worker (usage-limits-integration.test.ts:100 covers tier thresholds only, not the byte-sum query); re-test tenant-isolation gate |
| Records upload metadata and increments organization storage in one $transaction (upload.create + organizationUsage.upsert with increment) | storage-quota.repository.test.ts:59 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — upload-handlers.test.ts:23-44 queues a completed upload (R2 metadata + queue send) but does not assert a DB write that increments org storage; upload-router.test.ts:13-31 tests route dispatch to the completion handler, not the DB record+increment; usage-limits-integration.test.ts has no upload-record test | worker-shaped-rewrite | The atomic record-upload + increment-storage behaviour is relevant to the Worker (it processes uploads and enforces storage limits) but no Worker test asserts the DB write or the increment; the Prisma `$transaction` + `upsert` call shape is Express-specific machinery with no Worker analogue, but the record-and-increment behaviour must be re-tested against the Worker's DB model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — record-upload + increment-storage is untested in the Worker (upload-handlers.test.ts:23 queues R2 completion, not a DB increment); Prisma $transaction call shape retires |
| Marks an upload deleted and decrements usage when the file exists (findUnique → update status + organizationUsage.update decrement) | storage-quota.repository.test.ts:101 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — upload-handlers.test.ts has no delete/decrement test; upload-router.test.ts:33-50 tests a malformed-key 400, not upload deletion; usage-limits-integration.test.ts has no decrement test | worker-shaped-rewrite | The mark-deleted + decrement-storage behaviour is relevant to the Worker (storage usage must decrease when uploads are removed) but no Worker test asserts it; the Prisma `findUnique` + `update` + `organizationUsage.update` call shape retires, but the decrement-on-delete behaviour must be re-tested against the Worker's DB model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — mark-deleted + decrement-storage is untested in the Worker; Prisma call shape retires, re-test decrement behaviour |
| Does not decrement usage when the upload does not exist (findUnique returns null → no update, no organizationUsage.update) | storage-quota.repository.test.ts:126 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — upload-handlers.test.ts:46-59 maps a disappearing R2 object to 404 during sync processing, not a no-decrement guard on a missing upload row; upload-router.test.ts has no missing-upload test; usage-limits-integration.test.ts has no negative decrement test | worker-shaped-rewrite | The no-decrement-when-missing guard (negative/error gate) is relevant to the Worker (prevents storage-counter drift on a missing upload) but no Worker test asserts it; the Prisma `findUnique`-returns-null branch is Express-specific call shape, but the no-op-on-missing behaviour must be re-tested against the Worker's DB model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — no-decrement-when-missing guard is untested in the Worker (upload-handlers.test.ts:46 covers R2 404, not DB no-decrement); re-test negative/error gate |

---

### storage-quota.routes.test.ts

Route-level tests for the Express `storage-quota` router using **supertest** against a real
Express app with a **mocked `StorageQuotaService`** (constructor, `getStorageQuota`,
`canUploadFile` are `vi.fn()` stubs). Auth context is mimicked via `x-user-id` / `x-org-id`
headers injected by a stub middleware — no real Clerk, no real database. Tests cover two
endpoints: `GET /storage-quota/:userId` (quota payload) and `GET
/storage-quota/:userId/can-upload` (pre-upload feasibility check), asserting input validation
(non-numeric userId, invalid tier, missing file size), auth/authorization (401 unauthenticated,
403 cross-user, 401 missing org), happy-path payloads, and error handling (500 on service
throw, 500 on non-Error throw). The Worker registers `/api/organization/usage`
(`minimal-api-routes.test.ts:156`) — an org-scoped usage endpoint, not user-scoped — but no
Worker test asserts its validation, auth, payload, or error behaviour. The Worker has no
`/storage-quota/:userId` or can-upload route and no pre-upload quota check test.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| GET /storage-quota/:userId returns 400 when userId param is not numeric (service not constructed) | storage-quota.routes.test.ts:52 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — minimal-api-routes.test.ts:156 registers `/api/organization/usage` but no test asserts a 400 for a non-numeric param on any usage/quota route; usage-limits-integration.test.ts:100-138 asserts tier storage thresholds via stubs, not route validation | worker-shaped-rewrite | The non-numeric-param 400 validation (negative/error gate) is relevant to the Worker's usage endpoint but untested; the Express supertest + mocked-service wiring is Express-specific, but the param-validation behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — non-numeric userId 400 is untested on the Worker's usage endpoint (minimal-api-routes.test.ts:156 registers the route but no validation test); re-test negative/error gate |
| GET /storage-quota/:userId returns 401 when requester is unauthenticated (no x-user-id) | storage-quota.routes.test.ts:63 | authorization-precedence | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/auth-integration.test.ts) — minimal-api-routes.test.ts:156 registers `/api/organization/usage` but no test asserts a 401 for unauthenticated access to it; auth-integration.test.ts covers Clerk auth flows but not the usage endpoint specifically | worker-shaped-rewrite | The 401-unauthenticated check (authorization-precedence gate) is relevant to the Worker's usage endpoint (Clerk auth required) but untested; the Express x-user-id header mechanism is Express-specific, but the reject-unauthenticated behaviour must be re-tested against the Worker's Clerk auth model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — 401 unauthenticated on usage endpoint is untested in the Worker (auth-integration.test.ts covers Clerk flows generally, not the usage endpoint); re-test authorization-precedence gate |
| GET /storage-quota/:userId returns 403 when user accesses another user's quota (x-user-id mismatch) | storage-quota.routes.test.ts:73 | authorization-precedence | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/constants/roles.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts has no cross-user quota access test; require-role.test.ts:157-168 tests role-based access (admin vs team_member), not user-identity-scoped quota access; multi-tenant-isolation.test.ts covers org-scoped data isolation, not user-scoped quota | worker-shaped-rewrite | The cross-user 403 (authorization-precedence gate) is user-scoped in Express; the Worker's quota is org-scoped (`/api/organization/usage`), so the user-scoped check retires but the authorization-precedence behaviour (reject access to another principal's quota) must be re-tested against the Worker's org-scoped model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — user-scoped cross-user 403 has no direct Worker equivalent (Worker quota is org-scoped); re-test authorization-precedence gate against org-scoped model |
| GET /storage-quota/:userId returns 401 when organization context is missing (no x-org-id) | storage-quota.routes.test.ts:83 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts:156 registers `/api/organization/usage` but no test asserts a 401 for missing org context; multi-tenant-isolation.test.ts covers org-scoped data isolation but not a missing-org-context rejection on the usage endpoint | worker-shaped-rewrite | The 401-missing-org-context check (tenant-isolation gate: quota is org-scoped, reject without org) is relevant to the Worker's org-scoped usage endpoint but untested; the Express x-org-id header mechanism is Express-specific, but the reject-missing-org behaviour must be re-tested against the Worker's Clerk org-context model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — 401 missing org context on usage endpoint is untested in the Worker; re-test tenant-isolation gate |
| GET /storage-quota/:userId returns 400 when tier query is invalid ('starter' not in [free, pro, enterprise]) | storage-quota.routes.test.ts:93 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/utils/feature-gates.test.ts) — minimal-api-routes.test.ts has no tier-query validation test on the usage endpoint; usage-limits-integration.test.ts:26-53 asserts tier limit values (starter/professional/concierge) but not a 400 for an invalid tier query param; feature-gates.test.ts tests tier feature access, not tier-query validation | worker-shaped-rewrite | The invalid-tier 400 validation (negative/error gate) is relevant to the Worker's usage endpoint (which accepts a tier context) but untested; the Express query-param validation is Express-specific wiring, but the reject-invalid-tier behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — invalid-tier 400 on usage endpoint is untested in the Worker (usage-limits-integration.test.ts:26 covers tier values, not validation); re-test negative/error gate |
| GET /storage-quota/:userId returns 200 with quota payload when request is valid (service constructed with org-1, getStorageQuota called with tier) | storage-quota.routes.test.ts:107 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — minimal-api-routes.test.ts:156 registers `/api/organization/usage` but no test asserts its 200 response payload (used/limit/percentageUsed/tier/displayLimit); usage-limits-integration.test.ts:100-138 asserts tier storage thresholds via stubs, not a 200 quota payload; database.dashboard.pglite.node.test.ts covers dashboard stats, not a quota payload | worker-shaped-rewrite | The 200 quota-payload behaviour is relevant to the Worker's `/api/organization/usage` endpoint but untested; the Express mocked-service payload shape is Express-specific, but the return-quota-payload behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — 200 quota payload on usage endpoint is untested in the Worker (minimal-api-routes.test.ts:156 registers the route but no payload test); re-test against Worker Response model |
| GET /storage-quota/:userId returns 500 when quota service throws (Error) | storage-quota.routes.test.ts:128 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic (connection timeout, transient errors), not a usage-endpoint service-throw → 500; minimal-api-routes.test.ts:475-510 tests 503 for markdown-config save on missing schema, not a usage-endpoint 500 | worker-shaped-rewrite | The service-throw → 500 behaviour (negative/error gate) is relevant to the Worker's usage endpoint but untested; the Express error middleware forwarding is Express-specific, but the endpoint-returns-500-on-failure behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — usage endpoint 500-on-throw is untested in the Worker (error-handling.test.ts:18 covers retry logic only); re-test negative/error gate |
| GET /storage-quota/:userId returns 500 with 'Unknown error' when route catches a non-Error throw | storage-quota.routes.test.ts:143 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not a non-Error throw → 500 with 'Unknown error' message; minimal-api-routes.test.ts has no non-Error-throw test on any endpoint | worker-shaped-rewrite | The non-Error-throw → 500-with-Unknown-error behaviour (negative/error gate) is relevant to the Worker's error handling but untested; the Express catch-all error middleware is Express-specific, but the graceful-non-Error-handling behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — non-Error throw → 500 Unknown error is untested in the Worker; re-test negative/error gate |
| GET /storage-quota/:userId/can-upload returns 400 when userId param is not numeric (service not constructed) | storage-quota.routes.test.ts:160 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/upload/upload-router.test.ts) — minimal-api-routes.test.ts has no can-upload route registration or non-numeric-param test; upload-router.test.ts:13-31 dispatches `/api/upload/complete` (post-upload), not a can-upload (pre-upload) route with param validation | worker-shaped-rewrite | The non-numeric-param 400 on the can-upload endpoint (negative/error gate) is relevant if the Worker has a pre-upload feasibility check, but no Worker test covers it; the Express supertest wiring is Express-specific, but the param-validation behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — can-upload non-numeric userId 400 is untested in the Worker (no can-upload route registered in minimal-api-routes.test.ts); re-test negative/error gate |
| GET /storage-quota/:userId/can-upload returns 401 when requester is unauthenticated | storage-quota.routes.test.ts:171 | authorization-precedence | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/auth-integration.test.ts, workers/src/upload/upload-router.test.ts) — minimal-api-routes.test.ts has no can-upload route; auth-integration.test.ts covers Clerk auth flows but not a can-upload endpoint; upload-router.test.ts:13 dispatches `/api/upload/complete` without an auth check test | worker-shaped-rewrite | The 401-unauthenticated check on the can-upload endpoint (authorization-precedence gate) is relevant if the Worker has a pre-upload check but untested; the Express x-user-id mechanism is Express-specific, but the reject-unauthenticated behaviour must be re-tested against the Worker's Clerk auth model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — can-upload 401 unauthenticated is untested in the Worker (no can-upload route); re-test authorization-precedence gate |
| GET /storage-quota/:userId/can-upload returns 403 when user checks another user's quota | storage-quota.routes.test.ts:181 | authorization-precedence | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/constants/roles.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts has no can-upload route; require-role.test.ts:157-168 tests role-based access, not user-identity-scoped can-upload; multi-tenant-isolation.test.ts covers org-scoped isolation, not user-scoped can-upload | worker-shaped-rewrite | The cross-user 403 on can-upload (authorization-precedence gate) is user-scoped in Express; the Worker has no can-upload route and its quota is org-scoped, so the user-scoped check retires but the authorization-precedence behaviour must be re-tested against the Worker's org-scoped model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — can-upload cross-user 403 has no direct Worker equivalent (no can-upload route, Worker quota is org-scoped); re-test authorization-precedence gate |
| GET /storage-quota/:userId/can-upload returns 401 when organization context is missing | storage-quota.routes.test.ts:191 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/upload/upload-router.test.ts) — minimal-api-routes.test.ts has no can-upload route; multi-tenant-isolation.test.ts covers org-scoped isolation, not a missing-org rejection on can-upload; upload-router.test.ts:13 dispatches upload-complete without an org-context check | worker-shaped-rewrite | The 401-missing-org-context check on can-upload (tenant-isolation gate) is relevant if the Worker has a pre-upload check but untested; the Express x-org-id mechanism is Express-specific, but the reject-missing-org behaviour must be re-tested against the Worker's Clerk org-context model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — can-upload 401 missing org is untested in the Worker (no can-upload route); re-test tenant-isolation gate |
| GET /storage-quota/:userId/can-upload returns 400 when file size query is missing or invalid | storage-quota.routes.test.ts:201 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/upload/upload-router.test.ts, workers/src/upload/upload-handlers.test.ts) — minimal-api-routes.test.ts has no can-upload route or file-size query validation; upload-router.test.ts:33-50 tests a malformed-key 400, not a missing-file-size 400; upload-handlers.test.ts:23-44 tests R2 queue-completion, not a pre-upload file-size validation | worker-shaped-rewrite | The missing-file-size 400 validation (negative/error gate) is relevant if the Worker has a pre-upload feasibility check but untested; the Express query-param validation is Express-specific wiring, but the reject-missing-file-size behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — can-upload missing-file-size 400 is untested in the Worker (no can-upload route); re-test negative/error gate |
| GET /storage-quota/:userId/can-upload returns 400 when tier query is invalid (canUploadFile not called) | storage-quota.routes.test.ts:214 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/utils/feature-gates.test.ts) — minimal-api-routes.test.ts has no can-upload route or tier-query validation; usage-limits-integration.test.ts:26-53 asserts tier limit values, not a 400 for invalid tier on can-upload; feature-gates.test.ts tests tier feature access, not can-upload tier validation | worker-shaped-rewrite | The invalid-tier 400 on can-upload (negative/error gate) is relevant if the Worker has a pre-upload check but untested; the Express query-param validation is Express-specific, but the reject-invalid-tier behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — can-upload invalid-tier 400 is untested in the Worker (no can-upload route); re-test negative/error gate |
| GET /storage-quota/:userId/can-upload returns canUpload=false with remainingBytes when upload would exceed quota | storage-quota.routes.test.ts:229 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — usage-limits-integration.test.ts:100-138 asserts tier storage thresholds (100MB/1GB/Infinity) via stubs, not a canUpload=false response with remainingBytes; upload-handlers.test.ts:23-44 tests R2 queue-completion, not a pre-upload quota-exceeded check; upload-router.test.ts has no can-upload route | worker-shaped-rewrite | The canUpload=false-with-remainingBytes behaviour (feature-limits gate: pre-upload quota enforcement) is relevant to the Worker (it enforces storage limits per tier) but no Worker test asserts a pre-upload rejection with remaining-bytes; the Express mocked-service response shape is Express-specific, but the quota-exceeded-rejection behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — canUpload=false with remainingBytes is untested in the Worker (usage-limits-integration.test.ts:100 covers tier thresholds, not pre-upload rejection); re-test feature-limits gate |
| GET /storage-quota/:userId/can-upload returns canUpload=true when file is allowed | storage-quota.routes.test.ts:257 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — usage-limits-integration.test.ts:100-138 asserts tier storage thresholds via stubs, not a canUpload=true response; upload-handlers.test.ts:23-44 tests R2 queue-completion (post-upload), not a pre-upload canUpload=true check; upload-router.test.ts has no can-upload route | worker-shaped-rewrite | The canUpload=true behaviour (feature-limits gate: pre-upload quota allows file within limit) is relevant to the Worker but untested; the Express mocked-service response is Express-specific, but the within-quota-allow behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — canUpload=true is untested in the Worker (no pre-upload feasibility check test); re-test feature-limits gate |
| GET /storage-quota/:userId/can-upload returns 500 when can-upload flow throws unexpectedly (Error) | storage-quota.routes.test.ts:269 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/upload/upload-router.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not a can-upload service-throw → 500; minimal-api-routes.test.ts:475-510 tests 503 for markdown-config, not can-upload 500; upload-router.test.ts has no can-upload error test | worker-shaped-rewrite | The can-upload service-throw → 500 behaviour (negative/error gate) is relevant if the Worker has a pre-upload check but untested; the Express error middleware is Express-specific, but the endpoint-returns-500-on-failure behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — can-upload 500-on-throw is untested in the Worker (error-handling.test.ts:18 covers retry logic only); re-test negative/error gate |
| GET /storage-quota/:userId/can-upload returns 500 with 'Unknown error' when can-upload catches a non-Error throw | storage-quota.routes.test.ts:285 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not a non-Error throw → 500 with 'Unknown error' on can-upload; minimal-api-routes.test.ts has no non-Error-throw test on a can-upload endpoint | worker-shaped-rewrite | The non-Error-throw → 500-with-Unknown-error behaviour on can-upload (negative/error gate) is relevant to the Worker's error handling but untested; the Express catch-all error middleware is Express-specific, but the graceful-non-Error-handling behaviour must be re-tested against the Worker's Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — can-upload non-Error throw → 500 Unknown error is untested in the Worker; re-test negative/error gate |

---

### store-area.model.test.ts

Unit tests for `StoreAreaModel` with a **mocked SQLite database** (`db.get`, `db.all`, `db.run`
are `vi.fn()` stubs — no real database, no supertest). Tests cover `createTable` (SQLite DDL via
`sqlite_master` + schema migration via `pragma_index_list`/`pragma_index_info` + DROP/CREATE
reinsert), and CRUD operations (create, findById, findByName,
findByNameAndSubDepartment, findAll, update, delete) with row-mapping from snake_case DB
columns to camelCase model fields. The Worker uses Neon/Postgres via tagged-template `sql`
(no `db.get`/`db.all`/`db.run`, no `sqlite_master`, no `pragma_index_*`); the `store_areas`
table exists in the Worker's Postgres schema (seeded in
`database.store-walk.pglite.node.test.ts:57-71` and `database.dashboard.pglite.node.test.ts:55-56`)
but the Worker has no store-area CRUD methods or routes — only store-walk tracking routes
(`minimal-api-routes.test.ts:187-197` registers check-cycles, bay-checks, floor-progress).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| createTable creates the store_areas table when it does not exist (sqlite_master check + CREATE TABLE with UNIQUE(name, sub_department)) | store-area.model.test.ts:36 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/database.conformance.node.test.ts, workers/src/workers-deployment.test.ts) — store-walk pglite :74-82 asserts the Neon migration file (0004_add_store_walk_bay_tracking.up.sql) does not contain 'ON COMMIT DROP' and does contain a DROP TABLE IF EXISTS, but does not test a create-table-if-not-exists path; the Worker uses Neon migration files, not runtime sqlite_master DDL; conformance.test.ts has no store_areas DDL test; workers-deployment.test.ts has no DDL coverage | retire | The createTable-if-not-exists behaviour is SQLite-specific DDL (`sqlite_master` query, runtime CREATE TABLE) with no Worker analogue; the Worker applies schema via Neon migration files (0004_add_store_walk_bay_tracking.up.sql, tested at store-walk pglite :74-82 for safety, not for create-if-not-exists); the SQLite DDL machinery retires with the backend | PROPOSED: retire — SQLite runtime DDL (sqlite_master + CREATE TABLE) has no Worker analogue; Worker uses Neon migration files (store-walk pglite :74-82 tests migration safety, not runtime DDL) |
| createTable migrates old unique-name schema (pragma_index_list/info → DROP TABLE → CREATE TABLE → reinsert rows) | store-area.model.test.ts:50 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/database.conformance.node.test.ts) — store-walk pglite :74-82 tests the Neon migration file's safety (no ON COMMIT DROP, DROP TABLE IF EXISTS), not a pragma-based schema migration; conformance.test.ts has no store_areas schema-migration test; the Worker has no pragma_index_list/pragma_index_info equivalent (Postgres uses pg_indexes, not SQLite pragmas) | retire | The pragma-based schema migration is SQLite-specific machinery (`pragma_index_list`, `pragma_index_info`, DROP/CREATE/reinsert) with no Worker analogue; the Worker applies schema changes via Neon migration files, not runtime pragma inspection; the SQLite schema-migration machinery retires with the backend | PROPOSED: retire — SQLite pragma-based schema migration has no Worker analogue; Worker uses Neon migration files (store-walk pglite :74-82 tests migration safety, not pragma migration) |
| createTable rethrows migration errors (console.error logged, error propagated) | store-area.model.test.ts:82 | negative/error | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/__tests__/error-handling.test.ts) — store-walk pglite :74-82 tests migration file safety, not migration error propagation; error-handling.test.ts:18-97 tests `withNeonRetry` retry logic (connection timeout, transient errors), not a DDL migration error rethrow; the Worker has no runtime createTable migration to error on | retire | The migration-error rethrow is SQLite-specific DDL error handling (pragma failure → console.error + rethrow) with no Worker analogue; the Worker applies migrations via Neon migration files at deploy time, not at runtime; the SQLite migration-error machinery retires with the backend | PROPOSED: retire — SQLite migration error rethrow has no Worker analogue; Worker uses Neon migration files applied at deploy time, not runtime DDL (error-handling.test.ts:18 covers retry logic, not DDL migration errors) |
| create inserts store area data and maps returned row (INSERT + camelCase mapping) | store-area.model.test.ts:91 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — store-walk pglite :57-71 inserts store_areas as test setup (department + bays), not via a create-store-area CRUD method; minimal-api-routes.test.ts:187-197 registers check-cycles/bay-checks/floor-progress routes, not a store-area create route; dashboard pglite :55-56 inserts store_areas as test setup, not a CRUD test | worker-shaped-rewrite | The create-store-area CRUD behaviour is relevant domain behaviour (store areas are in the Worker's schema and used by store-walk tracking) but the Worker has no store-area create method or route; the SQLite `db.get` INSERT + row-mapping call shape retires, but the create-store-area behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — create-store-area CRUD is untested in the Worker (no store-area CRUD method or route; store-walk pglite :57 inserts as setup only); re-test against Worker DB model |
| findById returns null when no store area row exists (SELECT by id + null mapping) | store-area.model.test.ts:116 | negative/error | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite has no findById store-area query (it queries store_areas for last_checked cache at :112-116, not a findById CRUD method); minimal-api-routes.test.ts has no store-area findById route | worker-shaped-rewrite | The findById-returns-null behaviour (negative/error gate: graceful null on missing row) is relevant domain behaviour but the Worker has no store-area findById method; the SQLite `db.get` SELECT call shape retires, but the find-by-id-null behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — findById-returns-null is untested in the Worker (no store-area findById method); re-test negative/error gate |
| findById maps and returns the row when found (SELECT by id + camelCase mapping) | store-area.model.test.ts:125 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite :112-116 queries store_areas for last_checked cache update, not a findById CRUD method with row mapping; minimal-api-routes.test.ts has no store-area findById route | worker-shaped-rewrite | The findById-returns-mapped-row behaviour is relevant domain behaviour but the Worker has no store-area findById method; the SQLite `db.get` + row-mapping call shape retires, but the find-by-id behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — findById-returns-row is untested in the Worker (no store-area findById method); re-test against Worker DB model |
| findByName returns empty array when no rows are found (SELECT by name + empty mapping) | store-area.model.test.ts:141 | negative/error | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite has no findByName store-area query; minimal-api-routes.test.ts has no store-area findByName route; the Worker queries store_areas by organization_id (store-walk pglite :115), not by name | worker-shaped-rewrite | The findByName-returns-empty behaviour (negative/error gate: graceful empty array on no matches) is relevant domain behaviour but the Worker has no store-area findByName method; the SQLite `db.all` SELECT call shape retires, but the find-by-name-empty behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — findByName-returns-empty is untested in the Worker (no store-area findByName method); re-test negative/error gate |
| findByName maps all matching rows (SELECT by name + array mapping with null sub_department) | store-area.model.test.ts:149 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite has no findByName store-area query; minimal-api-routes.test.ts has no store-area findByName route; the Worker queries store_areas by organization_id (store-walk pglite :115), not by name | worker-shaped-rewrite | The findByName-returns-mapped-rows behaviour is relevant domain behaviour but the Worker has no store-area findByName method; the SQLite `db.all` + array-mapping call shape retires, but the find-by-name behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — findByName-returns-rows is untested in the Worker (no store-area findByName method); re-test against Worker DB model |
| findByNameAndSubDepartment returns null when no row exists (NULL-safe sub_department comparison) | store-area.model.test.ts:160 | negative/error | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite has no findByNameAndSubDepartment query; minimal-api-routes.test.ts has no store-area findByNameAndSubDepartment route; the Worker does not query store_areas by name+sub_department combination | worker-shaped-rewrite | The findByNameAndSubDepartment-returns-null behaviour (negative/error gate: NULL-safe comparison + graceful null) is relevant domain behaviour but the Worker has no equivalent method; the SQLite NULL-safe comparison pattern (`sub_department IS NULL AND ? IS NULL`) is SQLite-specific SQL, but the find-by-name-and-subdepartment behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — findByNameAndSubDepartment-returns-null is untested in the Worker (no equivalent method); re-test negative/error gate |
| findAll maps all rows sorted by name (SELECT ORDER BY name + array mapping) | store-area.model.test.ts:172 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — store-walk pglite has no findAll store-area query (getFloorProgress at :155 returns walk progress, not a store-area list); minimal-api-routes.test.ts:187-197 registers floor-progress, not a store-area list route; dashboard pglite has no store-area findAll query | worker-shaped-rewrite | The findAll-sorted-by-name behaviour is relevant domain behaviour (listing store areas for management) but the Worker has no store-area findAll method or route; the SQLite `db.all` SELECT ORDER BY call shape retires, but the find-all-sorted behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — findAll-sorted-by-name is untested in the Worker (no store-area findAll method or route; getFloorProgress at store-walk pglite :155 returns walk progress, not a store-area list); re-test against Worker DB model |
| update returns null when no fields are provided (no DB call made) | store-area.model.test.ts:191 | negative/error | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite has no store-area update method (recordBayCheck at :106 updates last_checked cache, not a general update); minimal-api-routes.test.ts has no store-area update route | worker-shaped-rewrite | The update-returns-null-on-empty-fields behaviour (negative/error gate: no-op when no fields provided) is relevant domain behaviour but the Worker has no store-area update method; the SQLite `db.get` no-op call shape retires, but the no-op-on-empty-update behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — update-returns-null-on-empty is untested in the Worker (no store-area update method; recordBayCheck at store-walk pglite :106 updates last_checked cache only); re-test negative/error gate |
| update maps subDepartment field and returns updated row (UPDATE SET sub_department + row mapping) | store-area.model.test.ts:198 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite :106-128 records a bay check and updates last_checked cache via recordBayCheck, not a general store-area field update; minimal-api-routes.test.ts has no store-area update route | worker-shaped-rewrite | The update-store-area-field behaviour is relevant domain behaviour but the Worker has no store-area update method (only last_checked cache update via recordBayCheck at store-walk pglite :106); the SQLite `db.get` UPDATE call shape retires, but the update-field behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — update-store-area-field is untested in the Worker (no general update method; store-walk pglite :106 updates last_checked cache only via recordBayCheck); re-test against Worker DB model |
| delete returns true when row deletion count is greater than zero (DELETE + changes > 0) | store-area.model.test.ts:210 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite has no store-area delete method (it deletes store_areas in beforeEach setup at :44, not via a CRUD method); minimal-api-routes.test.ts has no store-area delete route | worker-shaped-rewrite | The delete-store-area-returns-true behaviour is relevant domain behaviour but the Worker has no store-area delete method or route; the SQLite `db.run` DELETE + changes-count call shape retires, but the delete-store-area behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — delete-store-area-returns-true is untested in the Worker (no store-area delete method or route; store-walk pglite :44 deletes in setup only); re-test against Worker DB model |
| delete returns false when no rows are deleted (DELETE + changes = 0) | store-area.model.test.ts:219 | negative/error | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — store-walk pglite has no store-area delete method; minimal-api-routes.test.ts has no store-area delete route; the Worker does not expose a delete-store-area CRUD operation | worker-shaped-rewrite | The delete-returns-false-on-no-rows behaviour (negative/error gate: graceful false on missing row) is relevant domain behaviour but the Worker has no store-area delete method; the SQLite `db.run` changes=0 call shape retires, but the no-delete-on-missing behaviour must be re-tested against the Worker's Neon/sql model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — delete-returns-false-on-no-rows is untested in the Worker (no store-area delete method); re-test negative/error gate |

---

### store-area.repository.test.ts

Unit tests for `StoreAreaRepository` with a **mocked Prisma client** (`storeArea.findMany/findFirst/create/update` and `$queryRaw`/`$executeRaw`/`$transaction` are `vi.fn()` stubs). No real database, no supertest — pure call-shape verification that the repository issues the right Prisma calls and normalizes optional fields. The first four tests cover basic store-area CRUD (list/find/create/update); the last four cover the store-walk tracking layer (check cycles, bay checks, floor progress) which the Worker re-implements on Neon/Postgres via `database.store-walk.pglite.node.test.ts` (real SQL on pglite).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Lists all store areas for an organization ordered by name (findMany with where:{organizationId}, orderBy:{name:'asc'}) | store-area.repository.test.ts:47 | tenant-isolation | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/database.worklist.pglite.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — store-walk test seeds store_areas via raw SQL and tests only check-cycle/bay-check/floor-progress behaviour (database.store-walk.pglite.node.test.ts:84-181), not a list-store-areas-by-name endpoint; minimal-api-routes.test.ts:187-197 registers only /api/store-areas/check-cycles, /bay-checks, /floor-progress routes — no GET /api/store-areas list route; multi-tenant-isolation.test.ts:89-104 "Store areas scoped per organization" is a stub asserting only expect(true).toBe(true), not a real equivalent | retire | The Worker exposes no store-area CRUD list endpoint; the test asserts a Prisma findMany call shape (where/orderBy) which is Prisma-specific machinery with no Worker analogue; the tenant-isolation gate is preserved manifest-wide by real cross-tenant exclusion in database.dashboard.pglite.node.test.ts:110-125 and org-scoped store-walk queries in database.store-walk.pglite.node.test.ts:155 | PROPOSED: retire — store-area list endpoint absent from Worker API; Prisma findMany call shape retires; tenant-isolation gate preserved by dashboard cross-tenant test and store-walk org-scoped queries |
| Finds a store area by id scoped to its organization (findFirst with where:{id, organizationId}) | store-area.repository.test.ts:59 | tenant-isolation | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — no Worker endpoint fetches a single store area by id; store-walk test operates on bay/department ids seeded via SQL and never looks up a store area by id through the DB API; minimal-api-routes.test.ts:187-197 registers no GET /api/store-areas/:id route; multi-tenant-isolation.test.ts:89-104 store-area stub is not a real equivalent | retire | The Worker exposes no store-area-by-id endpoint; the test asserts a Prisma findFirst call shape with an org-scoped where clause — Prisma-specific; the cross-org lookup-prevention behaviour for store areas has no Worker API surface, but the tenant-isolation gate is preserved by database.dashboard.pglite.node.test.ts:110-125 (real cross-tenant exclusion) and org-scoped store-walk queries | PROPOSED: retire — no Worker store-area-by-id endpoint; Prisma findFirst call shape retires; tenant-isolation gate preserved by dashboard cross-tenant test |
| Creates a store area normalizing optional fields (empty subDepartment → null, date string → Date) | store-area.repository.test.ts:70 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker endpoint creates a store area; store-walk test seeds store_areas via raw INSERT (database.store-walk.pglite.node.test.ts:57-69) with no normalization layer; minimal-api-routes.test.ts:187-197 registers no POST /api/store-areas route | retire | The Worker exposes no store-area create endpoint; the empty-string-to-null and date-string-to-Date normalization is a Prisma-layer create call-shape concern with no Worker analogue | PROPOSED: retire — store-area create endpoint absent from Worker API; Prisma create call shape + normalization retires with the backend |
| Updates only provided store area fields with normalization (update with where:{id}, data:{provided fields, empty→null}) | store-area.repository.test.ts:89 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker endpoint updates a store area; minimal-api-routes.test.ts:187-197 registers no PUT/PATCH /api/store-areas/:id route | retire | The Worker exposes no store-area update endpoint; the partial-field update + normalization is a Prisma update call-shape test with no Worker analogue | PROPOSED: retire — store-area update endpoint absent from Worker API; Prisma update call shape retires with the backend |
| Creates a check cycle after verifying no active cycle exists ($queryRaw checks active, then inserts) | store-area.repository.test.ts:106 | — | workers/src/database.store-walk.pglite.node.test.ts:87 — db.createCheckCycle(ORG, { name: 'Morning walk' }) creates the first cycle and returns it, exercising the same "no active cycle exists" precondition (the immediately following lines 88-90 reject a second) | worker-equivalent-exists | The Worker's createCheckCycle on real Postgres (pglite) covers the same create-after-verifying-no-active behaviour; the Prisma $queryRaw call shape retires but the behaviour is equivalent on real SQL | PROPOSED: worker-equivalent-exists — create-check-cycle behaviour covered by store-walk pglite test on real SQL |
| Rejects creating a second active cycle in the same organization (throws 'Active check cycle already exists') | store-area.repository.test.ts:129 | negative/error | workers/src/database.store-walk.pglite.node.test.ts:88-90 — await expect(db.createCheckCycle(ORG, { name: 'Second walk' })).rejects.toThrow('Active check cycle already exists') | worker-equivalent-exists | The Worker test asserts the identical rejection with the identical error message on real Postgres; the negative/error gate is preserved | PROPOSED: worker-equivalent-exists — duplicate-active-cycle rejection covered identically by store-walk pglite test |
| Records a bay check inside a transaction and updates derived lastChecked ($transaction + $executeRaw) | store-area.repository.test.ts:137 | — | workers/src/database.store-walk.pglite.node.test.ts:102-128 — db.recordBayCheck records the check against the active cycle and the bay's last_checked cache is updated (asserted at lines 112-127) | worker-equivalent-exists | The Worker's recordBayCheck on real Postgres covers the same record-bay-check + derived-lastChecked-update behaviour; the Prisma $transaction/$executeRaw call shape retires but the behaviour is equivalent on real SQL | PROPOSED: worker-equivalent-exists — bay-check + lastChecked update covered by store-walk pglite test on real SQL |
| Builds floor progress with ordered departments and checked bay state (coveragePercent, departments, bay states) | store-area.repository.test.ts:173 | — | workers/src/database.store-walk.pglite.node.test.ts:143-181 — db.getFloorProgress(ORG) returns departments grouped with summary (totalBays/checkedBays/coveragePercent:50) and per-bay states, matching the Express shape (the unchecked state is named 'overdue' in the Worker vs 'not_checked' in Express — a vocabulary difference, not a behaviour difference) | worker-equivalent-exists | The Worker's getFloorProgress on real Postgres covers the same department-grouped floor-progress with bay states and coverage; the Prisma $queryRaw call shape retires but the behaviour is equivalent | PROPOSED: worker-equivalent-exists — floor-progress behaviour covered by store-walk pglite test on real SQL |

---

### store-area.routes.test.ts

Express **supertest** route tests for `store-area.routes` with the `StoreAreaService`, auth middleware, `requireOrgRole`, validation middleware, and rate limiter all **mocked**. Tests exercise the route handlers over a real Express app (`request(app)`) but no real database — service methods are `vi.fn()` stubs. Covers two concerns: the store-walk tracking routes (check cycles, bay checks, floor progress), which the Worker re-exposes under `/api/store-areas/*` and tests in `minimal-api-routes.test.ts` + `database.store-walk.pglite.node.test.ts`; and the store-area CRUD routes (GET/POST/PUT/DELETE `/store-areas`), which the Worker does not expose — only store-walk tracking routes are registered per `minimal-api-routes.test.ts:187-197`.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Lists check cycles from the requester organization context (GET /store-areas/check-cycles, org context → service) | store-area.routes.test.ts:132 | tenant-isolation | workers/src/minimal-api-routes.test.ts:190 — registers GET /api/store-areas/check-cycles; workers/src/database.store-walk.pglite.node.test.ts:94 — db.listCheckCycles(ORG) returns cycles ordered (['Second walk', 'Morning walk']); the GET check-cycles handler is not separately exercised in minimal-api-routes.test.ts:199-262 (only POST/complete/record/progress are), but the org-scoped list behaviour is covered at the DB layer | worker-equivalent-exists | listCheckCycles behaviour covered on real SQL and the route is registered; the route-handler dispatch for GET check-cycles is not separately exercised but the org-scoped list behaviour is equivalent | PROPOSED: worker-equivalent-exists — list-check-cycles behaviour covered by store-walk pglite test; route registered in minimal-api-routes |
| Creates a check cycle for admin or manager users (POST /store-areas/check-cycles, role gate allows admin/manager) | store-area.routes.test.ts:141 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` canonicalisation and fail-closed default; workers/src/minimal-api-routes.test.ts — the live admin gates return 403 AUTHORIZATION_ERROR for a non-admin before any write (`handleClearSupplierPolicy`, `handleBulkAttachPolicy`, supplier policy write) and admit a legacy `org:admin` string | worker-equivalent-exists | create-cycle route with admin context covered; manager-allowed role gate covered by the require-role middleware test; the Express 'manager' role maps to the Worker's canonical manager role | PROPOSED: worker-equivalent-exists — create-cycle route + admin/manager role allowance covered by minimal-api-routes and require-role tests (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| Blocks team members from creating check cycles (POST /store-areas/check-cycles → 403) | store-area.routes.test.ts:156 | authorization-precedence | workers/src/constants/roles.test.ts — `normalizeRole` canonicalisation and fail-closed default; workers/src/minimal-api-routes.test.ts — the live admin gates return 403 AUTHORIZATION_ERROR for a non-admin before any write (`handleClearSupplierPolicy`, `handleBulkAttachPolicy`, supplier policy write) and admit a legacy `org:admin` string | worker-equivalent-exists | the role-gate behaviour (team_member → 403) is preserved by the Worker's require-role middleware test, which is the middleware the check-cycle route uses; the route-specific team_member block is not separately tested but the gate is covered | PROPOSED: worker-equivalent-exists — team_member 403 block covered by require-role middleware test; check-cycle route-specific block not separately exercised (citation repointed by task 3.1.0; the behaviour is covered against live code) |
| Maps duplicate active cycle errors to conflict responses (POST /store-areas/check-cycles → 409) | store-area.routes.test.ts:167 | negative/error | none found for the HTTP 409 mapping (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.store-walk.pglite.node.test.ts) — minimal-api-routes.test.ts:215-224 mocks createCheckCycle to resolve (201), never asserting the 409 conflict mapping; the underlying DB rejection IS covered at database.store-walk.pglite.node.test.ts:88-90 (rejects 'Active check cycle already exists') but the error-to-409-status mapping in the route handler is not tested | worker-shaped-rewrite | the DB-level duplicate-cycle rejection is covered, but the Express controller's error-to-409-conflict HTTP mapping has no Worker test; the Worker route handler's error mapping must be re-tested against its Request/Response model | PROPOSED: worker-shaped-rewrite — DB rejection covered by store-walk pglite but HTTP 409 conflict mapping untested in Worker |
| Completes an active check cycle by id (POST /store-areas/check-cycles/:id/complete) | store-area.routes.test.ts:179 | — | workers/src/minimal-api-routes.test.ts:225-233 — POST /api/store-areas/check-cycles/11/complete returns 200, calls completeCheckCycle('org_123', 11); workers/src/database.store-walk.pglite.node.test.ts:92 — db.completeCheckCycle(ORG, first.id) returns status 'completed' | worker-equivalent-exists | complete-cycle route and DB behaviour both covered | PROPOSED: worker-equivalent-exists — complete-cycle route + DB behaviour covered by minimal-api-routes and store-walk pglite |
| Records a bay check with authenticated user context (POST /store-areas/bay-checks, userId passed through) | store-area.routes.test.ts:189 | — | workers/src/minimal-api-routes.test.ts:234-243 — POST /api/store-areas/bay-checks returns 201, calls recordBayCheck('org_123', 7, {storeAreaId, checkedAt, itemsAddedCount, notes}); workers/src/database.store-walk.pglite.node.test.ts:106-128 — db.recordBayCheck records check with userId | worker-equivalent-exists | record-bay-check route with authenticated user context and DB behaviour both covered | PROPOSED: worker-equivalent-exists — bay-check route + user context + DB behaviour covered |
| Maps active cycle and leaf validation errors for bay checks (409 for no active cycle, 400 for non-leaf) | store-area.routes.test.ts:206 | negative/error | none found for the HTTP error mapping (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.store-walk.pglite.node.test.ts) — minimal-api-routes.test.ts:234-243 mocks recordBayCheck to resolve (201), never asserting the 409/400 mapping; the underlying DB validation IS covered at database.store-walk.pglite.node.test.ts:130-141 (rejects 'Active check cycle is required' and 'Bay check must target a leaf bay') but the error-to-status mapping (409/400) in the route handler is not tested | worker-shaped-rewrite | DB-level bay-check validation covered, but the Express controller's error-to-409/400 HTTP mapping has no Worker test; re-test against Worker Request/Response model | PROPOSED: worker-shaped-rewrite — DB validation covered by store-walk pglite but HTTP 409/400 error mapping untested in Worker |
| Returns floor progress for the requester organization (GET /store-areas/floor-progress, org-scoped) | store-area.routes.test.ts:223 | tenant-isolation | workers/src/minimal-api-routes.test.ts:244 — GET /api/store-areas/floor-progress returns 200, calls getFloorProgress('org_123'); workers/src/database.store-walk.pglite.node.test.ts:155 — db.getFloorProgress(ORG) returns org-scoped departments/summary | worker-equivalent-exists | floor-progress route with org context and DB behaviour both covered; org-scoping preserved | PROPOSED: worker-equivalent-exists — floor-progress route + org-scoped DB behaviour covered |
| Requires a non-empty check cycle name (checkCycleCreateSchema.safeParse rejects empty name) | store-area.routes.test.ts:239 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.store-walk.pglite.node.test.ts) — minimal-api-routes.test.ts:215-224 sends a valid name and asserts 201; no Worker test asserts empty-name rejection for POST /api/store-areas/check-cycles | worker-shaped-rewrite | the Express test validates the Zod schema checkCycleCreateSchema directly; the Worker's create-cycle route is tested only with valid input; the reject-empty-name validation behaviour must be re-tested against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — empty-name rejection untested in Worker; re-test validation against Worker route model |
| Requires a positive store area id for bay checks (bayCheckCreateSchema.safeParse rejects missing/negative id) | store-area.routes.test.ts:248 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.store-walk.pglite.node.test.ts) — minimal-api-routes.test.ts:234-243 sends a valid storeAreaId and asserts 201; no Worker test asserts missing/negative storeAreaId rejection for POST /api/store-areas/bay-checks | worker-shaped-rewrite | the Express test validates the Zod schema bayCheckCreateSchema directly; the Worker's bay-checks route is tested only with valid input; the reject-missing/negative-id validation behaviour must be re-tested against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — missing/negative storeAreaId rejection untested in Worker; re-test validation against Worker route model |
| Returns all store areas for requester organization (GET /store-areas, org context → getAllStoreAreas) | store-area.routes.test.ts:258 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.store-walk.pglite.node.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts:187-197 registers no GET /api/store-areas list route (only check-cycles/bay-checks/floor-progress); multi-tenant-isolation.test.ts:89-104 store-area stub asserts only expect(true).toBe(true), not a real equivalent | retire | Worker exposes no store-area list endpoint; the test asserts Express route wiring (org context → service.getAllStoreAreas); tenant-isolation gate preserved by database.dashboard.pglite.node.test.ts:110-125 and store-walk org-scoped queries | PROPOSED: retire — store-area list route absent from Worker; Express route wiring retires; tenant-isolation gate preserved by dashboard cross-tenant test |
| Returns error message when get-all throws an Error (GET /store-areas → 500) | store-area.routes.test.ts:267 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no GET /api/store-areas route exists in the Worker (minimal-api-routes.test.ts:187-197 registers only store-walk tracking routes) | retire | Worker exposes no store-area list endpoint; the 500 error-mapping is Express controller machinery for a non-existent Worker route | PROPOSED: retire — no Worker store-area list route; error-mapping retires with the endpoint |
| Returns 400 for invalid store area id (GET /store-areas/:id, non-numeric param) | store-area.routes.test.ts:278 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no GET /api/store-areas/:id route in the Worker (minimal-api-routes.test.ts:187-197) | retire | Worker exposes no store-area-by-id endpoint; the invalid-id 400 is Express route-param validation for a non-existent Worker route | PROPOSED: retire — no Worker store-area-by-id route; 400 invalid-id retires with the endpoint |
| Returns 404 when store area is not found (GET /store-areas/:id) | store-area.routes.test.ts:286 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no GET /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area-by-id endpoint; the 404-not-found mapping is Express controller machinery for a non-existent Worker route | PROPOSED: retire — no Worker store-area-by-id route; 404 retires with the endpoint |
| Returns store area by id on success (GET /store-areas/:id, org-scoped lookup) | store-area.routes.test.ts:295 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — no GET /api/store-areas/:id route in the Worker; multi-tenant-isolation.test.ts:89-104 store-area stub is not a real equivalent | retire | Worker exposes no store-area-by-id endpoint; tenant-isolation gate preserved by database.dashboard.pglite.node.test.ts:110-125 and store-walk org-scoped queries | PROPOSED: retire — no Worker store-area-by-id route; tenant-isolation gate preserved by dashboard cross-tenant test |
| Returns 500 when get-by-id fails unexpectedly (GET /store-areas/:id) | store-area.routes.test.ts:303 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no GET /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area-by-id endpoint; the 500 error-mapping retires with the endpoint | PROPOSED: retire — no Worker store-area-by-id route; 500 error-mapping retires |
| Returns 404 when no store areas exist for name (GET /store-areas/name/:name) | store-area.routes.test.ts:314 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no GET /api/store-areas/name/:name route in the Worker | retire | Worker exposes no store-area-by-name endpoint; the 404 mapping retires with the endpoint | PROPOSED: retire — no Worker store-area-by-name route; 404 retires |
| Returns matching store areas by name (GET /store-areas/name/:name, org-scoped) | store-area.routes.test.ts:325 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts) — no GET /api/store-areas/name/:name route in the Worker | retire | Worker exposes no store-area-by-name endpoint; org-scoped name lookup retires with the endpoint; tenant-isolation gate preserved by database.dashboard.pglite.node.test.ts:110-125 | PROPOSED: retire — no Worker store-area-by-name route; tenant-isolation gate preserved by dashboard cross-tenant test |
| Returns 500 when get-by-name fails unexpectedly (GET /store-areas/name/:name) | store-area.routes.test.ts:335 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no GET /api/store-areas/name/:name route in the Worker | retire | Worker exposes no store-area-by-name endpoint; the 500 error-mapping retires | PROPOSED: retire — no Worker store-area-by-name route; 500 retires |
| Returns 400 when required name field is missing (POST /store-areas) | store-area.routes.test.ts:348 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no POST /api/store-areas route in the Worker (minimal-api-routes.test.ts:187-197) | retire | Worker exposes no store-area create endpoint; the missing-field 400 is Express validation for a non-existent Worker route | PROPOSED: retire — no Worker store-area create route; 400 missing-field retires |
| Returns 201 and created store area on success (POST /store-areas) | store-area.routes.test.ts:359 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — no POST /api/store-areas route in the Worker | retire | Worker exposes no store-area create endpoint; the create route retires | PROPOSED: retire — no Worker store-area create route; create retires |
| Returns fallback message when create throws non-Error value (POST /store-areas → 500) | store-area.routes.test.ts:374 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no POST /api/store-areas route in the Worker | retire | Worker exposes no store-area create endpoint; the non-Error fallback 500 is Express controller error-handling machinery for a non-existent Worker route | PROPOSED: retire — no Worker store-area create route; non-Error fallback retires |
| Returns 400 for invalid id (PUT /store-areas/:id, non-numeric param) | store-area.routes.test.ts:388 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no PUT /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area update endpoint; the invalid-id 400 retires with the endpoint | PROPOSED: retire — no Worker store-area update route; 400 invalid-id retires |
| Returns 404 when store area to update does not exist (PUT /store-areas/:id) | store-area.routes.test.ts:399 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no PUT /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area update endpoint; the 404 retires with the endpoint | PROPOSED: retire — no Worker store-area update route; 404 retires |
| Updates store area and returns updated payload (PUT /store-areas/:id) | store-area.routes.test.ts:411 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — no PUT /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area update endpoint; the update route retires | PROPOSED: retire — no Worker store-area update route; update retires |
| Passes lastChecked field when provided in update payload (PUT /store-areas/:id) | store-area.routes.test.ts:429 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — no PUT /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area update endpoint; the lastChecked-passthrough retires with the endpoint | PROPOSED: retire — no Worker store-area update route; lastChecked passthrough retires |
| Returns error message when update throws an Error (PUT /store-areas/:id → 500) | store-area.routes.test.ts:441 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no PUT /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area update endpoint; the 500 error-mapping retires | PROPOSED: retire — no Worker store-area update route; 500 retires |
| Returns 400 for invalid id (DELETE /store-areas/:id, non-numeric param) | store-area.routes.test.ts:455 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no DELETE /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area delete endpoint; the invalid-id 400 retires with the endpoint | PROPOSED: retire — no Worker store-area delete route; 400 invalid-id retires |
| Returns 404 when store area to delete does not exist (DELETE /store-areas/:id) | store-area.routes.test.ts:465 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no DELETE /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area delete endpoint; the 404 retires with the endpoint | PROPOSED: retire — no Worker store-area delete route; 404 retires |
| Returns success message when deletion succeeds (DELETE /store-areas/:id) | store-area.routes.test.ts:474 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — no DELETE /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area delete endpoint; the delete route retires | PROPOSED: retire — no Worker store-area delete route; delete retires |
| Returns fallback message when delete throws non-Error value (DELETE /store-areas/:id → 500) | store-area.routes.test.ts:482 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — no DELETE /api/store-areas/:id route in the Worker | retire | Worker exposes no store-area delete endpoint; the non-Error fallback 500 is Express controller error-handling machinery for a non-existent Worker route | PROPOSED: retire — no Worker store-area delete route; non-Error fallback retires |

### store-area-service.test.ts

Two describe blocks. The first (`StoreAreaService - findUnique fix`) uses a **real SQLite
Prisma client** (`new PrismaClient` against `test.db`) to verify cross-tenant store-area
isolation and null-on-missing behaviour end-to-end. The second (`StoreAreaService repository
injection`) uses a **mocked `StoreAreaRepository`** (`vi.fn()` stubs) to assert the service
delegates to the injected repository and passes validation errors through — this is
tsyringe/DI wiring plus call-shape verification, not business logic. The Worker has no DI
container and no `getStoreAreaById` handler, but its `database.store-walk.pglite.node.test.ts`
covers the check-cycle, bay-check, and floor-progress behaviours against real Postgres SQL.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns null when reading a store area by ID from a different organization | store-area-service.test.ts:73 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/database.store-walk.pglite.node.test.ts) — multi-tenant-isolation.test.ts:89-104 is a stub asserting `expect(true).toBe(true)` with no real DB query or cross-tenant null assertion; database.store-walk.pglite.node.test.ts scopes every SELECT by `organization_id` (e.g. line 115) but no test asserts that a cross-tenant `getStoreAreaById` returns null | worker-shaped-rewrite | The tenant-isolation gate is named for preservation; the Worker enforces org scoping in SQL but has no test asserting cross-tenant single-area read returns null, and no `getStoreAreaById` handler exists | PROPOSED: worker-shaped-rewrite — tenant-isolation gate has no Worker equivalent asserting cross-tenant null; re-test against Worker store-area read model |
| Returns the store area when reading by ID from own organization | store-area-service.test.ts:79 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts) — the store-walk pglite suite reads own-org `store_areas` via org-scoped WHERE clauses (line 112-116) but no test asserts a `getStoreAreaById`-style single-area positive read returning own-org data; no `getStoreAreaById` handler exists in the Worker | worker-shaped-rewrite | The positive own-org read behaviour is relevant to the Worker but has no direct test coverage; the Worker's store-area reads are via floor-progress/bay-check aggregates, not a by-id lookup | PROPOSED: worker-shaped-rewrite — own-org store-area-by-id read has no Worker equivalent; re-test against Worker store-area read model |
| Returns null for a non-existent store area ID | store-area-service.test.ts:86 | negative/error | none found (searched: workers/src/database.store-walk.pglite.node.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — no Worker test asserts a null/empty return for a non-existent store area ID; the store-walk pglite suite only reads IDs it just inserted | worker-shaped-rewrite | The negative/error gate is named for preservation; the Worker has no test for the missing-ID null case on store areas | PROPOSED: worker-shaped-rewrite — missing-store-area null case has no Worker equivalent; re-test against Worker store-area read model |
| Delegates store-area reads to the injected repository (DI call shape) | store-area-service.test.ts:96 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts) — the Worker's `createWorkersDatabase` returns a module object of functions, not a DI-injected repository; no Worker test asserts repository injection or delegation call shapes | retire | tsyringe/repository-injection wiring is Express-specific machinery with no Worker analogue; the Worker uses direct module function calls | PROPOSED: retire — DI repository-injection call-shape test is Express/tsyringe machinery with no Worker analogue |
| Creates a check cycle and returns it shaped with status/timestamps | store-area-service.test.ts:129 | — | workers/src/database.store-walk.pglite.node.test.ts:87 — asserts `db.createCheckCycle(ORG, { name })` returns a cycle; line 96-99 asserts `status`/`completedAt`/`name` on the returned and listed cycles | worker-equivalent-exists | The createCheckCycle behaviour is covered against real Postgres SQL in the Worker; only the "through the injected repository" DI call-shape half retires with the backend | PROPOSED: worker-equivalent-exists — createCheckCycle behaviour covered by Worker pglite suite; DI call-shape half retires |
| Passes active-cycle and no-active-cycle validation errors through unchanged | store-area-service.test.ts:163 | negative/error | workers/src/database.store-walk.pglite.node.test.ts:88-90 — asserts `createCheckCycle` rejects with 'Active check cycle already exists'; line 133-135 asserts `recordBayCheck` rejects with 'Active check cycle is required' | worker-equivalent-exists | Both validation-error messages are asserted against real Postgres SQL in the Worker; the pass-through-via-repository mechanism differs but the error behaviours match | PROPOSED: worker-equivalent-exists — both validation-error behaviours covered by Worker pglite suite at lines 88-90 and 133-135 |
| Rejects bay checks targeting a non-leaf department | store-area-service.test.ts:180 | negative/error | workers/src/database.store-walk.pglite.node.test.ts:138-140 — asserts `recordBayCheck` with `departmentId` rejects with 'Bay check must target a leaf bay' | worker-equivalent-exists | The non-leaf-bay rejection is asserted against real Postgres SQL in the Worker using the same department-vs-bay fixture shape | PROPOSED: worker-equivalent-exists — non-leaf-bay rejection covered by Worker pglite suite at line 138-140 |
| Returns floor progress grouped by department with bay states and checked-by | store-area-service.test.ts:191 | — | workers/src/database.store-walk.pglite.node.test.ts:143-181 — asserts `getFloorProgress(ORG)` returns `activeCycle`, `summary` (totalBays/checkedBays/coveragePercent), and `departments` with bays carrying `state` and `checkedBy` | worker-equivalent-exists | The floor-progress grouping behaviour is covered against real Postgres SQL in the Worker, including the checked/overdue state mapping; only the "using repository data" DI half retires | PROPOSED: worker-equivalent-exists — floor-progress grouping covered by Worker pglite suite at line 143-181; DI half retires |

---

### store-walk-tracking.test.ts

Pure unit tests of the **shared domain logic** in `shared/domain/store-walk-tracking`
(`resolveBayState` and `rollupCoverage`). No Prisma, no database, no Express — these are
deterministic pure functions over `StoreWalkBay` and `BayCheckForCycle` inputs. Because the
logic is shared (not Express-specific), it is not itself retirement candidate; the question is
whether the Worker's store-walk suite exercises the same state-resolution and coverage-rollup
behaviours. The Worker's `database.store-walk.pglite.node.test.ts` asserts the end-to-end
floor-progress output (which embodies these rules) against real Postgres SQL.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Resolves a bay with a check in the active cycle to the `checked` state | store-walk-tracking.test.ts:28 | — | workers/src/database.store-walk.pglite.node.test.ts:174-176 — asserts `getFloorProgress` maps 'Dairy Bay 1' (which had a bay check recorded at line 150-153) to state `'checked'` with `checkedBy` populated | worker-equivalent-exists | The `checked` state resolution is asserted end-to-end in the Worker pglite suite after a real bay-check insert; the shared pure-function test and the Worker SQL test assert the same state outcome | PROPOSED: worker-equivalent-exists — `checked` state covered by Worker pglite suite at line 174-176 |
| Resolves a bay with no check history to the `not_checked` state | store-walk-tracking.test.ts:37 | — | none found (searched: workers/src/database.store-walk.pglite.node.test.ts) — both bays in the pglite fixture have either an active-cycle check or a prior `lastChecked` (line 67 sets 'Dairy Bay 2' lastChecked to 2026-01-01), so every bay resolves to `checked` or `overdue`; no bay is left with null history to assert the `not_checked` state | worker-shaped-rewrite | The `not_checked` state is a distinct behaviour of the shared resolver that the Worker pglite suite never exercises because its fixture gives every bay some history; the shared logic is not retired, but the Worker needs a row asserting the no-history case | PROPOSED: worker-shaped-rewrite — `not_checked` state has no Worker equivalent (pglite fixture gives every bay history); re-test against Worker floor-progress model |
| Resolves a bay checked only before the active cycle to the `overdue` state | store-walk-tracking.test.ts:46 | — | workers/src/database.store-walk.pglite.node.test.ts:176 — asserts 'Dairy Bay 2' (lastChecked 2026-01-01, before the cycle startedAt 2026-07-09) maps to state `'overdue'` | worker-equivalent-exists | The `overdue` state resolution is asserted end-to-end in the Worker pglite suite using a bay whose lastChecked predates the active cycle; this matches the shared resolver's pre-cycle rule | PROPOSED: worker-equivalent-exists — `overdue` state covered by Worker pglite suite at line 176 |
| Rolls up coverage totals and percentages by department and store | store-walk-tracking.test.ts:61 | — | workers/src/database.store-walk.pglite.node.test.ts:158-173 — asserts store-level `summary` (totalBays/checkedBays/uncheckedBays/coveragePercent) and per-department summaries (totalBays/checkedBays/uncheckedBays/coveragePercent) for the 'Dairy' department | worker-equivalent-exists | The store- and department-level rollup with coverage percentages is asserted in the Worker pglite suite; the pglite summary uses a combined `uncheckedBays` count rather than the shared test's separate `notCheckedBays`/`overdueBays` split, but the grouping and percentage behaviours match | PROPOSED: worker-equivalent-exists — coverage rollup covered by Worker pglite suite at line 158-173; unchecked split modelled as a single `uncheckedBays` count |

---

### stripe-sync.job.test.ts

Unit tests for `StripeSyncJob` — a **node-cron scheduled job** that reconciles subscription
tiers from the Stripe API into the local DB. Uses a **mocked Stripe client**, mocked
`SubscriptionRepository`, mocked `node-cron` scheduler, and mocked tsyringe DI container
(`getDiContainer().resolve`). The sync reconciliation logic (tier divergence, pagination,
missing records, API errors) is business logic with no Worker equivalent — the Worker has no
Stripe sync job and no test file for one. The cron-start test (line 242) is Express/node-cron
+ DI wiring machinery. `subscription-validation.test.ts` is entirely stubs
(`expect(true).toBe(true)`) covering subscription-status gating concepts, not Stripe sync;
`health.test.ts` covers HTTP route dispatch and health checks with no scheduled-handler
coverage.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Syncs a diverged subscription tier from Stripe into the local DB | stripe-sync.job.test.ts:73 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — subscription-validation.test.ts:17-46 is stub-only (`expect(true).toBe(true)`) covering subscription-status gating, not Stripe→DB tier reconciliation; health.test.ts has no Stripe sync coverage; no stripe-sync test file exists in the 44-file Worker inventory | worker-shaped-rewrite | The tier-reconciliation behaviour is business logic (pull from Stripe, compare, update DB) relevant to the Worker's subscription model, but the Worker has no sync job and no test for it; the node-cron scheduling mechanism retires with the backend | PROPOSED: worker-shaped-rewrite — Stripe→DB tier reconciliation has no Worker equivalent; re-test against Worker cron-trigger subscription-sync model |
| Skips update when local tier already matches Stripe | stripe-sync.job.test.ts:109 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — subscription-validation.test.ts:191-207 asserts tier_level values from fixture data, not a no-op-when-matching reconciliation; no Worker test asserts a sync that skips unchanged records | worker-shaped-rewrite | The no-op-on-match behaviour is the idempotent half of reconciliation; the Worker has no sync job to assert it | PROPOSED: worker-shaped-rewrite — no-op-when-matching reconciliation has no Worker equivalent; re-test against Worker subscription-sync model |
| Handles subscriptions missing from Stripe without deleting local records | stripe-sync.job.test.ts:141 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — subscription-validation.test.ts:50-62 covers canceled-subscription access denial, not missing-in-Stripe reconciliation; no Worker test asserts the do-not-delete-on-missing behaviour | worker-shaped-rewrite | The missing-in-Stripe no-delete behaviour is a negative/error guard on the sync job; the Worker has no sync job to assert it | PROPOSED: worker-shaped-rewrite — missing-in-Stripe no-delete guard has no Worker equivalent; re-test against Worker subscription-sync model |
| Handles a Stripe API error without crashing the sync job | stripe-sync.job.test.ts:167 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — health.test.ts:171-179 asserts the health check degrades on a DB error, not that a Stripe sync survives an API error; subscription-validation.test.ts has no error-handling assertions; no Worker test covers Stripe API failure during sync | worker-shaped-rewrite | The Stripe-API-error resilience is a negative/error guard on the sync job; the Worker has no Stripe sync job to assert it | PROPOSED: worker-shaped-rewrite — Stripe API error resilience has no Worker equivalent; re-test against Worker subscription-sync model |
| Skips local records with a null stripeSubscriptionId without throwing | stripe-sync.job.test.ts:176 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — subscription-validation.test.ts:111-123 covers no-subscription access denial, not a sync skipping null-ID records; no Worker test asserts null-stripeSubscriptionId skip logic | worker-shaped-rewrite | The null-ID skip is a negative/error guard on the sync job; the Worker has no sync job to assert it | PROPOSED: worker-shaped-rewrite — null-stripeSubscriptionId skip has no Worker equivalent; re-test against Worker subscription-sync model |
| Handles paginated Stripe responses with has_more=true by fetching subsequent pages | stripe-sync.job.test.ts:198 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — neither file tests Stripe API pagination; no Worker test asserts paginated Stripe list consumption | worker-shaped-rewrite | The pagination behaviour is business logic of the Stripe sync job; the Worker has no sync job to assert it | PROPOSED: worker-shaped-rewrite — Stripe pagination handling has no Worker equivalent; re-test against Worker subscription-sync model |
| Starts the node-cron job on a schedule with a DI-resolved subscription repository | stripe-sync.job.test.ts:242 | — | none found (searched: workers/src/health.test.ts) — health.test.ts has no scheduled/cron handler test; it covers HTTP route dispatch and health checks only; the Worker uses Cloudflare Cron Triggers, not node-cron, and has no DI container | retire | node-cron scheduling plus tsyringe DI resolution is Express/Node bootstrap machinery with no Worker analogue; the Worker's scheduled-handler model (Cron Triggers) and module-function wiring are structurally different | PROPOSED: retire — node-cron + DI-resolved repository start-up is Express/Node machinery with no Worker analogue |

---

### stripe-utils.test.ts

Unit tests for the **singleton Stripe client factory** (`getStripeClient` /
`resetStripeClient` / `isStripeConfigured`) backed by `envConfig.STRIPE_SECRET_KEY`. Uses
`vi.doMock('stripe')` and dynamic `import()` to reset module state between cases — no database,
no HTTP. The singleton + module-level mutable state pattern is Express/Node machinery: Workers
are stateless per-request with no module-level singletons. The config-validation behaviours
(missing key, non-secret key) carry the `negative/error` gate and are marked for Worker-shaped
rewrite so the gate is preserved; the Worker reads subscription tiers from the DB, not the
Stripe API, so it has no Stripe client factory today. `subscription-validation.test.ts` is
stubs covering status gating; `health.test.ts` has no Stripe config coverage.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Creates and returns a singleton Stripe client (same instance on repeat calls) | stripe-utils.test.ts:33 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — the Worker has no module-level singleton Stripe client; Workers are stateless per-request; health.test.ts constructs no Stripe client and its env (line 116-138) omits STRIPE_SECRET_KEY | retire | The module-level singleton pattern is Express/Node machinery with no Worker analogue; Workers construct clients per-request or not at all | PROPOSED: retire — singleton Stripe client factory is Express/Node module-state machinery with no Worker analogue |
| Throws when STRIPE_SECRET_KEY is missing | stripe-utils.test.ts:44 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — neither file validates a STRIPE_SECRET_KEY; the Worker reads subscription tiers from the DB, not the Stripe API, so no Stripe config guard exists | worker-shaped-rewrite | The missing-key config guard is a negative/error gate that must be preserved; the Worker has no Stripe client today, but if it gains one (e.g. a cron-trigger sync), the missing-key guard must be re-tested against the Worker config model | PROPOSED: worker-shaped-rewrite — missing-key config guard has no Worker equivalent; re-test against Worker Stripe-config model if Stripe is introduced |
| Throws when STRIPE_SECRET_KEY does not look like a secret key (not `sk_`-prefixed) | stripe-utils.test.ts:53 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — neither file validates the Stripe key format; no Worker test asserts an `sk_`-prefix check | worker-shaped-rewrite | The invalid-key-format guard is a negative/error gate that must be preserved; the Worker has no Stripe client today, but the format-validation guard must be re-tested if Stripe is introduced | PROPOSED: worker-shaped-rewrite — invalid-key-format guard has no Worker equivalent; re-test against Worker Stripe-config model if Stripe is introduced |
| Reports Stripe configuration status accurately (isStripeConfigured boolean) | stripe-utils.test.ts:62 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — neither file exposes an `isStripeConfigured`-style boolean; the Worker does not gate on Stripe config presence | retire | The config-status boolean is Express/Node config machinery tied to the singleton factory; the Worker has no equivalent config-status check | PROPOSED: retire — isStripeConfigured config-status check is Express config machinery with no Worker analogue |
| Creates a fresh Stripe client after resetStripeClient | stripe-utils.test.ts:75 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts) — the Worker has no module-level singleton to reset; Workers are stateless per-request | retire | The singleton reset mechanism is Express/Node module-state machinery with no Worker analogue | PROPOSED: retire — singleton reset is Express/Node module-state machinery with no Worker analogue |

---

### stripe-webhook-signature.service.test.ts

Unit tests for `StripeWebhookSignatureService` — a thin wrapper around Stripe's
`webhooks.constructEvent` for verifying Stripe webhook signatures. Uses a **mocked Stripe
client** (`constructEvent` is a `vi.fn()`); no real crypto, no HTTP. Both rows carry named
gates (`webhook-security`, `negative/error`). The Worker's `clerk/webhook-signature.test.ts`
covers the analogous webhook-signature-verification behaviour but for **Clerk/Svix** webhooks
(HMAC-SHA256 via `crypto.subtle`), not Stripe's `constructEvent`; the webhook-security gate is
preserved by that Clerk test, but Stripe-specific verification has no Worker equivalent.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Verifies a Stripe webhook signature by calling constructEvent with payload, signature, and webhook secret | stripe-webhook-signature.service.test.ts:5 | webhook-security | none found (searched: workers/src/clerk/webhook-signature.test.ts, workers/src/health.test.ts) — clerk/webhook-signature.test.ts:41-58 asserts a valid **Clerk/Svix** signature is accepted via `verifyClerkSvixSignature` (HMAC-SHA256 over `id.timestamp.body`), which is a different provider and mechanism than Stripe's `constructEvent`; health.test.ts:365-379 rejects Clerk webhooks missing Svix headers but does not verify a Stripe signature; no Worker test handles Stripe webhooks | worker-shaped-rewrite | The webhook-security gate is preserved by the Clerk/Svix signature test at line 41-58, but Stripe-specific `constructEvent` verification has no Worker equivalent; if the Worker gains a Stripe webhook handler, the signature-verification behaviour must be re-tested against the Worker's `Request` model | PROPOSED: worker-shaped-rewrite — Stripe constructEvent verification has no Worker equivalent (Clerk/Svix test covers the gate for Clerk only); re-test against Worker Stripe-webhook model if introduced |
| Throws when the Stripe secret is missing | stripe-webhook-signature.service.test.ts:22 | negative/error | none found (searched: workers/src/clerk/webhook-signature.test.ts, workers/src/health.test.ts) — clerk/webhook-signature.test.ts:41-58 and 63-83 test valid-signature and stale-timestamp cases but neither asserts a missing-secret guard; health.test.ts:365-379 tests missing Svix headers, not a missing Stripe secret; no Worker test asserts a missing-Stripe-secret throw | worker-shaped-rewrite | The missing-secret guard is a negative/error gate that must be preserved; the Clerk test does not cover a missing-secret case, and the Worker has no Stripe webhook handler to assert it | PROPOSED: worker-shaped-rewrite — missing-Stripe-secret guard has no Worker equivalent; re-test against Worker Stripe-webhook model if introduced |

---

### subscription.repository.test.ts

Unit tests for `SubscriptionRepository` with a **fully mocked Prisma client** — every
`subscriptionTier`/`organizationUsage`/`tierFeatureFlag`/`inventoryItem` method is a `vi.fn()`
stub. No real database, no HTTP: these are pure Prisma call-shape verifications asserting the
repository issues the right Prisma method with the right `where`/`data`/`select` arguments.
Prisma-specific call shapes are explicit retirement candidates per the task brief. Four rows
carry the `feature-limits` gate; the Worker's `utils/feature-gates.test.ts` covers
`checkFeatureAccess` (tier-feature lookup) and `checkUsageLimit` (usage-limit checks) with a
mocked DB client, providing an equivalent for the tier-feature-flag lookup but not for the
expiry-item count, latest-subscription lookup, or get-or-create-usage upsert.
`usage-limits-integration.test.ts` is stub-only (tier-limit constants).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Counts only unresolved active expiry items (status notIn processed/completed/etc.) for quota enforcement | subscription.repository.test.ts:50 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — feature-gates.test.ts:88-97 and 139-148 test `checkUsageLimit` for `max_skus`/`max_users` (product/user counts), not an inventory-item count with a status-exclusion filter; usage-limits-integration.test.ts:142-154 is a stub (`expect(true).toBe(true)`) describing a count query but not asserting the status-notIn filter | worker-shaped-rewrite | The feature-limits gate is preserved by `checkUsageLimit` for SKU/user counts, but the specific expiry-item count with status exclusion has no Worker equivalent; the Prisma `inventoryItem.count` call shape retires, but the count-active-expiry-for-quota behaviour must be re-tested against the Worker's usage-limit model | PROPOSED: worker-shaped-rewrite — active-expiry-item count with status exclusion has no Worker equivalent; re-test against Worker usage-limit model |
| Updates a subscription's Stripe customer id | subscription.repository.test.ts:64 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/utils/feature-gates.test.ts) — neither file updates a Stripe customer id; the Worker reads subscription tiers from the DB and does not sync Stripe customer IDs | retire | The `subscriptionTier.update({ where: { id }, data: { stripeCustomerId } })` Prisma call shape is Prisma-specific with no Worker analogue; the Worker has no Stripe customer-id sync path | PROPOSED: retire — Stripe customer-id update is a Prisma call shape with no Worker analogue |
| Groups subscriptions by tier and status for admin metrics | subscription.repository.test.ts:76 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — neither file groups subscriptions by tier+status; subscription-validation.test.ts:191-207 asserts tier_level fixture values, not a groupBy aggregate | retire | The `subscriptionTier.groupBy({ by: ['tierLevel', 'status'], _count: true })` Prisma call shape is Prisma-specific admin-metrics machinery with no Worker analogue | PROPOSED: retire — tier/status groupBy admin metrics is a Prisma call shape with no Worker analogue |
| Finds subscriptions linked to Stripe for reconciliation | subscription.repository.test.ts:94 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/utils/feature-gates.test.ts) — neither file queries Stripe-linked subscriptions; the Worker has no Stripe reconciliation path | retire | The `subscriptionTier.findMany({ where: { stripeSubscriptionId: { not: null } } })` Prisma call shape is Prisma-specific reconciliation machinery with no Worker analogue | PROPOSED: retire — Stripe-linked subscription query is a Prisma call shape with no Worker analogue |
| Updates a subscription by Stripe subscription id for reconciliation | subscription.repository.test.ts:130 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/utils/feature-gates.test.ts) — neither file updates by Stripe subscription id; the Worker has no Stripe reconciliation path | retire | The `subscriptionTier.updateMany({ where: { stripeSubscriptionId }, data })` Prisma call shape is Prisma-specific reconciliation machinery with no Worker analogue | PROPOSED: retire — Stripe-id-keyed subscription update is a Prisma call shape with no Worker analogue |
| Finds a tier feature flag by tier and feature key, returning the limit value | subscription.repository.test.ts:151 | feature-limits | workers/src/utils/feature-gates.test.ts:53-57 — asserts `checkFeatureAccess('starter', AVAILABLE_FEATURES.MAX_SKUS)` returns `result.limitValue` equal to `TIER_LIMITS.starter.max_skus`, which is the Worker's tier-feature-flag lookup returning the limit for a (tier, feature) pair | worker-equivalent-exists | The tier-feature-flag lookup behaviour is covered by the Worker's `checkFeatureAccess` which returns the limit value for a (tier, feature) pair; the mechanism differs (in-memory `TIER_LIMITS` vs Prisma `tierFeatureFlag` table) but the behaviour matches; the Prisma call shape retires | PROPOSED: worker-equivalent-exists — tier-feature-flag lookup covered by Worker `checkFeatureAccess` at line 53-57; Prisma call shape retires |
| Finds the latest subscription by organizationId (orderBy createdAt desc) for feature-gate tier determination | subscription.repository.test.ts:175 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — feature-gates.test.ts:88-97 and 324-366 pass the tier directly to `checkUsageLimit`/`checkFeatureAccess` (e.g. `'starter'`), never querying the latest subscription by orgId to determine it; usage-limits-integration.test.ts:142-154 is a stub describing a usage query but not a latest-subscription lookup | worker-shaped-rewrite | The feature-limits gate is preserved by `checkUsageLimit`/`checkFeatureAccess` which consume the tier, but the "find latest subscription to determine the tier" step has no Worker equivalent; the Prisma `findFirst` call shape retires, but the latest-subscription-for-tier behaviour must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — latest-subscription-for-tier lookup has no Worker equivalent; re-test against Worker subscription model |
| Gets or creates organization usage record with default limits for feature-gate checks | subscription.repository.test.ts:195 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — feature-gates.test.ts:88-97 queries current usage via `mockDbClient` returning `[{ count: N }]` (a direct count, not an upsert of a usage record); no Worker test asserts a get-or-create upsert of an `organization_usage` row with default limits | worker-shaped-rewrite | The feature-limits gate is preserved by `checkUsageLimit` which checks usage against limits, but the get-or-create-usage upsert mechanism has no Worker equivalent; the Prisma `organizationUsage.upsert` call shape retires, but the get-or-create-usage behaviour must be re-tested against the Worker's usage model | PROPOSED: worker-shaped-rewrite — get-or-create-usage upsert has no Worker equivalent; re-test against Worker usage model |
| Counts tier feature flags | subscription.repository.test.ts:235 | — | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — neither file counts tier feature flags; the Worker uses in-memory `TIER_LIMITS`/`AVAILABLE_FEATURES` constants, not a countable `tierFeatureFlag` table | retire | The `tierFeatureFlag.count()` Prisma call shape is Prisma-specific admin machinery with no Worker analogue; the Worker has no DB-backed feature-flag table to count | PROPOSED: retire — tier-feature-flag count is a Prisma call shape with no Worker analogue |
| Upserts a tier feature flag and reports whether it was newly seeded | subscription.repository.test.ts:243 | — | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — neither file upserts tier feature flags; the Worker uses in-memory constants, not a DB-backed flag table requiring seeding | retire | The `tierFeatureFlag.findUnique` + `tierFeatureFlag.upsert` Prisma call shape is Prisma-specific admin-seed machinery with no Worker analogue | PROPOSED: retire — tier-feature-flag seed upsert is a Prisma call shape with no Worker analogue |

---

### subscription.routes.test.ts

Unit tests for seven Express subscription routes — `GET /subscription/current`, `GET /organization/usage`, `GET /subscription/trial-status`, `POST /subscription/convert-trial`, `POST /subscription/create-checkout-session`, `POST /subscription/create-portal-session`, and `POST /subscription/cancel` — using **supertest** against an Express app with **mocked Prisma** (`mockFindUnique`), **mocked Stripe client** (`mockStripeCustomersCreate`, `mockStripeCheckoutSessionCreate`, `mockStripeBillingPortalSessionCreate`), **mocked `SubscriptionService`** (`mockConvertTrialToPaid`, `mockCancelSubscription`), and **mocked URL/price validators**. The tsyringe DI container is wired with real imports but mocked instances. Auth middleware is stubbed to set `req.userId` from the `x-clerk-user-id` header; rate limiters are stubbed to pass through. The Worker registers 2 of the 7 subscription routes (`/api/subscription/current` and `/api/organization/usage`) at `minimal-api-routes.test.ts:152` but does not test their handler behaviour; the remaining 5 endpoints have no Worker route registration or behaviour test. The Worker's `subscription-validation.test.ts`, `usage-limits-integration.test.ts`, `feature-gates-integration.test.ts`, and `org-status-integration.test.ts` are stub tests (`expect(true).toBe(true)` or fixture-value assertions) that do not exercise HTTP route behaviour. `utils/feature-gates.test.ts` tests feature-gate logic (`checkFeatureAccess`, `checkUsageLimit`) but not the subscription endpoints.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| GET /subscription/current returns the current subscription (tierLevel, status, billingCycle, currentPeriodEnd, cancelAtPeriodEnd) for the authenticated organization | subscription.routes.test.ts:274 | — | workers/src/minimal-api-routes.test.ts:152 — registers `['GET', '/api/subscription/current']` in the route table via `arrayContaining`, but does not test the handler's response body, org-scoping, or field serialization | worker-shaped-rewrite | The route is registered in the Worker route table but no Worker test exercises the handler's response; the Express supertest assertion of the response body has no Worker equivalent; the behaviour must be re-tested against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — route registered at minimal-api-routes.test.ts:152 but handler behaviour untested; re-test response body against Worker Response model |
| GET /organization/usage returns usage (skus, users, storage, inventoryItems) for the authenticated organization | subscription.routes.test.ts:289 | feature-limits | workers/src/minimal-api-routes.test.ts:152 — registers `['GET', '/api/organization/usage']` via `arrayContaining`, but does not test the handler response; workers/src/utils/feature-gates.test.ts:88 — `checkUsageLimit('org-1', 'max_skus', 'starter', mockDbClient)` returns `{isWithinLimit: true, currentUsage: 250, limit: 5000, percentageUsed: 5}`, testing usage-check logic but not the `/api/organization/usage` endpoint response shape | worker-shaped-rewrite | The route is registered but no Worker test exercises the endpoint response; `checkUsageLimit` at feature-gates.test.ts:88 tests the underlying usage-check logic but not the HTTP endpoint returning `{skus, users, storage, inventoryItems}`; the feature-limits behaviour must be re-tested against the Worker's Response model | PROPOSED: worker-shaped-rewrite — route registered at :152, usage-check logic at feature-gates.test.ts:88, but endpoint response untested; re-test feature-limits gate |
| GET /subscription/trial-status returns 404 when user or organization is not found | subscription.routes.test.ts:303 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts:111 tests "Organization without subscription cannot access service" but is a stub (`expect(noSubscription.status).toBe(null)`); minimal-api-routes.test.ts:152 registers `/api/subscription/current` and `/api/organization/usage` but not `/api/subscription/trial-status` | worker-shaped-rewrite | The trial-status endpoint is not registered in the Worker route table and no Worker test covers its 404-on-missing-user behaviour; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — trial-status 404-on-missing-user untested, route not registered in Worker; re-test negative/error gate |
| GET /subscription/trial-status returns starter limits and null subscription when organization has no subscription | subscription.routes.test.ts:314 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — subscription-validation.test.ts:111 is a stub checking `testData.subscription({status: null}).status`; feature-gates.test.ts:24 tests `checkFeatureAccess('starter', MAX_SKUS)` returns `{isEnabled: true, limitValue: 5000}`, testing starter-tier feature access but not the trial-status endpoint's `{tierLimits: {maxUsers, maxProducts, maxStoreAreas, features}}` response; usage-limits-integration.test.ts:15 checks `tierLimits.starter.max_skus === 500` (fixture value), not the endpoint | worker-shaped-rewrite | The starter-limits response for no-subscription orgs is not covered by any Worker test; `checkFeatureAccess` at feature-gates.test.ts:24 tests starter-tier feature gates but not the trial-status endpoint response; the feature-limits gate must be re-tested against the Worker's Response model | PROPOSED: worker-shaped-rewrite — starter-limits-for-no-subscription untested in Worker; feature-gates.test.ts:24 tests tier logic but not endpoint response; re-test feature-limits gate |
| GET /subscription/trial-status returns active trial status when DB status is "TRIALING" (uppercase, normalized to lowercase) | subscription.routes.test.ts:339 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts:125 tests "Free trial creates temporary subscription" but is a stub (`expect(trialSubscription.status).toBe('trial')`); minimal-api-routes.test.ts:152 does not register `/api/subscription/trial-status`; no Worker test covers case-insensitive DB status normalization | worker-shaped-rewrite | The case-insensitive status normalization (TRIALING → trialing) is not covered by any Worker test; the stub at subscription-validation.test.ts:125 only checks a fixture value, not HTTP route behaviour or case normalization | PROPOSED: worker-shaped-rewrite — case-insensitive TRIALING status normalization untested in Worker; re-test against Worker Response model |
| GET /subscription/trial-status returns active trial status when DB status is "trialing" (lowercase) | subscription.routes.test.ts:339 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts:125 is a stub; no Worker test covers the trial-status endpoint or case-insensitive status normalization | worker-shaped-rewrite | Same behaviour as the TRIALING variant — the route normalizes DB status to lowercase; no Worker test covers this | PROPOSED: worker-shaped-rewrite — case-insensitive trialing status normalization untested in Worker; re-test against Worker Response model |
| GET /subscription/trial-status returns active trial status when DB status is "Trialing" (mixed case) | subscription.routes.test.ts:339 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts:125 is a stub; no Worker test covers the trial-status endpoint or case-insensitive status normalization | worker-shaped-rewrite | Same behaviour as the TRIALING variant — the route normalizes DB status to lowercase; no Worker test covers this | PROPOSED: worker-shaped-rewrite — case-insensitive Trialing status normalization untested in Worker; re-test against Worker Response model |
| GET /subscription/trial-status returns expired trial status (isTrialExpired: true, daysRemaining < 0) when trial end date is in the past | subscription.routes.test.ts:369 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts:82 tests "Expired subscription blocks access after grace period" but is a stub (`expect(expiredSubscription.status).toBe('expired')`); no Worker test covers the trial-status endpoint's expired-trial response (isTrialExpired, daysRemaining) | worker-shaped-rewrite | The expired-trial status response is not covered by any Worker test; the stub at subscription-validation.test.ts:82 only checks a fixture value, not the HTTP route's `{isInTrial: false, isTrialExpired: true, daysRemaining: <0}` response | PROPOSED: worker-shaped-rewrite — expired-trial status response untested in Worker; re-test against Worker Response model |
| GET /subscription/trial-status serializes trialConvertedAt as ISO string when a conversion timestamp exists | subscription.routes.test.ts:395 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers trialConvertedAt serialization; subscription-validation.test.ts has no trialConvertedAt field in any stub; minimal-api-routes.test.ts:152 does not register trial-status | worker-shaped-rewrite | The trialConvertedAt ISO-serialization behaviour is not covered by any Worker test; the field does not appear in any Worker subscription test | PROPOSED: worker-shaped-rewrite — trialConvertedAt serialization untested in Worker; re-test against Worker Response model |
| GET /subscription/trial-status falls back to starter limits and null date fields for unknown tier metadata (custom-enterprise) | subscription.routes.test.ts:421 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — subscription-validation.test.ts:191 tests "Subscription tier_level determines feature access" but is a stub checking fixture values for starter/professional/concierge; feature-gates.test.ts:24 tests `checkFeatureAccess` for known tiers but not unknown-tier fallback; feature-gates-integration.test.ts:16 checks `tierLimits.starter.features` (fixture), not unknown-tier handling | worker-shaped-rewrite | The unknown-tier fallback to starter limits is not covered by any Worker test; `checkFeatureAccess` at feature-gates.test.ts:24 tests known tiers only; the feature-limits gate for unknown-tier fallback must be re-tested against the Worker's Response model | PROPOSED: worker-shaped-rewrite — unknown-tier fallback to starter limits untested in Worker; re-test feature-limits gate |
| GET /subscription/trial-status returns 500 when trial-status lookup throws | subscription.routes.test.ts:459 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — subscription-validation.test.ts has no error-path test for trial-status; minimal-api-routes.test.ts:152 does not register trial-status; handlers.test.ts:43-52 tests "should NOT query products across organization boundaries" but is a stub (`expect(true).toBe(true)`), not a 500-error test | worker-shaped-rewrite | The trial-status 500-on-lookup-throw behaviour is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — trial-status 500-on-throw untested in Worker; re-test negative/error gate |
| POST /subscription/convert-trial returns 400 when paymentMethodId is missing | subscription.routes.test.ts:472 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts has no convert-trial test; minimal-api-routes.test.ts:152 does not register `/api/subscription/convert-trial`; no Worker test covers convert-trial validation | worker-shaped-rewrite | The convert-trial endpoint is not registered in the Worker and no Worker test covers its 400-on-missing-paymentMethodId validation; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — convert-trial 400-missing-paymentMethodId untested, route not registered in Worker; re-test negative/error gate |
| POST /subscription/convert-trial returns 400 when billingCycle is invalid (not "monthly" or "annual") | subscription.routes.test.ts:483 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers convert-trial or billingCycle validation; the route is not registered in the Worker | worker-shaped-rewrite | The billingCycle validation is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — convert-trial 400-invalid-billingCycle untested, route not registered in Worker; re-test negative/error gate |
| POST /subscription/convert-trial returns 404 when user organization cannot be found | subscription.routes.test.ts:494 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers convert-trial or its 404-on-missing-org; the route is not registered in the Worker | worker-shaped-rewrite | The convert-trial 404-on-missing-org is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — convert-trial 404-on-missing-org untested, route not registered in Worker; re-test negative/error gate |
| POST /subscription/convert-trial converts trial to annual paid plan on success (convertTrialToPaid called with BillingCycle.ANNUAL) | subscription.routes.test.ts:507 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts:125 tests "Free trial creates temporary subscription" but is a stub (`expect(trialSubscription.status).toBe('trial')`); minimal-api-routes.test.ts:152 does not register convert-trial; no Worker test covers trial-to-paid conversion | worker-shaped-rewrite | The trial-to-paid conversion behaviour is not covered by any Worker test; the stub at subscription-validation.test.ts:125 only checks a fixture value, not the conversion flow | PROPOSED: worker-shaped-rewrite — trial-to-paid conversion untested in Worker; re-test against Worker Response model |
| POST /subscription/convert-trial maps monthly billing cycle to BillingCycle.MONTHLY | subscription.routes.test.ts:518 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers convert-trial or billingCycle mapping; the route is not registered in the Worker | worker-shaped-rewrite | The monthly billingCycle mapping is not covered by any Worker test; the route is not registered in the Worker | PROPOSED: worker-shaped-rewrite — monthly billingCycle mapping untested in Worker; re-test against Worker Response model |
| POST /subscription/convert-trial uses statusCode from thrown error object when conversion fails (402 card declined) | subscription.routes.test.ts:528 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers convert-trial error handling or statusCode propagation from thrown errors; the route is not registered in the Worker | worker-shaped-rewrite | The convert-trial 402-on-card-declined behaviour (propagating statusCode from thrown error) is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — convert-trial 402-statusCode-propagation untested in Worker; re-test negative/error gate |
| POST /subscription/convert-trial returns fallback error message for non-Error conversion failures (500) | subscription.routes.test.ts:542 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers convert-trial non-Error fallback handling; the route is not registered in the Worker | worker-shaped-rewrite | The convert-trial 500-on-non-Error-throw fallback is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — convert-trial 500-non-Error-fallback untested in Worker; re-test negative/error gate |
| POST /subscription/create-checkout-session returns 400 when required checkout fields are missing (priceId, successUrl, cancelUrl) | subscription.routes.test.ts:556 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers create-checkout-session or its field validation; minimal-api-routes.test.ts:152 does not register `/api/subscription/create-checkout-session` | worker-shaped-rewrite | The checkout-session endpoint is not registered in the Worker and no Worker test covers its 400-on-missing-fields validation; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — checkout 400-missing-fields untested, route not registered in Worker; re-test negative/error gate |
| POST /subscription/create-checkout-session returns 400 when request payload fails URL/price validation (invalid priceId length) | subscription.routes.test.ts:568 | penetration | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/utils/minimal-rate-limit.test.ts) — no Worker test covers checkout-session price-ID format validation or redirect-URL validation; minimal-api-routes.test.ts:152 does not register checkout-session; middleware.test.ts covers auth middleware, not Stripe price-ID validation | worker-shaped-rewrite | The price-ID format validation and redirect-URL validation are security controls preventing injection of arbitrary Stripe price IDs or open-redirect URLs; no Worker test covers this; the penetration gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — checkout price-ID/URL validation untested in Worker; re-test penetration gate against Worker Request model |
| POST /subscription/create-checkout-session rejects valid-looking Stripe price IDs that are not backend configured (attacker-controlled priceId) | subscription.routes.test.ts:587 | penetration | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/utils/minimal-rate-limit.test.ts) — no Worker test covers checkout-session price-ID allowlisting; minimal-api-routes.test.ts:152 does not register checkout-session; middleware.test.ts covers auth, not Stripe price-ID allowlisting | worker-shaped-rewrite | The price-ID allowlist check (rejecting `price_attacker_controlled_1234567890`) is a critical security control preventing an attacker from charging their own Stripe price; no Worker test covers this; the penetration gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — checkout price-ID allowlist rejection untested in Worker; re-test penetration gate against Worker Request model |
| POST /subscription/create-checkout-session returns 500 when Stripe checkout price configuration is missing in production | subscription.routes.test.ts:602 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers checkout-session or its 500-on-missing-config-in-production; minimal-api-routes.test.ts:152 does not register checkout-session | worker-shaped-rewrite | The 500-on-missing-Stripe-config-in-production is a server-misconfiguration error path; no Worker test covers this; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — checkout 500-missing-config-in-prod untested in Worker; re-test negative/error gate |
| POST /subscription/create-checkout-session returns 404 when checkout is requested without an organization | subscription.routes.test.ts:625 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers checkout-session or its 404-on-missing-org; minimal-api-routes.test.ts:152 does not register checkout-session | worker-shaped-rewrite | The checkout 404-on-missing-org is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — checkout 404-on-missing-org untested in Worker; re-test negative/error gate |
| POST /subscription/create-checkout-session creates checkout session using existing Stripe customer id (no new customer created) | subscription.routes.test.ts:641 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers checkout-session or its existing-customer reuse; minimal-api-routes.test.ts:152 does not register checkout-session | worker-shaped-rewrite | The existing-Stripe-customer reuse behaviour is not covered by any Worker test; the route is not registered in the Worker | PROPOSED: worker-shaped-rewrite — checkout existing-customer reuse untested in Worker; re-test against Worker Response model |
| POST /subscription/create-checkout-session creates checkout session for configured annual Stripe price id | subscription.routes.test.ts:665 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers checkout-session or annual price handling; minimal-api-routes.test.ts:152 does not register checkout-session | worker-shaped-rewrite | The annual-price checkout behaviour is not covered by any Worker test; the route is not registered in the Worker | PROPOSED: worker-shaped-rewrite — checkout annual-price untested in Worker; re-test against Worker Response model |
| POST /subscription/create-checkout-session creates and persists Stripe customer id when missing on existing subscription | subscription.routes.test.ts:683 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers checkout-session or Stripe customer creation/persistence; minimal-api-routes.test.ts:152 does not register checkout-session | worker-shaped-rewrite | The create-and-persist-Stripe-customer behaviour is not covered by any Worker test; the route is not registered in the Worker | PROPOSED: worker-shaped-rewrite — checkout create-and-persist-customer untested in Worker; re-test against Worker Response model |
| POST /subscription/create-checkout-session creates checkout session even when org has no subscription record yet | subscription.routes.test.ts:717 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers checkout-session for orgs with no subscription; minimal-api-routes.test.ts:152 does not register checkout-session | worker-shaped-rewrite | The checkout-for-no-subscription-record behaviour is not covered by any Worker test; the route is not registered in the Worker | PROPOSED: worker-shaped-rewrite — checkout for no-subscription-record untested in Worker; re-test against Worker Response model |
| POST /subscription/create-checkout-session passes undefined email to Stripe customer creation when contactEmail is null | subscription.routes.test.ts:743 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers checkout-session or null-contactEmail handling; minimal-api-routes.test.ts:152 does not register checkout-session | worker-shaped-rewrite | The null-contactEmail → undefined-email behaviour is not covered by any Worker test; the route is not registered in the Worker | PROPOSED: worker-shaped-rewrite — checkout null-contactEmail handling untested in Worker; re-test against Worker Response model |
| POST /subscription/create-checkout-session returns fallback checkout error message for non-Error throws (500) | subscription.routes.test.ts:773 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers checkout-session non-Error fallback handling; minimal-api-routes.test.ts:152 does not register checkout-session | worker-shaped-rewrite | The checkout 500-on-non-Error-throw fallback is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — checkout 500-non-Error-fallback untested in Worker; re-test negative/error gate |
| POST /subscription/create-portal-session returns 400 when returnUrl is invalid (open-redirect prevention) | subscription.routes.test.ts:791 | penetration | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/utils/minimal-rate-limit.test.ts) — no Worker test covers portal-session returnUrl validation; minimal-api-routes.test.ts:152 does not register `/api/subscription/create-portal-session`; middleware.test.ts covers auth, not URL validation | worker-shaped-rewrite | The returnUrl validation (rejecting `not-a-url`) is an open-redirect prevention security control; no Worker test covers this; the penetration gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — portal returnUrl validation untested in Worker; re-test penetration gate against Worker Request model |
| POST /subscription/create-portal-session returns 402 when portal session is requested without organization | subscription.routes.test.ts:805 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers portal-session or its 402-on-missing-org; minimal-api-routes.test.ts:152 does not register portal-session | worker-shaped-rewrite | The portal 402-on-missing-org is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — portal 402-on-missing-org untested in Worker; re-test negative/error gate |
| POST /subscription/create-portal-session returns 402 when no Stripe customer exists for portal session | subscription.routes.test.ts:819 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers portal-session or its 402-on-no-Stripe-customer; minimal-api-routes.test.ts:152 does not register portal-session | worker-shaped-rewrite | The portal 402-on-no-Stripe-customer is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — portal 402-on-no-Stripe-customer untested in Worker; re-test negative/error gate |
| POST /subscription/create-portal-session creates portal session using provided returnUrl | subscription.routes.test.ts:836 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers portal-session creation; minimal-api-routes.test.ts:152 does not register portal-session | worker-shaped-rewrite | The portal-session creation behaviour is not covered by any Worker test; the route is not registered in the Worker | PROPOSED: worker-shaped-rewrite — portal session creation untested in Worker; re-test against Worker Response model |
| POST /subscription/create-portal-session defaults portal return_url when request omits returnUrl | subscription.routes.test.ts:850 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers portal-session default returnUrl; minimal-api-routes.test.ts:152 does not register portal-session | worker-shaped-rewrite | The portal default-returnUrl behaviour is not covered by any Worker test; the route is not registered in the Worker | PROPOSED: worker-shaped-rewrite — portal default-returnUrl untested in Worker; re-test against Worker Response model |
| POST /subscription/create-portal-session returns portal error message when Stripe portal creation throws Error (500) | subscription.routes.test.ts:863 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers portal-session error handling; minimal-api-routes.test.ts:152 does not register portal-session | worker-shaped-rewrite | The portal 500-on-Stripe-throw is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — portal 500-on-Stripe-throw untested in Worker; re-test negative/error gate |
| POST /subscription/cancel cancels subscription at period end and returns success (cancelSubscription called with org-1) | subscription.routes.test.ts:877 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts:50 tests "Canceled subscription blocks all API access" but is a stub (`expect(canceledSubscription.status).toBe('canceled')`); minimal-api-routes.test.ts:152 does not register `/api/subscription/cancel`; no Worker test covers the cancel endpoint | worker-shaped-rewrite | The cancel-at-period-end behaviour is not covered by any Worker test; the stub at subscription-validation.test.ts:50 only checks a fixture value, not the cancel flow | PROPOSED: worker-shaped-rewrite — cancel-at-period-end untested in Worker; re-test against Worker Response model |
| POST /subscription/cancel returns 404 when user has no organization | subscription.routes.test.ts:890 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers the cancel endpoint or its 404-on-no-org; minimal-api-routes.test.ts:152 does not register cancel | worker-shaped-rewrite | The cancel 404-on-no-org is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — cancel 404-on-no-org untested in Worker; re-test negative/error gate |
| POST /subscription/cancel returns 500 when cancellation service throws unexpected error | subscription.routes.test.ts:900 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test covers the cancel endpoint or its 500-on-service-throw; minimal-api-routes.test.ts:152 does not register cancel | worker-shaped-rewrite | The cancel 500-on-service-throw is not covered by any Worker test; the negative/error gate must be preserved via a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — cancel 500-on-service-throw untested in Worker; re-test negative/error gate |

### subscription-access.helpers.test.ts

Pure unit tests for `hasActiveStripeAccessWindow` — a helper that inspects a Stripe
subscription object (`items.data[0].current_period_end`, `status`, `cancel_at_period_end`)
to decide whether the org still has access. No database, no supertest, no mocks beyond
hand-built Stripe-shaped fixtures. The behaviour under test — "canceled subscription keeps
access until the billing period ends, then closes it" — is a feature-limits/access-control
gate. The Worker's `subscription-validation.test.ts` and `feature-gates-integration.test.ts`
describe this scenario in comments but every assertion is a stub (`expect(true).toBe(true)`
or `expect(sub.status).toBe('canceled')`), so no Worker test actually exercises access-window
determination logic. The Stripe-specific helper shape (`items.data[0].current_period_end`)
retires with the backend, but the access-window behaviour must be re-tested against the
Worker's subscription model in Phase 3.2.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Keeps access open for non-canceled (active) subscriptions regardless of period_end | subscription-access.helpers.test.ts:13 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts) — subscription-validation.test.ts:17 describes "Active subscription allows all API access" but only asserts `expect(activeSubscription.status).toBe('active')` (a stub on the fixture, not access-window logic); feature-gates-integration.test.ts:16 tests tier-level feature arrays, not subscription status access windows; org-status-integration.test.ts:16 tests org status, not Stripe subscription access windows | worker-shaped-rewrite | The "active subscription grants access" behaviour is relevant to the Worker but only stubbed (subscription-validation.test.ts:17 asserts the fixture's status field, not the access-window determination); the Stripe `items.data[0].current_period_end` helper shape retires with the backend, but the access-grant behaviour must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — active-subscription access-window logic is stubbed only in subscription-validation.test.ts:17; re-test feature-limits gate against Worker subscription model |
| Keeps access open for canceled subscriptions until the period end is in the future | subscription-access.helpers.test.ts:23 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts) — subscription-validation.test.ts:50 describes "Canceled subscription blocks all API access" but asserts only `expect(canceledSubscription.status).toBe('canceled')` (a stub, and it asserts the opposite — blocking, not grace-until-period-end); no Worker test asserts that a canceled sub with a future period_end still grants access; feature-gates and org-status tests cover tier/org status, not Stripe cancellation grace windows | worker-shaped-rewrite | The "canceled but still within billing period → access open" grace behaviour is the core of the access-window gate and is not tested by any Worker test (the stub at subscription-validation.test.ts:50 even asserts blocking, not grace); the Stripe `current_period_end` shape retires, but the grace-until-period-end behaviour must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — canceled-sub grace-until-period-end is untested (subscription-validation.test.ts:50 stub asserts blocking, not grace); re-test feature-limits gate |
| Closes access once the canceled subscription's period end is in the past | subscription-access.helpers.test.ts:35 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts) — subscription-validation.test.ts:82 describes "Expired subscription blocks access after grace period" but asserts only `expect(expiredSubscription.status).toBe('expired')` (a stub on the fixture, not access-window logic); no Worker test asserts that a canceled sub with a past period_end returns false/blocks access; org-status-integration.test.ts:53 tests org inactive status, not Stripe subscription period-end closure | worker-shaped-rewrite | The "period ended → access closed" behaviour is the negative half of the access-window gate and is not tested by any Worker test (subscription-validation.test.ts:82 is a stub); the Stripe `current_period_end` comparison shape retires, but the period-end-closure behaviour must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — period-end access closure is stubbed only in subscription-validation.test.ts:82; re-test feature-limits gate against Worker subscription model |

---

### subscription-billing.helpers.test.ts

Pure unit tests for three Stripe billing helpers — `mapStripeSubscriptionStatusToLocal`,
`getPriceIdForTier`, and `getConfiguredStripePriceIds` — with hand-built fixtures and
`process.env` manipulation for price-ID configuration. No database, no supertest, no mocks.
The status-mapping helper translates Stripe API statuses to internal `SubscriptionStatus`
enum values; the price-ID helpers resolve env-var-configured Stripe Checkout price IDs per
tier/billing-cycle and enforce a Checkout allowlist. The Worker's `subscription-validation.test.ts`
stubs describe subscription status checking but never test a Stripe→local status mapping
function or any Checkout price-ID logic; no Worker test file in the 44-file inventory covers
Stripe Checkout integration. The status-mapping behaviour is relevant domain logic the Worker
would need if it handles Stripe subscriptions (untested → rewrite); the Checkout price-ID
configuration helpers are Stripe-Checkout-specific machinery with no Worker test analogue
(retire); the "reject non-Checkout tiers" guard is a feature-limits named gate that must be
preserved (untested → rewrite).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Maps Stripe statuses (active, canceled, past_due, trialing) to local SubscriptionStatus enum values | subscription-billing.helpers.test.ts:10 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/auth-integration.test.ts) — subscription-validation.test.ts:31 creates `testData.subscription({ status: 'active' })` and asserts `expect(activeSubscription.status).toBe('active')` (a stub on the fixture, not a Stripe→local mapping function); feature-gates-integration.test.ts tests tier feature arrays, not status mapping; auth-integration.test.ts tests JWT validation, not subscription status mapping; no Worker test imports or calls a status-mapping helper | worker-shaped-rewrite | The Stripe→local status mapping is relevant domain logic the Worker needs if it handles Stripe subscriptions (the stubs in subscription-validation.test.ts:31 imply it does), but no Worker test exercises the mapping; the Stripe API status string shape retires with the backend, but the mapping behaviour must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — Stripe→local status mapping is untested in the Worker (subscription-validation.test.ts:31 is a fixture stub, not a mapping test); re-test against Worker subscription model |
| Defaults incomplete and unknown Stripe statuses to ACTIVE | subscription-billing.helpers.test.ts:17 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/auth-integration.test.ts) — subscription-validation.test.ts:121 creates `testData.subscription({ status: null })` and asserts `expect(noSubscription.status).toBe(null)` (a stub, and it tests null not incomplete/unknown); no Worker test covers the "unknown Stripe status defaults to active" fallback behaviour; feature-gates and auth-integration tests do not cover status fallback logic | worker-shaped-rewrite | The "unknown/incomplete status defaults to active" fallback is a negative/error behaviour (graceful handling of unexpected Stripe statuses) that is not tested by any Worker test; the Stripe status string handling retires with the backend, but the fallback behaviour must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — unknown-status-defaults-to-active fallback is untested in the Worker (subscription-validation.test.ts:121 tests null, not incomplete/unknown); re-test negative/error gate |
| Returns configured Starter and Professional price IDs for monthly and annual billing cycles | subscription-billing.helpers.test.ts:70 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/auth-integration.test.ts) — no Worker test references Stripe price IDs, Checkout session creation, or env-var-based price configuration; subscription-validation.test.ts covers subscription status, not Checkout price lookup; feature-gates-integration.test.ts covers tier feature arrays, not price IDs; auth-integration.test.ts covers JWT validation | retire | The Stripe Checkout price-ID lookup is Express-specific billing integration machinery (env-var-based `STRIPE_*_PRICE_ID` configuration) with no Worker test analogue in the 44-file inventory; the Worker test suite has no Checkout or price-ID coverage, indicating this is Express-specific billing machinery | PROPOSED: retire — Stripe Checkout price-ID lookup is Express-specific billing machinery with no Worker test analogue (no Worker test references price IDs or Checkout) |
| Rejects 'free' as a Checkout tier (throws "Stripe Checkout is not available for tier: free") | subscription-billing.helpers.test.ts:79 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — subscription-validation.test.ts covers subscription status, not Checkout tier restrictions; feature-gates-integration.test.ts:16 tests tier feature arrays (starter lacks advanced_analytics), not Checkout tier eligibility; usage-limits-integration.test.ts covers usage quotas, not Checkout tier gating; no Worker test asserts that 'free' tier is rejected from Checkout | worker-shaped-rewrite | The "reject non-Checkout tiers" guard is a feature-limits named gate (only starter/professional can use Checkout) that must be preserved; no Worker test covers Checkout tier eligibility; the Stripe Checkout-specific throw shape retires, but the tier-eligibility gate must be re-tested against the Worker's billing model | PROPOSED: worker-shaped-rewrite — 'free' tier rejected from Checkout is untested in the Worker; re-test feature-limits gate against Worker billing model |
| Rejects 'enterprise' as a Checkout tier (throws "Stripe Checkout is not available for tier: enterprise") | subscription-billing.helpers.test.ts:79 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test covers Checkout tier eligibility or rejects 'enterprise' from Checkout; feature-gates-integration.test.ts tests tier feature arrays, not Checkout tier gating; usage-limits-integration.test.ts covers usage quotas, not Checkout tier restrictions | worker-shaped-rewrite | The "reject non-Checkout tiers" guard is a feature-limits named gate that must be preserved; no Worker test covers 'enterprise' tier Checkout rejection; the throw shape retires, but the tier-eligibility gate must be re-tested | PROPOSED: worker-shaped-rewrite — 'enterprise' tier rejected from Checkout is untested in the Worker; re-test feature-limits gate against Worker billing model |
| Rejects 'premium' as a Checkout tier (throws "Stripe Checkout is not available for tier: premium") | subscription-billing.helpers.test.ts:79 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test covers Checkout tier eligibility or rejects 'premium' from Checkout; feature-gates-integration.test.ts tests tier feature arrays, not Checkout tier gating; usage-limits-integration.test.ts covers usage quotas, not Checkout tier restrictions | worker-shaped-rewrite | The "reject non-Checkout tiers" guard is a feature-limits named gate that must be preserved; no Worker test covers 'premium' tier Checkout rejection; the throw shape retires, but the tier-eligibility gate must be re-tested | PROPOSED: worker-shaped-rewrite — 'premium' tier rejected from Checkout is untested in the Worker; re-test feature-limits gate against Worker billing model |
| Rejects 'concierge' as a Checkout tier (throws "Stripe Checkout is not available for tier: concierge") | subscription-billing.helpers.test.ts:79 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test covers Checkout tier eligibility or rejects 'concierge' from Checkout; feature-gates-integration.test.ts:95 tests concierge tier has all features, not that concierge is rejected from Checkout; usage-limits-integration.test.ts covers usage quotas, not Checkout tier restrictions | worker-shaped-rewrite | The "reject non-Checkout tiers" guard is a feature-limits named gate that must be preserved; no Worker test covers 'concierge' tier Checkout rejection; the throw shape retires, but the tier-eligibility gate must be re-tested | PROPOSED: worker-shaped-rewrite — 'concierge' tier rejected from Checkout is untested in the Worker; re-test feature-limits gate against Worker billing model |
| Rejects 'custom-tier' as a Checkout tier (throws "Stripe Checkout is not available for tier: custom-tier") | subscription-billing.helpers.test.ts:79 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test covers Checkout tier eligibility or rejects arbitrary/custom tiers from Checkout; feature-gates-integration.test.ts tests known tier feature arrays, not custom-tier Checkout rejection; usage-limits-integration.test.ts covers usage quotas, not Checkout tier restrictions | worker-shaped-rewrite | The "reject non-Checkout tiers" guard is a feature-limits named gate that must be preserved; no Worker test covers custom/arbitrary tier Checkout rejection; the throw shape retires, but the tier-eligibility gate must be re-tested | PROPOSED: worker-shaped-rewrite — 'custom-tier' rejected from Checkout is untested in the Worker; re-test feature-limits gate against Worker billing model |
| Exposes exactly the four configured launch price IDs (starter monthly/annual, professional monthly/annual) | subscription-billing.helpers.test.ts:88 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/auth-integration.test.ts) — no Worker test references Stripe price IDs or a configured-price-ID allowlist; subscription-validation.test.ts covers subscription status, not price-ID configuration; feature-gates-integration.test.ts covers tier feature arrays, not price IDs; auth-integration.test.ts covers JWT validation | retire | The configured-price-ID allowlist is Stripe Checkout configuration validation machinery (env-var-based `STRIPE_*_PRICE_ID` enumeration) with no Worker test analogue; the Worker test suite has no Checkout or price-ID coverage, indicating this is Express-specific billing machinery | PROPOSED: retire — configured-price-ID allowlist is Express-specific Stripe Checkout machinery with no Worker test analogue (no Worker test references price IDs) |
| Does not allow configured legacy price variables (STRIPE_PREMIUM_MONTHLY_PRICE_ID) into the Checkout allowlist | subscription-billing.helpers.test.ts:99 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/auth-integration.test.ts) — no Worker test references legacy Stripe price variables or a Checkout allowlist exclusion; subscription-validation.test.ts covers subscription status, not price-ID allowlist filtering; feature-gates-integration.test.ts covers tier feature arrays, not price-ID exclusion | retire | The legacy-price-ID exclusion is Stripe Checkout configuration validation machinery (filtering `STRIPE_PREMIUM_*` env vars from the Checkout allowlist) with no Worker test analogue; the Worker test suite has no Checkout or price-ID coverage, indicating this is Express-specific billing machinery | PROPOSED: retire — legacy-price-ID exclusion is Express-specific Stripe Checkout machinery with no Worker test analogue (no Worker test references price IDs or allowlist filtering) |

---

### subscription-lifecycle-services.test.ts

Unit tests for three subscription lifecycle services — `SubscriptionAccessService`,
`SubscriptionTrialLifecycleService`, and `SubscriptionBillingLifecycleService` — using
**mocked Prisma and mocked Stripe** (`vi.fn()` stubs on `prisma.*` and `stripe.*`). No real
database, no real Stripe API, no supertest. Tests cover access-check isolation from mutation
workflows, trial-setup orchestration (Stripe customer + Prisma subscriptionTier/usage/trialEvent),
missing-organization error handling, and billing cancellation orchestration (Stripe subscription
update + Prisma subscriptionTier update). The Worker's `subscription-validation.test.ts` stubs
describe trial creation (:125) and cancellation (:50) but never test the orchestration logic;
no Worker test covers lifecycle service coordination between Stripe and the database. The
Prisma-specific call shapes and tsyringe DI construction retire with the backend, but the
lifecycle behaviours (trial setup, cancellation, access-mutation isolation, missing-org error)
are relevant to the Worker and untested.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Keeps access-check decisions isolated from subscription mutation workflows (isAccessActive reads Stripe, does not call prisma.subscriptionTier.update) | subscription-lifecycle-services.test.ts:57 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts) — subscription-validation.test.ts:145 describes "requireActiveSubscription() validates before handler" but asserts only `expect(expected).toBe(true)` (a stub, not an isolation test); no Worker test asserts that an access-check path does not mutate subscription state; feature-gates and org-status tests cover tier/org status, not access-mutation isolation | worker-shaped-rewrite | The "access check is read-only (no mutation)" architectural invariant is relevant to the Worker (it should also separate access checks from mutations) but untested; the Prisma-specific `prisma.subscriptionTier.update` not-called assertion retires with the backend, but the isolation behaviour must be re-tested against the Worker's service model | PROPOSED: worker-shaped-rewrite — access-check-mutation isolation is untested in the Worker (subscription-validation.test.ts:145 is a stub); re-test against Worker service model |
| Creates trial setup through the trial lifecycle service (Stripe customer, subscriptionTier with TRIALING status, organizationUsage, trialEvent with trial_started) | subscription-lifecycle-services.test.ts:76 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/auth-integration.test.ts) — subscription-validation.test.ts:125 describes "Free trial creates temporary subscription" but asserts only `expect(trialSubscription.status).toBe('trial')` (a stub on the fixture, not trial-setup orchestration); no Worker test covers Stripe customer creation, subscription-tier creation, usage-record creation, or trial-event logging; feature-gates and auth-integration tests do not cover trial setup | worker-shaped-rewrite | The trial-setup orchestration (create customer, create subscription tier with TRIALING status, create usage record, log trial_started event) is relevant to the Worker but untested; the Prisma-specific call shapes (`prisma.subscriptionTier.create`, `prisma.organizationUsage.create`, `prisma.trialEvent.create`) retire with the backend, but the trial-setup behaviour must be re-tested against the Worker's lifecycle model | PROPOSED: worker-shaped-rewrite — trial-setup orchestration is stubbed only in subscription-validation.test.ts:125; re-test against Worker lifecycle model |
| Throws NotFoundError from trial setup when the organization is missing | subscription-lifecycle-services.test.ts:109 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/auth-integration.test.ts) — subscription-validation.test.ts:111 describes "Organization without subscription cannot access service" but asserts only `expect(noSubscription.status).toBe(null)` (a stub, not a missing-org error during trial setup); no Worker test covers a NotFoundError thrown when creating a trial for a missing organization; feature-gates and auth-integration tests do not cover trial-setup error handling | worker-shaped-rewrite | The "missing organization → NotFoundError" negative/error behaviour is relevant to the Worker (it should also reject trial setup for a missing org) but untested; the Prisma-specific `prisma.organization.findUnique` returning null retires with the backend, but the missing-org error behaviour must be re-tested against the Worker's lifecycle model | PROPOSED: worker-shaped-rewrite — missing-org NotFoundError during trial setup is untested in the Worker (subscription-validation.test.ts:111 is a stub); re-test negative/error gate |
| Updates billing state through the billing lifecycle service (cancelSubscription calls stripe.subscriptions.update with cancel_at_period_end, updates prisma.subscriptionTier) | subscription-lifecycle-services.test.ts:116 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts) — subscription-validation.test.ts:50 describes "Canceled subscription blocks all API access" but asserts only `expect(canceledSubscription.status).toBe('canceled')` (a stub on the fixture, not cancellation orchestration); no Worker test covers Stripe subscription update with cancel_at_period_end or subscription-tier status update; feature-gates and org-status tests cover tier/org status, not billing cancellation | worker-shaped-rewrite | The cancellation orchestration (Stripe subscription update with cancel_at_period_end, subscription-tier status update) is relevant to the Worker but untested; the Prisma-specific `prisma.subscriptionTier.update` call shape and Stripe `subscriptions.update` call shape retire with the backend, but the cancellation behaviour must be re-tested against the Worker's lifecycle model | PROPOSED: worker-shaped-rewrite — cancellation orchestration is stubbed only in subscription-validation.test.ts:50; re-test against Worker lifecycle model |

---

### subscription-trial.helpers.test.ts

Pure unit test for `buildTrialSubscriptionSetup` — a helper that constructs the data
structures for a trial subscription: `trialEndDate` (UTC midnight after the 14-day trial
period), `subscriptionTierData` (professional tier, trialing status, org/customer IDs),
and `organizationUsageData` (tier limits from `TIER_LIMITS.professional`). No database, no
Stripe, no mocks — just a fixed `startedAt` input and structural assertions on the output.
The Worker's `usage-limits-integration.test.ts` asserts the tier limit *values*
(`:40` max_skus=2000, `:82` max_users=10) that correspond to the same limits used here, but
no Worker test covers the trial-setup helper itself (trial-end-date calculation at UTC
midnight, subscription-tier data assembly, usage-data assembly with tier limits). The
`subscription-validation.test.ts:125` stub describes trial creation but only asserts
`expect(trialSubscription.status).toBe('trial')`.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Builds a professional trial with trialEndDate at UTC midnight after the trial period, subscriptionTierData (professional/trialing), and organizationUsageData with TIER_LIMITS.professional limits | subscription-trial.helpers.test.ts:5 | feature-limits | workers/src/__tests__/usage-limits-integration.test.ts:40 — `expect(tierLimits.professional.max_skus).toBe(2000)` asserts the professional SKU limit value; workers/src/__tests__/usage-limits-integration.test.ts:82 — `expect(tierLimits.professional.max_users).toBe(10)` asserts the professional user-seat limit value; these cover the tier-limit *values* used in the trial setup's organizationUsageData but not the trial-setup helper behaviour itself (trial-end-date at UTC midnight, subscriptionTierData assembly, usage-data assembly) | worker-shaped-rewrite | The tier-limit values (max_skus=2000, max_users=10) are covered by usage-limits-integration.test.ts:40 and :82, but the trial-setup helper behaviour — trial-end-date calculation at UTC midnight after 14 days, subscriptionTierData with professional/trialing status, organizationUsageData assembly with those limits — is not tested by any Worker test; the Prisma-specific data shapes retire with the backend, but the trial-setup behaviour must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — tier-limit values covered by usage-limits-integration.test.ts:40 and :82, but trial-setup helper (trial-end-date, tier-data, usage-data assembly) is untested; re-test feature-limits gate against Worker subscription model |

---

### supplier-credit.repository.test.ts

Unit tests for `SupplierCreditRepository` with a **mocked Prisma client** (`$queryRaw`,
`brand.findUnique/create/update`, `product.updateMany`, `catalogueCorrection.findFirst/create`,
`catalogueSeedRun.findMany`, `supplier.updateMany/findFirst` are all `vi.fn()` stubs). No real
database, no supertest — this is a pure call-shape and orchestration test. Tests cover catalogue
enrichment (retired filtering, barcode→SKU fallback, ambiguous-SKU unmatched, confirmed-brand
reuse), catalogue provenance pagination, policy review ordering, numbered catalogue review with
filtering/pagination, and policy clearing. The Worker has strong equivalents: `catalogue-import-upsert.node.test.ts`
tests enrichment end-to-end with real SQL (pglite), `database.catalogue-provenance.node.test.ts`
tests provenance pagination, and `database.supplier-policy.conformance.node.test.ts` tests policy
review ordering, numbered review filtering, and policy clearing — all with real SQL and SQLite
conformance checks. The Prisma-specific call shapes retire with the backend, but the behaviours
are covered by Worker tests with real SQL.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Filters retired entries across the parenthesized barcode-or-SKU predicate (SQL contains WHERE retired_at IS NULL AND (…UPPER(TRIM(ch2_sku)) = ?)) | supplier-credit.repository.test.ts:36 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:137 — asserts products with retired barcode (`RETIRED-BARCODE`) have `brand_name: null` (retired match ignored); workers/src/__tests__/catalogue-import-upsert.node.test.ts:145 — asserts corrections contain `{ barcode: 'RETIRED-BARCODE', kind: 'UNMATCHED' }` (retired barcode treated as unmatched, not enriched) | worker-equivalent-exists | The retired-filtering behaviour is covered end-to-end by catalogue-import-upsert.node.test.ts:137 (retired barcode → no brand) and :145 (retired barcode → UNMATCHED correction); the Prisma `$queryRaw` SQL string inspection retires with the backend, but the filtering behaviour is covered by the Worker's real-SQL test | PROPOSED: worker-equivalent-exists — retired-filtering covered by catalogue-import-upsert.node.test.ts:137 (no brand) and :145 (UNMATCHED correction); Prisma SQL string inspection is Express-specific |
| Falls back to a normalized wholesaler SKU when the barcode has no catalogue match (creates REFERENCE brand, updates product brandId, no correction) | supplier-credit.repository.test.ts:51 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:190 — asserts `bySku.get('api-two')` equals `bySku.get('IGNORED-SKU')` (SKU fallback reuses same brand as barcode match); workers/src/__tests__/catalogue-import-upsert.node.test.ts:219 — asserts brands have `source: 'REFERENCE'` with `suggested_supplier_name` set (e.g. 'Maker One'), matching the backend's brand creation with REFERENCE source | worker-equivalent-exists | The barcode→SKU fallback with REFERENCE brand creation and product update is covered by catalogue-import-upsert.node.test.ts:190 (SKU fallback reuses brand) and :219 (REFERENCE brand with suggested_supplier_name); the Prisma `brand.create`/`product.updateMany` call shapes retire, but the enrichment behaviour is covered by the Worker's real-SQL test | PROPOSED: worker-equivalent-exists — barcode→SKU fallback with REFERENCE brand covered by catalogue-import-upsert.node.test.ts:190 and :219; Prisma call shapes are Express-specific |
| Treats an ambiguous normalized SKU as unmatched (no brand create, no product update, creates UNMATCHED catalogue correction) | supplier-credit.repository.test.ts:89 | negative/error | workers/src/__tests__/catalogue-import-upsert.node.test.ts:194 — asserts `bySku.get('AMBIGUOUS')` is null (ambiguous SKU matching two catalogue entries is not enriched); workers/src/__tests__/catalogue-import-upsert.node.test.ts:243 — asserts corrections contain `{ barcode: 'STORE-AMB-BARCODE', kind: 'UNMATCHED' }` (ambiguous SKU → UNMATCHED correction) | worker-equivalent-exists | The ambiguous-SKU-as-unmatched behaviour is covered by catalogue-import-upsert.node.test.ts:194 (no brand assigned) and :243 (UNMATCHED correction created); the Prisma `catalogueCorrection.create` call shape retires, but the negative/error behaviour is covered by the Worker's real-SQL test | PROPOSED: worker-equivalent-exists — ambiguous-SKU unmatched covered by catalogue-import-upsert.node.test.ts:194 (null brand) and :243 (UNMATCHED correction); Prisma call shape is Express-specific |
| Reuses an existing confirmed brand without overwriting its advisory fields (no brand.create, no brand.update, product.updateMany with existing brandId) | supplier-credit.repository.test.ts:120 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:190 — asserts `bySku.get('IGNORED-SKU')` is not null and `bySku.get('api-two')` equals `bySku.get('IGNORED-SKU')` (brand reused across products matched by barcode and SKU); however, this covers REFERENCE brand reuse, not CONFIRMED brand non-overwrite — the Worker test creates new REFERENCE brands and does not test the case where an existing CONFIRMED brand is reused without overwriting advisory fields | worker-shaped-rewrite | Brand reuse is partially covered by catalogue-import-upsert.node.test.ts:190 (same brand reused for barcode and SKU matches), but the specific "existing CONFIRMED brand not overwritten" behaviour is not tested — the Worker test only creates new REFERENCE brands; the Prisma `brand.findUnique` returning CONFIRMED and `brand.update` not-called assertions retire with the backend, but the CONFIRMED-non-overwrite behaviour must be re-tested against the Worker's enrichment model | PROPOSED: worker-shaped-rewrite — brand reuse partially covered by catalogue-import-upsert.node.test.ts:190 (REFERENCE brands reused), but CONFIRMED-brand non-overwrite is untested; re-test against Worker enrichment model |
| Returns one latest run plus at most twenty prior global runs (orderBy version desc, take 21, latest + 20 history) | supplier-credit.repository.test.ts:152 | — | workers/src/database.catalogue-provenance.node.test.ts:50 — asserts `result.latest` matches `{ version: 22, seededAt: '2026-07-22T00:00:00.000Z', inserted: 22 }` (latest run returned); workers/src/database.catalogue-provenance.node.test.ts:55 — asserts `result.history` has length 20 (at most twenty prior runs); workers/src/database.catalogue-provenance.node.test.ts:56 — asserts `result.history[19].version` is 2 (oldest history entry) | worker-equivalent-exists | The provenance pagination behaviour (latest + 20 history, ordered by version desc) is directly covered by database.catalogue-provenance.node.test.ts:50 (latest), :55 (history length 20), and :56 (oldest entry); the Prisma `catalogueSeedRun.findMany` with `take: 21` call shape retires, but the behaviour is covered by the Worker's real-SQL test | PROPOSED: worker-equivalent-exists — provenance pagination covered by database.catalogue-provenance.node.test.ts:50, :55, :56 (latest + 20 history); Prisma call shape is Express-specific |
| Orders null policy timestamps first and then timestamp, brand name, and ID (null-first, then policyUpdatedAt ASC, name ASC, id ASC) | supplier-credit.repository.test.ts:185 | tenant-isolation | workers/src/database.supplier-policy.conformance.node.test.ts:276 — asserts `workerRows.map((row) => [row.brandName, row.status]))` equals `[['Alpha Unassigned', 'MISSING'], ['Zulu Missing', 'MISSING'], ['Beta Earlier', 'ATTACHED'], ['Alpha Later', 'ATTACHED']]` (null-first ordering: both MISSING entries with null timestamps first, then ATTACHED entries ordered by timestamp; Alpha Unassigned before Zulu Missing by name); workers/src/database.supplier-policy.conformance.node.test.ts:275 — asserts `workerRows` equals `sqliteRows` (SQLite conformance for ordering and status) | worker-equivalent-exists | The null-first ordering and status computation (MISSING/ATTACHED) is directly covered by database.supplier-policy.conformance.node.test.ts:276 (ordered rows with null-first, then timestamp, then name) and :275 (SQLite conformance); the Prisma `brand.findMany` with `include: { supplier: true }` call shape retires, but the ordering behaviour is covered by the Worker's real-SQL test; the org-scoped query (tenant-isolation) is also covered at :276 (OTHER_ORG brand excluded) | PROPOSED: worker-equivalent-exists — null-first ordering and status covered by database.supplier-policy.conformance.node.test.ts:276 and :275 (SQLite conformance); org-scoped query (tenant-isolation) also covered; Prisma call shape is Express-specific |
| Filters before counting and returns deterministic page metadata (title startsWith filter, titleDesc sort, skip/take pagination, totalItems/totalPages/nextCursor) | supplier-credit.repository.test.ts:227 | tenant-isolation | workers/src/database.supplier-policy.conformance.node.test.ts:322 — asserts `db.reviewBrands(ORG, { page: 1, pageSize: 2, title: 'VITAMIN', titleMatch: 'startsWith', sort: 'titleAsc' })` equals `sqliteCatalogueReview(sqlite, ORG, options)` (title filtering, page boundaries, stable ordering, and page metadata via SQLite conformance); workers/src/database.supplier-policy.conformance.node.test.ts:325 — asserts page 2 matches SQLite (pagination across pages) | worker-equivalent-exists | The title filtering (startsWith), pagination (page 1 and 2), page metadata (totalPages, nextCursor via SQLite conformance), and stable ordering are covered by database.supplier-policy.conformance.node.test.ts:322 and :325; the Worker tests titleAsc sort (not titleDesc), but the sort-ordering behaviour is equivalent; the Prisma `product.count`/`product.findMany` call shapes retire, but the filtering/pagination behaviour is covered by the Worker's real-SQL test; the org-scoped query (tenant-isolation) is covered (ORG-scoped, OTHER_ORG excluded) | PROPOSED: worker-equivalent-exists — filtering/pagination/page-metadata covered by database.supplier-policy.conformance.node.test.ts:322 and :325 (SQLite conformance); titleDesc not specifically tested but titleAsc is equivalent; org-scoped query (tenant-isolation) covered; Prisma call shapes are Express-specific |
| Resets credit type to NONE with the rest of the policy (clearSupplierPolicy calls supplier.updateMany with creditType: NONE) | supplier-credit.repository.test.ts:277 | — | workers/src/database.supplier-policy.conformance.node.test.ts:223 — asserts `cleared` matches `{ creditType: 'NONE', contactEmail: 'claims@example.com', contactPhone: '03 9999 0000', creditPolicyNote: '', policyWriteOffQty: null, policyCreditQty: null, followUpDays: 7, representativeName: null, representativeEmail: null }` (creditType reset to NONE and all policy fields cleared) | worker-equivalent-exists | The clearSupplierPolicy behaviour (creditType reset to NONE, all policy fields cleared) is directly covered by database.supplier-policy.conformance.node.test.ts:223 (asserts cleared result with creditType: NONE and all fields reset); the Prisma `supplier.updateMany` call shape retires, but the clearing behaviour is covered by the Worker's real-SQL test | PROPOSED: worker-equivalent-exists — clearSupplierPolicy covered by database.supplier-policy.conformance.node.test.ts:223 (creditType: NONE, all fields reset); Prisma call shape is Express-specific |

---

### supplier-credit.routes.test.ts

Route-wiring tests for the Express `supplierCreditRouter` and `platformCatalogueCorrectionRouter`
using **supertest** against a real Express app with **mocked controllers** (`vi.hoisted` mocks
that return 204). Tests verify route registration (11 supplier-credit routes, 3 platform routes),
platform-router mounting behind `authenticateToken` (by reading `index.ts` source), and input
validation (non-numeric supplier ID → 400 before controller access, via `it.each` over
PUT/PATCH/DELETE). The Worker's `minimal-api-routes.test.ts:161` registers all supplier-credit
and platform routes in the Worker route table, covering the route-registration behaviours. The
Express `app.use` mounting with `authenticateToken` is Express-specific middleware wiring with
no Worker analogue (the Worker uses a route table with auth middleware applied universally).
The input-validation behaviour (reject non-numeric ID) is handled differently by the Worker
(regex route patterns non-match → 404, not Express 400) and must be re-tested.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Exposes brand review, confirmation, override, and disposal endpoints (11 routes: GET /brands, GET /brand-review, POST /brands, PUT /brands/:id/supplier, PUT /products/:productId/supplier, POST /claimable-pool/:transactionId/dispose, PATCH /suppliers/:id, DELETE /suppliers/:id/policy, GET /policy-review, POST /policy-review/bulk-attach, POST /brands/bulk-link) | supplier-credit.routes.test.ts:45 | — | workers/src/minimal-api-routes.test.ts:161 — asserts `getMinimalRoutes()` contains all 11 supplier-credit routes: `GET /api/supplier-credits/brands` (:172), `GET /api/supplier-credits/brand-review` (:173), `POST /api/supplier-credits/brands` (:174), `PUT /api/supplier-credits/brands/:id/supplier` (:175), `PUT /api/supplier-credits/products/:id/supplier` (:176), `POST /api/supplier-credits/claimable-pool/:id/dispose` (:177), `PATCH /api/supplier-credits/suppliers/:id` (:167), `DELETE /api/supplier-credits/suppliers/:id/policy` (:168), `GET /api/supplier-credits/policy-review` (:169), `POST /api/supplier-credits/policy-review/bulk-attach` (:170), `POST /api/supplier-credits/brands/bulk-link` (:171) | worker-equivalent-exists | All 11 supplier-credit routes are registered in the Worker route table at minimal-api-routes.test.ts:161-184 (arrayContaining with all 11 routes matching the backend's route list, prefixed with `/api/supplier-credits/`); the Express router stack inspection (`router.stack.filter`) retires with the backend, but the route-registration behaviour is covered by the Worker's route table test | PROPOSED: worker-equivalent-exists — all 11 supplier-credit routes covered by minimal-api-routes.test.ts:161-184 (route table arrayContaining); Express router stack inspection is Express-specific |
| Exposes correction review and provenance operations on the platform router (3 routes: GET /catalogue-corrections, GET /catalogue/provenance, PATCH /catalogue-corrections/:id) | supplier-credit.routes.test.ts:63 | — | workers/src/minimal-api-routes.test.ts:181 — asserts route table contains `GET /api/platform/catalogue-corrections`; workers/src/minimal-api-routes.test.ts:182 — asserts route table contains `PATCH /api/platform/catalogue-corrections/:id`; the `GET /api/platform/catalogue/provenance` route is not asserted in the Worker route table test, but the underlying `getCatalogueProvenance` DB method is tested at workers/src/database.catalogue-provenance.node.test.ts:50 | worker-equivalent-exists | 2 of 3 platform routes are covered by minimal-api-routes.test.ts:181 (GET catalogue-corrections) and :182 (PATCH catalogue-corrections/:id); the provenance route is not asserted in the Worker route table, but its DB method is tested at database.catalogue-provenance.node.test.ts:50; the Express router stack `toEqual` (exact 3-route match) retires with the backend, but the route-registration behaviour is covered for 2 of 3 routes with the third covered at the DB level | PROPOSED: worker-equivalent-exists — 2 of 3 platform routes covered by minimal-api-routes.test.ts:181 and :182; provenance route not in route table test but DB method covered at database.catalogue-provenance.node.test.ts:50; Express router stack exact-match is Express-specific |
| Mounts the platform router behind normal authentication (index.ts contains app.use('/api/platform', authenticateToken, platformCatalogueCorrectionRouter)) | supplier-credit.routes.test.ts:71 | authorization-precedence | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts:161 registers platform routes in the Worker route table but does not assert that platform routes require authentication; the Worker applies auth middleware universally to all routes (not per-router mounting like Express `app.use`), so there is no equivalent of the `app.use('/api/platform', authenticateToken, ...)` mounting pattern; the test reads `index.ts` source code to verify Express middleware wiring, which is Express-specific | retire | The Express `app.use` mounting with `authenticateToken` is Express-specific middleware wiring with no Worker analogue; the Worker uses a route table with auth middleware applied universally (not per-router mounting), so the "platform router behind auth" behaviour is architecturally different; the source-code-reading test mechanism (`fs.readFileSync(index.ts)`) is Express-specific machinery | PROPOSED: retire — Express app.use mounting with authenticateToken is Express-specific middleware wiring; Worker applies auth universally via route table, not per-router mounting; source-code-reading test is Express-specific |
| Rejects an invalid supplier ID on PUT /suppliers/not-a-number before controller access (400, controller not called) | supplier-credit.routes.test.ts:78 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts:166 registers `PUT /api/supplier-credits/suppliers/:id` with regex `/^\/api\/supplier-credits\/suppliers\/\d+$/`, so a non-numeric ID would not match the route pattern (returning null/404, not 400); no Worker test asserts that a non-numeric supplier ID is rejected with a 400 response; the Worker's regex-based route matching handles invalid IDs differently from Express's route parameter parsing | worker-shaped-rewrite | The "reject invalid supplier ID" negative/error behaviour is relevant but the Worker handles it differently (regex non-match → 404, not Express 400); the controller-not-called guarantee is covered by the Worker's regex route matching (non-matching routes return null, handler never invoked), but the 400 response code is not; the validation behaviour must be re-tested against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — invalid supplier ID rejection (400) is untested in the Worker; regex route matching prevents controller access but returns 404 not 400; re-test negative/error gate against Worker Response model |
| Rejects an invalid supplier ID on PATCH /suppliers/not-a-number before controller access (400, controller not called) | supplier-credit.routes.test.ts:78 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts:167 registers `PATCH /api/supplier-credits/suppliers/:id` with regex `/^\/api\/supplier-credits\/suppliers\/\d+$/`, so a non-numeric ID would not match (returning null/404, not 400); no Worker test asserts a 400 rejection for non-numeric supplier ID on PATCH; the Worker's regex-based route matching handles invalid IDs differently from Express's route parameter parsing | worker-shaped-rewrite | The "reject invalid supplier ID on PATCH" negative/error behaviour is relevant but the Worker handles it differently (regex non-match → 404, not 400); the controller-not-called guarantee is covered by regex route matching, but the 400 response is not; the validation behaviour must be re-tested against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — invalid supplier ID rejection on PATCH (400) is untested in the Worker; regex route matching prevents controller access but returns 404 not 400; re-test negative/error gate against Worker Response model |
| Rejects an invalid supplier ID on DELETE /suppliers/not-a-number/policy before controller access (400, controller not called) | supplier-credit.routes.test.ts:78 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts:168 registers `DELETE /api/supplier-credits/suppliers/:id/policy` with regex `/^\/api\/supplier-credits\/suppliers\/\d+\/policy$/`, so a non-numeric ID would not match (returning null/404, not 400); no Worker test asserts a 400 rejection for non-numeric supplier ID on DELETE policy; the Worker's regex-based route matching handles invalid IDs differently from Express's route parameter parsing | worker-shaped-rewrite | The "reject invalid supplier ID on DELETE policy" negative/error behaviour is relevant but the Worker handles it differently (regex non-match → 404, not 400); the controller-not-called guarantee is covered by regex route matching, but the 400 response is not; the validation behaviour must be re-tested against the Worker's Request/Response model | PROPOSED: worker-shaped-rewrite — invalid supplier ID rejection on DELETE policy (400) is untested in the Worker; regex route matching prevents controller access but returns 404 not 400; re-test negative/error gate against Worker Response model |

---

### supplier-credit.service.test.ts

Unit tests for `SupplierCreditService` and its controller with a **mocked
`SupplierCreditRepository`** (`vi.fn()` stubs for every repo method; `withTransaction`
runs the callback against `undefined`). No real database, no supertest — this is a pure
service-layer test of authorization precedence (admin vs team_member field-level policy
changes), input validation, partial-merge/timestamp semantics, catalogue-review paging,
bulk brand/product operations, claimable-pool grouping, and write-off disposal. The Worker
has no service layer: `createWorkersDatabase` exposes DB functions called directly from
route handlers, with authorization pushed to coarse path-level RBAC
(`middleware/require-role`). DB-level mechanics are covered by the pglite/better-sqlite3
dual-conformance suites (`database.supplier-policy.conformance`,
`database.credit-claim.conformance`); field-level authz and service-boundary validation are
not. `it.each` cases are expanded one row per parameterized case.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Controller passes numbered pagination and title controls to the service | supplier-credit.service.test.ts:88 | — | workers/src/database.supplier-policy.conformance.node.test.ts:295 — asserts db.reviewBrands honours page/pageSize/title/titleMatch/sort at the DB layer (the behaviour the controller delegates to) | worker-equivalent-exists | The DB-layer reviewBrands accepts the same numbered pagination/title controls; the Express controller query-string parsing is the delivery mechanism only | PROPOSED: worker-equivalent-exists — pagination/title controls covered at Worker DB layer (conformance:295); Express controller query-parsing is delivery mechanism |
| Accepts only numeric IDs in a fully valid comma-separated platform-admin allowlist | supplier-credit.service.test.ts:125 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/__tests__/authorized-parties.test.ts) — require-role.test.ts:157-182 covers coarse org-role RBAC (admin/team_member on paths), not a cross-org platform-admin numeric allowlist; authorized-parties.test.ts covers Clerk party origins, not platform-admin identity | retire | The env-based numeric platform-admin allowlist is Express-specific auth machinery; the Worker uses Clerk org roles with no platform-admin concept, so there is no Worker surface to rewrite against | PROPOSED: retire — platform-admin numeric allowlist is Express env-auth machinery with no Worker analogue; authorization-precedence gate carried by the role-based policy rows below |
| Fails closed for malformed platform-admin configuration `undefined` | supplier-credit.service.test.ts:130 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/__tests__/authorized-parties.test.ts) — require-role.test.ts covers coarse org-role RBAC, not a platform-admin allowlist; authorized-parties.test.ts covers Clerk party origins, not platform-admin config parsing | retire | Fail-closed parsing of a malformed env allowlist is Express-specific config validation; the Worker has no platform-admin allowlist to malformed-parse | PROPOSED: retire — malformed-allowlist fail-closed is Express env-config validation with no Worker analogue |
| Fails closed for malformed platform-admin configuration `''` | supplier-credit.service.test.ts:130 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/__tests__/authorized-parties.test.ts) — require-role.test.ts covers coarse org-role RBAC, not a platform-admin allowlist; authorized-parties.test.ts covers Clerk party origins, not platform-admin config parsing | retire | Fail-closed parsing of an empty env allowlist is Express-specific config validation; the Worker has no platform-admin allowlist to malformed-parse | PROPOSED: retire — malformed-allowlist fail-closed is Express env-config validation with no Worker analogue |
| Fails closed for malformed platform-admin configuration `7,not-a-number` | supplier-credit.service.test.ts:130 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/__tests__/authorized-parties.test.ts) — require-role.test.ts covers coarse org-role RBAC, not a platform-admin allowlist; authorized-parties.test.ts covers Clerk party origins, not platform-admin config parsing | retire | Fail-closed parsing of a non-numeric token in the env allowlist is Express-specific config validation; the Worker has no platform-admin allowlist to malformed-parse | PROPOSED: retire — malformed-allowlist fail-closed is Express env-config validation with no Worker analogue |
| Fails closed for malformed platform-admin configuration `7,,12` | supplier-credit.service.test.ts:130 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/__tests__/authorized-parties.test.ts) — require-role.test.ts covers coarse org-role RBAC, not a platform-admin allowlist; authorized-parties.test.ts covers Clerk party origins, not platform-admin config parsing | retire | Fail-closed parsing of an empty token in the env allowlist is Express-specific config validation; the Worker has no platform-admin allowlist to malformed-parse | PROPOSED: retire — malformed-allowlist fail-closed is Express env-config validation with no Worker analogue |
| Fails closed for malformed platform-admin configuration `0,7` | supplier-credit.service.test.ts:130 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/__tests__/authorized-parties.test.ts) — require-role.test.ts covers coarse org-role RBAC, not a platform-admin allowlist; authorized-parties.test.ts covers Clerk party origins, not platform-admin config parsing | retire | Fail-closed parsing of a zero ID in the env allowlist is Express-specific config validation; the Worker has no platform-admin allowlist to malformed-parse | PROPOSED: retire — malformed-allowlist fail-closed is Express env-config validation with no Worker analogue |
| Rejects an unknown credit type at the service boundary | supplier-credit.service.test.ts:138 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:196 persists a FULL_CREDIT supplier but no Worker test rejects an unknown credit type; the DB layer carries no creditType validation | worker-shaped-rewrite | The service-boundary creditType validation is business logic the Worker needs but does not test | PROPOSED: worker-shaped-rewrite — unknown-credit-type rejection untested in Worker; re-test validation against Worker route model |
| Persists a supplier with a complete credit ratio | supplier-credit.service.test.ts:146 | — | workers/src/database.supplier-policy.conformance.node.test.ts:196 — asserts db.createSupplier persists name/creditType/contact/policy ratios/followUpDays/representative and returns the created row | worker-equivalent-exists | The full-field supplier creation behaviour is asserted at the Worker DB layer | PROPOSED: worker-equivalent-exists — complete-ratio supplier persistence covered by SPC:196 |
| Rejects a half-specified credit ratio | supplier-credit.service.test.ts:178 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:196 creates with a full ratio and SPC:348 rejects a policy-less supplier in bulk-attach, but no Worker test rejects a half-specified (writeOff without credit) ratio at create | worker-shaped-rewrite | The half-ratio validation is service-boundary business logic not present in the Worker DB layer | PROPOSED: worker-shaped-rewrite — half-ratio rejection untested in Worker; re-test validation |
| Allows a team member to create a bare supplier with claim contact details | supplier-credit.service.test.ts:185 | authorization-precedence | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/constants/roles.test.ts) — SPC:196 tests createSupplier persistence with no role gating; require-role.test.ts covers coarse path RBAC, not team-member-allowed bare-supplier creation | worker-shaped-rewrite | The team-member-allowed-bare-supplier authorization is fine-grained authz with no Worker equivalent | PROPOSED: worker-shaped-rewrite — team-member bare-supplier creation authz has no Worker equivalent; re-test against Worker route model |
| Rejects changed policy content from a non-admin | supplier-credit.service.test.ts:200 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — require-role.test.ts:168 blocks team_member on admin-only paths but not field-level policy-content changes; SPC DB layer has no authz | worker-shaped-rewrite | Field-level non-admin policy-change rejection is fine-grained authz absent from the Worker's coarse RBAC and DB layer | PROPOSED: worker-shaped-rewrite — non-admin policy-change rejection has no Worker equivalent; re-test authorization precedence |
| Returns structured policy validation failures for admins | supplier-credit.service.test.ts:210 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:196 tests successful persistence only; no Worker test asserts a structured POLICY_VALIDATION_ERROR with field/errors shape | worker-shaped-rewrite | The structured validation-error shape is service-boundary business logic untested in the Worker | PROPOSED: worker-shaped-rewrite — structured policy validation error shape untested in Worker |
| Requires admin authorization for a credit-type-only classification change | supplier-credit.service.test.ts:223 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — require-role.test.ts:168 blocks team_member on admin-only paths but not credit-type-only classification changes; SPC DB layer has no authz | worker-shaped-rewrite | Credit-type-only change admin authz is field-level authz not present in the Worker | PROPOSED: worker-shaped-rewrite — credit-type-change admin authz has no Worker equivalent |
| Persists and timestamps an admin credit-type-only classification change | supplier-credit.service.test.ts:231 | — | workers/src/database.supplier-policy.conformance.node.test.ts:215 — asserts db.updateSupplier persists a change with a policyUpdatedAt timestamp | worker-equivalent-exists | The timestamped supplier update mechanics are asserted at the Worker DB layer (the admin-authz aspect is row :223) | PROPOSED: worker-equivalent-exists — timestamped supplier update covered by SPC:215 |
| 404s when the supplier is not in the org | supplier-credit.service.test.ts:241 | tenant-isolation | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:237 asserts org isolation in policy-review listing (foreign supplier excluded) but no Worker test asserts a 404/NotFoundError when updating a supplier outside the org | worker-shaped-rewrite | The cross-org update non-disclosure is a tenant-isolation behaviour not covered by the Worker's listing-isolation tests | PROPOSED: worker-shaped-rewrite — cross-org update 404 untested; re-test tenant isolation on update |
| Merges partial input and permits contact-only changes without a policy timestamp bump | supplier-credit.service.test.ts:248 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:215 updates with full data and an explicit new timestamp; no Worker test asserts partial-input merge with no policyUpdatedAt bump on a contact-only change | worker-shaped-rewrite | The partial-merge and no-timestamp-bump semantics are service-layer business logic untested in the Worker | PROPOSED: worker-shaped-rewrite — partial-merge no-timestamp-bump semantics untested in Worker |
| Treats normalized unchanged policy as an ordinary non-admin update | supplier-credit.service.test.ts:266 | authorization-precedence | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:215 updates with an explicit new timestamp; no Worker test asserts normalized-unchanged-policy detection that skips the timestamp bump | worker-shaped-rewrite | The normalize-and-compare-to-skip-bump logic is service-layer business logic untested in the Worker | PROPOSED: worker-shaped-rewrite — normalized-unchanged-policy no-bump untested in Worker |
| Stamps the policy timestamp when an admin changes effective policy content | supplier-credit.service.test.ts:279 | — | workers/src/database.supplier-policy.conformance.node.test.ts:215 — asserts db.updateSupplier persists a new policyUpdatedAt timestamp on a policy change | worker-equivalent-exists | The policy-change timestamping mechanics are asserted at the Worker DB layer (admin authz is row :223) | PROPOSED: worker-equivalent-exists — admin policy-change timestamping covered by SPC:215 |
| Clears policy only for admins while preserving supplier contact fields | supplier-credit.service.test.ts:295 | authorization-precedence | workers/src/database.supplier-policy.conformance.node.test.ts:222 — asserts db.clearSupplierPolicy clears policy fields while preserving contactEmail/contactPhone; the admin-only authz half is not covered (see row :200) | worker-equivalent-exists | The clear-and-preserve-contact mechanics are covered; the admin-only authorization is carried by the worker-shaped-rewrite rows above | PROPOSED: worker-equivalent-exists — clear/preserve-contact covered by SPC:222; admin-only authz carried by rows :200/:223 |
| Preserves legacy full PUT replacement semantics separately from PATCH merging | supplier-credit.service.test.ts:311 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:215 updates with full data and SPC:222 clears, but no Worker test asserts full PUT replacement nulling unspecified fields distinct from PATCH merge | worker-shaped-rewrite | The PUT-vs-PATCH replacement contract is an API-shape behaviour untested in the Worker | PROPOSED: worker-shaped-rewrite — PUT-vs-PATCH replacement semantics untested in Worker |
| Authorizes a non-admin full replacement before validating its credit ratio | supplier-credit.service.test.ts:341 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — require-role.test.ts does coarse path RBAC; no Worker test asserts authz-before-validation ordering for a non-admin full replacement | worker-shaped-rewrite | The authz-before-validation ordering is fine-grained authz logic absent from the Worker | PROPOSED: worker-shaped-rewrite — authz-before-validation ordering has no Worker equivalent |
| 404s when assigning a supplier that is not in the org | supplier-credit.service.test.ts:360 | tenant-isolation | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:354 returns BRAND_NOT_FOUND for a foreign brand in bulk-attach, but no Worker test asserts assignProductSupplier 404 when the supplier is not in the org | worker-shaped-rewrite | The cross-org assign-supplier non-disclosure is a tenant-isolation behaviour not covered by the Worker | PROPOSED: worker-shaped-rewrite — assign-supplier cross-org 404 untested |
| Clears a product supplier without a supplier lookup | supplier-credit.service.test.ts:365 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — no Worker test asserts that clearing a product supplier (null) skips the supplier lookup | worker-shaped-rewrite | The skip-lookup-on-clear orchestration is service-layer logic untested in the Worker | PROPOSED: worker-shaped-rewrite — clear-supplier-skip-lookup orchestration untested in Worker |
| 404s when the product is not in the org | supplier-credit.service.test.ts:372 | tenant-isolation | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:354 tests bulk brand-not-found but no Worker test asserts assignProductSupplier 404 when the product is not in the org | worker-shaped-rewrite | The cross-org assign-product non-disclosure is a tenant-isolation behaviour not covered by the Worker | PROPOSED: worker-shaped-rewrite — assign-product cross-org 404 untested |
| Passes the authenticated user through for transactional correction capture | supplier-credit.service.test.ts:377 | — | workers/src/database.supplier-policy.conformance.node.test.ts:346 — asserts db.bulkAttachSupplier(ORG, supplierId, brandIds, userId) threads the authenticated user through for correction capture | worker-equivalent-exists | The user-threading for correction capture is asserted at the Worker DB layer | PROPOSED: worker-equivalent-exists — user-threading for correction capture covered by SPC:346 |
| Lists and reviews only through the service organization scope | supplier-credit.service.test.ts:385 | tenant-isolation | workers/src/database.supplier-policy.conformance.node.test.ts:237 — asserts listPolicyReview(ORG) is org-scoped and excludes OTHER_ORG rows; SPC:213 asserts listSuppliers(ORG) | worker-equivalent-exists | The org-scoped listing/review isolation is asserted at the Worker DB layer | PROPOSED: worker-equivalent-exists — org-scoped listing/review covered by SPC:237 |
| Adds a user-confirmed brand and correction through one repository operation | supplier-credit.service.test.ts:403 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:360 tests bulkLinkProducts but no Worker test asserts addBrandForProduct as a single brand+correction operation | worker-shaped-rewrite | The single addBrand+correction operation is service-layer logic untested in the Worker | PROPOSED: worker-shaped-rewrite — single addBrand+correction op untested in Worker |
| Confirms only an organization-owned supplier for a brand | supplier-credit.service.test.ts:413 | tenant-isolation | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — MCC:31 projects brand credit context but no Worker test asserts confirmBrandSupplier rejects a non-org supplier | worker-shaped-rewrite | The confirm-brand-supplier org-ownership check is a tenant-isolation behaviour untested in the Worker | PROPOSED: worker-shaped-rewrite — confirm-brand-supplier org-ownership check untested |
| Marks an organization-owned brand confirmed | supplier-credit.service.test.ts:418 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — MCC:63 projects a CONFIRMED brand's context but no Worker test asserts the confirmBrandSupplier mutation marking a brand CONFIRMED | worker-shaped-rewrite | The mark-brand-confirmed mutation is service-layer logic untested in the Worker | PROPOSED: worker-shaped-rewrite — mark-brand-confirmed mutation untested in Worker |
| Uses CONFIRMED for catalogue setup rather than claimability | supplier-credit.service.test.ts:424 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:295 tests reviewBrands pagination/title but no Worker test asserts the CONFIRMED state filter for catalogue setup | worker-shaped-rewrite | The CONFIRMED-state catalogue filter is service-layer logic untested in the Worker | PROPOSED: worker-shaped-rewrite — CONFIRMED-state catalogue filter untested in Worker |
| Normalizes numbered pagination, title matching, and title ordering | supplier-credit.service.test.ts:435 | — | workers/src/database.supplier-policy.conformance.node.test.ts:295 — asserts db.reviewBrands normalizes page/pageSize/title/titleMatch/sort and matches SQLite numbered pagination/ordering | worker-equivalent-exists | The pagination/title normalization and ordering are asserted at the Worker DB layer | PROPOSED: worker-equivalent-exists — pagination/title normalization covered by SPC:295 |
| Rejects invalid catalogue paging option `page=0` | supplier-credit.service.test.ts:455 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:295 tests valid paging options but no Worker test rejects page:0 | worker-shaped-rewrite | The invalid-paging rejection is service-boundary validation untested in the Worker | PROPOSED: worker-shaped-rewrite — invalid paging rejection untested in Worker |
| Rejects invalid catalogue paging option `pageSize=101` | supplier-credit.service.test.ts:455 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:295 tests valid paging options but no Worker test rejects pageSize:101 | worker-shaped-rewrite | The invalid-paging rejection is service-boundary validation untested in the Worker | PROPOSED: worker-shaped-rewrite — invalid paging rejection untested in Worker |
| Rejects invalid catalogue paging option `page+cursor` combined | supplier-credit.service.test.ts:455 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:295 tests valid paging options but no Worker test rejects combining page and cursor | worker-shaped-rewrite | The invalid-paging rejection is service-boundary validation untested in the Worker | PROPOSED: worker-shaped-rewrite — invalid paging rejection untested in Worker |
| Rejects invalid catalogue paging option `titleMatch=equals` | supplier-credit.service.test.ts:455 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:295 tests startsWith/contains title matching but no Worker test rejects titleMatch:equals | worker-shaped-rewrite | The invalid-paging rejection is service-boundary validation untested in the Worker | PROPOSED: worker-shaped-rewrite — invalid paging rejection untested in Worker |
| Rejects invalid catalogue paging option `sort=newest` | supplier-credit.service.test.ts:455 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:295 tests titleAsc/titleDesc sorting but no Worker test rejects sort:newest | worker-shaped-rewrite | The invalid-paging rejection is service-boundary validation untested in the Worker | PROPOSED: worker-shaped-rewrite — invalid paging rejection untested in Worker |
| Rejects claimability states as catalogue-review filters | supplier-credit.service.test.ts:470 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:272 tests policy-status filters (ATTACHED/MISSING) but no Worker test rejects claimability states (CLAIMABLE) as catalogue-review filters | worker-shaped-rewrite | The claimability-state filter rejection is service-boundary validation untested in the Worker | PROPOSED: worker-shaped-rewrite — claimability-state filter rejection untested in Worker |
| Passes org-scoped policy review filters to the repository | supplier-credit.service.test.ts:479 | — | workers/src/database.supplier-policy.conformance.node.test.ts:272 — asserts listPolicyReview(ORG, {status, supplier}) threads org-scoped filters | worker-equivalent-exists | The org-scoped policy-review filter threading is asserted at the Worker DB layer | PROPOSED: worker-equivalent-exists — org-scoped policy review filters covered by SPC:272 |
| Deduplicates brand IDs after enforcing the raw cap and requires an admin | supplier-credit.service.test.ts:491 | authorization-precedence | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/constants/roles.test.ts) — SPC:330 tests bulk-attach atomicity/no-ops/policy-less rejection but not brand-ID dedup, the 501 raw cap, or admin authorization; require-role.test.ts is coarse path RBAC | worker-shaped-rewrite | The dedup, raw-cap, and admin-authz behaviours are service-layer logic not covered by the Worker's bulk-attach mechanics test | PROPOSED: worker-shaped-rewrite — dedup/raw-cap/admin-authz untested in Worker; bulk-attach mechanics covered by SPC:330 |
| Rejects 501 raw IDs even when they are duplicates | supplier-credit.service.test.ts:502 | feature-limits | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:330 tests bulk-attach but no Worker test asserts the 501 raw-ID cap rejection | worker-shaped-rewrite | The 501-ID cap is a feature-limit not asserted in the Worker | PROPOSED: worker-shaped-rewrite — 501-ID cap rejection untested; re-test feature-limits gate |
| Deduplicates products and accepts exactly one brand target | supplier-credit.service.test.ts:514 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:360 tests bulkLinkProducts atomicity/conflict but no Worker test asserts product-ID dedup or rejecting both brandId and brandName | worker-shaped-rewrite | The product-dedup and one-brand-target validation are service-boundary logic untested in the Worker | PROPOSED: worker-shaped-rewrite — product dedup/one-brand-target validation untested in Worker |
| Rejects invalid brand ID `0` in the service boundary | supplier-credit.service.test.ts:531 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:360 tests bulkLinkProducts with a valid brandId but no Worker test rejects brandId 0 | worker-shaped-rewrite | The invalid-brand-ID validation is service-boundary logic untested in the Worker | PROPOSED: worker-shaped-rewrite — invalid-brand-ID rejection untested in Worker |
| Rejects invalid brand ID `-1` in the service boundary | supplier-credit.service.test.ts:531 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:360 tests bulkLinkProducts with a valid brandId but no Worker test rejects a negative brandId | worker-shaped-rewrite | The invalid-brand-ID validation is service-boundary logic untested in the Worker | PROPOSED: worker-shaped-rewrite — invalid-brand-ID rejection untested in Worker |
| Rejects invalid brand ID `1.5` in the service boundary | supplier-credit.service.test.ts:531 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — SPC:360 tests bulkLinkProducts with a valid brandId but no Worker test rejects a non-integer brandId | worker-shaped-rewrite | The invalid-brand-ID validation is service-boundary logic untested in the Worker | PROPOSED: worker-shaped-rewrite — invalid-brand-ID rejection untested in Worker |
| Uses stable cursor pagination for pending corrections | supplier-credit.service.test.ts:544 | — | none found (searched: workers/src/database.credit-claim.conformance.node.test.ts) — CCC:355 tests listCatalogueCorrections({status:'PENDING', limit}) but no Worker test asserts stable cursor pagination | worker-shaped-rewrite | The cursor-pagination behaviour is untested; only status/limit listing is covered by CCC:355 | PROPOSED: worker-shaped-rewrite — cursor pagination for corrections untested; status/limit listing covered by CCC:355 |
| Only accepts terminal review statuses | supplier-credit.service.test.ts:554 | negative/error | none found (searched: workers/src/database.credit-claim.conformance.node.test.ts) — CCC:364 tests terminal ACCEPTED/REJECTED decisions but no Worker test rejects a non-terminal status like PENDING as a review decision | worker-shaped-rewrite | The non-terminal-status rejection is service-boundary validation untested in the Worker | PROPOSED: worker-shaped-rewrite — non-terminal status rejection untested in Worker |
| 404s a missing correction without mutating other records | supplier-credit.service.test.ts:561 | negative/error | workers/src/database.credit-claim.conformance.node.test.ts:377 — asserts reviewCatalogueCorrection(999999,'REJECTED') returns NOT_FOUND for a missing correction | worker-equivalent-exists | The missing-correction NOT_FOUND is asserted at the Worker DB layer | PROPOSED: worker-equivalent-exists — missing-correction NOT_FOUND covered by CCC:377 |
| Conflicts when a correction already has a terminal decision | supplier-credit.service.test.ts:568 | negative/error | workers/src/database.credit-claim.conformance.node.test.ts:374 — asserts a second reviewCatalogueCorrection returns ALREADY_REVIEWED after a terminal decision | worker-equivalent-exists | The terminal-decision conflict is asserted at the Worker DB layer | PROPOSED: worker-equivalent-exists — terminal-decision conflict covered by CCC:374 |
| Groups claimable write-offs by supplier via the shared rollup | supplier-credit.service.test.ts:578 | — | workers/src/database.credit-claim.conformance.node.test.ts:265 — asserts getClaimablePool groups by supplier via the shared rollupClaimablePool, with expectedCreditValueTotal:20 and supplier-name ordering | worker-equivalent-exists | The claimable-pool grouping and rollup are asserted at the Worker DB layer | PROPOSED: worker-equivalent-exists — claimable-pool grouping covered by CCC:265 |
| Is idempotent for an already disposed transaction | supplier-credit.service.test.ts:617 | — | none found (searched: workers/src/database.disposition.pglite.node.test.ts, workers/src/database.credit-claim.conformance.node.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — DISP tests processExpiredItem markdown capture, CCC tests getClaimablePool, MCC tests credit-context projections; none tests disposeWriteOff idempotency (ALREADY_DISPOSED→DISPOSED) | worker-shaped-rewrite | The disposeWriteOff idempotency behaviour has no Worker test coverage | PROPOSED: worker-shaped-rewrite — disposeWriteOff idempotency untested in Worker |
| Conflicts after the transaction enters a claim | supplier-credit.service.test.ts:625 | negative/error | none found (searched: workers/src/database.disposition.pglite.node.test.ts, workers/src/database.credit-claim.conformance.node.test.ts) — no Worker test asserts disposeWriteOff conflicts when a transaction is already claimed | worker-shaped-rewrite | The dispose-claimed conflict is a negative/error behaviour untested in the Worker | PROPOSED: worker-shaped-rewrite — dispose-claimed conflict untested in Worker |
| Does not disclose a transaction outside the organization | supplier-credit.service.test.ts:630 | tenant-isolation | none found (searched: workers/src/database.disposition.pglite.node.test.ts, workers/src/database.credit-claim.conformance.node.test.ts) — CCC:294 asserts getClaimablePool excludes other orgs, but no Worker test asserts disposeWriteOff returns NotFound (non-disclosure) for a cross-org transaction | worker-shaped-rewrite | The disposeWriteOff cross-org non-disclosure is a tenant-isolation behaviour untested in the Worker; pool-level isolation is covered by CCC:294 | PROPOSED: worker-shaped-rewrite — disposeWriteOff cross-org non-disclosure untested; tenant-isolation gate carried by CCC:294 for pool, re-test for dispose |

### supplier-credit-brand-review-parity.regression-1.test.ts

Regression test for ISSUE-QA-002/004 (Express returned raw Prisma products instead of Worker
DTOs). Uses a **mocked Prisma client** (`product.findMany` stubbed with `vi.fn`) — no real
database, no supertest. Asserts `SupplierCreditRepository.reviewBrands` maps raw Prisma product
rows into the shared `{productId, sku, barcode, productName, brand}` contract that the
Worker/frontend expect, with cursor pagination (`nextCursor`).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Maps product IDs/names to the shared Worker/frontend response contract (`productId`, `sku`, `barcode`, `productName`, `brand`, `nextCursor`) | supplier-credit-brand-review-parity.regression-1.test.ts:8 | — | workers/src/database.supplier-policy.conformance.node.test.ts:322 — `db.reviewBrands(ORG, options)` is asserted to equal `sqliteCatalogueReview(...)` whose items are `{productId, sku, barcode, productName, brand}` (lines 93-99), the same shared DTO contract; line 325-327 also asserts page-2 cursor pagination | worker-equivalent-exists | The Worker database test asserts the identical brand-review DTO shape and pagination contract against a SQLite reference, so the parity behaviour the regression guards is already covered on the Worker side | PROPOSED: worker-equivalent-exists — Worker `reviewBrands` conformance test asserts the same shared DTO contract and pagination |

---

### supplier-policy.test.ts

Unit tests for the **shared domain** module `shared/domain/supplier-policy` — pure functions
with no I/O, no Prisma, no supertest. Covers `hasPolicy`, `brandPolicyStatus`, `isPolicyWrite`,
`creditScopeForSupplier`, and `validatePolicyWrite`. Since the Worker imports these same
functions (conformance test at `database.supplier-policy.conformance.node.test.ts:5`), the
question is whether the Worker suite exercises each behaviour, not whether the code is shared.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| `hasPolicy` returns true for a supplier with non-blank instructions | supplier-policy.test.ts:24 | — | workers/src/database.supplier-policy.conformance.node.test.ts:288 — asserts `hasPolicy(row.supplier) === (row.status === 'ATTACHED')` for the 'Beta Earlier'/'Alpha Later' rows which have `credit_policy_note = 'Return monthly'`, exercising the true branch | worker-equivalent-exists | The Worker conformance test uses `hasPolicy` as an oracle and asserts it returns true for suppliers with non-blank policy notes | PROPOSED: worker-equivalent-exists — Worker conformance test exercises `hasPolicy` true branch via ATTACHED suppliers |
| `hasPolicy` returns false for whitespace-only instructions | supplier-policy.test.ts:24 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts) — the conformance test's 'No Policy' supplier at line 239 uses `credit_policy_note = '   '` (3 spaces) and asserts status MISSING, which exercises `hasPolicy` on whitespace; however no Worker test directly asserts `hasPolicy('  ') === false` as a unit | worker-equivalent-exists | The conformance test's 'No Policy' row (note = `'   '`, line 239) maps to MISSING status, which requires `hasPolicy` to return false on whitespace — the behaviour is covered through the oracle assertion at line 282-289 | PROPOSED: worker-equivalent-exists — whitespace `hasPolicy` false branch is covered via the 'No Policy' conformance row |
| `hasPolicy` returns false for null instructions | supplier-policy.test.ts:24 | negative/error | workers/src/database.supplier-policy.conformance.node.test.ts:277 — 'Alpha Unassigned' brand has `supplier_id = null` so `row.supplier` is null, and line 288 asserts `hasPolicy(null) === false` (status MISSING); 'Zulu Missing' at line 257 has supplier with `credit_policy_note = null` (line 239 default), also MISSING | worker-equivalent-exists | The conformance test exercises both the null-supplier and null-note cases through the MISSING status assertion | PROPOSED: worker-equivalent-exists — null `hasPolicy` false branch covered via MISSING-status conformance rows |
| `hasPolicy` returns false for a null supplier | supplier-policy.test.ts:24 | negative/error | workers/src/database.supplier-policy.conformance.node.test.ts:277 — 'Alpha Unassigned' has `supplier_id = null` (line 258), so `row.supplier` is null; line 282 asserts `brandPolicyStatus(row, null)` returns MISSING, and line 288 asserts `hasPolicy(null) === false` | worker-equivalent-exists | The null-supplier case is directly exercised in the conformance oracle assertion | PROPOSED: worker-equivalent-exists — null-supplier `hasPolicy` covered by 'Alpha Unassigned' conformance row |
| `brandPolicyStatus` returns ATTACHED for a supplier with policy, MISSING otherwise | supplier-policy.test.ts:33 | — | workers/src/database.supplier-policy.conformance.node.test.ts:282 — asserts `row.status === brandPolicyStatus(row, row.supplier)` for all 4 rows (ATTACHED for 'Beta Earlier'/'Alpha Later', MISSING for 'Alpha Unassigned'/'Zulu Missing'), covering both branches | worker-equivalent-exists | The conformance test uses `brandPolicyStatus` as an oracle and asserts both ATTACHED and MISSING outcomes | PROPOSED: worker-equivalent-exists — Worker conformance test asserts `brandPolicyStatus` for both ATTACHED and MISSING branches |
| `isPolicyWrite` treats a credit-type-only change to FULL_CREDIT as a policy write | supplier-policy.test.ts:41 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/database.markdown-credit-context.pglite.node.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — the conformance test exercises `createSupplier`/`updateSupplier`/`clearSupplierPolicy` (lines 212-234) but never asserts the `isPolicyWrite` decision logic that gates whether `policyUpdatedAt` is stamped | worker-shaped-rewrite | `isPolicyWrite` is shared domain logic used by the Express controller to decide whether to stamp `policyUpdatedAt`; the Worker's `updateSupplier` stamps `policyUpdatedAt` internally but no Worker test asserts the write-detection decision | PROPOSED: worker-shaped-rewrite — `isPolicyWrite` credit-type-change detection has no Worker test; re-test against Worker update model |
| `isPolicyWrite` uses bare supplier defaults as the create baseline (no write for name/followUpDays) | supplier-policy.test.ts:45 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `createSupplier` at line 212 creates with full policy data but never tests creating a bare supplier (name only) and asserting `policyUpdatedAt` is not stamped | worker-shaped-rewrite | The create-baseline behaviour (bare supplier → no policy write) is not asserted in any Worker test; the Worker's `createSupplier` always sets `policyUpdatedAt` when policy fields are present, but the no-policy create case is untested | PROPOSED: worker-shaped-rewrite — bare-supplier create baseline for `isPolicyWrite` has no Worker test; re-test against Worker create model |
| `isPolicyWrite` compares normalized effective records (trimmed strings) | supplier-policy.test.ts:53 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `updateSupplier` at line 215 changes `contactPhone` but does not test that a trimmed-equivalent policy note is detected as a no-op | worker-shaped-rewrite | The normalization comparison (trim whitespace before comparing) is not asserted in any Worker test; the Worker's update path does not expose whether it normalizes before deciding to stamp | PROPOSED: worker-shaped-rewrite — normalized-record comparison in `isPolicyWrite` has no Worker test; re-test against Worker update model |
| `isPolicyWrite` does not gate contact-only changes but treats explicit policy clearing as a write | supplier-policy.test.ts:66 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `clearSupplierPolicy` at line 222 clears all policy fields but does not test a contact-only update (no policy stamp) vs. an explicit `creditPolicyNote: null` clear (policy stamp) | worker-shaped-rewrite | The contact-only-no-write vs. explicit-clear-is-write distinction is not asserted in any Worker test; `clearSupplierPolicy` clears everything atomically, a different shape | PROPOSED: worker-shaped-rewrite — contact-only vs. explicit-clear `isPolicyWrite` distinction has no Worker test; re-test against Worker update model |
| `creditScopeForSupplier` returns NO_CREDIT for a null supplier | supplier-policy.test.ts:76 | negative/error | workers/src/database.markdown-credit-context.pglite.node.test.ts:94-97 — 'Foreign Product' has `supplier_id = null` (line 77), and the assertion at line 94-97 shows `creditScope: 'NO_CREDIT'`, `creditScopeReason: 'NEEDS_BRAND'`, covering the null-supplier case | worker-equivalent-exists | The markdown-credit-context test asserts `creditScope: 'NO_CREDIT'` for a product with no supplier, which is the null-supplier branch of `creditScopeForSupplier` | PROPOSED: worker-equivalent-exists — null-supplier `creditScopeForSupplier` covered by 'Foreign Product' credit-context assertion |
| `creditScopeForSupplier` returns NO_CREDIT for creditType NONE | supplier-policy.test.ts:76 | negative/error | none found (searched: workers/src/database.markdown-credit-context.pglite.node.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — the credit-context test's suppliers all use `credit_type = 'FULL_CREDIT'` (lines 34-35, 65-66); no Worker test inserts a supplier with `credit_type = 'NONE'` and asserts `creditScope: 'NO_CREDIT'` | worker-shaped-rewrite | The NONE-credit-type branch of `creditScopeForSupplier` is not directly asserted in any Worker test; all Worker test suppliers are FULL_CREDIT | PROPOSED: worker-shaped-rewrite — `creditType: NONE` branch of `creditScopeForSupplier` has no Worker test; re-test with a NONE supplier |
| `creditScopeForSupplier` returns NO_CREDIT for creditType UNKNOWN | supplier-policy.test.ts:76 | negative/error | none found (searched: workers/src/database.markdown-credit-context.pglite.node.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — no Worker test inserts a supplier with `credit_type = 'UNKNOWN'`; the fail-safe mapping (UNKNOWN → NO_CREDIT) is untested in the Worker suite | worker-shaped-rewrite | The UNKNOWN-credit-type fail-safe branch is not asserted in any Worker test | PROPOSED: worker-shaped-rewrite — `creditType: UNKNOWN` fail-safe branch has no Worker test; re-test with an UNKNOWN supplier |
| `creditScopeForSupplier` returns FULL_CREDIT for creditType FULL_CREDIT | supplier-policy.test.ts:76 | — | workers/src/database.markdown-credit-context.pglite.node.test.ts:49-54 — 'Direct' product has supplier with `credit_type = 'FULL_CREDIT'` (line 34), and assertion at line 49-54 shows `creditScope: 'FULL_CREDIT'`, `creditScopeReason: 'FULL_CREDIT'` | worker-equivalent-exists | The credit-context test asserts `creditScope: 'FULL_CREDIT'` for a FULL_CREDIT supplier, covering this branch | PROPOSED: worker-equivalent-exists — FULL_CREDIT branch of `creditScopeForSupplier` covered by 'Direct' credit-context assertion |
| `validatePolicyWrite` accepts instructions with a claim email | supplier-policy.test.ts:87 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/__tests__/api-contract-compatibility.test.ts) — the conformance test's `createSupplier` at line 212 includes `contactEmail: 'claims@example.com'` but never asserts that `validatePolicyWrite` returns no errors; no Worker test calls `validatePolicyWrite` directly | worker-shaped-rewrite | The validation-accepts-email behaviour is not asserted in any Worker test; the Worker may validate at the route layer but no test checks the validation result | PROPOSED: worker-shaped-rewrite — `validatePolicyWrite` email-accept case has no Worker test; re-test against Worker route validation |
| `validatePolicyWrite` accepts instructions with a phone | supplier-policy.test.ts:87 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `createSupplier` at line 212 includes `contactPhone: '02 1234 5678'` but never asserts `validatePolicyWrite` returns no errors for a phone-only contact | worker-shaped-rewrite | The validation-accepts-phone behaviour is not asserted in any Worker test | PROPOSED: worker-shaped-rewrite — `validatePolicyWrite` phone-accept case has no Worker test; re-test against Worker route validation |
| `validatePolicyWrite` accepts instructions with a representative email | supplier-policy.test.ts:87 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `createSupplier` at line 212 includes `representativeEmail: 'alex@example.com'` but never asserts `validatePolicyWrite` returns no errors for a representative-email-only contact | worker-shaped-rewrite | The validation-accepts-representative-email behaviour is not asserted in any Worker test | PROPOSED: worker-shaped-rewrite — `validatePolicyWrite` representative-email-accept case has no Worker test; re-test against Worker route validation |
| `validatePolicyWrite` uses existing contact values when validating a partial edit | supplier-policy.test.ts:97 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `updateSupplier` at line 215 changes `contactPhone` but never tests a partial edit (instructions-only) that relies on existing contact for validation | worker-shaped-rewrite | The partial-edit-uses-existing-contact behaviour is not asserted in any Worker test | PROPOSED: worker-shaped-rewrite — `validatePolicyWrite` partial-edit-with-existing-contact has no Worker test; re-test against Worker update validation |
| `validatePolicyWrite` returns field errors for missing instructions and missing contact | supplier-policy.test.ts:106 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/__tests__/api-contract-compatibility.test.ts) — the conformance test's `bulkAttachSupplier` at line 348 returns `SUPPLIER_POLICY_MISSING` for a bare supplier, which is a different check (supplier has no policy) not field-level validation of a write payload; no Worker test asserts `validatePolicyWrite` field errors | worker-shaped-rewrite | The field-error validation behaviour (missing instructions + missing contact) is not asserted in any Worker test; the Worker's `SUPPLIER_POLICY_MISSING` is a bulk-attach guard, not write-payload validation | PROPOSED: worker-shaped-rewrite — `validatePolicyWrite` field-error case has no Worker test; re-test against Worker route validation |
| `validatePolicyWrite` rejects an update that explicitly clears existing instructions | supplier-policy.test.ts:113 | negative/error | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `clearSupplierPolicy` at line 222 is an explicit clear operation (different from an update that sets `creditPolicyNote` to whitespace), and no Worker test asserts validation rejection of a clearing update | worker-shaped-rewrite | The reject-clearing-update behaviour is not asserted in any Worker test; `clearSupplierPolicy` is a dedicated operation, not a validated update | PROPOSED: worker-shaped-rewrite — `validatePolicyWrite` reject-clearing-update has no Worker test; re-test against Worker update validation |
| `validatePolicyWrite` does not validate an unchanged normalized policy payload | supplier-policy.test.ts:122 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `updateSupplier` at line 215 always changes a field; no Worker test asserts that an unchanged (after normalization) payload skips validation | worker-shaped-rewrite | The skip-validation-for-unchanged behaviour is not asserted in any Worker test | PROPOSED: worker-shaped-rewrite — `validatePolicyWrite` unchanged-skip case has no Worker test; re-test against Worker update validation |

---

### supplier-policy-request-schema.test.ts

Unit tests for **Express-specific Zod request schemas** (`supplierCreateSchema`,
`supplierUpdateSchema`, `supplierPatchSchema`, `bulkAttachPolicySchema`,
`bulkLinkProductsSchema`) imported from `../../schemas`. No Prisma, no supertest — pure
`safeParse` calls asserting accept/reject. The Worker uses inline route-handler validation
(not Zod schemas from `../../schemas`), so the schema definitions are Express machinery; the
validation *behaviours* (field limits, credit-type enum, ratio deferral, bulk cardinality) are
business rules the Worker must also enforce.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| `supplierCreateSchema` accepts documented field limits (creditPolicyNote ≤10000, contactPhone ≤80, representativeName ≤120, valid email) | supplier-policy-request-schema.test.ts:10 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/__tests__/api-contract-compatibility.test.ts) — minimal-api-routes.test.ts:161 registers the supplier POST route but never sends a request body to test field-limit acceptance; error-handling.test.ts:131-149 tests product creation validation, not supplier; api-contract-compatibility.test.ts covers inventory field mapping only | worker-shaped-rewrite | The field-limit acceptance behaviour is a business rule the Worker must enforce at the route level; the Zod schema is Express machinery but the limits are shared; no Worker test asserts them | PROPOSED: worker-shaped-rewrite — supplier field-limit acceptance has no Worker test; re-test against Worker route validation |
| `supplierCreateSchema` rejects creditPolicyNote exceeding 10000 chars | supplier-policy-request-schema.test.ts:24 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test sends an over-length creditPolicyNote to the supplier POST route and asserts a 400 | worker-shaped-rewrite | The over-length rejection is a business rule; the Zod schema retires but the Worker must reject the same input; no Worker test covers it | PROPOSED: worker-shaped-rewrite — over-length creditPolicyNote rejection has no Worker test; re-test against Worker route validation |
| `supplierCreateSchema` rejects contactPhone exceeding 80 chars | supplier-policy-request-schema.test.ts:24 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test sends an over-length contactPhone and asserts rejection | worker-shaped-rewrite | The phone-length limit is a business rule; no Worker test asserts the rejection | PROPOSED: worker-shaped-rewrite — over-length contactPhone rejection has no Worker test; re-test against Worker route validation |
| `supplierCreateSchema` rejects representativeName exceeding 120 chars | supplier-policy-request-schema.test.ts:24 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test sends an over-length representativeName and asserts rejection | worker-shaped-rewrite | The name-length limit is a business rule; no Worker test asserts the rejection | PROPOSED: worker-shaped-rewrite — over-length representativeName rejection has no Worker test; re-test against Worker route validation |
| `supplierCreateSchema` rejects an invalid representativeEmail format | supplier-policy-request-schema.test.ts:24 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test sends a malformed representativeEmail to the supplier route and asserts a 400 | worker-shaped-rewrite | The email-format validation is a business rule; no Worker test asserts the rejection | PROPOSED: worker-shaped-rewrite — invalid representativeEmail rejection has no Worker test; re-test against Worker route validation |
| `supplierPatchSchema` allows one ratio leg (service merges with existing record) | supplier-policy-request-schema.test.ts:35 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `updateSupplier` at line 215 sends a full data object, not a partial patch with one ratio leg; no Worker test asserts partial-patch acceptance | worker-shaped-rewrite | The partial-patch-accepts-one-ratio-leg behaviour is a business rule; the Worker's PATCH route must also allow partial updates; no Worker test covers it | PROPOSED: worker-shaped-rewrite — partial-patch one-ratio-leg acceptance has no Worker test; re-test against Worker PATCH route |
| `supplierCreateSchema`/`supplierUpdateSchema`/`supplierPatchSchema` accept credit type NONE | supplier-policy-request-schema.test.ts:39 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test creates with `creditType: 'FULL_CREDIT'` (line 200) and clears to `creditType: 'NONE'` (line 223), but these are database operations, not request-body validation; no Worker test sends `creditType: 'NONE'` to a route and asserts acceptance | worker-shaped-rewrite | The credit-type-NONE acceptance at the request layer is not tested in the Worker suite; the database test covers the DB layer, not request validation | PROPOSED: worker-shaped-rewrite — credit-type NONE request acceptance has no Worker route test; re-test against Worker route validation |
| `supplierCreateSchema`/`supplierUpdateSchema`/`supplierPatchSchema` accept credit type FULL_CREDIT | supplier-policy-request-schema.test.ts:39 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test creates with `creditType: 'FULL_CREDIT'` (line 200) at the DB layer, but no Worker test sends a request body with `creditType: 'FULL_CREDIT'` to a route and asserts a 2xx response | worker-shaped-rewrite | The credit-type-FULL_CREDIT request acceptance is not tested at the Worker route layer | PROPOSED: worker-shaped-rewrite — credit-type FULL_CREDIT request acceptance has no Worker route test; re-test against Worker route validation |
| All three supplier write schemas reject unknown credit types (e.g. PARTIAL) | supplier-policy-request-schema.test.ts:52 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — no Worker test sends `creditType: 'PARTIAL'` to a supplier route and asserts a 400; the conformance test only uses FULL_CREDIT and NONE at the DB layer | worker-shaped-rewrite | The unknown-credit-type rejection is a business rule the Worker must enforce; no Worker test asserts it | PROPOSED: worker-shaped-rewrite — unknown credit-type rejection has no Worker route test; re-test against Worker route validation |
| `supplierCreateSchema` defers full-write ratio validation to the policy service | supplier-policy-request-schema.test.ts:62 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `createSupplier` at line 212 includes both `policyWriteOffQty` and `policyCreditQty`; no Worker test sends a create with only one ratio leg and asserts the schema accepts it (deferring to service) | worker-shaped-rewrite | The ratio-deferral behaviour (schema accepts, service validates) is a design decision the Worker must replicate; no Worker test covers it | PROPOSED: worker-shaped-rewrite — ratio-deferral-to-service has no Worker test; re-test against Worker route/service boundary |
| `supplierUpdateSchema` defers full-write ratio validation to the policy service | supplier-policy-request-schema.test.ts:62 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `updateSupplier` at line 215 sends full data; no Worker test sends an update with only `policyWriteOffQty` and asserts schema acceptance | worker-shaped-rewrite | Same ratio-deferral design for updates; no Worker test covers it | PROPOSED: worker-shaped-rewrite — update ratio-deferral-to-service has no Worker test; re-test against Worker route/service boundary |
| `bulkAttachPolicySchema`/`bulkLinkProductsSchema` leave raw cardinality for the service 422 boundary | supplier-policy-request-schema.test.ts:71 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `bulkAttachSupplier` at line 346 sends 3 brand IDs and `bulkLinkProducts` at line 378 sends 3 product IDs; no Worker test sends 501 brand IDs or 0 product IDs to assert the schema accepts and the service returns 422 | worker-shaped-rewrite | The cardinality-deferral behaviour (schema accepts, service enforces 422 boundary) is a design decision; no Worker test covers the boundary | PROPOSED: worker-shaped-rewrite — bulk cardinality deferral has no Worker test; re-test against Worker route/service boundary |
| `bulkLinkProductsSchema` requires exactly one bulk-link brand target (rejects both brandId+brandName, rejects missing brandId) | supplier-policy-request-schema.test.ts:82 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.supplier-policy.conformance.node.test.ts) — the conformance test's `bulkLinkProducts` at line 378 always sends `{ brandId: targetId }`; no Worker test sends both `brandId` and `brandName` or omits `brandId` to assert rejection | worker-shaped-rewrite | The exactly-one-brand-target validation is a business rule; no Worker test asserts the rejection | PROPOSED: worker-shaped-rewrite — bulk-link brand-target validation has no Worker test; re-test against Worker route validation |

---

### supplier-policy-schema.test.ts

Schema/migration parity tests using **real filesystem reads** (`fs.readFileSync`) and an
**in-memory SQLite database** (`better-sqlite3`). No Prisma client, no supertest. Verifies
(1) Prisma schema files contain supplier policy fields, (2) Neon SQL migration files exist
with correct columns and a conditional backfill, and (3) SQLite migration 017 applies
idempotently and backfills only real policies. The Worker uses Neon PostgreSQL (no Prisma, no
SQLite migrations); its runtime schema is verified by the conformance test which inserts and
queries these columns.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| `prisma/schema.prisma` contains every supplier policy field (representativeName, representativeEmail, contactPhone, policyUpdatedAt) | supplier-policy-schema.test.ts:19 | — | workers/src/database.supplier-policy.conformance.node.test.ts:212 — `createSupplier(ORG, data)` with `representativeName`, `representativeEmail`, `contactPhone`, `policyUpdatedAt` fields succeeds and the returned supplier is asserted at line 213, implicitly verifying these columns exist in the Neon schema | worker-equivalent-exists | The Prisma schema file check is Express-specific machinery, but the underlying behaviour (the database schema has these fields) is covered by the Worker conformance test which creates and queries suppliers with all four fields | PROPOSED: worker-equivalent-exists — Neon schema has all supplier policy fields, verified by conformance test `createSupplier` with those fields |
| `prisma/production/schema.prisma` contains every supplier policy field | supplier-policy-schema.test.ts:19 | — | workers/src/database.supplier-policy.conformance.node.test.ts:212 — same `createSupplier` call with all policy fields succeeds against the Neon schema, verifying the production schema has these columns; the Worker has a single Neon schema (no separate production Prisma file) | worker-equivalent-exists | The production Prisma schema check is Express-specific; the Worker's single Neon schema is verified at runtime by the conformance test | PROPOSED: worker-equivalent-exists — production Neon schema has all fields, verified by conformance test `createSupplier` |
| Neon migration 0007 ships forward and rollback with conditional backfill (`trim(credit_policy_note) <> ''` → `policy_updated_at = updated_at`) | supplier-policy-schema.test.ts:30 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/workers-deployment.test.ts, workers/src/health.test.ts) — the conformance test inserts suppliers with explicit `policy_updated_at` values (line 247) and does not test a backfill scenario; workers-deployment.test.ts is a deployment smoke test with no migration-file content checks; no Worker test reads or validates migration SQL files | retire | Migration-file content validation is build-time/migration-time machinery; the Worker's Neon database has migrations applied out-of-band, and the runtime schema is verified by the conformance test; the conditional-backfill SQL check has no Worker runtime analogue | PROPOSED: retire — Neon migration file content validation is migration-time machinery; Worker runtime schema verified by conformance test |
| SQLite migration 017 applies idempotently and backfills only real policies (non-blank note → `policy_updated_at = updated_at`, blank → null) | supplier-policy-schema.test.ts:50 | — | none found (searched: workers/src/database.supplier-policy.conformance.node.test.ts, workers/src/workers-deployment.test.ts) — the conformance test uses Neon PostgreSQL via pglite, not SQLite; no Worker test applies a SQLite migration; the backfill-only-real-policies behaviour is not tested at runtime in the Worker suite | retire | SQLite migration testing is Express-specific machinery (the Worker uses Neon PostgreSQL, not SQLite); the idempotent-apply and conditional-backfill behaviours are migration-time concerns with no Worker runtime analogue | PROPOSED: retire — SQLite migration 017 idempotency/backfill is Express-specific SQLite machinery; Worker uses Neon PostgreSQL |

---

### trial-abuse-prevention.test.ts

Integration tests for `ClerkWebhookService` trial abuse prevention (task 16A.C.3) using a
**real Prisma client** against SQLite (`file:./test.db`) with mocked `SubscriptionService` and
Sentry. Tests the 90-day trial abuse check: blocking trials for emails that had a trial within
90 days, allowing trials for emails with no recent trial, and detecting duplicate emails during
the `user.created` webhook. The Worker has a Clerk bootstrap handler that creates trials for
new users but does not implement the 90-day email-based abuse check.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns ConflictError when a duplicate email is created during user.created webhook | trial-abuse-prevention.test.ts:55 | webhook-security | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/auth-integration.test.ts) — bootstrap-handler.node.test.ts:175 links a new user to an existing org but does not test duplicate-email detection or ConflictError; webhook-signature.test.ts:41-58 tests signature verification only; auth-integration.test.ts is JWT token validation with placeholder assertions; no Worker test asserts duplicate-email rejection during webhook processing | worker-shaped-rewrite | The duplicate-email detection is a webhook-security behaviour that must be preserved; the Worker's bootstrap handler does not check for duplicate emails, and no Worker test covers this; the Prisma/SQLite call shape retires but the duplicate-detection gate must be re-tested against the Worker's webhook/bootstrap model | PROPOSED: worker-shaped-rewrite — duplicate-email ConflictError has no Worker equivalent; re-test against Worker webhook/bootstrap model |
| Blocks trial for an email used in a trial within the last 90 days (trial not created, Sentry notified) | trial-abuse-prevention.test.ts:96 | feature-limits | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — bootstrap-handler.node.test.ts:207 creates a trial for a brand-new user without checking email history; subscription-validation.test.ts:125 is a placeholder checking `testData.subscription({status: 'trial'}).status`; feature-gates-integration.test.ts tests tier feature access, not trial abuse; no Worker test checks 90-day trial history by email | worker-shaped-rewrite | The 90-day trial abuse block is a feature-limits gate that must be preserved; the Worker has no email-based trial history check and no test for it; the Prisma query for past trials retires but the abuse-prevention gate must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — 90-day trial abuse block has no Worker equivalent; re-test against Worker subscription/bootstrap model |
| Allows trial for an email whose last trial was more than 90 days ago | trial-abuse-prevention.test.ts:155 | feature-limits | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/subscription-validation.test.ts) — bootstrap-handler.node.test.ts:207 creates a trial for a new user but does not test the 90-day-expiry allow path; subscription-validation.test.ts:82-95 tests expired subscription access with placeholder assertions, not trial-abuse allow-after-90-days; no Worker test inserts a trial older than 90 days and asserts a new trial is allowed | worker-shaped-rewrite | The allow-after-90-days behaviour is the complement of the abuse block and must be preserved; no Worker test covers the 90-day boundary; the Prisma query retires but the gate must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — 90-day-expiry trial allow has no Worker equivalent; re-test against Worker subscription model |
| Allows trial for a completely new email (no prior trial history) | trial-abuse-prevention.test.ts:204 | feature-limits | workers/src/clerk/bootstrap-handler.node.test.ts:234 — asserts `trial[0]?.status === 'trialing'` after `handleOrganizationBootstrap` creates a new org + trial for a brand-new user (`clerk-founder`, `founder@startup.test`), verifying a trial is created for a user with no prior history | worker-equivalent-exists | The bootstrap handler test covers the same outcome (new user → trial created) as the Express test's "allow trial for completely new email"; the Worker does not check email history first, but the behaviour (new email gets a trial) is asserted | PROPOSED: worker-equivalent-exists — bootstrap handler test asserts trial creation for a brand-new user, covering the new-email-gets-trial behaviour |
| Allows trial for an email with a past trial that already expired (status no longer trialing) | trial-abuse-prevention.test.ts:224 | feature-limits | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/org-status-integration.test.ts) — bootstrap-handler.node.test.ts has no test with a pre-existing expired trial for the same email; subscription-validation.test.ts:82-95 is a placeholder for expired subscription access; org-status-integration.test.ts covers org status, not trial expiry; no Worker test inserts an expired trial for an email and asserts a new trial is allowed | worker-shaped-rewrite | The expired-trial-allow behaviour is a feature-limits gate that must be preserved; no Worker test covers the expired-trial-then-new-trial scenario; the Prisma query retires but the gate must be re-tested against the Worker's subscription model | PROPOSED: worker-shaped-rewrite — expired-trial-then-allow has no Worker equivalent; re-test against Worker subscription model |

---

### unit/upload.controller.test.ts

Unit tests for `UploadController.status` with a **mocked `UploadRepository`**
(`findStatusByFileKey` is a `vi.fn()` stub) and an empty `UploadService`. No real
database, no supertest — this is a pure controller call-shape test verifying the
controller decodes the upload key, fetches status from the repository, maps the
repository row to a response payload (progress %, row counts, columns), and
enforces org-scoped access (403 when the repository row's `organizationId`
differs from the authenticated org). The Worker's equivalent is
`handleUploadStatus` exercised in `health.test.ts` (real `Request`/`Response`
model, mocked Clerk token + mocked R2/DB), which returns status payloads but
uses **user-scoped** key ownership (`userOwnsUploadKey`) rather than org-scoped
denial.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns upload status from the repository for the authenticated organization (decodes key, maps row to payload with progress/row counts/columns) | upload.controller.test.ts:25 | — | workers/src/health.test.ts:564 — `handleUploadStatus` returns 200 with `body.status`/`body.progress` for an existing upload key; workers/src/health.test.ts:658 — `handleUploadStatus` returns 200 with `statusBody.importedCount`/`updatedCount`/`skippedCount`/`rowsProcessed`/`rowsTotal`, asserting the status payload shape | worker-equivalent-exists | The status-return behaviour is covered by health.test.ts:564 (status + progress) and :658 (full row-count payload); the Express controller's repository→payload mapping is Express-specific wiring, but the status-payload behaviour is equivalent in the Worker | PROPOSED: worker-equivalent-exists — upload status payload covered by health.test.ts:564 (status/progress) and :658 (row counts); controller repository-mapping is Express-specific |
| Denies status access for another organization's upload (403 + Access denied when repository row organizationId differs) | upload.controller.test.ts:70 | tenant-isolation | none found (searched: workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — health.test.ts:564/658 return 200 for the owning user's key with no cross-org denial assertion; upload-handlers.test.ts:18 checks `userOwnsUploadKey` (user-scoped prefix, not org-scoped 403); upload-router.test.ts:33 tests malformed-key 400, not cross-org 403; multi-tenant-isolation.test.ts:127 asserts orgId filters at the DB query level but has no upload-status cross-org denial test | worker-shaped-rewrite | The cross-org upload-status denial (tenant-isolation gate) is not covered by any Worker test; the Worker uses user-scoped key ownership rather than org-scoped 403 denial, so the org-scoped tenant-isolation behaviour must be re-tested against the Worker's Request/Response model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — cross-org upload-status 403 denial is untested in the Worker (Worker uses user-scoped ownership, upload-handlers.test.ts:18); re-test tenant-isolation gate |

---

### upload.repository.test.ts

Unit tests for `UploadRepository` with a **mocked Prisma client** (`$queryRaw`
and `upload.updateMany` are `vi.fn()` stubs). No real database — this is a pure
call-shape test verifying the repository issues the right Prisma calls to find
upload status by file key and to mark an upload completed/failed with metrics.
The Worker uses raw SQL via `database.sql()` (no Prisma), and exercises the
same upload-status transitions end-to-end in the catalogue-import node test
suite against real Postgres (pglite).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Finds upload status fields by file key ($queryRaw called once, SQL contains file key) | upload.repository.test.ts:23 | — | workers/src/health.test.ts:564 — `handleUploadStatus` returns 200 with `body.status`/`body.progress` for an existing upload key, asserting status is read by key; workers/src/health.test.ts:658 — `handleUploadStatus` returns 200 with full row-count payload read by key | worker-equivalent-exists | The find-status-by-key behaviour is covered by health.test.ts:564 and :658 (status read and returned by key); the Prisma `$queryRaw` call shape is Express-specific machinery that retires with the backend, but the read-status behaviour is equivalent | PROPOSED: worker-equivalent-exists — find-status-by-key covered by health.test.ts:564 and :658; Prisma $queryRaw call shape is Express-specific |
| Marks an upload completed with processing metrics (updateMany with COMPLETED status + row counts) | upload.repository.test.ts:34 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:272 — after `processCatalogueImportJob`, `upload.status` is 'completed_with_errors' with `rows_total`/`rows_imported`/`rows_updated`/`rows_skipped`/`row_error_count`/`upload_progress` set; workers/src/__tests__/catalogue-import-upsert.node.test.ts:386 — `upload.status` is 'completed' with metrics after a successful import | worker-equivalent-exists | The mark-completed-with-metrics behaviour is covered end-to-end by catalogue-import-upsert.node.test.ts:272 (completed_with_errors + full metrics) and :386 (completed); the Prisma `updateMany` call shape is Express-specific, but the status-transition + metrics behaviour is equivalent in the Worker | PROPOSED: worker-equivalent-exists — mark-completed-with-metrics covered by catalogue-import-upsert.node.test.ts:272 and :386; Prisma updateMany call shape is Express-specific |
| Marks an upload failed with optional error details (updateMany with FAILED status + errorMessage + zeroed counts) | upload.repository.test.ts:64 | negative/error | workers/src/__tests__/catalogue-import-upsert.node.test.ts:359 — after a quota-exceeded import, `upload.status` is 'failed' with `failure_category` 'quota', asserting the failed-status transition with error categorization | worker-equivalent-exists | The mark-failed behaviour is covered by catalogue-import-upsert.node.test.ts:359 (failed status + failure_category); the Worker uses `failure_category` rather than a free-text `errorMessage`, but the failed-status transition behaviour is equivalent; the Prisma `updateMany` call shape is Express-specific | PROPOSED: worker-equivalent-exists — mark-failed covered by catalogue-import-upsert.node.test.ts:359 (failed + failure_category); Prisma updateMany call shape is Express-specific |

---

### upload.routes.test.ts

Route-dispatch tests using **supertest** against a real Express app with
**mocked controllers/middleware** (`vi.mock` for auth, `checkUsageLimit`,
`validateRequest`, rate limiters, and `createUploadControllerForRequest`). No
real database — the controllers are stubbed to return `{ route: '<name>' }`.
Tests verify route→controller delegation with org context, the storage usage
guard wiring (3× `checkUsageLimit('storage_bytes')`), and a 10MB file-size
rejection (413) via Express's `LIMIT_FILE_SIZE` error middleware. The Worker
uses a `Request`/`Response` router model (`upload-router.test.ts`) and a
route-exists smoke test (`health.test.ts`), with no mocked-controller
delegation test or Express error middleware.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Registers storage usage guard for upload write endpoints (checkUsageLimit called 3x with 'storage_bytes') | upload.routes.test.ts:95 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — usage-limits-integration.test.ts:100-137 asserts tier storage *values* (100MB/1GB/Infinity) but not enforcement wiring on upload write endpoints; feature-gates.test.ts and feature-gates-integration.test.ts cover feature flags, not storage-byte guard registration | worker-shaped-rewrite | The storage-usage guard on upload write endpoints (feature-limits gate) is not covered by any Worker test; the Worker tests tier limit values but not the guard-on-write-endpoint behaviour; the Express middleware wiring (`checkUsageLimit` called at router build) is Express-specific, but the storage-enforcement gate must be re-tested against the Worker's route model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — storage-usage guard on upload write endpoints is untested in the Worker (usage-limits-integration.test.ts:100 checks values only); re-test feature-limits gate |
| Delegates POST /upload/initiate to UploadController.initiate with organization context | upload.routes.test.ts:102 | — | none found (searched: workers/src/upload/upload-router.test.ts, workers/src/health.test.ts, workers/src/minimal-api-routes.test.ts) — upload-router.test.ts:13 dispatches /api/upload/complete, not /initiate; health.test.ts:354-363 routes direct/presigned/complete/error-report but not initiate; minimal-api-routes.test.ts has no upload route registration | worker-shaped-rewrite | The initiate route delegation is not covered by any Worker test; the Worker has no /initiate route test (health.test.ts:354 omits initiate); the org-context DI (`createUploadControllerForRequest` with organizationId) is Express-specific, but the initiate route dispatch behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — /upload/initiate route delegation is untested in the Worker (health.test.ts:354 omits initiate); re-test against Worker Request model |
| Delegates POST /upload/direct to UploadController.direct for multipart requests | upload.routes.test.ts:117 | — | workers/src/health.test.ts:355 — `SELF.fetch` POST /api/upload/direct/... returns non-404, asserting the direct upload route is registered and reachable | worker-equivalent-exists | The direct-upload route registration is covered by health.test.ts:355 (route not 404); the mocked-controller delegation + multipart (multer) handling + org-context DI are Express-specific machinery; the route-exists behaviour is equivalent in the Worker | PROPOSED: worker-equivalent-exists — direct upload route registration covered by health.test.ts:355 (non-404); mocked-controller delegation and multer multipart are Express-specific |
| Rejects direct uploads larger than 10MB before controller execution (413 + File too large) | upload.routes.test.ts:133 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — usage-limits-integration.test.ts:100 checks tier storage values, not per-file 10MB 413 rejection; health.test.ts has no file-size rejection test; upload-handlers.test.ts:23 queues a 123-byte upload with no size-limit assertion; upload-router.test.ts:33 tests malformed-key 400, not file-size 413 | worker-shaped-rewrite | The 10MB file-size rejection (feature-limits gate) is not covered by any Worker test; the Worker enforces file-size limits in route code but no test asserts the 413 rejection; the Express `LIMIT_FILE_SIZE` error middleware is Express-specific, but the file-size-limit gate must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — 10MB file-size 413 rejection is untested in the Worker (usage-limits-integration.test.ts:100 checks values only); re-test feature-limits gate |
| Delegates POST /upload/complete to UploadController.complete | upload.routes.test.ts:147 | — | workers/src/upload/upload-router.test.ts:13 — `handleWorkerUploadRoute` dispatches /api/upload/complete to `handleUploadComplete`, asserting `handleUploadComplete` called with (Request, env, db) and 200 response | worker-equivalent-exists | The complete-route delegation is exactly equivalent; upload-router.test.ts:13 asserts the complete route dispatches to the handler with the right args and returns 200; the Express org-context DI is Express-specific, but the route→handler delegation behaviour is equivalent | PROPOSED: worker-equivalent-exists — /upload/complete delegation covered by upload-router.test.ts:13 (dispatches to handleUploadComplete, 200); org-context DI is Express-specific |
| Delegates GET /upload/status/:key to UploadController.status | upload.routes.test.ts:162 | — | workers/src/upload/upload-router.test.ts:33 — `handleWorkerUploadRoute` dispatches /api/upload/status/:key (malformed key → 400, handler mocked); workers/src/health.test.ts:341 — `SELF.fetch` GET /api/upload/status/:key returns non-404, asserting the status route is registered | worker-equivalent-exists | The status-route delegation is covered by upload-router.test.ts:33 (status route dispatched, malformed-key 400) and health.test.ts:341 (route registered); the valid-key handler call is exercised directly at health.test.ts:564; the Express mocked-controller delegation + org-context DI is Express-specific, but the route→handler dispatch behaviour is equivalent | PROPOSED: worker-equivalent-exists — /upload/status route delegation covered by upload-router.test.ts:33 (dispatched, 400 on bad key) and health.test.ts:341 (registered); org-context DI is Express-specific |

---

### unit/upload.service.test.ts

Unit tests for `UploadService` with **mocked `StorageProvider`**, **mocked
`CSVParserService`**, **mocked `StorageQuotaService`**, and **mocked
`UploadRepository`** (all `vi.fn()` stubs). No real database or storage — this
is a pure orchestration test verifying the service selects direct vs presigned
upload strategy by file size, enforces a max-size limit, completes uploads
(exists→download→parse→recordUpload→markCompleted), routes import types
(product-catalog vs expiry-list), rejects cross-org keys, handles direct
uploads, and deletes uploads with quota updates. The Worker splits the
complete-upload flow into a queue + `processCatalogueImportJob` (tested in
`catalogue-import-upsert.node.test.ts` against real Postgres) and uses R2
directly (no `StorageQuotaService`).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns direct upload strategy for small files (1MB → direct, POST /api/upload/direct, org-scoped key) | upload.service.test.ts:53 | — | workers/src/health.test.ts:499 — `handleUploadInitiate` returns 200 with `body.strategy` 'direct', `body.method` 'POST', `body.key` containing 'uploads/user-7/', asserting direct strategy for small files | worker-equivalent-exists | The direct-strategy selection is exactly equivalent; health.test.ts:499 asserts direct strategy + POST + org/user-scoped key; the Express `StorageProvider` mock is Express-specific, but the strategy-selection behaviour is equivalent | PROPOSED: worker-equivalent-exists — direct strategy for small files covered by health.test.ts:499 (strategy=direct, POST, org-scoped key) |
| Returns presigned upload strategy for large files in production (5MB → presigned, PUT, presigned URL) | upload.service.test.ts:62 | — | workers/src/health.test.ts:531 — `handleUploadInitiate` returns 200 with `body.strategy` 'presigned', `body.method` 'PUT', `body.uploadUrl` containing '/api/upload/presigned/' + 'token=', asserting presigned strategy for files > 2MB | worker-equivalent-exists | The presigned-strategy selection is exactly equivalent; health.test.ts:531 asserts presigned strategy + PUT + presigned URL with token; the Express env-config mocking (NODE_ENV/threshold) is Express-specific, but the strategy-selection behaviour is equivalent | PROPOSED: worker-equivalent-exists — presigned strategy for large files covered by health.test.ts:531 (strategy=presigned, PUT, presigned URL) |
| Throws error for files exceeding maximum size (10MB+1 → error) | upload.service.test.ts:87 | feature-limits | none found (searched: workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — health.test.ts:1348 sets MAX_FILE_SIZE env but no test asserts initiate rejects oversized files; upload-handlers.test.ts has no max-size assertion; usage-limits-integration.test.ts:100 checks tier storage values, not per-file max-size rejection on initiate | worker-shaped-rewrite | The max-size rejection on initiate (feature-limits gate) is not covered by any Worker test; the Worker has MAX_FILE_SIZE config (health.test.ts:1348) but no test asserts the rejection; the Express envConfig threshold check is Express-specific, but the max-size-limit gate must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — max-size rejection on initiate is untested in the Worker (MAX_FILE_SIZE config exists at health.test.ts:1348 but no rejection test); re-test feature-limits gate |
| Completes upload and processes file successfully (exists→download→parse→recordUpload→markCompleted with metrics) | upload.service.test.ts:97 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:272 — after `processCatalogueImportJob`, `upload.status` is 'completed_with_errors' with `rows_total`/`rows_imported`/`rows_updated`/`rows_skipped`/`row_error_count`/`upload_progress` set, asserting the complete-and-mark-completed-with-metrics behaviour | worker-equivalent-exists | The complete-upload-and-process behaviour is covered end-to-end by catalogue-import-upsert.node.test.ts:272 (completed status + full metrics); the Worker splits the flow into queue + job (no synchronous exists→download→parse orchestration), and has no `StorageQuotaService.recordUpload` (Express-specific), but the process-and-mark-completed behaviour is equivalent | PROPOSED: worker-equivalent-exists — complete-and-process covered by catalogue-import-upsert.node.test.ts:272 (completed + metrics); StorageQuotaService.recordUpload is Express-specific with no Worker analogue |
| Passes explicit import type to parser context (expiry-list → processFile called with importType: 'expiry-list') | upload.service.test.ts:145 | — | workers/src/upload/upload-handlers.test.ts:103 — `processStoredUpload` with importType 'expiry-list' dispatches to the expiry processor, surfacing 'No expiry rows found' (not the catalogue 'No product rows found'), asserting import-type routing | worker-equivalent-exists | The import-type routing is equivalent; upload-handlers.test.ts:103 asserts expiry-list routes to the expiry processor (different error message than catalogue); the Express `CSVParserService.processFile` mock is Express-specific, but the import-type-dispatch behaviour is equivalent | PROPOSED: worker-equivalent-exists — expiry-list import-type routing covered by upload-handlers.test.ts:103 (dispatches to expiry processor, not catalogue) |
| Throws error if file does not exist in storage (exists returns false → verification failed) | upload.service.test.ts:177 | negative/error | workers/src/health.test.ts:1327 — `handleUploadComplete` returns 404 when R2 `get` returns null for the upload key, asserting the file-not-found rejection; workers/src/upload/upload-handlers.test.ts:46 — `processCompletedUploadSync` maps a disappearing R2 object to 404 with 'Upload not found' | worker-equivalent-exists | The file-not-found rejection (negative/error gate) is covered by health.test.ts:1327 (404 on null R2 get) and upload-handlers.test.ts:46 (404 on disappearing object); the Express `StorageProvider.exists` mock is Express-specific, but the not-found rejection behaviour is equivalent | PROPOSED: worker-equivalent-exists — file-not-found rejection covered by health.test.ts:1327 (404 on null R2) and upload-handlers.test.ts:46 (404 on disappearing object) |
| Rejects upload keys outside the current organization scope (Access denied, no storage calls) | upload.service.test.ts:185 | tenant-isolation | none found (searched: workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — health.test.ts:499/593 use the owning user's key with no cross-org rejection assertion; upload-handlers.test.ts:18 checks `userOwnsUploadKey` (user-scoped prefix, not org-scoped Access-denied); upload-router.test.ts:33 tests malformed-key 400; multi-tenant-isolation.test.ts:127 asserts orgId filters at DB query level but has no upload-key org-scope rejection test | worker-shaped-rewrite | The cross-org upload-key rejection (tenant-isolation gate) is not covered by any Worker test; the Worker uses user-scoped key ownership (upload-handlers.test.ts:18) rather than org-scoped Access-denied; the org-scope tenant-isolation behaviour must be re-tested against the Worker's Request model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — cross-org upload-key rejection is untested in the Worker (Worker uses user-scoped ownership, upload-handlers.test.ts:18); re-test tenant-isolation gate |
| Handles direct upload successfully (storage.upload→exists→download→parse, returns key + processingResult with counts) | upload.service.test.ts:196 | — | workers/src/health.test.ts:593 — `handleUploadDirect` processes a direct CSV upload via FormData, stores to R2, and returns 200 with `directBody.importedCount` 1, asserting direct upload with real import counts | worker-equivalent-exists | The direct-upload handling is covered by health.test.ts:593 (handleUploadDirect with FormData, R2 put, import counts); the Express `StorageProvider.upload` mock + synchronous parse is Express-specific, but the direct-upload-and-process behaviour is equivalent | PROPOSED: worker-equivalent-exists — direct upload handling covered by health.test.ts:593 (handleUploadDirect, R2 put, importedCount) |
| Deletes upload and updates storage quota (storage.delete + markUploadDeleted called) | upload.service.test.ts:237 | — | none found (searched: workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test covers upload deletion; the Worker has `env.CSV_UPLOADS.delete(key)` in code (index-minimal.ts:3603) but no test asserts it; usage-limits-integration.test.ts has no upload-deletion quota test; there is no `StorageQuotaService.markUploadDeleted` equivalent in the Worker | worker-shaped-rewrite | The upload-deletion + quota-update behaviour is not covered by any Worker test; the Worker has the R2 delete call in code but no test, and no storage-quota-update mechanism; the `StorageQuotaService` is Express-specific, but the delete-upload behaviour must be re-tested against the Worker's R2 model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — upload deletion + quota update is untested in the Worker (CSV_UPLOADS.delete exists in code at index-minimal.ts:3603 but no test); re-test against Worker R2 model |
| Throws error if storage deletion fails (delete rejects → error propagated) | upload.service.test.ts:249 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test covers upload-deletion failure; error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not R2 delete failure; the Worker has no upload-delete test at all | worker-shaped-rewrite | The storage-deletion-failure propagation (negative/error gate) is not covered by any Worker test; the Worker has no upload-delete test, and error-handling.test.ts covers DB retry logic, not R2 delete errors; the deletion-failure behaviour must be re-tested against the Worker's R2 model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — storage-deletion failure is untested in the Worker (no upload-delete test; error-handling.test.ts:18 covers DB retry only); re-test negative/error gate |

---

### upload-status.test.ts

State-machine tests using a **real `PrismaClient`** against SQLite (creates
org/user/upload rows in `beforeEach`, deletes in `afterEach`). Verifies the
canonical upload lifecycle `pending → uploading → processing →
completed/failed`, storage-quota exclusion of failed uploads, `UploadStatus`
enum validity (no legacy 'complete'), and terminal-state immutability
(documentation-style — creates a terminal upload and asserts its status, no
actual transition attempt). The Worker uses string-literal statuses ('queued',
'processing', 'completed', 'completed_with_errors', 'failed') with no
`UploadStatus` enum, and exercises the same transitions end-to-end in
`catalogue-import-upsert.node.test.ts` against real Postgres (pglite).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Creates upload in pending state by default (Prisma default status) | upload-status.test.ts:75 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:72 — `insertUpload` creates an upload row with default `status: 'queued'`, asserting the initial upload status on creation | worker-equivalent-exists | The default-initial-status behaviour is equivalent; the Worker uses 'queued' (catalogue-import-upsert.node.test.ts:72) vs Express 'pending', but the "upload starts in an initial non-terminal state" behaviour is the same; the Prisma `upload.create` call shape is Express-specific | PROPOSED: worker-equivalent-exists — default initial status covered by catalogue-import-upsert.node.test.ts:72 (queued default); Prisma create call shape is Express-specific |
| Transitions from pending → processing via recordUpload (StorageQuotaService.recordUpload creates in PROCESSING state) | upload-status.test.ts:90 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:383 — `processCatalogueImportJob` processes a 'processing'-status upload to 'completed', asserting the processing→completed transition; workers/src/upload/catalogue-import.ts:126 (source) sets status='processing' during import | worker-equivalent-exists | The →processing transition is covered by catalogue-import-upsert.node.test.ts:383 (processing→completed job) and the Worker sets 'processing' during import (catalogue-import.ts:126); the Express `StorageQuotaService.recordUpload` is Express-specific with no Worker analogue, but the transition-to-processing behaviour is equivalent | PROPOSED: worker-equivalent-exists — →processing transition covered by catalogue-import-upsert.node.test.ts:383 (processing job); StorageQuotaService.recordUpload is Express-specific |
| Marks upload as completed after successful processing (COMPLETED + rowsProcessed/rowsImported/rowsUpdated) | upload-status.test.ts:110 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:386 — after `processCatalogueImportJob`, `upload.status` is 'completed' with `failure_category` null, asserting the completed-after-success transition | worker-equivalent-exists | The completed-after-success transition is covered by catalogue-import-upsert.node.test.ts:386 (completed, no failure); the Prisma `upload.update` call shape is Express-specific, but the mark-completed behaviour is equivalent | PROPOSED: worker-equivalent-exists — completed-after-success covered by catalogue-import-upsert.node.test.ts:386 (completed, failure_category null) |
| Marks upload as failed on processing error (FAILED + errorMessage + zeroed counts) | upload-status.test.ts:145 | negative/error | workers/src/__tests__/catalogue-import-upsert.node.test.ts:359 — after a quota-exceeded import, `upload.status` is 'failed' with `failure_category` 'quota', asserting the failed-on-error transition | worker-equivalent-exists | The failed-on-error transition (negative/error gate) is covered by catalogue-import-upsert.node.test.ts:359 (failed + failure_category); the Worker uses `failure_category` instead of free-text `errorMessage`, but the mark-failed behaviour is equivalent | PROPOSED: worker-equivalent-exists — failed-on-error covered by catalogue-import-upsert.node.test.ts:359 (failed + failure_category) |
| Does not increment storage quota for failed uploads (aggregate counts only COMPLETED) | upload-status.test.ts:179 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/health.test.ts) — usage-limits-integration.test.ts:100-137 asserts tier storage *values* but not that failed uploads are excluded from quota; catalogue-import-upsert.node.test.ts:359 marks failed but does not assert quota exclusion; health.test.ts has no storage-quota aggregation test; the Worker has no `StorageQuotaService` or storage-quota aggregation | worker-shaped-rewrite | The failed-upload quota exclusion (feature-limits gate) is not covered by any Worker test; the Worker has no storage-quota aggregation mechanism and no test asserting failed uploads are excluded; the Express `StorageQuotaService` + Prisma aggregate is Express-specific, but the quota-exclusion gate must be re-tested against the Worker's quota model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — failed-upload quota exclusion is untested in the Worker (no StorageQuotaService or quota aggregation test); re-test feature-limits gate |
| Only allows valid status values (UploadStatus enum contains pending/uploading/processing/completed/failed, excludes legacy 'complete') | upload-status.test.ts:210 | — | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts) — the Worker uses string-literal statuses ('queued', 'processing', 'completed', 'completed_with_errors', 'failed') with no `UploadStatus` enum object to validate; catalogue-import-upsert.node.test.ts:272/359/386 assert specific string statuses but not an enum's valid-values set | retire | The `UploadStatus` enum validation is Express/Prisma-specific machinery; the Worker uses string literals with no enum object to validate, so the enum-validity behaviour has no Worker analogue and retires with the backend | PROPOSED: retire — UploadStatus enum validation is Express/Prisma-specific (Worker uses string literals, no enum object) |
| Uses canonical COMPLETED status in services (UploadStatus.COMPLETED='completed', FAILED='failed', etc.) | upload-status.test.ts:223 | — | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/health.test.ts) — the Worker uses string literals directly ('completed', 'failed', 'processing') with no `UploadStatus` enum constant to assert; catalogue-import-upsert.node.test.ts:386 asserts `upload.status` is 'completed' (the string), not an enum constant | retire | The `UploadStatus` enum constant assertion is Express/Prisma-specific machinery; the Worker uses string literals directly, so the enum-canonical-value behaviour has no Worker analogue and retires with the backend | PROPOSED: retire — UploadStatus enum constant assertion is Express/Prisma-specific (Worker uses string literals, no enum constants) |
| Does not transition from completed to other states (terminal-state immutability documentation) | upload-status.test.ts:233 | — | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts) — no Worker test asserts that a completed upload cannot transition to another state; catalogue-import-upsert.node.test.ts:386 asserts completed status but does not test immutability; the Worker has no terminal-state-guard test | worker-shaped-rewrite | The terminal-state immutability (completed) is not covered by any Worker test; the Express test is documentation-style (creates completed, asserts status, no transition attempt), but the invariant must be re-tested against the Worker's state model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — completed terminal-state immutability is untested in the Worker (no transition-guard test); re-test against Worker state model |
| Does not transition from failed to other states except retry (terminal-state immutability documentation) | upload-status.test.ts:254 | — | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/health.test.ts, workers/src/upload/upload-handlers.test.ts) — no Worker test asserts that a failed upload cannot transition except via retry; catalogue-import-upsert.node.test.ts:359 asserts failed status but does not test immutability; the Worker has no terminal-state-guard test | worker-shaped-rewrite | The terminal-state immutability (failed, except retry) is not covered by any Worker test; the Express test is documentation-style, but the invariant must be re-tested against the Worker's state model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — failed terminal-state immutability is untested in the Worker (no transition-guard test); re-test against Worker state model |

---

### url-validator.test.ts

Pure unit tests for two Express utility functions — `validateRedirectUrl`
(open-redirect prevention: allows relative URLs, rejects empty/malformed/non-http
protocols, allow-lists FRONTEND_URL/CORS_ORIGIN domains, allows localhost in
test, rejects non-allow-listed domains) and `validateStripePriceId` (Stripe
checkout price-ID validation: accepts configured IDs, rejects unconfigured/empty/
wrong-prefix/too-short/too-long IDs). No database, no HTTP — these are pure
function tests using `envConfig`/`process.env` mocks. The Worker has **no
redirect-URL flow** (no success_url/cancel_url/returnTo) and **no Stripe
checkout flow** (no priceId/checkout-session surface), so neither utility has
a Worker analogue. The Worker's `authorized-parties.test.ts` covers
FRONTEND_URL-based origin allow-listing for Clerk parties (a related but
different security surface).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Allows relative urls (no throw for /dashboard?tab=imports) | url-validator.test.ts:21 | — | none found (searched: workers/src/__tests__/authorized-parties.test.ts, workers/src/utils/worker-response.test.ts, workers/src/utils/minimal-rate-limit.test.ts, workers/src/health.test.ts) — authorized-parties.test.ts:44 allow-lists FRONTEND_URL origins for Clerk parties, not relative redirect URLs; worker-response.test.ts:14 tests CORS origin handling, not redirect-URL validation; no Worker test validates relative redirect URLs | retire | The relative-redirect-URL allowance is Express-specific redirect machinery; the Worker has no redirect-URL flow (no success_url/cancel_url/returnTo in workers/src), so the behaviour has no Worker analogue and retires with the backend | PROPOSED: retire — relative redirect-URL validation is Express-specific (Worker has no redirect-URL flow) |
| Rejects empty values (throws 'URL must be a non-empty string') | url-validator.test.ts:25 | negative/error | none found (searched: workers/src/__tests__/authorized-parties.test.ts, workers/src/utils/worker-response.test.ts, workers/src/utils/minimal-rate-limit.test.ts, workers/src/health.test.ts) — authorized-parties.test.ts:109 rejects a malformed FRONTEND_URL but not empty redirect-URL input; worker-response.test.ts tests CORS origin, not redirect-URL empty validation; no Worker test validates redirect-URL input | worker-shaped-rewrite | The empty-redirect-URL rejection (negative/error gate) is not covered by any Worker test; the Worker has no redirect-URL flow, but the negative/error gate is named for preservation and must be re-tested against the Worker's model in Phase 3.2 if redirect flows are added | PROPOSED: worker-shaped-rewrite — empty redirect-URL rejection is untested in the Worker (no redirect-URL flow); re-test negative/error gate |
| Rejects malformed absolute urls (throws 'URL is not a valid URL: http://') | url-validator.test.ts:29 | negative/error | none found (searched: workers/src/__tests__/authorized-parties.test.ts, workers/src/utils/worker-response.test.ts, workers/src/utils/minimal-rate-limit.test.ts) — authorized-parties.test.ts:109 rejects a malformed FRONTEND_URL ('not a url') for Clerk party derivation, not redirect-URL input validation; no Worker test validates malformed redirect URLs | worker-shaped-rewrite | The malformed-redirect-URL rejection (negative/error gate) is not covered by any Worker test; authorized-parties.test.ts:109 covers malformed FRONTEND_URL for Clerk parties (different surface); the negative/error gate must be re-tested against the Worker's model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — malformed redirect-URL rejection is untested in the Worker (authorized-parties.test.ts:109 is Clerk-party, not redirect-URL); re-test negative/error gate |
| Rejects non-http protocols (throws 'URL must use http or https protocol' for javascript:alert(1)) | url-validator.test.ts:33 | penetration | none found (searched: workers/src/__tests__/authorized-parties.test.ts, workers/src/utils/worker-response.test.ts, workers/src/utils/minimal-rate-limit.test.ts, workers/src/health.test.ts) — authorized-parties.test.ts tests HTTPS origin allow-listing for Clerk parties, not javascript:-protocol rejection on redirect URLs; no Worker test rejects non-http protocols on redirect URLs | worker-shaped-rewrite | The non-http-protocol rejection (penetration gate — open-redirect via javascript:) is not covered by any Worker test; the Worker has no redirect-URL flow, but the penetration gate is named for preservation and must be re-tested against the Worker's model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — javascript:-protocol redirect rejection is untested in the Worker (no redirect-URL flow); re-test penetration gate |
| Allows domains from FRONTEND_URL and CORS_ORIGIN (no throw for app.example.com/api.example.com) | url-validator.test.ts:39 | — | none found (searched: workers/src/__tests__/authorized-parties.test.ts, workers/src/utils/worker-response.test.ts, workers/src/middleware/cors.middleware.ts) — authorized-parties.test.ts:44 includes FRONTEND_URL in Clerk authorized parties (origin allow-list, not redirect-URL allow-list); worker-response.test.ts:14 tests CORS origin from FRONTEND_URL, not redirect-URL domain validation; no Worker test validates redirect-URL domains against FRONTEND_URL/CORS_ORIGIN | retire | The FRONTEND_URL/CORS_ORIGIN redirect-URL domain allow-list is Express-specific redirect machinery; the Worker's authorized-parties.test.ts:44 covers FRONTEND_URL for Clerk parties (different surface), not redirect URLs; the Worker has no redirect-URL flow, so the behaviour retires with the backend | PROPOSED: retire — FRONTEND_URL/CORS_ORIGIN redirect-URL allow-list is Express-specific (Worker authorized-parties.test.ts:44 is Clerk-party origin, not redirect-URL) |
| Allows localhost variants in test environment (localhost/127.0.0.1/[::1]) | url-validator.test.ts:44 | — | none found (searched: workers/src/__tests__/authorized-parties.test.ts, workers/src/utils/worker-response.test.ts, workers/src/utils/minimal-rate-limit.test.ts) — authorized-parties.test.ts:30/40 includes localhost/127.0.0.1 in Clerk parties for dev, not redirect-URL localhost validation; worker-response.test.ts:31 tests dev CORS with undefined FRONTEND_URL, not redirect-URL localhost; no Worker test validates localhost redirect URLs | retire | The localhost redirect-URL allowance is Express-specific redirect machinery; the Worker's authorized-parties.test.ts:30 includes localhost for Clerk parties (different surface); the Worker has no redirect-URL flow, so the behaviour retires with the backend | PROPOSED: retire — localhost redirect-URL allowance is Express-specific (Worker authorized-parties.test.ts:30 is Clerk-party, not redirect-URL) |
| Rejects domains outside the allow-list (throws 'URL domain "evil.example.net" is not allowed') | url-validator.test.ts:50 | penetration | none found (searched: workers/src/__tests__/authorized-parties.test.ts, workers/src/utils/worker-response.test.ts, workers/src/utils/minimal-rate-limit.test.ts, workers/src/health.test.ts) — authorized-parties.test.ts:87/98 tests that non-allow-listed hosts (staging subdomain, pages.dev) are NOT included in Clerk parties, but does not test redirect-URL domain rejection; no Worker test rejects non-allow-listed redirect-URL domains | worker-shaped-rewrite | The non-allow-listed-domain redirect rejection (penetration gate — open-redirect to evil domains) is not covered by any Worker test; authorized-parties.test.ts:87 covers Clerk-party exclusion (different surface); the penetration gate is named for preservation and must be re-tested against the Worker's model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — non-allow-listed redirect-domain rejection is untested in the Worker (authorized-parties.test.ts:87 is Clerk-party, not redirect-URL); re-test penetration gate |
| Accepts configured Stripe price ids (no throw for price_professional_monthly/annual) | url-validator.test.ts:80 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — subscription-validation.test.ts:17/97/145 tests subscription status validation (active/grace/expired), not Stripe price-ID validation; no Worker test covers Stripe price-ID acceptance; the Worker has no Stripe checkout flow (no priceId/checkout-session surface) | retire | The Stripe price-ID acceptance is Express-specific Stripe checkout machinery; the Worker has no Stripe checkout flow (no priceId surface in workers/src), so the behaviour has no Worker analogue and retires with the backend | PROPOSED: retire — Stripe price-ID acceptance is Express-specific (Worker has no Stripe checkout/priceId flow) |
| Rejects valid-looking Stripe price ids that are not configured (throws 'priceId is not configured for checkout') | url-validator.test.ts:85 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts tests subscription status, not Stripe price-ID configuration validation; no Worker test rejects unconfigured price IDs; the Worker has no Stripe checkout flow | worker-shaped-rewrite | The unconfigured-price-ID rejection (negative/error gate) is not covered by any Worker test; the Worker has no Stripe checkout flow, but the negative/error gate is named for preservation and must be re-tested against the Worker's model in Phase 3.2 if Stripe checkout is added | PROPOSED: worker-shaped-rewrite — unconfigured price-ID rejection is untested in the Worker (no Stripe checkout flow); re-test negative/error gate |
| Rejects empty ids (throws 'priceId must be a non-empty string') | url-validator.test.ts:91 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts tests subscription status, not price-ID empty validation; no Worker test rejects empty price IDs; the Worker has no Stripe checkout flow | worker-shaped-rewrite | The empty-price-ID rejection (negative/error gate) is not covered by any Worker test; the Worker has no Stripe checkout flow, but the negative/error gate is named for preservation and must be re-tested against the Worker's model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — empty price-ID rejection is untested in the Worker (no Stripe checkout flow); re-test negative/error gate |
| Rejects ids without the expected prefix (throws 'priceId must be a valid Stripe price ID (starts with "price_")') | url-validator.test.ts:95 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts tests subscription status, not price-ID prefix validation; no Worker test rejects wrong-prefix price IDs; the Worker has no Stripe checkout flow | worker-shaped-rewrite | The wrong-prefix price-ID rejection (negative/error gate) is not covered by any Worker test; the Worker has no Stripe checkout flow, but the negative/error gate is named for preservation and must be re-tested against the Worker's model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — wrong-prefix price-ID rejection is untested in the Worker (no Stripe checkout flow); re-test negative/error gate |
| Rejects ids that are too short (throws 'priceId has invalid length') | url-validator.test.ts:101 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts tests subscription status, not price-ID length validation; no Worker test rejects too-short price IDs; the Worker has no Stripe checkout flow | worker-shaped-rewrite | The too-short price-ID rejection (negative/error gate) is not covered by any Worker test; the Worker has no Stripe checkout flow, but the negative/error gate is named for preservation and must be re-tested against the Worker's model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — too-short price-ID rejection is untested in the Worker (no Stripe checkout flow); re-test negative/error gate |
| Rejects ids that are too long (throws 'priceId has invalid length') | url-validator.test.ts:105 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/health.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts tests subscription status, not price-ID length validation; no Worker test rejects too-long price IDs; the Worker has no Stripe checkout flow | worker-shaped-rewrite | The too-long price-ID rejection (negative/error gate) is not covered by any Worker test; the Worker has no Stripe checkout flow, but the negative/error gate is named for preservation and must be re-tested against the Worker's model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — too-long price-ID rejection is untested in the Worker (no Stripe checkout flow); re-test negative/error gate |

---

### user.repository.test.ts

Unit tests for `UserRepository` with a **mocked Prisma client** (`prisma.user.findMany` /
`findFirst` / `findUnique` / `create` / `update` are `vi.fn()` stubs). No real database, no
supertest — every test asserts the repository issues the correct Prisma call shape (`where`,
`data`, `include`, `select`) and returns the mocked value. The Worker has no Prisma and no
repository class; it issues raw SQL via Neon. The closest Worker analogue is
`clerk/bootstrap-handler.node.test.ts`, which exercises real SQL (pglite) for user
lookup/creation/linking during organization bootstrap. `utils/auth.test.ts` covers JWT/subscription
auth context, not user-table CRUD.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Finds users for an organization (findMany with where organizationId) | user.repository.test.ts:43 | tenant-isolation | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/__tests__/auth-integration.test.ts, workers/src/index-minimal.ts) — bootstrap-handler.node.test.ts:116 returns a single existing user by clerk_user_id, not a list of users for an org; multi-tenant-isolation.test.ts:16 tests product isolation (placeholder `expect(true)`), not user listing; auth-integration.test.ts and utils/auth.test.ts cover JWT/subscription auth, not user-table queries | retire | The test asserts a Prisma `findMany` call shape with `where: { organizationId }`; the Worker has no Prisma and no tested user-list-by-org query; the Prisma call shape is Express-specific machinery with no Worker analogue, and no Worker test lists users for an organization | PROPOSED: retire — Prisma findMany call shape with no Worker user-list test; tenant-isolation gate is preserved by product/inventory isolation tests, not user listing |
| Finds a user by id within an organization (findFirst with where id+organizationId) | user.repository.test.ts:54 | tenant-isolation | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/index-minimal.ts) — bootstrap-handler.node.test.ts:116 looks up a user by clerk_user_id (string), not by numeric id scoped to an org; no Worker test fetches a user by numeric id + organizationId | retire | The test asserts a Prisma `findFirst` call shape with `where: { id, organizationId }`; the Worker's bootstrap handler uses clerk_user_id for user lookup, not numeric id; the Prisma call shape and numeric-id lookup pattern are Express-specific machinery with no Worker analogue | PROPOSED: retire — Prisma findFirst-by-numeric-id call shape has no Worker analogue (Worker uses clerk_user_id lookup) |
| Creates a Clerk-backed user record (create with org/clerkUserId/email/username/role) | user.repository.test.ts:65 | — | workers/src/clerk/bootstrap-handler.node.test.ts:175 — links a new user to an existing org via INSERT into users table (201, role team_member, asserts DB row has organizationId+role); workers/src/clerk/bootstrap-handler.node.test.ts:207 — creates a new org + first admin user via INSERT (201, role admin, asserts DB row has role admin) | worker-equivalent-exists | The "create a Clerk-backed user" behaviour is covered by bootstrap-handler.node.test.ts:175 (new user linked to existing org) and :207 (first admin user created); both assert real SQL INSERTs into the users table with organization_id, clerk_user_id, email, role; the Prisma `create` call shape retires but the user-creation behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — user creation covered by bootstrap-handler.node.test.ts:175 (link new user) and :207 (first admin); Prisma create call shape is Express-specific |
| Updates a user role within an organization (update with where id+organizationId, data role) | user.repository.test.ts:87 | authorization-precedence | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/auth-integration.test.ts, workers/src/index-minimal.ts) — bootstrap-handler.node.test.ts creates users but never updates a role; auth-integration.test.ts covers JWT expiry/validation, not role updates; utils/auth.test.ts covers auth context extraction, not user-table updates | retire | The test asserts a Prisma `update` call shape with `where: { id, organizationId }` and `data: { role }`; no Worker test updates a user's role; the Prisma update call shape is Express-specific machinery, and the Worker has no tested role-update path | PROPOSED: retire — Prisma update call shape for role change has no Worker test; role assignment happens at bootstrap creation time, not via update |
| Finds a Clerk user with organization subscription data (findUnique with nested include) | user.repository.test.ts:98 | — | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/index-minimal.ts) — bootstrap-handler.node.test.ts:116 returns a user with organizationId/role but does not join subscription_tiers in the same query; utils/auth.test.ts:321 (authenticateWorkerRequest) does a separate subscription lookup by organizationId, not a joined user+org+subscription query | retire | The test asserts a Prisma `findUnique` with nested `include: { organization: { include: { subscriptionTiers: true } } }` — a Prisma-specific eager-load call shape; the Worker splits user lookup (bootstrap-handler) and subscription lookup (auth) across separate SQL queries, so the joined-query call shape has no Worker analogue | PROPOSED: retire — Prisma nested-include call shape has no Worker analogue; Worker splits user and subscription lookups across separate queries |
| Finds a Clerk user's organization id (findUnique with select organizationId) | user.repository.test.ts:116 | — | workers/src/clerk/bootstrap-handler.node.test.ts:116 — returns the existing user with `organizationId: 'org-1'` from a lookup by clerk_user_id (sub: 'clerk-user-1'), asserting the org id is retrievable from the user record | worker-equivalent-exists | The "find org id by clerk user id" behaviour is covered by bootstrap-handler.node.test.ts:116, which returns organizationId from a user lookup by clerk_user_id; the Prisma `findUnique` with `select` call shape retires but the behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — org-id-by-clerk-user-id covered by bootstrap-handler.node.test.ts:116 (returns organizationId from user lookup); Prisma select call shape is Express-specific |
| Finds an active Clerk user for authentication (findUnique with deletedAt:null, select id/role/orgId) | user.repository.test.ts:128 | — | workers/src/clerk/bootstrap-handler.node.test.ts:116 — returns the existing user with `userId`, `organizationId: 'org-1'`, `role: 'manager'` from a lookup by clerk_user_id, asserting id/role/orgId are retrievable for an active user | worker-equivalent-exists | The "find active user by clerk id for auth" behaviour is covered by bootstrap-handler.node.test.ts:116, which returns id/role/organizationId for an existing (active) user; the `deletedAt: null` filter is not explicitly asserted in the Worker test (bootstrap seeds only active users), but the active-user-lookup behaviour is equivalent | PROPOSED: worker-equivalent-exists — active-user auth lookup covered by bootstrap-handler.node.test.ts:116 (returns id/role/orgId); deletedAt:null filter not explicitly tested but active-user behaviour is equivalent |

---

### user.routes.test.ts

Supertest route tests for the Express `/users` router with a **mocked `UserService`** (all service
methods are `vi.fn()` stubs). Auth middleware is stubbed to set `req.organizationId` from the
`x-org-id` header; `requireOrgRole` is stubbed to always pass. Tests cover the full CRUD surface
(GET list, GET by id, POST create, PUT update, DELETE) plus org-scoped access control (403
cross-org), input validation (400 invalid id, missing pin/role), auth context (401 missing org),
and error forwarding (500 on service failure). The Worker has **no user CRUD endpoints** —
`minimal-api-routes.test.ts` registers routes for products, reports, supplier-credits,
store-areas, markdown-config, and subscription, but no `/api/users` routes. The Worker handles
users only via `clerk/bootstrap-handler` (create/link on org bootstrap) and auth context lookup
(`utils/auth.test.ts`). The Express route wiring, supertest dispatch, mocked-service DI, and
Express error middleware are Express-specific machinery with no Worker analogue.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| GET /users returns all users for requester organization (UserService constructed with orgId, getUsers called once) | user.routes.test.ts:109 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts:141-185 registers product, report, supplier-credit, store-area, markdown-config, and subscription routes, but no `/api/users` list route; bootstrap-handler.node.test.ts:116 returns a single user by clerk_user_id, not a user list; multi-tenant-isolation.test.ts:16 tests product isolation, not user listing | retire | The Worker has no user-list endpoint; the Express supertest route dispatch with mocked UserService is Express-specific machinery; the tenant-isolation gate is preserved by SQL-level orgId filters in other Worker tests (multi-tenant-isolation.test.ts:127) | PROPOSED: retire — Express user-list route has no Worker analogue; tenant-isolation gate preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) |
| GET /users returns 500 when getUsers fails | user.routes.test.ts:124 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — minimal-api-routes.test.ts has no user-list route or user-list error test; error-handling.test.ts:18-97 tests `withNeonRetry` retry logic, not user-list endpoint 500s | retire | The Worker has no user-list endpoint, so there is no user-list 500-on-failure behaviour to test; the Express error middleware forwarding is Express-specific machinery; the negative/error gate is preserved by other Worker tests (minimal-api-routes.test.ts:418 rejects invalid input with 400, :509 returns 503 for missing schema) | PROPOSED: retire — Express user-list 500-on-failure has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:418 and :509 |
| GET /users/:id returns 400 when user id param is invalid (not a number) | user.routes.test.ts:135 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no `/api/users/:id` route; no Worker test validates a user-id path parameter | retire | The Worker has no user-by-id endpoint; the Express route param validation (`isNaN` check) is Express-specific route wiring with no Worker analogue; the negative/error gate is preserved by other Worker input-validation tests (minimal-api-routes.test.ts:399 rejects non-numeric markdown percentages with 400) | PROPOSED: retire — Express user-id param validation has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:399 |
| GET /users/:id returns 404 when user is not found | user.routes.test.ts:143 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no `/api/users/:id` route; no Worker test covers a user-not-found 404 | retire | The Worker has no user-by-id endpoint; the Express 404-not-found route behaviour is Express-specific route wiring with no Worker analogue | PROPOSED: retire — Express user-by-id 404 has no Worker analogue (no user CRUD endpoint) |
| GET /users/:id returns 403 when user belongs to different organization | user.routes.test.ts:152 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts has no user-by-id route; multi-tenant-isolation.test.ts:127 asserts orgId filters at SQL level (placeholder `expect(true)`), not a user-level cross-org 403 | retire | The Worker has no user-by-id endpoint, so there is no user-level cross-org 403 to test; the Express route-level org-membership check is Express-specific machinery; the tenant-isolation gate is preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) and product-level isolation (multi-tenant-isolation.test.ts:16) | PROPOSED: retire — Express user cross-org 403 has no Worker endpoint; tenant-isolation gate preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) |
| GET /users/:id returns user details for same organization | user.routes.test.ts:163 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no `/api/users/:id` route; no Worker test returns user details by numeric id | retire | The Worker has no user-by-id endpoint; the Express supertest route dispatch with mocked UserService is Express-specific machinery with no Worker analogue | PROPOSED: retire — Express user-by-id detail route has no Worker analogue (no user CRUD endpoint) |
| GET /users/:id returns 500 when getUserById throws | user.routes.test.ts:176 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — minimal-api-routes.test.ts has no user-by-id route; error-handling.test.ts:18-97 tests retry logic, not user-by-id 500s | retire | The Worker has no user-by-id endpoint; the Express error middleware forwarding is Express-specific machinery; the negative/error gate is preserved by other Worker error tests (minimal-api-routes.test.ts:509 returns 503) | PROPOSED: retire — Express user-by-id 500-on-failure has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:509 |
| POST /users returns 400 when pin or role is missing | user.routes.test.ts:187 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no POST `/api/users` route; no Worker test validates pin/role presence for user creation | retire | The Worker has no standalone user-create endpoint; the Express route body validation (pin+role required) is Express-specific route wiring; the Worker's user creation happens via bootstrap with different required fields (clerk_user_id, email); the negative/error gate is preserved by minimal-api-routes.test.ts:399 (rejects invalid markdown input) | PROPOSED: retire — Express user-create pin/role validation has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:399 |
| POST /users returns 401 when organization context is missing (no x-org-id header) | user.routes.test.ts:198 | authorization-precedence | none found (searched: workers/src/index-minimal.ts `authenticateApiRequest`:830, workers/src/__tests__/auth-integration.test.ts) — the prior citation was the deleted `utils/auth.test.ts` / `middleware/auth.test.ts`, which tested modules behind the undeployed index.ts entry point (Finding 22, deleted in task 3.1.0); auth-integration.test.ts asserts `const expected = true` in 14 of its 17 cases, so it cannot fail. The live Clerk auth path has no unit coverage for this property | worker-equivalent-exists | The "reject request without org context" behaviour is covered by utils/index-minimal.ts:370 (token missing organizationId → rejected with 'tenant context' error); the mechanism differs (Express x-org-id header vs Worker JWT claim) but the authorization-precedence behaviour is equivalent | PROPOSED: worker-shaped-rewrite — REOPENED by task 3.1.0. The equivalence rested on the deleted `utils/auth.test.ts` / `middleware/auth.test.ts`, tests for modules with no production importer (Finding 22). The live Clerk auth path is untested for this property, so the behaviour must be re-tested Worker-shaped in task 3.2, not retired. Superseded decision: PROPOSED: worker-equivalent-exists — missing-org-context rejection covered by utils/auth.test.ts:370 (token missing organizationId rejected); mechanism differs (header vs JWT) but behaviour is equivalent |
| POST /users creates user successfully (201, UserService.createUser called with pin/role/orgId) | user.routes.test.ts:206 | — | workers/src/clerk/bootstrap-handler.node.test.ts:175 — links a new user to an existing org via INSERT into users table (201, role team_member); workers/src/clerk/bootstrap-handler.node.test.ts:207 — creates a new org + first admin user via INSERT (201, role admin) | worker-equivalent-exists | The "create a user" behaviour is covered by bootstrap-handler.node.test.ts:175 (new user linked to org) and :207 (first admin created); the mechanism differs (Express POST /users with pin+role vs Worker org-bootstrap with clerk_user_id+email) but the user-creation behaviour has a Worker equivalent | PROPOSED: worker-equivalent-exists — user creation covered by bootstrap-handler.node.test.ts:175 and :207; mechanism differs (standalone POST vs org-bootstrap) but creation behaviour is equivalent |
| POST /users returns 500 when createUser fails | user.routes.test.ts:227 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — minimal-api-routes.test.ts has no POST `/api/users` route; error-handling.test.ts:18-97 tests retry logic, not user-create 500s | retire | The Worker has no standalone user-create endpoint; the Express error middleware forwarding is Express-specific machinery; the negative/error gate is preserved by other Worker error tests (minimal-api-routes.test.ts:509) | PROPOSED: retire — Express user-create 500-on-failure has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:509 |
| PUT /users/:id returns 400 when user id param is invalid | user.routes.test.ts:241 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no PUT `/api/users/:id` route; no Worker test validates a user-id path parameter for updates | retire | The Worker has no user-update endpoint; the Express route param validation is Express-specific route wiring; the negative/error gate is preserved by minimal-api-routes.test.ts:399 | PROPOSED: retire — Express user-update param validation has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:399 |
| PUT /users/:id returns 404 when existing user is not found | user.routes.test.ts:252 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no PUT `/api/users/:id` route; no Worker test covers a user-update 404 | retire | The Worker has no user-update endpoint; the Express 404-not-found route behaviour is Express-specific route wiring with no Worker analogue | PROPOSED: retire — Express user-update 404 has no Worker analogue (no user CRUD endpoint) |
| PUT /users/:id returns 403 when existing user belongs to another organization | user.routes.test.ts:265 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts has no user-update route; multi-tenant-isolation.test.ts:127 asserts orgId filters at SQL level (placeholder), not a user-update cross-org 403 | retire | The Worker has no user-update endpoint; the Express route-level org-membership check is Express-specific machinery; the tenant-isolation gate is preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) | PROPOSED: retire — Express user-update cross-org 403 has no Worker endpoint; tenant-isolation gate preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) |
| PUT /users/:id updates user with partial payload and returns refreshed user | user.routes.test.ts:280 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no PUT `/api/users/:id` route; no Worker test covers user-role updates | retire | The Worker has no user-update endpoint; the Express supertest route dispatch with mocked UserService is Express-specific machinery with no Worker analogue | PROPOSED: retire — Express user-update route has no Worker analogue (no user CRUD endpoint) |
| PUT /users/:id updates user with pin+role payload | user.routes.test.ts:302 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no PUT `/api/users/:id` route; no Worker test covers pin+role user updates | retire | The Worker has no user-update endpoint; the Express supertest route dispatch with mocked UserService is Express-specific machinery with no Worker analogue | PROPOSED: retire — Express user pin+role update route has no Worker analogue (no user CRUD endpoint) |
| PUT /users/:id returns 404 when update returns false | user.routes.test.ts:315 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no PUT `/api/users/:id` route; no Worker test covers a user-update false-return 404 | retire | The Worker has no user-update endpoint; the Express 404-on-false-return route behaviour is Express-specific route wiring with no Worker analogue | PROPOSED: retire — Express user-update false-return 404 has no Worker analogue (no user CRUD endpoint) |
| PUT /users/:id returns 500 when update flow throws | user.routes.test.ts:327 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — minimal-api-routes.test.ts has no user-update route; error-handling.test.ts:18-97 tests retry logic, not user-update 500s | retire | The Worker has no user-update endpoint; the Express error middleware forwarding is Express-specific machinery; the negative/error gate is preserved by minimal-api-routes.test.ts:509 | PROPOSED: retire — Express user-update 500-on-failure has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:509 |
| DELETE /users/:id returns 400 when user id param is invalid | user.routes.test.ts:341 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no DELETE `/api/users/:id` route; no Worker test validates a user-id path parameter for deletion | retire | The Worker has no user-delete endpoint; the Express route param validation is Express-specific route wiring; the negative/error gate is preserved by minimal-api-routes.test.ts:399 | PROPOSED: retire — Express user-delete param validation has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:399 |
| DELETE /users/:id returns 404 when existing user is not found | user.routes.test.ts:349 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no DELETE `/api/users/:id` route; no Worker test covers a user-delete 404 | retire | The Worker has no user-delete endpoint; the Express 404-not-found route behaviour is Express-specific route wiring with no Worker analogue | PROPOSED: retire — Express user-delete 404 has no Worker analogue (no user CRUD endpoint) |
| DELETE /users/:id returns 403 when deleting user from another organization | user.routes.test.ts:359 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts has no user-delete route; multi-tenant-isolation.test.ts:54 tests product cross-org delete prevention (placeholder), not user cross-org delete | retire | The Worker has no user-delete endpoint; the Express route-level org-membership check is Express-specific machinery; the tenant-isolation gate is preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) and product-level cross-org delete prevention (multi-tenant-isolation.test.ts:54) | PROPOSED: retire — Express user-delete cross-org 403 has no Worker endpoint; tenant-isolation gate preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) and product cross-org delete (:54) |
| DELETE /users/:id returns 404 when delete operation reports no row removed | user.routes.test.ts:371 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — minimal-api-routes.test.ts has no DELETE `/api/users/:id` route; no Worker test covers a user-delete false-return 404 | retire | The Worker has no user-delete endpoint; the Express 404-on-false-return route behaviour is Express-specific route wiring with no Worker analogue | PROPOSED: retire — Express user-delete false-return 404 has no Worker analogue (no user CRUD endpoint) |
| DELETE /users/:id deletes user successfully (200, UserService.deleteUser called with id) | user.routes.test.ts:380 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — minimal-api-routes.test.ts has no DELETE `/api/users/:id` route; bootstrap-handler.node.test.ts creates/links users but never deletes them | retire | The Worker has no user-delete endpoint; the Express supertest route dispatch with mocked UserService is Express-specific machinery with no Worker analogue | PROPOSED: retire — Express user-delete route has no Worker analogue (no user CRUD endpoint; bootstrap only creates/links users) |
| DELETE /users/:id returns 500 when delete flow throws | user.routes.test.ts:388 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — minimal-api-routes.test.ts has no user-delete route; error-handling.test.ts:18-97 tests retry logic, not user-delete 500s | retire | The Worker has no user-delete endpoint; the Express error middleware forwarding is Express-specific machinery; the negative/error gate is preserved by minimal-api-routes.test.ts:509 | PROPOSED: retire — Express user-delete 500-on-failure has no Worker endpoint; negative/error gate preserved by minimal-api-routes.test.ts:509 |

---

### user.service.test.ts

Service-layer unit tests for `UserService` with a **mocked Prisma client** and **mocked
`AuthService`** (all Prisma methods and `validatePin`/`hashPin`/`verifyPin` are `vi.fn()` stubs).
Tests cover createUser (PIN validation, duplicate check, Prisma create), getUsers (list by org),
getUserById (findFirst scoped to org), getUserByPin (disabled — always returns undefined since
Clerk auth replaced PIN auth), updateUser (Prisma update with P2025 not-found handling), deleteUser
(Prisma delete with P2025 not-found handling), and a repository-injection test verifying the
tsyringe DI pattern. The Worker has no `UserService` class, no Prisma, no `AuthService`, no PIN
auth, and no DI container — it uses Clerk JWT auth and raw SQL via Neon. The service-layer
orchestration, PIN validation/hashing, Prisma call shapes, P2025 error handling, and tsyringe DI
wiring are all Express-specific machinery with no Worker analogue. User creation behaviour is
partially covered by `bootstrap-handler.node.test.ts` but via a different mechanism (org bootstrap
with clerk_user_id, not standalone create with PIN).

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| createUser creates a user with organizationId and role, validating PIN but not storing it (validatePin called, hashPin not called, prisma.user.create with data {organizationId, role}) | user.service.test.ts:38 | — | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/index-minimal.ts) — bootstrap-handler.node.test.ts:175 and :207 create users via INSERT with clerk_user_id+email+role, not via a service method with PIN validation; the Worker has no PIN validation, no AuthService, and no UserService class | retire | The test asserts service-layer orchestration (validatePin, findMany duplicate check, prisma.user.create) that is Express-specific machinery; the underlying user-creation behaviour is covered by bootstrap-handler.node.test.ts:175/:207 but via a different mechanism without PIN; the PIN validation and service-layer orchestration have no Worker analogue | PROPOSED: retire — service-layer createUser with PIN validation is Express-specific; user creation covered by bootstrap-handler:175/:207 but via different mechanism (no PIN) |
| createUser succeeds when PIN is already in use (PIN duplicate check is disabled — Clerk auth replaces PIN auth) | user.service.test.ts:83 | — | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/index-minimal.ts) — bootstrap-handler.node.test.ts creates users via clerk_user_id with no PIN concept; utils/auth.test.ts covers JWT/subscription auth, not PIN duplicate checks; the Worker has no PIN auth at all | retire | The test verifies that PIN duplicate checking is disabled (Clerk auth replaces PIN auth); the Worker never had PIN auth — it uses Clerk JWT from the start; the PIN-disable behaviour is Express-specific transition machinery with no Worker analogue | PROPOSED: retire — PIN duplicate check disable is Express-specific transition machinery; Worker uses Clerk auth, never had PIN auth |
| getUsers returns all users for the organization (prisma.user.findMany with where organizationId) | user.service.test.ts:108 | tenant-isolation | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts has no user-list route; bootstrap-handler.node.test.ts:116 returns a single user by clerk_user_id, not a list; multi-tenant-isolation.test.ts:16 tests product isolation, not user listing | retire | The Worker has no user-list endpoint or service; the Express service-layer findMany with org scope is Express-specific machinery; the tenant-isolation gate is preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) | PROPOSED: retire — Express user-list service has no Worker analogue; tenant-isolation gate preserved by SQL-level orgId filters (multi-tenant-isolation.test.ts:127) |
| getUserById returns a user when found in organization (prisma.user.findFirst with where id+organizationId) | user.service.test.ts:142 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — minimal-api-routes.test.ts has no user-by-id route; bootstrap-handler.node.test.ts:116 looks up by clerk_user_id, not numeric id | retire | The Worker has no user-by-id service or endpoint; the Express service-layer findFirst with numeric id + org scope is Express-specific machinery with no Worker analogue | PROPOSED: retire — Express user-by-id service has no Worker analogue (Worker uses clerk_user_id lookup, not numeric id) |
| getUserById returns undefined when user is not found in organization | user.service.test.ts:162 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — minimal-api-routes.test.ts has no user-by-id route; bootstrap-handler.node.test.ts:116 returns existing users but does not test a not-found case for numeric id lookup | retire | The Worker has no user-by-id service; the Express service-layer undefined-on-not-found behaviour is Express-specific machinery; the negative/error gate is preserved by other Worker not-found tests (bootstrap returns 200/201 only for existing/new users) | PROPOSED: retire — Express user-by-id not-found has no Worker analogue; negative/error gate preserved by other Worker error tests |
| getUserByPin returns undefined when PIN matches (PIN auth removed — service always returns undefined) | user.service.test.ts:172 | — | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/index-minimal.ts) — bootstrap-handler.node.test.ts has no PIN concept; utils/auth.test.ts covers JWT auth, not PIN lookup; the Worker never had PIN auth | retire | The test verifies that PIN-based user lookup is disabled (always returns undefined); the Worker uses Clerk JWT auth and never had PIN auth; the PIN-lookup-disable behaviour is Express-specific transition machinery with no Worker analogue | PROPOSED: retire — PIN-based user lookup is Express-specific transition machinery; Worker uses Clerk JWT auth, never had PIN auth |
| getUserByPin returns undefined when no PIN matches in organization | user.service.test.ts:194 | — | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/index-minimal.ts) — bootstrap-handler.node.test.ts has no PIN concept; utils/auth.test.ts covers JWT auth, not PIN lookup; the Worker never had PIN auth | retire | The test verifies PIN-based lookup returns undefined regardless of match; the Worker uses Clerk JWT auth and never had PIN auth; the PIN-lookup behaviour is Express-specific machinery with no Worker analogue | PROPOSED: retire — PIN-based user lookup is Express-specific machinery; Worker uses Clerk JWT auth, never had PIN auth |
| updateUser updates a user in organization and returns true (prisma.user.update with where id+organizationId, data role) | user.service.test.ts:212 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — minimal-api-routes.test.ts has no user-update route; bootstrap-handler.node.test.ts creates users but never updates a role | retire | The Worker has no user-update service or endpoint; the Express service-layer Prisma update call shape is Express-specific machinery with no Worker analogue | PROPOSED: retire — Express user-update service has no Worker analogue (no user CRUD endpoint; bootstrap only creates/links users) |
| updateUser returns false when user is not found (Prisma P2025 error caught and returns false) | user.service.test.ts:233 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — minimal-api-routes.test.ts has no user-update route; bootstrap-handler.node.test.ts has no P2025 error handling; the Worker uses raw SQL with no Prisma error codes | retire | The Worker has no user-update service; the Prisma P2025 error-code handling is Prisma-specific machinery with no Worker analogue (Worker uses raw SQL, not Prisma error codes); the negative/error gate is preserved by other Worker error tests (minimal-api-routes.test.ts:509) | PROPOSED: retire — Prisma P2025 not-found handling is Prisma-specific; Worker uses raw SQL with no Prisma error codes; negative/error gate preserved by minimal-api-routes.test.ts:509 |
| deleteUser deletes a user in organization and returns true (prisma.user.delete with where id+organizationId) | user.service.test.ts:244 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — minimal-api-routes.test.ts has no user-delete route; bootstrap-handler.node.test.ts creates/links users but never deletes them | retire | The Worker has no user-delete service or endpoint; the Express service-layer Prisma delete call shape is Express-specific machinery with no Worker analogue | PROPOSED: retire — Express user-delete service has no Worker analogue (no user CRUD endpoint; bootstrap only creates/links users) |
| deleteUser returns false when user is not found (Prisma P2025 error caught and returns false) | user.service.test.ts:264 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — minimal-api-routes.test.ts has no user-delete route; bootstrap-handler.node.test.ts has no P2025 error handling; the Worker uses raw SQL with no Prisma error codes | retire | The Worker has no user-delete service; the Prisma P2025 error-code handling is Prisma-specific machinery with no Worker analogue (Worker uses raw SQL, not Prisma error codes); the negative/error gate is preserved by other Worker error tests (minimal-api-routes.test.ts:509) | PROPOSED: retire — Prisma P2025 not-found handling is Prisma-specific; Worker uses raw SQL with no Prisma error codes; negative/error gate preserved by minimal-api-routes.test.ts:509 |
| UserService uses the injected UserRepository for organization-scoped user reads (tsyringe DI: UserService constructed with repository, repository.findByOrganization called) | user.service.test.ts:280 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/index-minimal.ts) — the Worker has no UserService class, no UserRepository class, and no DI container; it passes orgId directly to SQL queries and database methods (minimal-api-routes.test.ts:250 calls db.createCheckCycle with 'org_123' directly) | retire | The test verifies the tsyringe DI pattern (UserService receives an injected UserRepository); the Worker has no DI container, no service class, and no repository class — it passes orgId directly to database methods; the DI wiring is Express-specific machinery with no Worker analogue | PROPOSED: retire — tsyringe DI repository injection is Express-specific machinery; Worker passes orgId directly to DB methods with no service/repository layer |

---

### validate-tier-flags.test.ts

Unit tests for the `validate-tier-flags` utility — a **Prisma/SQLite bootstrap validator** that checks the `TierFeatureFlag` database table contains all required feature flags with correct limit values, and can seed missing rows. Uses a **real Prisma client** against SQLite (seeds and deletes rows in `beforeEach`/`afterEach`). The Worker has no `TierFeatureFlag` table — tier limits are hardcoded as `TIER_LIMITS` in `workers/src/utils/auth.ts` and tested via `feature-gates.test.ts` — so the DB validation and seeding behaviours have no Worker analogue, while the constant-value assertions (correct limits per tier, required features/tiers lists) map onto the Worker's hardcoded-constant tests.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| validateTierFeatureFlags returns valid=true when all required flags exist in DB | validate-tier-flags.test.ts:35 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — the Worker has no `TierFeatureFlag` DB table; tier limits are hardcoded as `TIER_LIMITS` in `utils/auth.ts` and tested via `checkFeatureAccess`/`checkUsageLimit` (feature-gates.test.ts:24-28), not via a DB-validation utility | retire | DB-backed flag validation is Prisma/SQLite bootstrap machinery; the Worker's hardcoded `TIER_LIMITS` constant needs no runtime validation | PROPOSED: retire — DB-backed tier-flag validation has no Worker analogue (Worker uses hardcoded TIER_LIMITS) |
| validateTierFeatureFlags returns valid=false when flags are missing from DB | validate-tier-flags.test.ts:60 | negative/error | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test validates DB flag completeness; feature-gates.test.ts:59-63 tests a feature-not-available error path but that is a runtime access check, not a DB-completeness validation | retire | Missing-flag detection is a DB-integrity check for the `TierFeatureFlag` table; the Worker has no such table and no equivalent validation utility | PROPOSED: retire — missing-flag DB validation is Prisma-specific with no Worker analogue |
| validateTierFeatureFlags detects a specific missing flag (max_inventory_items) | validate-tier-flags.test.ts:79 | negative/error | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test references `max_inventory_items`; feature-gates.test.ts tests `max_skus` and `max_users` limits but not `max_inventory_items` | retire | Per-flag missing detection is DB-validation machinery for a table the Worker does not have | PROPOSED: retire — per-flag DB completeness check is Prisma-specific; Worker has no TierFeatureFlag table |
| validateTierFeatureFlags returns warnings for incorrect limit values (valid=true, warnings non-empty) | validate-tier-flags.test.ts:105 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — the Worker's limits are hardcoded in `TIER_LIMITS` so cannot drift from expected values; feature-gates.test.ts:53-56 asserts `TIER_LIMITS.starter.max_skus` equals 5000 but this is a constant-value check, not a drift-warning mechanism | retire | Limit-drift warnings require a DB table whose rows can diverge from expected values; the Worker's hardcoded constants make this impossible by construction | PROPOSED: retire — limit-drift warning mechanism is DB-validation machinery with no hardcoded-constant analogue |
| validateTierFeatureFlags reports correct flag counts per tier | validate-tier-flags.test.ts:136 | — | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — no Worker test counts feature flags per tier; the Worker has no per-tier flag table to count | retire | Flag-count-per-tier is a DB-completeness metric for the `TierFeatureFlag` table; the Worker has no equivalent table or count operation | PROPOSED: retire — per-tier flag count is DB-validation machinery with no Worker analogue |
| quickValidateTierFeatureFlags returns true when all required flags exist (count check) | validate-tier-flags.test.ts:164 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test performs a quick-count validation of tier flags; the Worker uses hardcoded `TIER_LIMITS` with no DB count check | retire | Quick-count validation is a fast DB-integrity check for the `TierFeatureFlag` table; the Worker has no such table | PROPOSED: retire — quick-count DB validation is Prisma-specific with no Worker analogue |
| quickValidateTierFeatureFlags returns false when flags are missing | validate-tier-flags.test.ts:187 | negative/error | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test checks for missing tier flags via a count query; the Worker's hardcoded constants cannot be "missing" | retire | Missing-flag detection via count is DB-validation machinery; the Worker has no DB-backed flag table | PROPOSED: retire — missing-flag count check is Prisma-specific with no Worker analogue |
| quickValidateTierFeatureFlags handles database errors gracefully (returns false) | validate-tier-flags.test.ts:194 | negative/error | workers/src/utils/feature-gates.test.ts:129-137 — `checkUsageLimit` catches a mocked DB error and returns `isWithinLimit: false` with an error message, exercising the same graceful-degradation pattern | retire | The graceful-DB-error pattern is preserved in the Worker's `checkUsageLimit` (feature-gates.test.ts:129-137), but `quickValidateTierFeatureFlags` itself is a Prisma-specific validation utility with no Worker counterpart; the negative/error gate is preserved by the cited Worker test | PROPOSED: retire — validation utility is Prisma-specific; graceful-DB-error pattern preserved by checkUsageLimit (feature-gates.test.ts:129) |
| seedMissingTierFeatureFlags seeds missing flags into the DB | validate-tier-flags.test.ts:205 | — | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — the Worker has no tier-flag seeding operation; limits are hardcoded in `TIER_LIMITS` (auth.ts) and never seeded at runtime | retire | DB seeding is Prisma/SQLite bootstrap machinery; the Worker's hardcoded constants require no seeding | PROPOSED: retire — tier-flag seeding is Prisma-specific bootstrap machinery with no Worker analogue |
| seedMissingTierFeatureFlags does not re-seed existing flags (idempotent on existing data) | validate-tier-flags.test.ts:226 | — | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — no Worker test checks idempotent seeding; the Worker has no seeding operation to be idempotent about | retire | Idempotent-seed check is DB-bootstrap machinery; the Worker has no seeding operation | PROPOSED: retire — idempotent-seed check is Prisma-specific with no Worker analogue |
| seedMissingTierFeatureFlags correctly seeds max_inventory_items for all tiers with correct limit values | validate-tier-flags.test.ts:250 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test seeds or checks `max_inventory_items`; feature-gates.test.ts tests `max_skus` and `max_users` but not `max_inventory_items` | retire | Per-flag seeding with correct values is DB-bootstrap machinery; the Worker has no `max_inventory_items` in its tested feature set | PROPOSED: retire — max_inventory_items seeding is Prisma-specific with no Worker analogue |
| seedMissingTierFeatureFlags is idempotent and race-safe under concurrent calls (Promise.all) | validate-tier-flags.test.ts:272 | concurrency | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/__tests__/catalogue-import-loadtest.node.test.ts) — no Worker test exercises concurrent seeding; multi-tenant-isolation.test.ts covers tenant isolation under concurrent org access, not seed idempotency; the Worker has no seeding operation to race on | retire | Concurrent-seed idempotency is Prisma/SQLite-specific (relies on upsert/constraint behaviour); the Worker has no seeding operation; the concurrency gate is preserved by other Worker tests (multi-tenant-isolation, catalogue-import-loadtest) | PROPOSED: retire — concurrent-seed idempotency is Prisma-specific; concurrency gate preserved by multi-tenant-isolation.test.ts |
| EXPECTED_LIMITS has correct max_inventory_items per tier (free=500, starter=5000, professional=50000, enterprise=250000, premium/concierge normalized) | validate-tier-flags.test.ts:292 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test asserts `max_inventory_items` values; feature-gates.test.ts:53-56 asserts `TIER_LIMITS.starter.max_skus` = 5000 and :118-127 asserts concierge `max_skus` = 250000, but `max_inventory_items` is not tested in any Worker suite | worker-shaped-rewrite | The inventory-item limit is a feature-limits gate value that the Worker enforces but does not test; the constant-value assertion must be re-tested against the Worker's `TIER_LIMITS` to confirm `max_inventory_items` values match | PROPOSED: worker-shaped-rewrite — max_inventory_items limit values untested in Worker; re-test against TIER_LIMITS |
| EXPECTED_LIMITS has correct max_skus per tier (free=500, starter=5000, professional=50000, enterprise=250000, premium/concierge normalized) | validate-tier-flags.test.ts:302 | feature-limits | workers/src/utils/feature-gates.test.ts:55-56 — asserts `TIER_LIMITS.starter.max_skus` = 5000; workers/src/utils/feature-gates.test.ts:125 — asserts concierge `max_skus` = 250000; workers/src/__tests__/usage-limits-integration.test.ts:26-27 — asserts `tierLimits.starter.max_skus` = 500 (fixture value, diverges from production) | worker-equivalent-exists | The Worker tests starter and concierge SKU limits (feature-gates.test.ts:55-56, :125) matching Express EXPECTED_LIMITS; professional/enterprise/premium tiers are not individually asserted, and usage-limits-integration.test.ts uses divergent fixture values, but the core behaviour (SKU limit values are correct) is covered | PROPOSED: worker-equivalent-exists — starter and concierge SKU limits asserted in feature-gates.test.ts:55-56,125 matching EXPECTED_LIMITS |
| EXPECTED_LIMITS has correct max_users per tier (free=1, starter=3, professional=10, enterprise=10, premium/concierge=10) | validate-tier-flags.test.ts:312 | feature-limits | workers/src/utils/feature-gates.test.ts:247 — asserts professional `max_users` = 10; workers/src/__tests__/usage-limits-integration.test.ts:68-69 — asserts `tierLimits.starter.max_users` = 1; workers/src/__tests__/usage-limits-integration.test.ts:82-83 — asserts `tierLimits.professional.max_users` = 10 | worker-equivalent-exists | The Worker tests starter (1) and professional (10) user limits matching Express EXPECTED_LIMITS; the core behaviour (user limit values are correct) is covered, though not all tiers are individually asserted | PROPOSED: worker-equivalent-exists — starter and professional user limits asserted in feature-gates.test.ts:247 and usage-limits-integration.test.ts:68-69,82-83 |
| REQUIRED_FEATURES includes all critical features (max_skus, max_users, max_inventory_items, advanced_analytics) | validate-tier-flags.test.ts:323 | feature-limits | workers/src/utils/feature-gates.test.ts:24-28 — asserts `MAX_SKUS` is enabled for starter; workers/src/utils/feature-gates.test.ts:30-40 — asserts `ADVANCED_ANALYTICS` is enabled for professional+ and disabled for starter | worker-equivalent-exists | The Worker's `AVAILABLE_FEATURES` constant is tested to include `MAX_SKUS` and `ADVANCED_ANALYTICS` (feature-gates.test.ts:24-28, :30-40); `max_users` and `max_inventory_items` are not asserted as members of the features list, but the core behaviour (critical features are present) is partially covered | PROPOSED: worker-equivalent-exists — MAX_SKUS and ADVANCED_ANALYTICS presence asserted in feature-gates.test.ts:24-28,30-40 |
| REQUIRED_TIERS includes all tier levels (starter, professional, premium, concierge) | validate-tier-flags.test.ts:332 | — | workers/src/utils/feature-gates.test.ts:193-201 — iterates `['starter', 'professional', 'premium', 'concierge']` and asserts `analyticsFence` results for all four tiers | worker-equivalent-exists | The Worker tests all four tier levels (starter, professional, premium, concierge) in feature-gates.test.ts:193-201, matching the Express REQUIRED_TIERS set | PROPOSED: worker-equivalent-exists — all four tier levels asserted in feature-gates.test.ts:193-201 |

---

### validateRequest.test.ts

Unit tests for the Express `validateRequest` Zod validation middleware factory — `validateRequest`, `validateBody`, `validateParams`, `validateQuery`. Uses **mocked Express `Request`/`Response`/`NextFunction`** objects (no supertest, no real HTTP) to verify that valid requests call `next()` with no error, invalid requests call `next(new ValidationError(...))` with structured field-level errors, and edge cases (nested objects, arrays, empty body, non-Zod errors) are handled. The Worker has no equivalent Zod validation middleware — it validates inline at the route handler level (e.g., `minimal-api-routes.test.ts:399-422` rejects non-numeric percentages with 400) — so the middleware mechanism retires but the validation *behaviours* (reject invalid input, report field errors, standardized error format) map to Worker-shaped rewrites where no handler-level equivalent exists.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| validateRequest passes a valid combined request (body+params+query) by calling next() with no error | validateRequest.test.ts:50 | — | workers/src/minimal-api-routes.test.ts:296-348 — `persists a valid markdown matrix` sends a valid PUT body and asserts `response?.status).toBe(200)`, exercising the valid-request-accepted path at the handler level | worker-equivalent-exists | The Worker tests valid request acceptance at the route handler level (minimal-api-routes.test.ts:296-348); the Express middleware next()-call mechanism is not replicated, but the valid-request-passes behaviour is covered | PROPOSED: worker-equivalent-exists — valid request acceptance covered at handler level in minimal-api-routes.test.ts:296-348 |
| validateRequest rejects an invalid body with a ValidationError containing field/message error entries | validateRequest.test.ts:64 | negative/error | workers/src/minimal-api-routes.test.ts:399-422 — `rejects non-numeric markdown percentages` asserts `response?.status).toBe(400)` for invalid body values, but does not assert field-level error structure (no `field`/`message` properties in the response body) | worker-shaped-rewrite | The Worker rejects invalid body input at the handler level (minimal-api-routes.test.ts:399-422 → 400) but does not test structured field-level error reporting; the rejection behaviour is partially covered, the error-format behaviour is not | PROPOSED: worker-shaped-rewrite — invalid body rejection covered (400) but field-level error format untested; re-test against Worker response model |
| validateRequest rejects invalid params with a ValidationError identifying the 'id' field | validateRequest.test.ts:82 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/minimal-rate-limit.test.ts, workers/src/__tests__/error-handling.test.ts) — minimal-api-routes.test.ts:86-107 tests valid dynamic pathname dispatch via regex but no test asserts a 400/422 for non-numeric path params; error-handling.test.ts has no param-validation coverage | worker-shaped-rewrite | Invalid-param rejection is a negative/error gate behaviour with no Worker test coverage; the Worker uses regex route matching that silently fails to match invalid paths, but no test asserts the rejection response | PROPOSED: worker-shaped-rewrite — invalid param rejection untested in Worker; re-test against Worker route model |
| validateRequest reports multiple field errors in a single ValidationError (email, age, id) | validateRequest.test.ts:98 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts multiple field errors in a single validation response; minimal-api-routes.test.ts:399-422 tests a single-field rejection only | worker-shaped-rewrite | Multi-field error aggregation is a negative/error gate behaviour with no Worker equivalent; the Worker's inline validation does not collect multiple errors per request | PROPOSED: worker-shaped-rewrite — multi-field error aggregation untested in Worker; re-test against Worker response model |
| validateBody passes a valid body by calling next() with no error | validateRequest.test.ts:120 | — | workers/src/minimal-api-routes.test.ts:296-348 — `persists a valid markdown matrix` sends a valid PUT body and asserts `response?.status).toBe(200)`, exercising the valid-body-accepted path | worker-equivalent-exists | The Worker tests valid body acceptance at the handler level (minimal-api-routes.test.ts:296-348); the behaviour is covered though the mechanism differs (handler-level vs middleware) | PROPOSED: worker-equivalent-exists — valid body acceptance covered at handler level in minimal-api-routes.test.ts:296-348 |
| validateBody rejects a short username with a ValidationError identifying the 'username' field | validateRequest.test.ts:132 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts string-length validation on a username or named field; minimal-api-routes.test.ts:399-422 tests numeric validation on percentages only | worker-shaped-rewrite | String-length validation with field-level error reporting is a negative/error gate behaviour with no Worker test coverage | PROPOSED: worker-shaped-rewrite — string-length field validation untested in Worker; re-test against Worker response model |
| validateBody rejects a short password with a ValidationError identifying the 'password' field | validateRequest.test.ts:145 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts password length validation; error-handling.test.ts:175-185 tests missing Authorization header (a stub) but not password field validation | worker-shaped-rewrite | Password-length validation with field-level error reporting is a negative/error gate behaviour with no Worker test coverage | PROPOSED: worker-shaped-rewrite — password length validation untested in Worker; re-test against Worker response model |
| validateBody rejects missing required fields with a ValidationError containing the correct error count | validateRequest.test.ts:158 | negative/error | workers/src/__tests__/error-handling.test.ts:131-149 — `should handle missing required fields in product creation` calls `createProduct` without the required `name` field and asserts an error is thrown | worker-equivalent-exists | The Worker tests missing-required-field rejection at the handler level (error-handling.test.ts:131-149); the behaviour is covered though the error structure (thrown Error vs ValidationError with field array) differs | PROPOSED: worker-equivalent-exists — missing required field rejection covered in error-handling.test.ts:131-149 |
| validateParams passes valid params and transforms id from string to number via Zod .transform(Number) | validateRequest.test.ts:177 | — | workers/src/minimal-api-routes.test.ts:86-107 — `passes dynamic pathnames to matching handlers` dispatches a PUT to `/api/inventory-items/42` and asserts the handler receives the path string; no param transformation (string→number) is asserted | worker-equivalent-exists | The Worker tests valid param dispatch at the route level (minimal-api-routes.test.ts:86-107); the valid-params-pass behaviour is covered, but the Zod `.transform(Number)` param transformation is Express-middleware-specific and has no Worker equivalent | PROPOSED: worker-equivalent-exists — valid param dispatch covered in minimal-api-routes.test.ts:86-107; param transformation is Express-specific |
| validateParams rejects a non-numeric id param with a ValidationError | validateRequest.test.ts:189 | negative/error | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/minimal-rate-limit.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test asserts a 400/422 for non-numeric path params; the Worker uses regex route matching (`/^\/api\/inventory-items\/\d+$/`) that silently fails to match, but no test asserts the rejection response | worker-shaped-rewrite | Invalid-param rejection is a negative/error gate behaviour with no Worker test coverage; the Worker's regex-matching approach needs an explicit rejection test | PROPOSED: worker-shaped-rewrite — non-numeric param rejection untested in Worker; re-test against Worker route model |
| validateQuery passes valid query params (page, limit) by calling next() with no error | validateRequest.test.ts:207 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/minimal-rate-limit.test.ts, workers/src/__tests__/error-handling.test.ts) — middleware.test.ts:248-261 tests the query limiter capping a `limit` query param to a configured max, but no test asserts that valid query params pass through a validation layer | worker-shaped-rewrite | Valid-query-passes-validation is a behaviour with no direct Worker equivalent; the Worker has a query limiter middleware (middleware.test.ts:248-261) but no query validation middleware | PROPOSED: worker-shaped-rewrite — query validation pass-through untested in Worker; re-test against Worker middleware model |
| validateQuery applies defaults for missing optional query params (page='1', limit='20') | validateRequest.test.ts:220 | — | none found (searched: workers/src/utils/minimal-rate-limit.test.ts, workers/src/minimal-api-routes.test.ts) — middleware.test.ts:263-272 tests the query limiter not injecting a limit when missing, but no Worker test asserts default values applied for missing query params | worker-shaped-rewrite | Default-value application for missing query params is a behaviour with no Worker equivalent; the Worker's query limiter caps but does not default | PROPOSED: worker-shaped-rewrite — query param defaults untested in Worker; re-test against Worker middleware model |
| validateQuery rejects invalid query params (non-numeric page) with a ValidationError | validateRequest.test.ts:233 | negative/error | none found (searched: workers/src/utils/minimal-rate-limit.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test asserts a 400 for invalid query param format; middleware.test.ts:248-261 tests query limiter capping but not format rejection | worker-shaped-rewrite | Invalid-query-param rejection is a negative/error gate behaviour with no Worker test coverage | PROPOSED: worker-shaped-rewrite — invalid query param rejection untested in Worker; re-test against Worker middleware model |
| validateRequest returns errors in a standardized format ({ errors: [{ field, message }] }) | validateRequest.test.ts:253 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — error-handling.test.ts:316-331 `should return properly formatted error response` is a stub (`expect(true).toBe(true)`) with no actual format assertion; no Worker test asserts a `{ errors: [{ field, message }] }` structure | worker-shaped-rewrite | Standardized error format is a negative/error gate behaviour with no real Worker test coverage; error-handling.test.ts:316-331 documents the intent but does not assert it | PROPOSED: worker-shaped-rewrite — standardized error format untested in Worker (error-handling.test.ts:316 is a stub); re-test against Worker response model |
| validateRequest includes field names (email, pin) in error messages | validateRequest.test.ts:277 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test asserts that field names appear in validation error responses; minimal-api-routes.test.ts:399-422 checks only the 400 status, not the error body structure | worker-shaped-rewrite | Field-name inclusion in errors is a negative/error gate behaviour with no Worker test coverage | PROPOSED: worker-shaped-rewrite — field names in error responses untested in Worker; re-test against Worker response model |
| validateRequest handles nested object validation (user.address.street) without error | validateRequest.test.ts:296 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts nested object body validation; minimal-api-routes.test.ts:350-397 tests a `matrices` object body but does not assert deep nested validation | worker-shaped-rewrite | Nested object validation is a behaviour with no direct Worker test coverage; the Worker accepts JSON bodies with nested objects at the handler level but does not test schema validation of nested structures | PROPOSED: worker-shaped-rewrite — nested object validation untested in Worker; re-test against Worker response model |
| validateRequest handles array validation (items: [{ id, name }]) without error | validateRequest.test.ts:329 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/handlers/handlers.test.ts) — catalogue-import-upsert.node.test.ts tests CSV array parsing, not JSON request body array validation; no Worker test asserts array-of-objects body validation | worker-shaped-rewrite | Array body validation is a behaviour with no direct Worker test coverage; the Worker processes array data in CSV imports but does not test JSON array request body validation | PROPOSED: worker-shaped-rewrite — array body validation untested in Worker; re-test against Worker response model |
| validateRequest handles an empty body gracefully when all fields are optional | validateRequest.test.ts:358 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test asserts that an empty request body with optional-only fields is accepted; error-handling.test.ts:117-129 mentions malformed JSON but is a stub | worker-shaped-rewrite | Empty-body-with-optional-fields handling is a behaviour with no Worker test coverage | PROPOSED: worker-shaped-rewrite — empty body handling untested in Worker; re-test against Worker response model |
| validateRequest passes through non-Zod errors as-is (not wrapped in ValidationError) | validateRequest.test.ts:377 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/utils/minimal-rate-limit.test.ts) — error-handling.test.ts:18-40 tests `withNeonRetry` throwing DB errors directly, but that is retry logic, not validation middleware error classification; no Worker validation middleware exists to test error pass-through | retire | Non-Zod-error pass-through is Express validation middleware error-classification logic (distinguishing ZodError from generic Error before calling next()); the Worker has no validation middleware and no error-wrapping layer, so this behaviour has no Worker analogue | PROPOSED: retire — validation middleware error classification is Express-specific machinery with no Worker analogue |

### validation.middleware.test.ts

Unit tests for Express body-validation middleware (`validateProductInput`,
`validateInventoryItemInput`, `validateUserInput`, `validateStoreAreaInput`) using **mocked
`Request`/`Response`/`NextFunction`** — no real database, no supertest. Each validator either calls
`next()` on valid input or returns a 400 JSON error with a field-specific message. The Worker has
no Express-style validation middleware; input validation is done inline in route handlers
(`index-minimal.ts`) and in CSV parsers (`catalogue-parser`, `expiry-parser`), tested via
`minimal-api-routes.test.ts` and the upload parser suites.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Calls next() for valid product data (barcode, sku, name, cost_price) | validation.middleware.test.ts:23 | — | workers/src/upload/catalogue-parser.test.ts:5-21 — accepts a product catalogue row with sku/name/barcode/costPrice and asserts the parsed shape | worker-equivalent-exists | The Worker validates product fields during CSV catalogue import, not in a request-body middleware; the field set and acceptance behaviour overlap (barcode, sku, name, cost) | PROPOSED: worker-equivalent-exists — product field validation covered by catalogue-parser acceptance test |
| Returns 400 for invalid barcode format (too short) | validation.middleware.test.ts:39 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/minimal-api-routes.test.ts) — catalogue-parser.test.ts:23-32 rejects a row with malformed cost ('not-money'), not a short barcode; no Worker test asserts barcode-length rejection | worker-shaped-rewrite | Barcode format validation is relevant to the Worker's catalogue import but has no test asserting the rejection; the Express middleware shape retires, but the barcode-format gate must be re-tested against the Worker's parser | PROPOSED: worker-shaped-rewrite — barcode-format rejection untested in Worker; re-test against catalogue parser |
| Returns 400 for invalid SKU format (too long) | validation.middleware.test.ts:56 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-parser.test.ts:5-21 accepts a valid SKU but no test asserts a max-length rejection; expiry-parser.test.ts:65-70 rejects missing SKU value, not over-length SKU | worker-shaped-rewrite | SKU length validation is relevant to the Worker's import pipeline but has no test asserting the upper-bound rejection | PROPOSED: worker-shaped-rewrite — SKU length-limit rejection untested in Worker; re-test against catalogue parser |
| Returns 400 for product name containing HTML tags (XSS rejection) | validation.middleware.test.ts:73 | penetration | workers/src/minimal-api-routes.test.ts:811 — rejects `{ name: '<b>Policy Supplier</b>' }` with 400 for a supplier create; the HTML-in-name rejection behaviour is asserted | worker-equivalent-exists | The Worker tests HTML-tag rejection in a supplier name, not a product name, but the XSS-rejection behaviour (reject HTML tags in a name field) is the same gate | PROPOSED: worker-equivalent-exists — HTML-in-name rejection covered by supplier create validation test |
| Returns 400 for invalid (negative) cost price | validation.middleware.test.ts:90 | negative/error | workers/src/upload/catalogue-parser.test.ts:23-32 — rejects a product catalogue row where cost is 'not-money' (parseProductCatalogRow returns null) | worker-equivalent-exists | The Worker rejects unparseable cost values during CSV import; the Express test rejects negative cost — both assert cost-price validation, though the specific negative-value case is not tested in the Worker | PROPOSED: worker-equivalent-exists — cost-price validation covered by catalogue-parser malformed-cost rejection |
| Calls next() for valid inventory item data (product_id, expiry_date, location_id) | validation.middleware.test.ts:109 | — | workers/src/upload/expiry-parser.test.ts:10-27 — accepts a canonical expiry record with SKU, item description, used-by date, and department | worker-equivalent-exists | The Worker validates expiry import records (SKU + date), not a JSON body with product_id/location_id, but the valid-expiry-item acceptance behaviour overlaps | PROPOSED: worker-equivalent-exists — valid expiry item acceptance covered by expiry-parser canonical test |
| Returns 400 for invalid expiry date format (dd/mm/yyyy instead of YYYY-MM-DD) | validation.middleware.test.ts:128 | negative/error | workers/src/upload/expiry-date-parser.test.ts:26-29 — rejects 'Dec/2026' and '2026-12-31' as non-numeric/wrong-format dates with ok=false | worker-equivalent-exists | The Worker rejects wrong-format expiry dates in its parser; the Express test rejects dd/mm/yyyy in a JSON body, the Worker rejects non-dd/mm/yyyy CSV tokens — both assert date-format validation | PROPOSED: worker-equivalent-exists — expiry date format rejection covered by expiry-date-parser |
| Returns 400 for invalid product_id (zero, must be positive integer) | validation.middleware.test.ts:146 | negative/error | none found (searched: workers/src/upload/expiry-parser.test.ts, workers/src/upload/expiry-date-parser.test.ts, workers/src/minimal-api-routes.test.ts) — expiry-parser.test.ts:65-70 rejects a missing SKU value, not a zero product_id; no Worker test asserts positive-integer product_id validation | worker-shaped-rewrite | Product ID positive-integer validation is relevant to the Worker's inventory routes but has no test coverage; the Express middleware shape retires, but the gate must be re-tested against the Worker's route handlers | PROPOSED: worker-shaped-rewrite — product_id positive-integer validation untested in Worker; re-test against route handler |
| Calls next() for valid user data (pin, role) | validation.middleware.test.ts:168 | — | none found (searched: workers/src/index-minimal.ts, workers/src/constants/roles.test.ts) — the Worker uses Clerk authentication with org roles (admin/manager/team_member), not a PIN+role body field; no Worker test validates a PIN/role request body | retire | PIN-based user creation is Express-specific auth machinery replaced by Clerk; the Worker has no PIN field and no body-level role validation | PROPOSED: retire — PIN/role body validation is Express auth machinery with no Worker analogue (Clerk replaces PIN auth) |
| Returns 400 for invalid PIN (too short, <4 digits) | validation.middleware.test.ts:182 | negative/error | none found (searched: workers/src/index-minimal.ts) — the Worker has no PIN field; auth.test.ts tests Clerk token verification, not PIN validation | retire | PIN validation is Express-specific auth machinery; the Worker uses Clerk and has no PIN concept | PROPOSED: retire — PIN length validation is Express auth machinery with no Worker analogue |
| Returns 400 for invalid PIN (too long, >6 digits) | validation.middleware.test.ts:197 | negative/error | none found (searched: workers/src/index-minimal.ts) — the Worker has no PIN field; no PIN length test exists | retire | PIN validation is Express-specific auth machinery; the Worker uses Clerk and has no PIN concept | PROPOSED: retire — PIN length validation is Express auth machinery with no Worker analogue |
| Returns 400 for invalid PIN (contains non-digits) | validation.middleware.test.ts:212 | negative/error | none found (searched: workers/src/index-minimal.ts) — the Worker has no PIN field; no PIN character-class test exists | retire | PIN validation is Express-specific auth machinery; the Worker uses Clerk and has no PIN concept | PROPOSED: retire — PIN character validation is Express auth machinery with no Worker analogue |
| Returns 400 for invalid role (not "Manager" or "Team Member") | validation.middleware.test.ts:227 | authorization-precedence | none found (searched: workers/src/constants/roles.test.ts, workers/src/index-minimal.ts) — require-role.test.ts:46-151 tests admin/manager/team_member role enforcement via middleware on authenticated requests, not body-field role validation; the Worker's role model is Clerk org roles, not a free-text body field | retire | Body-field role validation is Express-specific; the Worker enforces roles via Clerk org roles in middleware (require-role.test.ts), not by validating a role string in the request body | PROPOSED: retire — body-field role validation is Express auth machinery; Worker uses Clerk org roles via middleware instead |
| Calls next() for valid store area data (name only) | validation.middleware.test.ts:246 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — minimal-api-routes.test.ts:199-262 tests store-walk check-cycle CRUD but does not validate a store-area name field; no Worker test asserts store-area name acceptance | worker-shaped-rewrite | Store area creation is a Worker route (`/api/store-areas`) but name validation is not tested; the Express middleware shape retires, but the valid-name gate must be re-tested against the Worker's route handler | PROPOSED: worker-shaped-rewrite — store-area name validation untested in Worker; re-test against route handler |
| Returns 400 for store area name containing HTML tags | validation.middleware.test.ts:259 | penetration | workers/src/minimal-api-routes.test.ts:811 — rejects `{ name: '<b>Policy Supplier</b>' }` with 400 for a supplier create, asserting HTML-in-name rejection | worker-equivalent-exists | The Worker tests HTML-tag rejection in a supplier name, not a store-area name, but the XSS-rejection behaviour (reject HTML tags in a name field) is the same gate | PROPOSED: worker-equivalent-exists — HTML-in-name rejection covered by supplier create validation test |

---

### webhook-event-dispatcher.test.ts

Unit tests for `dispatchStripeWebhookEvent` — a pure dispatcher that routes a `Stripe.Event` to
the correct handler callback based on `event.type`. Uses **mocked handler functions** (`vi.fn()`)
with no real Stripe, no HTTP, no database. The Worker has Clerk webhook signature verification
(`clerk/webhook-signature.test.ts`) but no Stripe webhook event-dispatch test; the Worker's
subscription tests cover status-based access control, not event routing.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Routes `customer.subscription.created` events to the subscription-created handler (and does not call unhandled) | webhook-event-dispatcher.test.ts:5 | webhook-security | none found (searched: workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/__tests__/error-handling.test.ts) — webhook-signature.test.ts:41-61 verifies Clerk Svix signature acceptance, not Stripe event routing; subscription-validation.test.ts:17-33 tests active-subscription access control, not webhook event dispatch; handlers.test.ts covers multi-tenant isolation, not webhooks; error-handling.test.ts covers DB retry, not webhook routing | worker-shaped-rewrite | Stripe webhook event routing is relevant to the Worker (subscription lifecycle management) but has no test coverage; the Express dispatcher shape retires, but the event-routing gate must be re-tested against the Worker's webhook handler model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — Stripe event-to-handler routing untested in Worker; re-test against Worker webhook handler |
| Routes unknown event types to the unhandled callback | webhook-event-dispatcher.test.ts:31 | webhook-security | none found (searched: workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test asserts unknown-event fallback handling for Stripe webhooks; webhook-signature.test.ts covers signature verification only, not event-type dispatch | worker-shaped-rewrite | Unknown-event fallback is a webhook-security gate (silent vs. explicit handling of unrecognised events); the Worker has no test for this behaviour; the Express dispatcher shape retires, but the gate must be re-tested against the Worker's webhook handler model | PROPOSED: worker-shaped-rewrite — unknown-event fallback untested in Worker; re-test against Worker webhook handler |

---

### webhook-monitoring.test.ts

Unit tests for `HourlyWebhookCheckJob` — a scheduled monitoring job that queries
`SaasMetricsService` (mocked) for webhook health metrics and raises Sentry alerts (mocked
`@sentry/node`) when thresholds are breached. No real database, no real Sentry — this is a pure
threshold/alert logic test. The Worker has no scheduled monitoring job, no Sentry alerting test,
and no webhook-metrics service; Cloudflare Queue ack/retry handles webhook reliability instead.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Captures a Sentry alert when daily webhook error count exceeds 1 | webhook-monitoring.test.ts:33 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/clerk/webhook-signature.test.ts) — health.test.ts:140-212 tests health-check connectivity (R2/database), not webhook error-rate alerting; workers-deployment.test.ts:24-32 is a deployment smoke test; error-handling.test.ts:18-40 tests DB retry logic, not webhook monitoring; webhook-signature.test.ts covers signature verification, not error-count alerting | worker-shaped-rewrite | Webhook error-rate alerting is a webhook-security gate (detecting handler failures); the Worker has no scheduled monitoring job or Sentry alerting test; the Express scheduled-job + SaasMetricsService + Sentry shape retires, but the alerting gate must be re-tested against the Worker's monitoring model in Phase 3.2 | PROPOSED: worker-shaped-rewrite — webhook error-count alerting untested in Worker; re-test against Worker monitoring model |
| Does NOT alert when daily webhook error count is 0 | webhook-monitoring.test.ts:47 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test asserts the negative case (no alert when webhook errors are within bounds); health.test.ts:140-146 asserts healthy status when checks pass, but for infrastructure health, not webhook error rates | worker-shaped-rewrite | The no-alert-when-healthy behaviour is the negative half of the webhook-security monitoring gate; the Worker has no test for this; the Express job shape retires, but the gate must be preserved in the Worker rewrite | PROPOSED: worker-shaped-rewrite — no-alert-when-healthy untested in Worker; re-test against Worker monitoring model |
| Captures a Sentry alert when replay-attack growth rate exceeds threshold (15x baseline) | webhook-monitoring.test.ts:61 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/error-handling.test.ts) — webhook-signature.test.ts:63-83 rejects stale timestamps (replay prevention at the signature layer), but no Worker test monitors event-volume growth rate as a replay-attack signal; health.test.ts and workers-deployment.test.ts have no replay-attack detection coverage | worker-shaped-rewrite | Replay-attack detection via growth-rate monitoring is a webhook-security gate; the Worker prevents replay at the signature layer (timestamp window) but does not test volume-based anomaly detection; the Express scheduled-job shape retires, but the gate must be re-tested against the Worker's monitoring model | PROPOSED: worker-shaped-rewrite — replay-attack growth-rate alerting untested in Worker; re-test against Worker monitoring model |
| Does NOT alert when growth rate is normal (<=5x baseline) | webhook-monitoring.test.ts:75 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/workers-deployment.test.ts, workers/src/clerk/webhook-signature.test.ts) — no Worker test asserts the negative case (no alert when webhook event growth is within normal bounds); webhook-signature.test.ts:41-61 accepts valid signatures but does not test event-volume thresholds | worker-shaped-rewrite | The no-alert-when-normal-growth behaviour is the negative half of the replay-attack monitoring gate; the Worker has no test for this; the Express job shape retires, but the gate must be preserved in the Worker rewrite | PROPOSED: worker-shaped-rewrite — no-alert-when-normal-growth untested in Worker; re-test against Worker monitoring model |

---

### webhook.routes.test.ts

Integration tests for the Express webhook router (`/webhooks/stripe` and `/webhooks/clerk`) using
**supertest** against a real Express app with **mocked services** (`webhookService`,
`clerkWebhookService`, `ApplicationMonitoringService`, `@sentry/node`). Tests cover signature
header validation, signature verification, duplicate-event idempotency, successful processing, and
error classification (200 for non-recoverable vs. 500 for transient). The Worker has a Clerk
webhook route (`/api/webhooks/clerk`) tested in `health.test.ts` and signature verification in
`clerk/webhook-signature.test.ts`, but no Stripe webhook route test and no webhook error-classification test.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Returns 400 when stripe-signature header is missing | webhook.routes.test.ts:65 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/error-handling.test.ts) — health.test.ts:365-379 tests the Clerk webhook route rejecting requests without Svix headers (400), but no Worker test covers the Stripe webhook route rejecting a missing stripe-signature header; no Stripe webhook route test exists | worker-shaped-rewrite | Missing-signature-header rejection is a webhook-security gate; the Worker tests this for Clerk (Svix) but not for Stripe; the Express route shape retires, but the gate must be re-tested against the Worker's Stripe webhook handler | PROPOSED: worker-shaped-rewrite — Stripe missing-signature rejection untested in Worker; re-test against Worker Stripe webhook handler |
| Returns 400 when Stripe signature verification fails | webhook.routes.test.ts:76 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/error-handling.test.ts) — clerk/webhook-signature.test.ts:63-83 rejects stale Clerk Svix signatures, but no Worker test covers Stripe signature verification failure; no Stripe webhook signature test exists | worker-shaped-rewrite | Signature verification failure is a webhook-security gate; the Worker tests this for Clerk but not for Stripe; the Express route shape retires, but the gate must be re-tested against the Worker's Stripe webhook handler | PROPOSED: worker-shaped-rewrite — Stripe signature verification failure untested in Worker; re-test against Worker Stripe webhook handler |
| Returns 200 without processing when Stripe event is duplicate (idempotency skip) | webhook.routes.test.ts:91 | webhook-security | workers/src/health.test.ts:870-903 — acknowledges duplicate delivery for an already-completed catalogue queue job (acks without reprocessing, does not call R2 get) | worker-equivalent-exists | The Worker's queue duplicate-ack tests the same behaviour (detect already-processed, skip, return success) at the queue level; the Express test tests it at the webhook event level — the idempotency gate is covered by a different mechanism | PROPOSED: worker-equivalent-exists — duplicate-event idempotency skip covered by queue duplicate-delivery ack test |
| Returns 200 when Stripe event is processed successfully (handleEvent + markEventProcessed + monitoring) | webhook.routes.test.ts:110 | — | none found (searched: workers/src/health.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/subscription-validation.test.ts) — health.test.ts:365-379 tests the Clerk webhook route exists and rejects missing headers, but no Worker test covers successful Stripe webhook event processing end-to-end; subscription-validation.test.ts tests subscription status access control, not webhook processing | worker-shaped-rewrite | Successful Stripe webhook processing is relevant to the Worker (subscription lifecycle) but has no test coverage; the Express route shape retires, but the processing gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — Stripe webhook successful processing untested in Worker; re-test against Worker webhook handler |
| Returns 200 for non-recoverable mapping errors to avoid Stripe retry storms | webhook.routes.test.ts:131 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/clerk/webhook-signature.test.ts) — health.test.ts:924-953 marks a queue job failed and acks on final retry, but this is queue error handling, not webhook error classification (200 vs. 500 for non-recoverable vs. transient); error-handling.test.ts:18-40 tests DB retry, not webhook retry-storm prevention | worker-shaped-rewrite | Non-recoverable error classification (return 200 to stop retries) is a negative/error gate for webhook security; the Worker has no test for this error-classification behaviour; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — non-recoverable error 200-classification untested in Worker; re-test against Worker webhook handler |
| Returns 500 for transient processing errors so Stripe retries | webhook.routes.test.ts:147 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — health.test.ts:956-983 retries a queue message before the final attempt, but this is queue retry logic, not webhook 500-response-for-retry; no Worker test asserts a 500 response to a transient webhook processing error | worker-shaped-rewrite | Transient error classification (return 500 to trigger retry) is a negative/error gate; the Worker has no test for this error-classification behaviour; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — transient error 500-classification untested in Worker; re-test against Worker webhook handler |
| Returns 200 for NotFoundError to stop Stripe retries on non-recoverable issues | webhook.routes.test.ts:165 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/clerk/webhook-signature.test.ts) — no Worker test covers NotFoundError-as-200 in a webhook context; health.test.ts:1310-1341 returns 404 for a missing upload, not a webhook NotFound-as-200 | worker-shaped-rewrite | NotFoundError-as-200 (stop retries for non-recoverable missing-resource) is a negative/error gate; the Worker has no test for this webhook-specific error mapping; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — NotFound-as-200 retry-stop untested in Worker; re-test against Worker webhook handler |
| Returns 500 for unexpected Stripe handler failures | webhook.routes.test.ts:178 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker test covers unexpected failure in a Stripe webhook handler returning 500; error-handling.test.ts:77-79 tests graceful failure after max retries, but for DB operations, not webhook handlers | worker-shaped-rewrite | Unexpected-failure 500 is a negative/error gate; the Worker has no test for this in a Stripe webhook context; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — unexpected Stripe handler failure 500 untested in Worker; re-test against Worker webhook handler |
| Returns 400 when required Svix headers are missing for Clerk webhook | webhook.routes.test.ts:212 | webhook-security | workers/src/health.test.ts:365-379 — routes /api/webhooks/clerk and rejects requests without Svix headers with 400, asserting body.error contains 'Svix' | worker-equivalent-exists | The Worker tests the exact same behaviour (Clerk webhook missing Svix headers → 400 with Svix error message) via the deployed minimal Worker entrypoint | PROPOSED: worker-equivalent-exists — Clerk missing-Svix-header rejection covered by health.test.ts route test |
| Returns 400 when Clerk signature verification fails | webhook.routes.test.ts:223 | webhook-security | workers/src/clerk/webhook-signature.test.ts:63-83 — rejects signatures outside the allowed timestamp window, throwing 'Webhook timestamp outside allowed window' | worker-equivalent-exists | The Worker tests Clerk signature verification failure (stale timestamp rejection); the Express test tests invalid-signature rejection — both assert signature verification failure produces an error response | PROPOSED: worker-equivalent-exists — Clerk signature verification failure covered by webhook-signature stale-timestamp test |
| Returns 400 when Clerk payload is not an object after signature verification | webhook.routes.test.ts:240 | webhook-security | none found (searched: workers/src/clerk/webhook-signature.test.ts, workers/src/health.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — webhook-signature.test.ts:41-61 tests valid signature acceptance but does not test payload shape validation after verification; health.test.ts:365-379 tests missing headers, not invalid payload; bootstrap-handler tests org bootstrap, not webhook payload validation | worker-shaped-rewrite | Payload-shape validation after signature verification is a webhook-security gate; the Worker has no test for this behaviour; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — Clerk payload-shape validation untested in Worker; re-test against Worker webhook handler |
| Returns 200 without processing when Clerk event is duplicate (idempotency skip) | webhook.routes.test.ts:255 | webhook-security | none found (searched: workers/src/health.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — health.test.ts:870-903 acknowledges duplicate queue delivery for catalogue jobs, but no Worker test covers Clerk webhook event idempotency (isNewEvent check + skip); webhook-signature.test.ts covers signature verification only, not duplicate detection | worker-shaped-rewrite | Clerk webhook event idempotency is a webhook-security gate; the Worker has queue-level duplicate ack but no Clerk webhook event-level idempotency test; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — Clerk webhook duplicate-event idempotency untested in Worker; re-test against Worker webhook handler |
| Returns 200 when Clerk event is processed successfully (handleEvent + markEventProcessed + monitoring) | webhook.routes.test.ts:272 | — | none found (searched: workers/src/health.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts) — health.test.ts:365-379 tests the Clerk webhook route rejects missing headers but does not test successful event processing end-to-end; webhook-signature.test.ts:41-61 tests signature acceptance, not event handling; bootstrap-handler tests org bootstrap, not webhook event processing | worker-shaped-rewrite | Successful Clerk webhook event processing is relevant to the Worker but has no end-to-end test coverage; the Express route shape retires, but the processing gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — Clerk webhook successful processing untested in Worker; re-test against Worker webhook handler |
| Returns 409 for duplicate-email conflict errors from Clerk webhook | webhook.routes.test.ts:295 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/auth-integration.test.ts) — health.test.ts:1430-1453 tests 409 for duplicate email on the register endpoint, not on a Clerk webhook; bootstrap-handler.node.test.ts:175-205 links a new user without testing conflict; no Worker test covers Clerk webhook ConflictError-as-409 | worker-shaped-rewrite | Duplicate-email conflict from a Clerk webhook is a negative/error gate; the Worker tests 409 on the register endpoint but not in a webhook context; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — Clerk webhook conflict-409 untested in Worker; re-test against Worker webhook handler |
| Returns 500 for transient Clerk processing errors | webhook.routes.test.ts:312 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/clerk/webhook-signature.test.ts) — no Worker test covers transient error in a Clerk webhook handler returning 500; error-handling.test.ts:18-40 tests DB retry, not webhook transient error response | worker-shaped-rewrite | Transient Clerk webhook error 500 is a negative/error gate; the Worker has no test for this in a Clerk webhook context; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — Clerk webhook transient error 500 untested in Worker; re-test against Worker webhook handler |
| Returns 500 for unexpected Clerk handler failures | webhook.routes.test.ts:327 | negative/error | none found (searched: workers/src/health.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/clerk/webhook-signature.test.ts) — no Worker test covers unexpected failure in a Clerk webhook handler returning 500; error-handling.test.ts covers DB retry exhaustion, not webhook handler unexpected failure | worker-shaped-rewrite | Unexpected Clerk webhook handler failure 500 is a negative/error gate; the Worker has no test for this in a Clerk webhook context; the Express route shape retires, but the gate must be re-tested against the Worker's webhook handler | PROPOSED: worker-shaped-rewrite — Clerk webhook unexpected failure 500 untested in Worker; re-test against Worker webhook handler |

---


### webhook.service.test.ts

Unit tests for `WebhookService` — the Stripe webhook event processor. Uses a **mocked Prisma
client** (`processedWebhookEvent`, `organization`, `subscriptionTier`, `organizationUsage`,
`auditLog`, `trialEvent` are all `vi.fn()` stubs) plus a mocked `Stripe` instance and a mocked
`EmailService`. No real database, no supertest, no HTTP — this is a pure call-shape test
verifying that each Stripe event type triggers the right Prisma writes, emails, audit logs, and
idempotency guards. The Worker has no Stripe webhook handler test at all; its only webhook test
(`clerk/webhook-signature.test.ts`) verifies Clerk Svix signature verification, a different
provider and concern. The subscription/org/usage/feature-gates Worker suites are spec stubs
(`expect(true).toBe(true)`) that assert status-gating contracts, not Stripe event processing.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| `isNewEvent` returns true when event has not been processed (findUnique returns null) | webhook.service.test.ts:125 | webhook-security | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/handlers/handlers.test.ts) — subscription-validation.test.ts:17-33 asserts active-subscription API access, not webhook event dedup; clerk/webhook-signature.test.ts:41-61 verifies Clerk Svix signature acceptance, not Stripe event idempotency; handlers.test.ts:43-53 is a multi-tenant isolation stub | worker-shaped-rewrite | Event-level idempotency (skip already-processed events) is the core webhook replay guard and must be preserved; the Prisma `findUnique` call shape retires but the dedup behaviour has no Worker test | PROPOSED: worker-shaped-rewrite — webhook replay/dedup guard has no Worker equivalent; re-test event-idempotency check against Worker Stripe handler |
| `isNewEvent` returns false when event already processed (findUnique returns row) | webhook.service.test.ts:136 | webhook-security | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/clerk/webhook-signature.test.ts) — subscription-validation.test.ts:50-62 asserts canceled-subscription API blocking, not duplicate-event rejection; clerk/webhook-signature.test.ts:63-83 rejects expired Clerk signatures, not duplicate Stripe events | worker-shaped-rewrite | The "reject duplicate event" half of the idempotency guard is security-relevant (replay protection) and has no Worker coverage | PROPOSED: worker-shaped-rewrite — duplicate-event rejection has no Worker equivalent; re-test idempotency skip against Worker Stripe handler |
| `markEventProcessed` swallows Prisma P2002 unique-constraint errors (concurrent dedup) | webhook.service.test.ts:144 | webhook-security | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/error-handling.test.ts) — subscription-validation.test.ts has no write-path coverage; error-handling.test.ts:18-40 tests Neon connection retry, not Prisma P2002 dedup on concurrent event marking | worker-shaped-rewrite | Concurrent duplicate-event marking must be safe (two handlers race to mark the same event); the P2002 error code is Prisma-specific and retires, but the concurrent-dedup behaviour must be re-tested against the Worker's Postgres unique-constraint model | PROPOSED: worker-shaped-rewrite — concurrent event-marking dedup is Prisma-P2002-specific; re-test dedup-on-conflict against Worker Postgres model |
| `markEventProcessed` rethrows non-unique errors (DB-down propagates) | webhook.service.test.ts:153 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/__tests__/subscription-validation.test.ts) — error-handling.test.ts:77-97 tests retry-exhaustion on connection errors, not error-discrimination in the event-marking path; subscription-validation.test.ts has no error-path coverage | worker-shaped-rewrite | Non-dedup errors must propagate (a real DB failure is not silently swallowed); the Prisma error-code discrimination retires but the propagate-real-errors behaviour must be preserved | PROPOSED: worker-shaped-rewrite — error discrimination in event-marking is Prisma-specific; re-test non-dedup error propagation against Worker model |
| `handleEvent` ignores unhandled event types without throwing or reporting | webhook.service.test.ts:164 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/clerk/webhook-signature.test.ts) — subscription-validation.test.ts has no event-type-dispatch coverage; error-handling.test.ts:117-129 tests malformed-JSON rejection, not unknown-event-type tolerance; clerk/webhook-signature.test.ts covers Clerk signature validation only | worker-shaped-rewrite | Silently ignoring unknown event types (no throw, no Sentry report) is a forward-compatibility guard that must be preserved in the Worker handler | PROPOSED: worker-shaped-rewrite — unknown-event-type tolerance has no Worker equivalent; re-test no-op-on-unhandled against Worker Stripe handler |
| `handleSubscriptionCreated` creates subscriptionTier, upserts usage, writes audit log | webhook.service.test.ts:176 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/org-status-integration.test.ts, workers/src/handlers/handlers.test.ts) — subscription-validation.test.ts:17-33 asserts active-sub API access (stub), not subscription-record creation from a Stripe event; org-status-integration.test.ts:16-36 is an active-org access stub; handlers.test.ts:43-53 is a tenant-isolation stub | worker-shaped-rewrite | Subscription-record creation on `customer.subscription.created` is core billing lifecycle behaviour with no Worker test; the Prisma `subscriptionTier.create` call shape retires but the create-on-event behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — subscription-created record creation has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionUpdated` sends downgrade warning email when downgrading over SKU limit | webhook.service.test.ts:210 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/subscription-validation.test.ts) — usage-limits-integration.test.ts:15-28 asserts the Starter 500-SKU limit value (stub), not downgrade-warning email on a Stripe update event; feature-gates-integration.test.ts:16-30 asserts Starter lacks advanced_analytics (stub), not downgrade email; subscription-validation.test.ts:209-222 is a downgrade-feature-access stub | worker-shaped-rewrite | Downgrade-over-limit warning is a feature-limits gate triggered by a Stripe event; the Worker has tier-limit stubs but no test tying a subscription update to the warning email | PROPOSED: worker-shaped-rewrite — downgrade-warning-on-stripe-event has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionUpdated` sets `isCreationLocked=true` on org when downgrading over SKU limit | webhook.service.test.ts:249 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts) — usage-limits-integration.test.ts:15-28 asserts the SKU limit constant, not creation-lock on downgrade; feature-gates-integration.test.ts:118-130 is a requireFeatureAccess stub; org-status-integration.test.ts:183-196 is an active→suspended transition stub, not a downgrade lock | worker-shaped-rewrite | Creation-lock on downgrade-over-limit is a feature-limits enforcement action with no Worker test; the Prisma `organization.update` call shape retires but the lock-on-downgrade behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — creation-lock on downgrade-over-limit has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionDeleted` applies downgrade warning when over Starter limit | webhook.service.test.ts:280 | feature-limits | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — subscription-validation.test.ts:50-62 asserts canceled-sub API blocking (stub), not deletion-event downgrade warning; usage-limits-integration.test.ts:15-28 asserts the SKU limit constant; feature-gates-integration.test.ts has no deletion coverage | worker-shaped-rewrite | Subscription-deleted downgrade warning (cancel → free tier, warn if over limit) is a feature-limits gate with no Worker test | PROPOSED: worker-shaped-rewrite — deletion downgrade-warning has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionDeleted` sets `isCreationLocked=true` when over Starter limit | webhook.service.test.ts:308 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — usage-limits-integration.test.ts:15-28 asserts the SKU limit value, not creation-lock on deletion; org-status-integration.test.ts:280-295 is an admin-deactivate stub; feature-gates-integration.test.ts has no deletion-lock coverage | worker-shaped-rewrite | Creation-lock on subscription deletion over limit is a feature-limits enforcement action with no Worker test | PROPOSED: worker-shaped-rewrite — creation-lock on deletion-over-limit has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionUpdated` does NOT lock org when downgrade is within new SKU limit | webhook.service.test.ts:328 | feature-limits | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts) — usage-limits-integration.test.ts:30-42 asserts the Professional 2000-SKU limit (stub), not the negative "no lock when within limit" case; feature-gates-integration.test.ts:49-62 asserts Professional has advanced_analytics, not no-lock-on-downgrade | worker-shaped-rewrite | The negative case (no lock when within limit) is the feature-limits guard against false-positive lockout and must be preserved; no Worker test covers it | PROPOSED: worker-shaped-rewrite — no-lock-when-within-limit negative case has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionUpdated` creates subscription record when updated arrives before created (out-of-order) | webhook.service.test.ts:357 | webhook-security | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/error-handling.test.ts) — subscription-validation.test.ts has no out-of-order event coverage; clerk/webhook-signature.test.ts:41-61 covers signature validity, not event ordering; error-handling.test.ts has no ordering coverage | worker-shaped-rewrite | Out-of-order event handling (updated before created) is a webhook-delivery robustness guard; no Worker test covers event-ordering resilience | PROPOSED: worker-shaped-rewrite — out-of-order event resilience has no Worker equivalent; re-test against Worker Stripe handler |
| `handleCheckoutSessionCompleted` retrieves subscription, updates tier, unlocks org, audit-logs trial conversion | webhook.service.test.ts:390 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/org-status-integration.test.ts, workers/src/handlers/handlers.test.ts) — subscription-validation.test.ts:125-141 is a free-trial-creates-subscription stub, not checkout-session completion; org-status-integration.test.ts:198-208 is a suspended→active transition stub; handlers.test.ts has no checkout coverage | worker-shaped-rewrite | Checkout-session completion (trial → paid conversion, unlock org) is core billing lifecycle behaviour with no Worker test | PROPOSED: worker-shaped-rewrite — checkout-completion trial conversion has no Worker equivalent; re-test against Worker Stripe handler |
| `handleInvoicePaymentFailed` sets past_due and records pastDueSince on first failure | webhook.service.test.ts:424 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/__tests__/org-status-integration.test.ts) — subscription-validation.test.ts has no past_due coverage; error-handling.test.ts:18-40 tests DB retry, not invoice-failure status transition; org-status-integration.test.ts has no payment-failure coverage | worker-shaped-rewrite | First-failure past_due transition with pastDueSince timestamp is a dunning-lifecycle behaviour with no Worker test | PROPOSED: worker-shaped-rewrite — first-failure past_due transition has no Worker equivalent; re-test against Worker Stripe handler |
| `handleInvoicePaymentFailed` does NOT reset pastDueSince on retry failures (idempotent timestamp) | webhook.service.test.ts:450 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/error-handling.test.ts) — subscription-validation.test.ts has no retry-failure coverage; error-handling.test.ts:42-75 tests retry-until-success, not idempotent-timestamp-on-retry-failure | worker-shaped-rewrite | The "do not reset pastDueSince on retry" idempotency guard preserves the original failure timestamp; no Worker test covers this dunning-retry semantics | PROPOSED: worker-shaped-rewrite — idempotent pastDueSince on retry has no Worker equivalent; re-test against Worker Stripe handler |
| `handleTrialWillEnd` sends trial reminder email with org id and trial end | webhook.service.test.ts:473 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/org-status-integration.test.ts) — subscription-validation.test.ts:125-141 is a free-trial-creates-subscription stub, not trial-ending reminder; org-status-integration.test.ts has no trial-reminder coverage | worker-shaped-rewrite | Trial-will-end reminder email is a billing-lifecycle notification with no Worker test; the EmailService call shape retires but the notify-on-trial-end behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — trial-end reminder has no Worker equivalent; re-test against Worker Stripe handler |
| `handlePaymentIntentSucceeded` skips processing when no trialing subscription exists (no-op) | webhook.service.test.ts:489 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/error-handling.test.ts) — subscription-validation.test.ts has no payment-intent coverage; error-handling.test.ts has no payment-intent coverage | worker-shaped-rewrite | The no-op-when-no-trial negative case prevents spurious trial conversions; no Worker test covers it | PROPOSED: worker-shaped-rewrite — skip-when-no-trial negative case has no Worker equivalent; re-test against Worker Stripe handler |
| `handlePaymentIntentFailed` logs trial event and sends payment-failed email | webhook.service.test.ts:505 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/__tests__/org-status-integration.test.ts) — subscription-validation.test.ts has no payment-intent-failure coverage; error-handling.test.ts:18-40 tests DB retry, not payment-failure notification; org-status-integration.test.ts has no payment-failure coverage | worker-shaped-rewrite | Payment-intent-failure trial-event logging and email is a dunning notification with no Worker test | PROPOSED: worker-shaped-rewrite — payment-failed notification has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionCreated` records webhook metrics (total, byEvent count, avgLatency) via ApplicationMonitoringService singleton | webhook.service.test.ts:531 | — | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/health.test.ts, workers/src/workers-deployment.test.ts) — error-handling.test.ts has no webhook-metric coverage; health.test.ts is a deployment health smoke test; workers-deployment.test.ts has no metric coverage | retire | The `ApplicationMonitoringService` singleton with `metrics.webhook.byEvent`/`avgLatencyMs` is Express-specific in-process monitoring machinery; the Worker has no equivalent singleton and uses different observability; the metric-recording mechanism retires with the backend | PROPOSED: retire — ApplicationMonitoringService webhook-metric singleton is Express-specific monitoring machinery with no Worker analogue |
| `handleSubscriptionCreated` captures Sentry exception and records failure metric when handler errors | webhook.service.test.ts:567 | negative/error | none found (searched: workers/src/__tests__/error-handling.test.ts, workers/src/health.test.ts) — error-handling.test.ts:18-40 tests Neon retry on connection errors, not Sentry capture on webhook handler failure; health.test.ts has no Sentry-capture coverage | worker-shaped-rewrite | Error capture (Sentry) on handler failure is a cross-cutting reliability behaviour that must be preserved; the `ApplicationMonitoringService` failure-metric mechanism retires but the capture-and-count-failures behaviour needs a Worker-shaped rewrite | PROPOSED: worker-shaped-rewrite — Sentry capture on handler error has no Worker equivalent; re-test error capture against Worker Stripe handler |
| `handleSubscriptionCreated` throws NotFoundError when Stripe customer has been deleted | webhook.service.test.ts:607 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/clerk/webhook-signature.test.ts) — subscription-validation.test.ts has no deleted-customer coverage; error-handling.test.ts:131-149 tests missing-field validation, not deleted-customer rejection; clerk/webhook-signature.test.ts covers Clerk signatures only | worker-shaped-rewrite | Rejecting a deleted Stripe customer is a data-integrity negative case that must be preserved; no Worker test covers it | PROPOSED: worker-shaped-rewrite — deleted-customer rejection has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionCreated` throws when customer metadata is missing organizationId | webhook.service.test.ts:630 | negative/error | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/error-handling.test.ts) — subscription-validation.test.ts has no metadata-validation coverage; error-handling.test.ts:131-149 tests missing product fields, not missing Stripe metadata | worker-shaped-rewrite | Missing-organizationId-metadata rejection is a negative case preventing unattributed subscription writes; no Worker test covers it | PROPOSED: worker-shaped-rewrite — missing-metadata rejection has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionCreated` throws NotFoundError when organization is missing in DB | webhook.service.test.ts:649 | negative/error | none found (searched: workers/src/__tests__/org-status-integration.test.ts, workers/src/__tests__/subscription-validation.test.ts) — org-status-integration.test.ts:53-70 asserts inactive-org API blocking (stub), not missing-org rejection on webhook; subscription-validation.test.ts has no missing-org coverage | worker-shaped-rewrite | Missing-organization rejection prevents webhook writes to a nonexistent org; no Worker test covers it | PROPOSED: worker-shaped-rewrite — missing-org rejection has no Worker equivalent; re-test against Worker Stripe handler |
| `handleSubscriptionCreated` reports critical failure (with customer email) when organization missing from metadata lookup | webhook.service.test.ts:664 | negative/error | none found (searched: workers/src/__tests__/org-status-integration.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/__tests__/subscription-validation.test.ts) — org-status-integration.test.ts:280-295 is an admin-deactivate stub, not critical-failure reporting; error-handling.test.ts:315-331 is an error-format stub, not critical-webhook-failure reporting; subscription-validation.test.ts has no critical-failure coverage | worker-shaped-rewrite | Critical-failure reporting (with customer email for support escalation) on missing org is a negative-case reliability behaviour that must be preserved; no Worker test covers it | PROPOSED: worker-shaped-rewrite — critical-failure reporting on missing org has no Worker equivalent; re-test against Worker Stripe handler |

---

### xlsx.upload.test.ts

Unit tests for `ProductService.processCSVUpload` invoked on **real `.xlsx` files written to
disk** via the `xlsx` library, with a **mocked Prisma client** (`product.findUnique`/`create`/
`findMany`/`update`/`upsert` and `organizationUsage.findUnique` are `vi.fn()` stubs). Despite the
name `processCSVUpload`, the fixture builder uses `XLSX.utils.aoa_to_sheet` + `XLSX.writeFile` to
produce `.xlsx` spreadsheets, then reads them back through the service. The Worker upload path
parses **CSV** (not XLSX) via `csv-parser` + `catalogue-parser`, runs the import through a queued
`processCatalogueImportJob` against Postgres (pglite real-SQL in tests), and stores files in R2.
Header-alias mapping, invalid-cost rejection, and the insert/update classification have direct
Worker equivalents; XLSX-specific file handling and the missing-column/unexpected-column error
messages do not.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Processes an XLSX with basic SKU/Name/Cost/Barcode headers — 3 imported, 0 errors, 3 product creates | xlsx.upload.test.ts:104 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:249-296 — classifies insert/update/unchanged/conflict end-to-end via pglite and asserts `rows_imported === 1` for the insert row (S5) with zero unexpected errors; workers/src/upload/catalogue-parser.test.ts:5-21 — asserts known headers parse to a zero-error product row | worker-equivalent-exists | The pglite import test asserts the same import-count/zero-error happy-path semantics end-to-end against real SQL, and the catalogue parser asserts zero-error row extraction; the XLSX file format and the Prisma `product.create` call-count assertion are Express-specific machinery | PROPOSED: worker-equivalent-exists — pglite catalogue-import-upsert and catalogue-parser cover import-count + zero-error row extraction; XLSX format and Prisma create-count are Express-specific |
| Processes an XLSX with alternative header names (Item Code / Product Name / Item Cost / GTIN) | xlsx.upload.test.ts:117 | — | workers/src/upload/catalogue-parser.test.ts:5-21 — `validateCatalogueRecords` accepts `['Item Code', 'Item Description', 'Cost Ex', 'Barcode']` aliases and maps them to sku/name/costPrice/barcode with zero fatalErrors/rowErrors | worker-equivalent-exists | The Worker catalogue parser tests the exact same alternative-header-alias mapping behaviour (Item Code → sku, Cost Ex → costPrice, etc.) and asserts zero errors; the XLSX file format is the only Express-specific difference | PROPOSED: worker-equivalent-exists — catalogue-parser.test.ts:5-21 directly covers alternative-header-alias mapping |
| Returns errors for a missing required field (Barcode column absent) in XLSX | xlsx.upload.test.ts:138 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — catalogue-parser.test.ts:5-21 tests the header-alias success case and :23-32 tests malformed-value rejection (null return), neither asserts a missing-required-column error message; upload-handlers.test.ts:61-101 asserts "No product rows found" for a header-only CSV with correct headers, not a missing-required-field error | worker-shaped-rewrite | Missing-required-column detection is a negative/validation gate relevant to the Worker upload path, but no Worker test asserts the missing-header error; the Express "Missing required field - Barcode" message string is Express-specific, but the detection behaviour must be re-tested | PROPOSED: worker-shaped-rewrite — missing-required-column detection has no Worker equivalent; re-test against Worker catalogue parser |
| Returns errors for invalid cost values in XLSX | xlsx.upload.test.ts:159 | negative/error | workers/src/upload/catalogue-parser.test.ts:23-32 — `parseProductCatalogRow` with cost `'not-money'` returns null, rejecting the malformed cost value | worker-equivalent-exists | The Worker catalogue parser rejects non-numeric cost values by returning null, the same rejection behaviour the Express test asserts (Express checks the "Invalid cost value" message string); the assertion shape differs but the validated behaviour is identical | PROPOSED: worker-equivalent-exists — catalogue-parser.test.ts:23-32 directly covers invalid-cost-value rejection |
| Returns errors when required headers are missing (all wrong headers) in XLSX | xlsx.upload.test.ts:180 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — catalogue-parser.test.ts:5-21 tests only the correct-alias success case, with no all-headers-unmapped failure assertion; upload-handlers.test.ts:61-101 tests a header-only CSV with correct headers ("No product rows found"), not an all-wrong-headers "Missing required column" error | worker-shaped-rewrite | All-required-headers-missing detection is a negative/validation gate with no Worker test; the "Missing required column" message is Express-specific but the detection behaviour must be re-tested against the Worker header-mapping path | PROPOSED: worker-shaped-rewrite — all-headers-unmapped detection has no Worker equivalent; re-test against Worker catalogue parser |
| Returns errors for unexpected columns in XLSX | xlsx.upload.test.ts:201 | negative/error | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts) — catalogue-parser.test.ts:5-21 tests exact known header aliases with no extra-column rejection assertion; csv-parser.test.ts:5-21 tests quoted-comma/BOM handling, not unexpected-column detection | worker-shaped-rewrite | Unexpected-column rejection is a negative/validation gate with no Worker test; the "Unexpected columns found" message is Express-specific but the extra-column detection behaviour must be re-tested against the Worker parser | PROPOSED: worker-shaped-rewrite — unexpected-column detection has no Worker equivalent; re-test against Worker catalogue parser |
| Updates existing products in XLSX processing (existing SKU found → update, not create; imported=1, updated=1) | xlsx.upload.test.ts:288 | — | workers/src/__tests__/catalogue-import-upsert.node.test.ts:249-296 — seeds an existing product S1, imports a CSV row with the same SKU and a changed name, asserts `rows_updated === 1` and `rows_imported === 1`, and verifies the update applied (`S1` name → "New Name") end-to-end via pglite real SQL | worker-equivalent-exists | The pglite import test asserts the exact same update-existing-vs-insert-new classification (updated=1, imported=1) and verifies the applied update against real SQL — a stronger equivalent than the Express mocked-Prisma test; the XLSX format and Prisma `product.update` call shape are Express-specific | PROPOSED: worker-equivalent-exists — catalogue-import-upsert.node.test.ts:249-296 directly covers update-existing-product classification end-to-end |

---

