# Test-Coverage Manifest — Part 4 (non-unit suites)

> **Status: PROPOSED — inventory, not a decision record.** Every row carries a `PROPOSED:`
> prefix. Deletion authority is human; no keep/retire call in this file is settled until reviewed.

**Task 2.2** — `retire-express-unify-on-postgres`

**Scope:** the 41 backend test files outside `backend/src/tests/unit/` — `integration/`,
`services/`, `controllers/`, `contract/`, `performance/`, and the root `clerk-auth.middleware`
test. Parts 1–3 cover `unit/` in full. A file in scope with no section is a defect.

**Columns:** `Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision`

**Named gates:** `tenant-isolation`, `penetration`, `concurrency`, `feature-limits`,
`webhook-security`, `scheduled-job-idempotency`, `authorization-precedence`, `negative/error`,
or `—`.

**Targets:** `worker-equivalent-exists` | `worker-shaped-rewrite` | `retire`.

---

## Three findings that govern every `tenant-isolation` row below

Findings 1 and 2 were established before any row was written, because both change how the evidence
in this file must be read. Finding 3 came out of *working* two of the rows. None is a defect
introduced by this change; all three were pre-existing facts about the Worker that only become
load-bearing at Phase 4 deletion.

> **All three are now resolved — Findings 1 and 2 by PR #462 (`f148fc92`), Finding 3 by #466 (`2345ff49`).**
> They are kept in full rather than edited away, because the findings are what located the defects:
> chasing Finding 1 — a file whose name matched the property but whose assertions could not fail —
> led directly to four live authenticated routes that returned every tenant's rows to any signed-in
> user, and Finding 3 was found the same way one layer down. Rows changed by those fixes are marked
> **`UPDATED (#462)`** or **`UPDATED (#466)`** in their Decision cell, with the pre-fix reasoning
> retained beside the new one so the audit trail stays readable.
>
> The pattern is worth naming, since two more manifest parts remain: **the rows most worth working
> first are the ones asserting a property is untested.** Both vulnerabilities were found there, not
> in the rows that looked wrong.

#### Finding 1 — `workers/src/__tests__/multi-tenant-isolation.test.ts` asserts nothing

All 8 of its tests have the same body: a prose comment describing a scenario, then
`const expected = true; expect(expected).toBe(true)`. The file passes, is counted by the runner,
and constrains no behaviour. It is not alone — 90 such tests exist across 8 Worker files, and
`workers/src/handlers/handlers.test.ts` is likewise 20/20:

| Worker file | Tautological / total |
|---|---:|
| `__tests__/multi-tenant-isolation.test.ts` | 8 / 8 |
| `handlers/handlers.test.ts` | 20 / 20 |
| `__tests__/org-status-integration.test.ts` | 16 / 19 |
| `__tests__/error-handling.test.ts` | 19 / 28 |
| `__tests__/auth-integration.test.ts` | 14 / 18 |
| `__tests__/subscription-validation.test.ts` | 7 / 14 |
| `__tests__/usage-limits-integration.test.ts` | 4 / 13 |
| `__tests__/feature-gates-integration.test.ts` | 2 / 8 |

Because its **name** is the most plausible thing to cite as the Worker equivalent of the
backend's tenant-isolation gate, citing it as positive evidence would retire a real gate against
a test that cannot fail. **No row in Parts 1–3 does this** — all 345 citations of these files
across the merged manifests appear inside negative evidence ("searched X, none found"), which is
correct use. `scripts/verify-audit-manifest.js` now fails the build on the positive form, so the
property is enforced rather than merely observed.

**Resolved for this file (#462).** `__tests__/multi-tenant-isolation.test.ts` is now a
`describe.skip` block pointing at the real coverage in
`workers/src/database.tenant-isolation.pglite.node.test.ts`. It was kept rather than deleted
because audit-manifest rows across Parts 1–4 cite the path as negative evidence. The other seven
files in the table above are **unchanged** and still tautological — the counts remain accurate,
and the citations of them below remain correct.

#### Finding 2 — the Worker's *real* isolation coverage is not run by CI

The genuine cross-tenant assertions live in the pglite/conformance suites — real SQL, real
seeded `org-b` rows, real expectations:

- `workers/src/database.dashboard.pglite.node.test.ts:110` — "scopes counts to the requested
  organization"; seeds two `OTHER_ORG` items and asserts `expiringItems` is 1, not 3.
- `workers/src/database.dashboard.pglite.node.test.ts:161` — an `OTHER_ORG` loss of 500 is
  excluded from a 150 total.
- `workers/src/database.dashboard.pglite.node.test.ts:213` — "ignores other organizations and
  returns null when none completed".
- `workers/src/database.report.pglite.node.test.ts:82-87` — `org-b` rows in the same windows must
  not inflate `org-a` counts.
- `workers/src/database.supplier-policy.conformance.node.test.ts:237` — asserts organization
  isolation alongside status and ordering.

These are cited below as real equivalents. **But `npm run test:db` and the workers vitest pool ran
in no CI workflow** (no `.github/workflows/*` references either), so the coverage that survives
Phase 4 deletion was coverage nothing enforced on a pull request. That is a separate problem from
this manifest's, and it was recorded here so the `worker-equivalent-exists` rows below were not
read as stronger than they were.

**Resolved (#462).** `.github/workflows/workers-test.yml` now runs both suites on every pull
request, and `Workers CI Gate` is a required status check on the `main` ruleset. The gate is the
`if: always()` job rather than the test jobs themselves, because GitHub counts a *skipped* job as
success — requiring the test jobs directly would let a failed detection step merge untested work.

Two things that workflow caught on its first run are worth recording, because they bear on how
much the citations above are worth: `src/health.test.ts` had two tests red on `main` since
2026-06-09 (`d11d1f97`), unnoticed for two months; and the suite did not pass on a clean checkout
at all, because it needed a built bundle and backend dependencies that only a developer's
accumulated working copy happened to have. The `worker-equivalent-exists` rows below can now be
read at face value.

#### Finding 3 — a cross-tenant leak on the *write* path, found by auditing the two rows above (#466)

Added after Findings 1 and 2, when the two `worker-shaped-rewrite` rows for cross-tenant
UPDATE/DELETE were worked. Those rows predicted a mutation keyed on id alone; the mutations turned
out to be correctly scoped. The defect was one layer over:

`updateInventoryItem` validated that a supplied `locationId` belonged to the caller's organization
but did **not** validate `productId` — while `createInventoryItem` validated **both**. Meanwhile 20
JOINs across `database.ts` resolved `products`, `store_areas` and `inventory_items` by id alone,
without correlating on organization (`JOIN products p ON ii.product_id = p.id`). The two compose:

```
PATCH /api/inventory-items/{own_id}   {"productId": <victim's product id>}   -> 200 OK
GET   /api/reports/loss-by-sku                                               -> victim's sku, name, cost_price
```

Product ids are `SERIAL`, so the victim id is enumerable. Confirmed by real-SQL test, not by
inspection: the update returned the item with the foreign `productId`, and `getLossBySkuReport`
for the attacker's organization then returned `SKU-SECRET` — a row belonging to another tenant.

Fixed on both sides, because either alone leaves a hole: the missing `productId` check (which stops
the bad reference being created) and all 20 uncorrelated JOINs (which stop any pre-existing or
future bad reference being resolved). Verified by mutation — removing the `productId` check fails
exactly one test, removing the JOIN correlation fails exactly two.

**Why this matters for the manifest method.** A `worker-shaped-rewrite` row is a claim that a
property is untested, and the natural way to close one is to write the test and move on. Here,
writing the test is what surfaced a live vulnerability that no Express row described — the Express
suite has no equivalent, because Prisma's service-scoped constructor makes the reference-repointing
shape hard to express. Rows should be worked by exercising the Worker against real SQL, not by
reading the Worker and judging it equivalent.

**One deliberate exception, recorded so it is not "fixed".** `reviewCatalogueCorrection` is the
only mutation in `database.ts` with no organization predicate, and it is correct: catalogue
corrections are reviewed by a **platform** admin across all tenants, gated by `isPlatformAdminUser`
rather than tenant membership. A sweep for "mutations missing `organization_id`" flags it first and
the guard that justifies it lives in another file, so the reasoning is now a comment at the method.

#### Finding 4 — the Worker enforces no usage limit on any interactive path

Established while working the `concurrency` and `feature-limits` files. Unlike Findings 1–3 this is
**not fixed** — it is a parity gap for Phase 3.1 to close, recorded here because it governs every
`feature-limits` and `concurrency` row below and would otherwise be rediscovered three times.

Three independent facts, each enumerable rather than judged:

1. **`workers/src/utils/feature-gates.ts` has no production importer.** A repo-wide search for the
   module path across `workers/src/**` outside `*.test.ts` returns **nothing**. Its five exports
   (`checkFeatureAccess`, `checkUsageLimit`, `requireFeatureAccess`, `enforceUsageLimit`,
   `formatUsageLimitCTA`) are referenced only by `utils/feature-gates.test.ts`,
   `__tests__/feature-gates-integration.test.ts`, and `__tests__/usage-limits-integration.test.ts`.
   The module is dead code carrying a 362-line test suite.

2. **Its SQL cannot run against the schema.** `checkUsageLimit` queries `"Product"`, `"User"`,
   `"InventoryItem"` and `"Upload"` — quoted PascalCase Prisma table names — while the Postgres
   schema uses `products`, `users`, `inventory_items`, `uploads`. Exercised against pglite, every
   branch throws and falls into the catch:

   ```
   [FeatureGate] Error checking usage limit: error: relation "Product" does not exist
   PROBE max_skus => {"isWithinLimit":false,"currentUsage":0,"limit":5000,"error":"Failed to check max_skus limit"}
   [FeatureGate] Error checking usage limit: error: relation "User" does not exist
   [FeatureGate] Error checking usage limit: error: relation "Upload" does not exist
   ```

   The module fails closed, so wiring it up as-is would reject every create. Its own test suite
   passes because it supplies a mock `dbClient` returning canned rows, which never validates SQL —
   the same class of defect as Finding 1, one layer down: a test that exercises a query string
   nothing will ever execute.

3. **The one inline gate reads a counter nothing maintains.** `POST /api/users`
   (`index-minimal.ts:2673`) does check `max_users`, against `organization_usage`. But
   `organization_usage` is written in exactly one place in the entire Worker — a lazy
   `INSERT ... ON CONFLICT DO NOTHING` of literal zeros inside `handleGetOrganizationUsage`
   (`index-minimal.ts:3084`), seeding `active_users` 0, `max_users` 1, `total_skus` 0. There is no
   `UPDATE organization_usage` anywhere, and no database trigger (no `CREATE TRIGGER` exists in any
   migration). So `active_users` is permanently `0`, the guard `activeUsers >= maxUsers` evaluates
   `0 >= 1` → false, and the limit **never fires**. The same staleness makes
   `GET /api/organization/usage` report `total_skus: 0` for every organization regardless of
   catalogue size.

`POST /api/products` (`handleCreateProduct`, `index-minimal.ts:2266`) performs no limit check at
all. The **only** working usage enforcement in the Worker is on the bulk catalogue-import path,
where `isCatalogueWithinLimit` (`upload/catalogue-import.ts:114`) compares a projected SKU count
against the `max_skus_snapshot` captured at job creation from `LAUNCH_TIER_LIMITS`
(`index-minimal.ts:3980`) — a separate mechanism that does not consult `organization_usage` and
does not cover interactive creates.

None of this is a tenant-isolation defect: no organization can reach another's data through it.
It is a revenue-and-capacity gap, and it means **no `feature-limits` row below may be resolved
`worker-equivalent-exists` on the strength of `feature-gates.ts`.**

---

### integration/multi-tenant-cross-tenant-isolation.test.ts

Integration tests against a **real Prisma client** (not mocked), verifying database-level tenant
isolation through the service layer. Two organizations are seeded per test with their own users,
subscription tiers, and usage rows; services are constructed scoped to one org
(`new ProductService(prisma, orgA.id)`) and asserted not to reach the other's data. This is the
backend's primary `tenant-isolation` gate. The Worker enforces the same property at the SQL layer
(`WHERE organization_id = ...`) and tests it in the pglite suites for **reads** — as of #462 with
11 dedicated assertions in `workers/src/database.tenant-isolation.pglite.node.test.ts`, covering
list, fetch-by-id, search, count, inventory and store-area reads. It still has **no test at all**
for cross-tenant **writes and deletes**, so the two rows below targeting those remain
`worker-shaped-rewrite` and are now the highest-consequence uncovered rows in this file.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Read scoping: a service scoped to org A returns only org A products, and org B sees only its own (6 products total, zero leaks) | multi-tenant-cross-tenant-isolation.test.ts:161 | tenant-isolation | workers/src/database.dashboard.pglite.node.test.ts:110 — "scopes counts to the requested organization" seeds one `ORG` item plus two `OTHER_ORG` items and asserts `stats.expiringItems` is 1 and `expiredActionItems` is 0, so the other org's rows are excluded by the query itself; workers/src/database.report.pglite.node.test.ts:82-87 — `org-b` rows seeded into the same 0-30 and 91-120 windows, and `expiry_risk_count` is 2 / `next_month_markdown_count` is 1, ignoring them | worker-equivalent-exists | The property under test — a query scoped to one organization does not return another's rows — is asserted with real SQL and real cross-org seed data in both cited tests. The mechanism differs (Prisma service constructor vs `WHERE organization_id = ${org}`), but the behaviour is the same and genuinely exercised. When written, this row carried the caveat that the cited coverage ran in no CI workflow; #462 added `workers-test.yml` and the required `Workers CI Gate`, so the caveat no longer applies | PROPOSED: worker-equivalent-exists — cross-org read scoping covered by database.dashboard.pglite.node.test.ts:110 and database.report.pglite.node.test.ts:82-87 with real seeded org-b rows; NOT the same as multi-tenant-isolation.test.ts, which asserts nothing |
| Write protection: org A updating org B's product returns null and leaves the row unchanged | multi-tenant-cross-tenant-isolation.test.ts:225 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/database.dashboard.pglite.node.test.ts, workers/src/database.report.pglite.node.test.ts, workers/src/database.conformance.node.test.ts, workers/src/handlers/handlers.test.ts, workers/src/minimal-api-routes.test.ts) — the only Worker reference to a cross-tenant update is the prose comment at multi-tenant-isolation.test.ts:28-29, inside a test whose sole assertion is `expect(expected).toBe(true)`; a repo-wide search for `updateProduct` in `workers/src/**/*.test.ts` returns only that placebo file and unrelated `createProduct` mocks in health.test.ts. **As of #466:** workers/src/database.tenant-isolation-writes.pglite.node.test.ts — `updateInventoryItem` against another org's item returns null AND the row is read back unchanged; `updateStoreArea` and `updateUserRole` likewise; each paired with a same-org test so a method that rejects everything cannot pass | worker-equivalent-exists | Cross-tenant **write** protection is a distinct property from read scoping and is not implied by it: a query can filter correctly on SELECT and still update by primary key alone. Auditing this row found the Worker's UPDATEs were in fact correctly scoped — `organization_id` appears in both the ownership pre-check and the mutating statement — but nothing held that in place. It is now pinned by real-SQL tests | UPDATED (#466): worker-equivalent-exists — was `worker-shaped-rewrite`. The Worker's UPDATE scoping turned out to be correct already; the gap was coverage, not behaviour, and is now closed. Note the audit did find one real write-path defect on a neighbouring surface — see the `updateInventoryItem` productId row added below |
| Delete protection: org A deleting org B's product returns false and the row still exists | multi-tenant-cross-tenant-isolation.test.ts:258 | tenant-isolation | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/database.dashboard.pglite.node.test.ts, workers/src/database.conformance.node.test.ts, workers/src/handlers/handlers.test.ts, workers/src/minimal-api-routes.test.ts) — multi-tenant-isolation.test.ts:54-66 is titled "Organization cannot delete another organization product" but its body is a comment plus `expect(expected).toBe(true)`; a search for `deleteProduct` across `workers/src/**/*.test.ts` matches only that file's import line and the comment at :59. **As of #466:** workers/src/database.tenant-isolation-writes.pglite.node.test.ts — `deleteInventoryItem` and `deleteStoreArea` against another org's row return false and the row is read back still present; `softDeleteUser` leaves `deleted_at` null; each paired with a same-org test that must still succeed | worker-equivalent-exists | Cross-tenant **delete** protection had a Worker test in name only, and was flagged here as the highest-consequence uncovered row. Acting on it found the DELETEs correctly scoped, so the exposure was narrower than the row feared — but the row was right that nothing would have caught a regression. Two further properties are pinned that the Express test does not check: no audit row is written for a rejected cross-tenant delete (a phantom entry would leak the foreign item id into the caller's audit trail), and `deleteStoreArea`'s in-use pre-check does not consult another tenant's inventory, which would disclose that a foreign row references it | UPDATED (#466): worker-equivalent-exists — was `worker-shaped-rewrite`. DELETE scoping was already correct; the missing piece was coverage, now closed with audit-trail and error-disclosure assertions beyond the Express original |
| Inventory scoping: an inventory service scoped to org A returns zero items when only org B has inventory | multi-tenant-cross-tenant-isolation.test.ts:282 | tenant-isolation | workers/src/database.dashboard.pglite.node.test.ts:110 — seeds `OTHER_ORG` inventory items (productId 3, locationId 2) at +5 and -5 days and asserts `getDashboardStats(ORG)` reports `expiringItems` 1 and `expiredActionItems` 0, so another org's inventory rows are excluded; workers/src/database.dashboard.pglite.node.test.ts:161 — an `OTHER_ORG` `expired_item_transactions` row of 500 is excluded from the 150 sum | worker-equivalent-exists | Inventory-level cross-tenant exclusion is asserted with real seeded org-b inventory in both cited tests. The Express test asserts an empty list where the Worker asserts an unaffected count; both fail if the org filter is dropped | PROPOSED: worker-equivalent-exists — cross-org inventory scoping covered by database.dashboard.pglite.node.test.ts:110 and :161 with real seeded OTHER_ORG inventory and transaction rows |
| User scoping: a direct query filtered by organizationId returns only that org's user | multi-tenant-cross-tenant-isolation.test.ts:320 | tenant-isolation | none found (searched: workers/src/database.conformance.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/middleware/auth.test.ts) — bootstrap-handler.node.test.ts covers Clerk user provisioning into an organization, not a cross-org user listing; no Worker test lists users filtered by organization | retire | This test asserts that `prisma.user.findMany({ where: { organizationId } })` honours its own `where` clause — it exercises Prisma, not application code. There is no service or handler between the test and the ORM, so it constrains no behaviour this project owns; the equivalent Worker assertion would test Postgres | PROPOSED: retire — asserts an ORM `where` clause rather than application behaviour; no application code under test |
| Service-layer filtering: a service scoped to org A returns only org A products when both orgs have one | multi-tenant-cross-tenant-isolation.test.ts:336 | tenant-isolation | workers/src/database.dashboard.pglite.node.test.ts:110 — asserts counts are scoped to the requested organization with real cross-org seed data; workers/src/database.report.pglite.node.test.ts:82-87 — asserts org-b rows in the same expiry windows do not inflate org-a counts; workers/src/database.supplier-policy.conformance.node.test.ts:237 — "matches SQLite/shared policy status, null-first order, and organization isolation", seeding an `OTHER_ORG` supplier (:242) and brand (:261) | worker-equivalent-exists | This duplicates the :161 read-scoping property at a narrower scale (one product per org). The Worker's equivalent is the `organization_id` predicate exercised in all three cited tests with foreign-org rows present | PROPOSED: worker-equivalent-exists — service-layer org filtering covered by the same real cross-org scoping assertions as :161, plus supplier-policy conformance at :237 |
| A seeded subscription tier is associated with its organization and has a recognised tier level | multi-tenant-cross-tenant-isolation.test.ts:365 | — | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/database.conformance.node.test.ts) — subscription-validation.test.ts:191-207 asserts tier_level determines feature access, which is a different property; 7 of its 14 tests are tautological (see finding 1) and the tier-association assertion is not among the real ones | retire | This asserts that a row created in `beforeEach` with `organizationId: orgA.id` and `tierLevel: 'professional'` reads back with those values — it verifies the test's own fixture, not application behaviour. The `toContain` check against a four-element literal list is likewise satisfied by construction | PROPOSED: retire — asserts the test's own seed data round-trips; no application behaviour under test |
| Usage tracking is independent per organization (each org has its own usage row) | multi-tenant-cross-tenant-isolation.test.ts:379 | tenant-isolation | none found (searched: workers/src/__tests__/usage-limits-integration.test.ts, workers/src/database.conformance.node.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — usage-limits-integration.test.ts:15-54 asserts per-tier limit constants (max_skus, max_users, max_storage) rather than per-org usage-row separation, and 4 of its 13 tests are tautological; no Worker test reads a per-organization usage row | worker-shaped-rewrite | The final assertion — `usageA.organizationId` is not `usageB.organizationId` — is true by construction and proves nothing. But the underlying property, that usage counters are tracked per organization and cannot be read or incremented across tenants, is real, is enforced by the Worker's usage queries, and is untested there. Worth preserving as a gate even though this particular test does not earn it | PROPOSED: worker-shaped-rewrite — the assertion as written is tautological, but per-org usage separation is a real tenant-isolation property with no Worker test; re-scope and re-test in Phase 3.2 |

---

### integration/multi-tenant-penetration.test.ts

The backend's `penetration` gate: 20 supertest-driven attack simulations against the real Express
app with a real Prisma client, in three groups — injection/IDOR/mass-assignment (9), JWT
validation (6), and route-parameter tampering (5).

**Read the assertions before trusting the file name.** Several tests in the first group send **no
`Authorization` header at all**, so the 403 they assert is the unauthenticated-request path and
the tampering they are named for is never exercised (`:248`, `:321`, `:395`). Others assert a
disjunction satisfied either way — `:196` and `:274` accept *any* status >= 400 *or* an empty
200 body, and `:457` accepts `[200, 403, 500]`, so a 500 passes a test named "should accept valid
JWT". These are recorded per row; where a test does not constrain the behaviour it names, that is
stated rather than carried forward as coverage.

**The Worker's structural position matters for every `org_id`-tampering row.** In
`workers/src/index-minimal.ts` there is exactly **one** assignment of an organization identifier —
`const organizationId = auth.organizationId` at `index-minimal.ts:3352`, taken from the verified
JWT. No route parameter, query string, or request body is ever read into it. The attack surface
these tests probe is therefore *absent by construction* in the Worker, not merely untested — the
same positive-evidence standard applied to the Clerk-managed invite rows in Part 2, rather than
the weaker "no Worker analogue found" argument.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| SQL injection in the orgId query parameter returns no cross-tenant rows | multi-tenant-penetration.test.ts:196 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/utils/auth.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.conformance.node.test.ts) — multi-tenant-isolation.test.ts:143 is titled "No raw SQL concatenation enables isolation bypass" but its body is a comment plus `expect(expected).toBe(true)` (see Finding 1); no Worker test submits an injection payload | retire | The Worker never reads an organization id from a query parameter — `index-minimal.ts:3352` takes it from the verified JWT and that is the only assignment — so this injection point does not exist there. The Express assertion is also a disjunction (status >= 400 OR empty 200 body) satisfied by an unauthenticated rejection, so it does not currently prove injection is neutralised even in Express | PROPOSED: retire — the orgId query-parameter injection point is absent by construction in the Worker (organization id comes only from the verified JWT at index-minimal.ts:3352) |
| Accessing another org's product by direct ID returns 404 | multi-tenant-penetration.test.ts:219 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.dashboard.pglite.node.test.ts) — multi-tenant-isolation.test.ts:39-52 is titled "Organization B cannot read Organization A product by ID" and asserts `expect(expected).toBe(true)`; handlers.test.ts is 20/20 tautological; the pglite suites covered aggregate read scoping, not fetch-by-id across tenants. **As of #462:** workers/src/database.tenant-isolation.pglite.node.test.ts — "findProductById returns null for another organization's product id" seeds an `org-b` product, fetches it as `org-a`, and asserts null; the paired case asserts `org-b` still gets its own row, so the test cannot pass by returning null unconditionally | worker-equivalent-exists | Unlike the query-parameter rows, this attack surface **does** exist in the Worker: a product id arrives in the route path and is looked up. When this row was written, that lookup was `SELECT ... WHERE id = $1` with no organization predicate — the leak this row predicted was real and live on `main`. #462 added the predicate and the test. The Worker now also returns 404 rather than 403 for a foreign id, since a 403 confirms the id exists and product ids are `SERIAL` | UPDATED (#462): worker-equivalent-exists — was `worker-shaped-rewrite` on the grounds that this was a live, untested Worker surface. It was worse than untested: the predicate was absent and `GET /api/products/:id` returned any tenant's product. Fixed and covered in #462; verified by mutation (removing the predicate fails this test) |
| orgId query-parameter tampering is rejected (403 "Access denied") | multi-tenant-penetration.test.ts:248 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/utils/auth.test.ts, workers/src/middleware/auth.test.ts) — no Worker test supplies an organization id via query string, because no Worker code path reads one | retire | Two independent reasons. The surface is absent in the Worker (`index-minimal.ts:3352`). And the Express test sends no `Authorization` header, so its 403 is the unauthenticated path — it would pass identically with the tampering removed, and so does not currently test what it is named for | PROPOSED: retire — surface absent by construction in the Worker; the Express test also passes for an unrelated reason (no auth header sent) |
| Special characters and injection patterns in orgId are handled without leaking rows | multi-tenant-penetration.test.ts:274 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/upload/csv-parser.test.ts) — multi-tenant-isolation.test.ts:143 claims Neon `sql` parameterization but asserts nothing; csv-parser.test.ts covers upload parsing, not query-parameter sanitisation | retire | Same structural argument: five payloads (SQL, path traversal, XSS) are sent as `orgId`, a parameter the Worker never reads. General SQL-injection resistance in the Worker rests on Neon's tagged-template parameterization, which is a different property against a different surface and is not what this test measures | PROPOSED: retire — orgId sanitisation targets a parameter the Worker does not read; general parameterization is a separate untested property, recorded in the residual-concerns note below |
| Service-layer mass assignment: a product created through an org-A-scoped service belongs to org A and is invisible to org B | multi-tenant-penetration.test.ts:296 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/health.test.ts, workers/src/database.conformance.node.test.ts) — health.test.ts:489 mocks `createProduct` on a stub Database and asserts import-pipeline behaviour, not that the persisted row carries the caller's organization; handlers.test.ts is 20/20 tautological | worker-shaped-rewrite | The property — a created row is stamped with the authenticated organization and is not readable by another — is real, applies to the Worker's INSERT path, and is untested there. This row's Express assertions are genuine (they check the persisted `organizationId` and cross-org invisibility), so the gate is worth carrying forward | PROPOSED: worker-shaped-rewrite — write-path organization stamping has no real Worker test; re-test the penetration gate in Phase 3.2 |
| Mass assignment of organizationId in a POST body is rejected | multi-tenant-penetration.test.ts:321 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts) — no Worker test posts an `organizationId` in a request body, because no Worker code path reads one | retire | The Worker cannot honour an `organizationId` in a body: the only assignment is from `auth.organizationId` (`index-minimal.ts:3352`), so a body field of that name is inert. The Express test additionally sends `Bearer token:tenant1` against a Clerk-authenticated route and asserts 403, which is the auth-failure path rather than a demonstration that the body field was ignored | PROPOSED: retire — body-supplied organizationId is inert in the Worker by construction; the Express test also asserts an auth failure rather than the mass-assignment defence |
| IDOR: an org-A-scoped service returns null for org B's product id, while org B's own service returns it | multi-tenant-penetration.test.ts:339 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/handlers/handlers.test.ts, workers/src/database.dashboard.pglite.node.test.ts, workers/src/database.report.pglite.node.test.ts) — the pglite suites asserted aggregate and list scoping with seeded `org-b` rows but never a single-record fetch by a foreign id; multi-tenant-isolation.test.ts:39 claimed exactly this behaviour and asserted nothing. **As of #462:** workers/src/database.tenant-isolation.pglite.node.test.ts mirrors both halves of the Express assertion — the foreign id yields null for `org-a`, and `org-b` fetching its own id still receives the row | worker-equivalent-exists | This is the strongest-assertion test in the file — it checks both halves (foreign id yields null, own id yields the record), so it cannot pass by accident. That property is precisely why it was worth acting on rather than filing: the Worker path existed, was untested, and was in fact broken. The Worker equivalent was written to preserve both halves for the same reason | UPDATED (#462): worker-equivalent-exists — was `worker-shaped-rewrite`, called out here as the highest-value penetration row to rewrite. It was rewritten immediately rather than deferred to Phase 3.2, because the audit found the underlying IDOR was live: `findProductById` carried no organization predicate. Both halves of the Express assertion are preserved |
| Enumeration: responses do not reveal which organization IDs exist | multi-tenant-penetration.test.ts:367 | penetration | none found (searched: workers/src/utils/auth.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/utils/worker-response.test.ts) — worker-response.test.ts covers response envelope shape, not error-response uniformity across existing and non-existing organizations | retire | The assertion is `expect([200, 400, 403]).toContain(response.status)` with no `Authorization` header, so every request takes the same unauthenticated path and the check is satisfied regardless of whether responses differ for real versus fabricated org IDs. It does not measure enumeration resistance. The underlying concern is real but belongs to a rewritten test, not a port of this one | PROPOSED: retire — the assertion does not depend on the property it names (uniform 403 for all inputs, no auth header sent); residual concern recorded below |
| Null, undefined, and empty organizationId are all rejected with 403 | multi-tenant-penetration.test.ts:395 | penetration | workers/src/utils/auth.test.ts:348 — `authenticateWorkerRequest` rejects a request with no token; workers/src/utils/auth.test.ts:370 — rejects a token missing `organizationId`; workers/src/utils/auth.test.ts:165 — `extractOrganizationId` returns null for an empty-string organizationId; workers/src/middleware/auth.test.ts:75 — rejects a request without an Authorization header | worker-equivalent-exists | The behaviour actually exercised here is "a request with no usable organization context is rejected", and the Worker asserts each variant separately with real assertions: absent token, token without the claim, and empty-string claim. The Worker coverage is strictly stronger than the Express test, which reaches all three 403s via the same missing-header path | PROPOSED: worker-equivalent-exists — missing/empty organization context covered by utils/auth.test.ts:348, :370, :165 and middleware/auth.test.ts:75 |
| JWT with an invalid signature is rejected (403 "Invalid token") | multi-tenant-penetration.test.ts:412 | penetration | workers/src/utils/auth.test.ts:96 — `verifyJWT` returns null for a JWT signed with the wrong secret; workers/src/middleware/auth.test.ts:36 — "should reject invalid token signature" rejects a token whose signature does not verify | worker-equivalent-exists | The Worker coverage is stronger in kind: the Express test mocks `jwt.verify` to invoke its callback with a `JsonWebTokenError`, so it exercises the middleware's error handling rather than signature verification, whereas utils/auth.test.ts:96 signs with a genuinely wrong secret and verifies the real rejection | PROPOSED: worker-equivalent-exists — invalid-signature rejection covered by utils/auth.test.ts:96 (real wrong-secret verification) and middleware/auth.test.ts:36 |
| JWT with a tampered payload is rejected (403 "Invalid token") | multi-tenant-penetration.test.ts:435 | penetration | workers/src/utils/auth.test.ts:96 — wrong-secret verification returns null, which is the same code path a payload edit produces since editing the payload invalidates the signature; workers/src/utils/auth.test.ts:108 — returns null for a malformed JWT; workers/src/middleware/auth.test.ts:36 and :44 — reject invalid signature and malformed token | worker-equivalent-exists | Payload tampering is not a distinct mechanism: any edit to the payload breaks the HMAC, so the defence is signature verification, which the Worker tests with real crypto rather than a mocked throw | PROPOSED: worker-equivalent-exists — tampered-payload rejection is signature verification, covered by utils/auth.test.ts:96 and :108 with real signing |
| A valid JWT is accepted and the correct organization is extracted | multi-tenant-penetration.test.ts:457 | penetration | workers/src/utils/auth.test.ts:72 — `verifyJWT` verifies a valid JWT with all required fields; workers/src/utils/auth.test.ts:124 — `extractOrganizationId` extracts the organizationId from a valid token; workers/src/utils/auth.test.ts:321 — `authenticateWorkerRequest` returns a valid auth context with an active subscription | worker-equivalent-exists | The Worker asserts the positive path directly and specifically. The Express test asserts `[200, 403, 500]` contains the status — a 500 passes a test named "should accept valid JWT" — so the Worker equivalent is materially stronger and nothing is lost by retiring this one in its favour | PROPOSED: worker-equivalent-exists — valid-token acceptance and organization extraction covered by utils/auth.test.ts:72, :124, :321 with specific assertions |
| Expired JWT is rejected | multi-tenant-penetration.test.ts:490 | penetration | workers/src/utils/auth.test.ts:85 — `verifyJWT` returns null for an expired JWT; workers/src/middleware/auth.test.ts:228 — asserts the issued token carries an expiration claim | worker-equivalent-exists | The Worker builds a genuinely expired token and asserts verification fails, where the Express test mocks `jwt.verify` to throw `TokenExpiredError`. Same property, real mechanism | PROPOSED: worker-equivalent-exists — expired-token rejection covered by utils/auth.test.ts:85 with a real expired token |
| JWT missing required fields is rejected ("Missing tenant context" or "Malformed token") | multi-tenant-penetration.test.ts:513 | penetration | workers/src/utils/auth.test.ts:144 — `extractOrganizationId` returns null for a token without organizationId; workers/src/utils/auth.test.ts:165 — returns null for an empty-string organizationId; workers/src/utils/auth.test.ts:370 — `authenticateWorkerRequest` rejects a token missing organizationId | worker-equivalent-exists | The specific missing field that matters is the tenant claim, and the Worker covers both its absence and its empty-string form, plus the end-to-end rejection. The Express test omits `organizationId` and `tierLevel` together; the tier-level half is subscription resolution, covered by the next row | PROPOSED: worker-equivalent-exists — missing tenant claim covered by utils/auth.test.ts:144, :165, :370 |
| A JWT for an organization with no subscription is rejected ("subscription not configured") | multi-tenant-penetration.test.ts:540 | penetration | workers/src/utils/auth.test.ts:215 — `validateOrganizationStatus` rejects a null subscription; workers/src/utils/auth.test.ts:390 — `authenticateWorkerRequest` rejects a request when the subscription lookup fails; workers/src/utils/auth.test.ts:403 — rejects a request with a canceled subscription | worker-equivalent-exists | "Organization without subscription" is the null-subscription case, asserted directly at :215 and end-to-end at :390. The Worker additionally distinguishes canceled (:403) from absent, which the Express suite does not | PROPOSED: worker-equivalent-exists — no-subscription rejection covered by utils/auth.test.ts:215, :390, :403 |
| A tampered org_id in the route query is rejected while the JWT names another org | multi-tenant-penetration.test.ts:580 | penetration | none found (searched: workers/src/utils/auth.test.ts, workers/src/middleware/auth.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — no Worker test supplies an `org_id` outside the token, because no Worker code path reads one | retire | The Worker resolves the organization solely from `auth.organizationId` (`index-minimal.ts:3352`); a query `org_id` is never consulted, so it cannot override the token. Unlike the earlier tampering rows, this test does send a valid mocked token, so it genuinely exercises the Express precedence rule — but that rule exists because Express *does* read the parameter, and the Worker does not | PROPOSED: retire — token-versus-parameter precedence is meaningless in the Worker, which reads no organization id outside the verified JWT |
| SQL injection in the org_id route parameter is rejected (403 "Access denied") | multi-tenant-penetration.test.ts:616 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.conformance.node.test.ts) — multi-tenant-isolation.test.ts:143 asserts nothing (Finding 1); no Worker test submits an injection payload in any parameter | retire | Same absent surface as :196 and :248 — the injected value would have to reach a query through `org_id`, which the Worker never reads. Neon's tagged templates parameterize regardless, but that is a distinct property against distinct inputs | PROPOSED: retire — org_id injection point absent by construction in the Worker |
| Null-byte injection in org_id is rejected | multi-tenant-penetration.test.ts:639 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/utils/auth.test.ts) — no Worker test sends a null byte in any organization identifier, and no Worker path reads org_id from the query string | retire | Null-byte truncation attacks target a string that reaches a query or filesystem path; the Worker's organization id comes from the verified JWT and never from this parameter. The row asserts only a status code, with no assertion distinguishing null-byte handling from any other rejection | PROPOSED: retire — org_id null-byte surface absent by construction in the Worker |
| Path traversal in org_id is rejected | multi-tenant-penetration.test.ts:661 | penetration | none found (searched: workers/src/__tests__/multi-tenant-isolation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts:23-44 uses flat R2 keys with no directory semantics and asserts no traversal defence; no Worker path reads org_id from the query string | retire | Path traversal needs a filesystem, and the Worker has none — R2 keys are a flat namespace (established in Part 2's local-storage rows). Combined with the absent org_id read, there is no mechanism for this payload to act on | PROPOSED: retire — no filesystem and no org_id read in the Worker; traversal via this parameter is not expressible |
| Tenant isolation holds when org_id is supplied by a different method (POST body) | multi-tenant-penetration.test.ts:683 | penetration | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/handlers/handlers.test.ts, workers/src/utils/auth.test.ts, workers/src/__tests__/multi-tenant-isolation.test.ts) — no Worker test posts an `org_id` in a body; handlers.test.ts is 20/20 tautological | retire | This is the body-borne variant of :580 and fails for the same reason: `index-minimal.ts:3352` is the sole assignment and reads the token. A body `org_id` is inert. Note the Express route under test (`/api/secure/inventory/adjust`) is itself Express-only machinery | PROPOSED: retire — body-supplied org_id is inert in the Worker by construction |

**Residual concerns not carried by any row above.** Two properties this file gestures at are real,
are not preserved by retiring these rows, and are not covered anywhere in the Worker suite:
general SQL-injection resistance on the parameters the Worker *does* read (barcode, SKU, and
search terms, which reach Neon tagged templates), and uniformity of error responses across
existing and non-existing organizations. Neither is measured by the tests above — the injection
rows target a parameter the Worker ignores, and the enumeration row asserts a set membership that
holds regardless. They are recorded here so that "retire" is not later misread as "already
covered". Both belong in the Phase 3.2 rewrite scope.

---

### integration/multi-tenant-concurrency-load.test.ts

Nominally the backend's `concurrency` gate: six tests firing 10–100 simultaneous writes at an
organization sitting on a tier boundary, to prove limits hold under races.

**None of the six exercises application code.** Every operation is a direct `prisma.product.create`,
`prisma.user.create`, or `prisma.organizationUsage.update` issued by the test body itself. No
service, no controller, no route, no feature-gate middleware is on the path. The file's own comments
say so at `:110`, `:185`, `:436` and `:523` — *"Direct Prisma writes bypass feature-gate
enforcement"* — and the assertions were then written to match that reality rather than the test
names.

The result is a variant of Finding 1 worth naming separately, because it is not tautological and so
does not show up in a `expect(true).toBe(true)` sweep. These tests are **inverted**: the assertion
codifies the very outcome the name promises to prevent. `should prevent SKU overshoot under extreme
concurrent load` seeds 490 SKUs against a 500 cap, fires 100 concurrent creates, and asserts
`expect(finalCount).toBe(590)`. The test passes *because* the limit was exceeded, and would fail if
enforcement were ever added. It is not a gate; it is a regression test against having one.

Consequently **no row here can be retired "in favour of" Worker coverage, and none can be carried
forward as a preserved gate** — there is no gate to preserve. What must survive into Phase 3.2 is
the *property* each test is named for, none of which this file establishes and, per Finding 4, none
of which the Worker currently enforces either. Each row below therefore records the Express test as
`retire` (it constrains nothing) while naming the property in the Evidence cell so 3.2 has the list.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| SKU limit enforced at the 495/500 boundary under 10 concurrent creates | multi-tenant-concurrency-load.test.ts:39 | concurrency | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/catalogue-import-loadtest.node.test.ts) — the three feature-gate suites test `workers/src/utils/feature-gates.ts`, which per Finding 4 no production code imports and whose SQL cannot run against the schema; catalogue-import-loadtest.node.test.ts covers the bulk-import quota, a different mechanism on a different path | retire | The Express test asserts `finalCount` is 505 against a 500 cap and `successes` is 10 — it records that all ten creates succeeded past the limit, because `prisma.product.create` is called directly with no gate in front of it. It does not test enforcement; it documents its absence. The property it is named for — a concurrent create at the boundary is rejected — is real and unimplemented in the Worker (`handleCreateProduct`, index-minimal.ts:2266, performs no limit check), so it belongs in 3.1 as implementation, not in 3.2 as a port | PROPOSED: retire — asserts the overshoot rather than the limit; no application code under test. Property "interactive product create is gated on max_skus" recorded for Phase 3.1 implementation, not test migration |
| SKU overshoot prevented under 100 concurrent creates from 490/500 | multi-tenant-concurrency-load.test.ts:121 | concurrency | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/catalogue-import-loadtest.node.test.ts) — no Worker test issues concurrent interactive creates against a tier cap; the only concurrency coverage in the Worker suite is catalogue-import batch resume, which is sequential | retire | The clearest instance of the inversion: the test is named "should prevent SKU overshoot" and its sole assertion is `expect(finalCount).toBe(590)` — 90 SKUs past a 500 cap. Adding enforcement to the Worker would make the *equivalent* of this test fail. Migrating the assertion would import a defect | PROPOSED: retire — the assertion is the negation of the behaviour named; migrating it would encode the bug. Property folded into the 3.1 max_skus implementation item |
| SKU counts stay isolated between two tenants writing concurrently | multi-tenant-concurrency-load.test.ts:193 | tenant-isolation | workers/src/database.tenant-isolation.pglite.node.test.ts — `countProducts` is scoped to the caller's organization with foreign `org-b` rows seeded that would inflate the count if the predicate were dropped, asserted on identity rather than count alone (#462); workers/src/database.dashboard.pglite.node.test.ts:110 — `OTHER_ORG` rows excluded from the requested organization's counts | worker-equivalent-exists | Two properties are tangled in this test and they separate cleanly. The **isolation** half — org1's count reflects only org1's writes — is genuine and is covered by real-SQL cross-org scoping from #462. The **concurrency** half is not tested at all: the assertion is `org1Successes` is 5 and `org2Successes` is 5, i.e. every write succeeded past a 498/500 boundary, so the run proves only that Prisma inserts rows from two orgs without mixing them. Concurrency adds nothing the sequential #462 tests do not already establish | PROPOSED: worker-equivalent-exists for the isolation property only — per-org count scoping covered by database.tenant-isolation.pglite.node.test.ts and database.dashboard.pglite.node.test.ts:110. The concurrency framing carries no additional constraint and is not preserved |
| Storage quota not overshot by 10 concurrent 20MB uploads from 900MB/1GB | multi-tenant-concurrency-load.test.ts:310 | concurrency | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/upload/upload-handlers.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test asserts a storage quota is enforced on upload; `storage_bytes` is one of the four dead `checkUsageLimit` branches from Finding 4 | retire | The test implements the check-then-update itself, inside the test body: it reads `organizationUsage`, compares `storageUsedBytes + size` against a hard-coded `1073741824`, then increments. That is a textbook TOCTOU, and it is the *test's* race, not the application's — no product code participates. The final assertion then reconciles the total against however many branches happened to win, so it holds for any interleaving. The underlying property (storage quota enforced atomically across concurrent uploads) is real, is unimplemented in the Worker, and needs the counter from Finding 4 point 3 to exist first | PROPOSED: retire — the race under test is constructed by the test body; no application code participates and the assertion is satisfied by any interleaving. Blocked behind the Finding 4 counter gap |
| User limit enforced under 5 concurrent invitations at 1/1 | multi-tenant-concurrency-load.test.ts:380 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts) — minimal-api-routes.test.ts drives `POST /api/users` against a mocked `db.sql` and asserts the success and duplicate-username paths, never the 402 limit path; usage-limits-integration.test.ts:15-54 asserts per-tier limit constants, not enforcement, and 4 of its 13 tests are tautological | retire | This is the row that led to Finding 4 point 3. The Express test asserts `failures` is 0 and `userCount` is 6 against a `maxUsers` of 1 — again the inversion. Working it against the Worker found the Worker *does* have an inline gate (`index-minimal.ts:2673`, returning 402), which is more than the Express path has — but it compares `active_users` against `max_users`, and `active_users` is written exactly once, as a literal `0`, by the lazy upsert at `index-minimal.ts:3084`, with no `UPDATE` and no trigger anywhere. The gate is structurally present and permanently open. Separately it is a bare read-then-insert with no transaction or unique constraint, so it would race even with a correct counter | PROPOSED: retire — the Express assertion codifies the limit being exceeded. The Worker's own gate is recorded as a Phase 3.1 defect (stale `active_users`, plus an unguarded read-then-insert), not as an equivalent |
| ACID consistency under 20 product creates and 5 user creates interleaved | multi-tenant-concurrency-load.test.ts:449 | concurrency | none found (searched: workers/src/database.conformance.node.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-import-upsert.node.test.ts exercises real transactional upsert semantics against pglite, but on the import path and without concurrency | retire | The test is named for ACID properties and asserts `productCount` is 20 and `userCount` is 5 — that 25 independent inserts all landed. No transaction is opened, no invariant spans the two tables, and no rollback is exercised, so there is nothing atomic, consistent, isolated or durable under test beyond Postgres inserting rows. Both tier limits (2000 SKUs, 3 users) are set high enough or ignored such that neither is approached — the user limit is 3 with 5 created, and the comment at `:523` again notes the gate is bypassed | PROPOSED: retire — no transaction and no cross-table invariant under test; the name describes properties the body does not exercise |

**What this file's retirement costs, stated explicitly.** Retiring all six rows removes no coverage,
because the file constrains no application behaviour. But it also means the `concurrency` named gate
required by task 2.2 has **no surviving source** — the backend never had a working one. Phase 3.2
must write it from the property list above rather than port it, and Phase 3.1 must first implement
the enforcement it would test: an interactive `max_skus` check on `handleCreateProduct`, a
maintained `organization_usage.active_users`, and atomicity (a transaction, a conditional
`UPDATE ... WHERE active_users < max_users`, or a unique/check constraint) on both paths. Recorded
here so the gate is not later reported as satisfied on the strength of a green Express suite.

---

### integration/multi-tenant-usage-limits.test.ts

The backend's `feature-limits` gate, and — unlike the concurrency file above — **largely a real
one.** It builds a throwaway Express app per test, mounts the production
`checkUsageLimit('max_skus'|'max_users')` middleware from
`backend/src/middleware/feature-gate.middleware.ts`, and routes through the real `ProductService`
and `StorageQuotaService`. Fourteen of its twenty tests constrain production behaviour.

That changes the conclusion relative to the concurrency file. Express here has a **working,
transactional** SKU gate: `ProductService.createProduct` (`product.service.ts:136-170`) opens a
`$transaction`, re-reads usage inside it, throws `SKU limit reached for this organization` when
`totalSkus >= maxSkus`, and increments the counter in the same transaction. The strongest test in
this file (`:554`) fires three concurrent `createProduct` calls at a limit of 2 and asserts exactly
two fulfil and one rejects with that message — a genuine atomicity assertion that would fail if the
check-and-increment were split.

**The Worker has no counterpart to any of it.** Per Finding 4: `handleCreateProduct`
(`index-minimal.ts:2266`) performs no limit check; `organization_usage.total_skus` is never written;
`storage_used_bytes` is read for display (`index-minimal.ts:3027`) and seeded as a literal `0`
(`:3092`) but never incremented on upload; and the sole inline gate (`max_users`) reads a counter
nothing maintains. So the rows below are not a test-migration list — they are a **Phase 3.1
implementation list** that Phase 3.2 then covers. Recording them as `worker-shaped-rewrite` is
correct but understates the dependency, so each such row names the missing implementation.

Six rows are `retire`: the storage-tracking tests at `:628`, `:660`, `:699`, `:734`, `:767` perform
the increment or decrement **in the test body** with a direct
`prisma.organizationUsage.update({ increment })`, then assert the value they just wrote. `:734` is
labelled an idempotency test but its comment concedes the point — *"In production,
StorageQuotaService.markUploadDeleted checks status first"* — and the body never calls it. `:506`
is a partial case, recorded as such.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Product creation is allowed at 499/500 SKUs and increments the counter to 500 | multi-tenant-usage-limits.test.ts:142 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/minimal-api-routes.test.ts) — per Finding 4 the three feature-gate suites cover a module with no production importer whose SQL cannot run; minimal-api-routes.test.ts drives `POST /api/products` against a mocked `db.sql` and asserts only the created-row shape and the 409 duplicate path | worker-shaped-rewrite | Genuine: the real middleware admits the request and the real `ProductService` transaction increments `totalSkus` to exactly 500. Both halves matter — the allow path and the counter side effect. The Worker implements neither; `handleCreateProduct` calls `db.createProduct` with no gate and no counter write | PROPOSED: worker-shaped-rewrite — blocked on Phase 3.1 implementing a `max_skus` check plus `total_skus` maintenance on the Worker's interactive create path; there is nothing to test until that exists |
| Product creation is blocked at 500/500 SKUs with 403 and the counter does not move | multi-tenant-usage-limits.test.ts:169 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/catalogue-import-loadtest.node.test.ts) — catalogue-import-loadtest.node.test.ts asserts the bulk-import quota rejects an over-limit catalogue, which is a different mechanism (a `max_skus_snapshot` taken at job creation) on a path that does not touch `organization_usage` | worker-shaped-rewrite | The strongest single-request assertion in the file: 403, `message` containing both "Usage limit reached" and "max_skus", `currentUsage` 500, `limit` 500, `upgradeCTA` present, and a re-read confirming `totalSkus` is still 500 so the rejection had no side effect. That last assertion is the one most worth preserving — a gate that rejects but still increments is a slow leak | PROPOSED: worker-shaped-rewrite — preserve all three parts (status, error body shape, no counter side effect) once Phase 3.1 adds the Worker gate |
| The 403 response carries an upgrade CTA and upgrade URL | multi-tenant-usage-limits.test.ts:199 | feature-limits | none found (searched: workers/src/utils/worker-response.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/utils/feature-gates.test.ts) — worker-response.test.ts covers the success/error envelope but has no upgrade-CTA field; `formatUsageLimitCTA` exists in the dead module and is exercised only by workers/src/utils/feature-gates.test.ts:288 | worker-shaped-rewrite | A genuine response-contract assertion (`upgradeCTA` contains "Upgrade", `upgradeUrl` is `/subscription/upgrade`) that the frontend presumably renders. It duplicates `:169`'s status and limit fields, so the distinct content is the CTA pair. Worth carrying as a contract row rather than folding into `:169`, because the Worker's error envelope has no slot for it today | PROPOSED: worker-shaped-rewrite — the limit-exceeded error contract (upgradeCTA + upgradeUrl) has no Worker equivalent and no field in the Worker error envelope; needs a response-shape decision in 3.1, not just a test |
| Product creation is allowed at 1999/2000 SKUs on the professional tier | multi-tenant-usage-limits.test.ts:226 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts:191-207 asserts tier level determines feature access but not numeric SKU caps, and 7 of its 14 tests are tautological | worker-shaped-rewrite | Same property as `:142` at a different tier, and the tier dimension is load-bearing rather than redundant: it proves the cap is read per-organization rather than hard-coded. Note the Worker's tier caps differ from the backend's — `LAUNCH_TIER_LIMITS` (index-minimal.ts:3980) gives professional 50000 SKUs against the backend's 2000 — so a migrated test must take its expected numbers from the Worker's table, not port these literals | PROPOSED: worker-shaped-rewrite — preserve the per-tier dimension, but re-derive the numeric caps from LAUNCH_TIER_LIMITS; the backend's 500/2000 values do not match the Worker's 500/5000/50000/250000 |
| Product creation is blocked at 2000/2000 SKUs on the professional tier | multi-tenant-usage-limits.test.ts:253 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test asserts a tier-specific numeric SKU cap on an interactive create | worker-shaped-rewrite | The professional-tier block path, again asserting the counter does not move after rejection. Together with `:169` this establishes that the gate is driven by the organization's own limit rather than a constant, which is the property that survives the differing cap values between the two systems | PROPOSED: worker-shaped-rewrite — same dependency as :169; keep the two-tier pairing so the test proves the cap is per-organization |
| User creation is blocked when the starter tier is at 1/1 users | multi-tenant-usage-limits.test.ts:284 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/__tests__/org-status-integration.test.ts) — minimal-api-routes.test.ts exercises `POST /api/users` against a mocked `db.sql` and covers the 201 and 409-duplicate paths only, never the 402 limit path; org-status-integration.test.ts is 16/19 tautological | worker-shaped-rewrite | This is the one row where the Worker has *some* implementation — `index-minimal.ts:2673` returns 402 when `active_users >= max_users` — and it is the row that exposed Finding 4 point 3. The gate cannot fire, because `active_users` is written exactly once as a literal `0` by the lazy upsert at `index-minimal.ts:3084` and there is no `UPDATE` and no trigger anywhere. Recording this `worker-equivalent-exists` on the strength of the 402 branch existing would retire a real gate against code that is structurally present and permanently open — the same mistake Finding 1 warns about, one level up from a tautological test | PROPOSED: worker-shaped-rewrite — the Worker's 402 branch exists but is unreachable while `active_users` stays 0. Phase 3.1 must maintain the counter (and use a conditional UPDATE or constraint, per the concurrency section) before this can be tested |
| User creation is allowed when under the max_users limit | multi-tenant-usage-limits.test.ts:324 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — the Worker's 201 path for `POST /api/users` is covered in minimal-api-routes.test.ts against a mocked `db.sql`, but with no usage row in play, so it does not exercise the limit branch either way | worker-shaped-rewrite | The paired allow case for `:284`. It is what stops a limit implementation from passing by rejecting everything — the same both-halves discipline #462 and #466 used. Must be migrated together with `:284` or neither is meaningful | PROPOSED: worker-shaped-rewrite — migrate as a pair with :284 so the gate cannot pass by rejecting unconditionally |
| The professional tier allows a 5th user at 4/5 | multi-tenant-usage-limits.test.ts:359 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/subscription-validation.test.ts) — no Worker test varies `max_users` by tier | worker-shaped-rewrite | The per-tier dimension for users, mirroring `:226` for SKUs. As with the SKU rows, the Worker's user caps are not the backend's, so the migrated test must read the limit from the organization's row rather than assert 5 | PROPOSED: worker-shaped-rewrite — preserve the per-tier dimension; derive the expected cap from the organization's usage row, not the backend literal |
| User creation is blocked when the professional tier is at 5/5 | multi-tenant-usage-limits.test.ts:395 | feature-limits | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker test reaches the 402 branch at index-minimal.ts:2683 | worker-shaped-rewrite | The professional block path. Same unreachable-gate dependency as `:284` | PROPOSED: worker-shaped-rewrite — same Finding 4 dependency as :284 |
| A usage warning is attached at 80% of the max_users limit | multi-tenant-usage-limits.test.ts:432 | feature-limits | none found (searched: workers/src/utils/worker-response.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts) — no Worker code emits a usage warning on a success response; `checkUsageLimit` in the dead module returns `percentageUsed` but no caller consumes it | worker-shaped-rewrite | A genuine and easily-lost property: the middleware attaches `res.locals.usageWarning` with `percentageUsed` on an *allowed* request approaching the cap, so the client can warn before the hard stop. This is a distinct capability from the 403, it has no Worker implementation and no place in the Worker's response envelope, and nothing else in the manifest carries it | PROPOSED: worker-shaped-rewrite — soft-warning-at-80% is a separate capability from the hard limit; flag explicitly for Phase 3.1 so it is not lost when the 402/403 gate is built |
| The usage counter increments atomically across three sequential product creations | multi-tenant-usage-limits.test.ts:473 | feature-limits | none found (searched: workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.conformance.node.test.ts) — catalogue-import-upsert.node.test.ts maintains `processed_count` on the import job against real pglite SQL, which is a per-job counter, not `organization_usage` | worker-shaped-rewrite | Genuine: three requests through the real middleware and service, then `totalSkus` is 3 and the product count is 3. The value is the cross-check — counter and row count agree — which catches an increment that fires without a row or vice versa. The Worker maintains no such counter at all, so both sides of the cross-check are missing | PROPOSED: worker-shaped-rewrite — preserve the counter/row-count cross-check; requires the Worker to maintain total_skus first |
| The counter stays accurate across creations and deletions | multi-tenant-usage-limits.test.ts:506 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.conformance.node.test.ts) — no Worker test asserts a usage counter tracks deletions | worker-shaped-rewrite | **Partial.** The five creations go through the real service, but the two deletions are direct `prisma.product.delete` calls and the decrement is then applied by the test itself (`totalSkus: { decrement: 2 }`), so the final assertion of 3 is partly satisfied by the test's own arithmetic rather than by production code. The real decrement does exist — `product.service.ts:243` decrements inside the delete transaction — but this test does not exercise it. The property is worth carrying; the assertion should be rewritten to delete through the service | PROPOSED: worker-shaped-rewrite — the property (counter tracks deletions) is real and lives at product.service.ts:243, but this test applies the decrement itself; rewrite to exercise the service delete path rather than porting the assertion |
| Concurrent product creations cannot exceed the SKU limit (3 requests, limit 2) | multi-tenant-usage-limits.test.ts:554 | concurrency | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/catalogue-import-loadtest.node.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.conformance.node.test.ts) — catalogue-import-loadtest.node.test.ts is the Worker's only load-shaped test and runs batches sequentially against a per-job snapshot, so it does not exercise concurrent check-and-increment | worker-shaped-rewrite | **The most valuable row in this file, and the only working `concurrency` assertion anywhere in the backend suite** — the file named for that gate (integration/multi-tenant-concurrency-load.test.ts, above) has none. Three concurrent `createProduct` calls against a limit of 2 assert exactly two fulfil, exactly one rejects, the rejection message is `SKU limit reached`, and the final counter is exactly 2. It fails if the check and the increment are ever split, which is precisely the TOCTOU the Worker's `max_users` gate has today. This row, not anything in the concurrency file, is what the `concurrency` named gate should be reproduced from in Phase 3.2 | PROPOSED: worker-shaped-rewrite — designate this the source for the `concurrency` named gate. Requires Phase 3.1 to implement an atomic check-and-increment on the Worker create path (conditional UPDATE or constraint; Neon has no `$transaction` equivalent, so the shape will differ from product.service.ts:136) |
| storageUsedBytes increments on upload | multi-tenant-usage-limits.test.ts:628 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts) — the Worker reads `storage_used_bytes` for the usage response (index-minimal.ts:3027) and seeds it as a literal 0 (index-minimal.ts:3092) but never increments it, so there is no behaviour to have a test for | retire | The test creates an `upload` row and then applies the increment itself with a direct `prisma.organizationUsage.update({ increment: 1024 })`, commented *"simulating StorageQuotaService.recordUpload"*, before asserting the value is 1024. It asserts its own arithmetic; no production code is on the path. The genuine version of this property is `:813`, which calls the service | PROPOSED: retire — the increment is performed by the test body; superseded by :813, which exercises StorageQuotaService.recordUpload |
| Storage accumulates across multiple uploads | multi-tenant-usage-limits.test.ts:660 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker code writes `storage_used_bytes` on any upload path | retire | Two test-applied increments of 1024 and 2048, then `expect(3072)`. This asserts that Prisma's `increment` accumulates, not that the application tracks storage. Retiring it loses nothing that `:813` does not cover better | PROPOSED: retire — asserts Prisma increment arithmetic applied by the test body; no application code under test |
| storageUsedBytes decrements when an upload is deleted | multi-tenant-usage-limits.test.ts:699 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker code decrements `storage_used_bytes`, and the Worker has no upload-deletion route recorded in the 2.1 route matrix | retire | Same shape: the test sets storage to 1024, marks the upload deleted, applies its own decrement, and asserts 0. Superseded by `:840`, which drives `StorageQuotaService.markUploadDeleted` | PROPOSED: retire — the decrement is applied by the test body; superseded by :840 |
| Double-deleting an upload does not decrement storage twice | multi-tenant-usage-limits.test.ts:734 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-import-upsert.node.test.ts covers idempotent resume on the import path, a different surface | retire | Named for idempotency but never attempts the second delete. It seeds an upload already at `status: 'deleted'`, sets storage to 0, reads the row back, and asserts the status is `'deleted'` and storage is 0 — both values the test itself wrote. Its own comment states the real guard is elsewhere: *"In production, StorageQuotaService.markUploadDeleted checks status first."* The property is real and worth keeping in scope, but this test does not establish it and porting it would carry a green result for an unverified guard | PROPOSED: retire — asserts fixture values it wrote and never performs the second delete; the idempotency property is recorded in the residual note below rather than carried by this row |
| Storage tracking is isolated per organization | multi-tenant-usage-limits.test.ts:767 | tenant-isolation | none found (searched: workers/src/database.tenant-isolation.pglite.node.test.ts, workers/src/database.tenant-isolation-writes.pglite.node.test.ts, workers/src/upload/upload-handlers.test.ts) — the #462 and #466 suites cover product, inventory, store-area and user scoping, but `organization_usage` is not among the tables they exercise | retire | The test writes 1024 to org A and 2048 to org B with two direct updates keyed on `organizationId`, then asserts each reads back what it wrote. That is Prisma honouring a `where` clause on a unique column — the same shape retired at multi-tenant-cross-tenant-isolation.test.ts:320. No application code chooses the organization. The genuine isolation concern for usage rows is that a request cannot read or increment another tenant's counters, which requires a Worker code path that does not yet exist | PROPOSED: retire — asserts Prisma honours a unique `where` clause, mirroring the retirement at multi-tenant-cross-tenant-isolation.test.ts:320; the real per-org usage-isolation property is recorded in the residual note below |
| StorageQuotaService.recordUpload creates the upload row and increments storage atomically | multi-tenant-usage-limits.test.ts:813 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/catalogue-sync-upload-retail.node.test.ts) — upload-handlers.test.ts covers R2 key construction and multipart handling with no storage accounting; catalogue-sync-upload-retail.node.test.ts covers retail ingestion | worker-shaped-rewrite | Genuine, and the row that should survive from the storage group: it calls the real service and asserts both effects — `storageUsedBytes` becomes 5120 **and** the upload row exists with `status: 'processing'`. The paired assertion is what makes it a real atomicity check rather than an arithmetic one. The Worker has an upload path but performs no storage accounting on it, so this is an implementation gap before it is a coverage gap | PROPOSED: worker-shaped-rewrite — carry this and :840 as the storage-quota gate; requires Phase 3.1 to add storage accounting to the Worker upload path |
| StorageQuotaService.markUploadDeleted decrements storage and marks the row deleted | multi-tenant-usage-limits.test.ts:840 | feature-limits | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker route deletes an upload or adjusts storage | worker-shaped-rewrite | The genuine counterpart to `:699`: records an upload through the service, asserts 4096, deletes through the service, asserts 0 and `status: 'deleted'`. Both directions exercised through production code in one test, which is what `:628`/`:699` only simulated | PROPOSED: worker-shaped-rewrite — pair with :813; same Phase 3.1 dependency on Worker-side storage accounting |

**Residual concerns not carried by any row above.** Two properties this file gestures at survive the
five storage retirements and are covered nowhere: **double-delete idempotency** (`:734` names it but
never performs the second delete, and its own comment locates the real guard in
`StorageQuotaService.markUploadDeleted`), and **per-organization isolation of usage counters**
(`:767` asserts only that Prisma honours a unique `where`). Both belong in Phase 3.2 scope. The
second one is worth flagging to the tenant-isolation gate specifically: `organization_usage` is not
among the tables covered by the #462/#466 suites, so when the Worker gains counter maintenance it
will gain a new cross-tenant write surface with no existing test to inherit.

---

### integration/multi-tenant-feature-gates.test.ts

The second half of the backend's `feature-limits` gate, covering **feature** gating (does this tier
get this endpoint at all?) rather than the **usage** gating of the file above. Nine tests, mounting
the production `requireFeature('advanced_analytics')` and `checkUsageLimit('max_skus')` middleware on
a throwaway app. Seven are genuine.

**The Worker has no feature-gating concept in production.** This is a stronger statement than
Finding 4's "the module is unreachable", and it was checked directly rather than inferred: every use
of tier in `index-minimal.ts` is a *quota* or a *display* value — `getOrganizationLaunchTier` feeds
the usage response (`:3114`), the catalogue-import quota snapshot (`:3458`, `:3577`), and
subscription reporting (`:3406`). **No route is gated on tier.** All twelve
`/api/reports/*` handlers (`index-minimal.ts:229-236` and following) are reachable by any
authenticated caller regardless of subscription level, which is exactly what
`requireFeature('advanced_analytics')` exists to prevent in Express. This is a deliberate product
question for Phase 3.1, not obviously a defect — but it must be decided rather than inherited.

**A vocabulary mismatch blocks direct migration.** This file treats `premium` as a first-class tier
and seeds `tierFeatureFlag` rows for it. The Worker's `LaunchTier` is
`free | starter | professional | enterprise`, with `premium` and `concierge` retained only as
legacy bridges that `normalizeLaunchTier` folds into `professional`/`enterprise`
(`utils/feature-gates.ts:69-71`). A migrated test asserting premium-tier behaviour would be
asserting against a tier the Worker deliberately does not have.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Starter tier is blocked from advanced analytics with 403 | multi-tenant-feature-gates.test.ts:160 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — feature-gates-integration.test.ts:118 exercises `requireFeatureAccess` from the dead module (Finding 4) and 2 of its 8 tests are tautological; no Worker test asserts a route is refused on tier grounds, because no Worker route is | worker-shaped-rewrite | Genuine: the real `requireFeature` middleware returns 403 with `message` naming the feature, `currentTier`, `upgradeCTA` and `upgradeUrl`. The property — a tier below the threshold cannot reach the endpoint — has no Worker implementation at all, not merely no Worker test. Whether the Worker *should* gate reports by tier is a product decision that Phase 3.1 must make explicitly | PROPOSED: worker-shaped-rewrite — blocked on a Phase 3.1 product decision, since no Worker route is tier-gated today and all /api/reports/* handlers are open to any authenticated caller |
| Premium tier is allowed through to advanced analytics with 200 | multi-tenant-feature-gates.test.ts:173 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/database.report.pglite.node.test.ts) — database.report.pglite.node.test.ts covers report SQL correctness against real data but performs no tier check, since the handlers do none | worker-shaped-rewrite | The paired allow case for `:160`, and the reason the pair is worth keeping: a gate that refuses every tier would pass `:160` alone. Note the tier used is `premium`, which the Worker folds into `professional` — the migrated pair must pick two tiers that exist on either side of whatever threshold 3.1 chooses | PROPOSED: worker-shaped-rewrite — migrate as a pair with :160 so the gate cannot pass by refusing everything; re-express `premium` as a LaunchTier the Worker actually has |
| The 403 body carries feature, currentTier, upgradeCTA and upgradeUrl | multi-tenant-feature-gates.test.ts:183 | feature-limits | none found (searched: workers/src/utils/worker-response.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts) — worker-response.test.ts covers the Worker's error envelope, which has no feature/currentTier/upgradeCTA/upgradeUrl fields; `formatFeatureUpgradeCTA` lives in the dead module | worker-shaped-rewrite | A response-contract row rather than a behaviour row: it pins five body fields the frontend consumes, including that `upgradeCTA` names the specific feature. It is the feature-gating twin of multi-tenant-usage-limits.test.ts:199, and carries the same implication — the Worker's error envelope needs new fields before this can be expressed | PROPOSED: worker-shaped-rewrite — same envelope-extension dependency as multi-tenant-usage-limits.test.ts:199; decide the two error contracts together in 3.1 so the shapes stay consistent |
| Feature flags are seeded correctly for starter and premium tiers | multi-tenant-feature-gates.test.ts:200 | — | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/feature-gates-integration.test.ts, workers/src/database.conformance.node.test.ts) — the Worker has no `tier_feature_flags` reader; its tier capabilities are the hard-coded `tierFeatureMap` and `LAUNCH_TIER_LIMITS` tables | retire | Asserts the test's own fixture: `seedFeatureFlags` writes `advanced_analytics` as `false` for starter and `true` for premium, and the test reads those two rows back. It is also conditionally seeded (`if (existingFlags === 0)`), so against a database another suite populated it asserts rows this test never wrote — passing or failing on suite ordering rather than on behaviour. Worth noting for 2.4: the authoritative 54-row `tier_feature_flags` set now lives in `src/database/migrations/seed.ts:34` and is enforced by `migrate:verify`, which is the real guard for this data | PROPOSED: retire — asserts conditionally-seeded fixture data, with a suite-ordering dependency; the authoritative flag set is covered by migrate:verify against seed.ts:34 |
| A request with no organizationId or tierLevel is denied with "Missing tenant context" | multi-tenant-feature-gates.test.ts:228 | authorization-precedence | workers/src/utils/auth.test.ts:348 — `authenticateWorkerRequest` rejects a request with no token; workers/src/utils/auth.test.ts:370 — rejects a token missing `organizationId`; workers/src/utils/auth.test.ts:165 — `extractOrganizationId` returns null for an empty-string organizationId; workers/src/middleware/auth.test.ts:75 — rejects a request without an Authorization header | worker-equivalent-exists | The property is "no usable tenant context means no access", and the Worker asserts each variant separately with real assertions rather than reaching all of them through one missing-header path. This is the same resolution reached for multi-tenant-penetration.test.ts:395, and for the same reason — the Worker coverage is strictly stronger. Note the Worker enforces this in the auth layer rather than the feature-gate layer, so the ordering the Express test implies (tenant context checked before the feature gate) holds a fortiori: there is no request that reaches a Worker handler without it | PROPOSED: worker-equivalent-exists — missing tenant context covered by utils/auth.test.ts:348, :370, :165 and middleware/auth.test.ts:75; enforced at the auth layer, so it precedes any gate by construction |
| Starter tier is blocked from creating the 501st product at 500/500 | multi-tenant-feature-gates.test.ts:260 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/minimal-api-routes.test.ts, workers/src/__tests__/catalogue-import-loadtest.node.test.ts) — same absence as multi-tenant-usage-limits.test.ts:169; `handleCreateProduct` (index-minimal.ts:2266) performs no limit check | worker-shaped-rewrite | Duplicates multi-tenant-usage-limits.test.ts:169's property with one addition: it asserts `limitKey` is `max_skus` as a distinct body field, where the other file asserts the limit key only as substring of `message`. Structured over stringly-typed is the better contract to carry forward | PROPOSED: worker-shaped-rewrite — fold into the :169 rewrite, preserving the structured `limitKey` field rather than the message-substring assertion |
| Starter tier is allowed to create a product at 400/500 | multi-tenant-feature-gates.test.ts:278 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/minimal-api-routes.test.ts) — the Worker's 201 path exists but with no limit branch to be under | worker-shaped-rewrite | The paired allow case for `:260`, duplicating multi-tenant-usage-limits.test.ts:142 without the counter-increment assertion that makes that one stronger. Migrate the other | PROPOSED: worker-shaped-rewrite — subsumed by the multi-tenant-usage-limits.test.ts:142 rewrite, which additionally asserts the counter side effect |
| A usage warning is shown at 80% usage (400/500 SKUs) | multi-tenant-feature-gates.test.ts:292 | feature-limits | none found (searched: workers/src/utils/worker-response.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/__tests__/usage-limits-integration.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker code emits a usage warning on a success response | retire | The test asserts `response.body.message` is `'Product created'` — byte-identical to `:278`, whose fixture is also 400/500. It tests nothing `:278` does not, and specifically not the warning it is named for. Its own comment concedes this: *"Warning is attached to res.locals.usageWarning // In a real scenario, this would be included in response headers or body"* — the middleware sets `res.locals` but this app never reads it. The genuine version is multi-tenant-usage-limits.test.ts:432, which does read `res.locals.usageWarning` and asserts `percentageUsed` | PROPOSED: retire — an exact duplicate of :278 under a name about warnings; the warning is never read. Property preserved by multi-tenant-usage-limits.test.ts:432 instead |
| A high-limit tier can create products well past the lower tier's cap | multi-tenant-feature-gates.test.ts:308 | feature-limits | none found (searched: workers/src/utils/feature-gates.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker test varies the SKU cap by tier on an interactive create | worker-shaped-rewrite | Weak but not hollow: it does drive the real middleware and asserts 201 at 1500 SKUs. Two defects to fix rather than port — the test is named for the **Professional** tier but its fixture is `orgPremium` at `tier: 'premium'`, and `maxSkus` is the arbitrary literal `999999` rather than the tier's actual limit, so it would pass for any tier whose cap exceeds 1500. The property worth keeping is that the cap is read per-organization, which multi-tenant-usage-limits.test.ts:226 asserts more precisely | PROPOSED: worker-shaped-rewrite — subsumed by the multi-tenant-usage-limits.test.ts:226 rewrite; do not port the name/fixture mismatch (named Professional, seeded premium) or the 999999 literal |

**Carried forward from this file.** Only two things survive that the usage-limits file does not
already cover better: **tier-based route gating exists in Express and does not exist in the Worker**
(rows `:160`/`:173`), and the **structured `limitKey` field** in the limit-exceeded body (`:260`).
Everything else duplicates `multi-tenant-usage-limits.test.ts` at lower assertion strength, and the
two retirements are a fixture read-back and an exact duplicate under a misleading name. Recorded so
Phase 3.2 rewrites the pair once rather than twice.

---

### integration/webhook.edge-cases.test.ts

The backend's `webhook-security` and `scheduled-job-idempotency` gates. Sixteen tests driving the
real `WebhookService` handlers against a real Prisma client with the Stripe **SDK** mocked — which
is correct rather than a smell: the external API is the thing that must be faked, and every handler,
every database write, and every error path is genuine. This is the strongest file in Part 4. No row
is retired for being hollow.

**Every Stripe row targets a Worker surface that does not exist.** The Worker handles Clerk
webhooks only; `POST /api/webhooks/stripe` is net-new work, already recorded at task 3.1. There is
no `stripe` import anywhere in `workers/src` outside a CSP directive in
`middleware/security-headers.middleware.ts` and an unrelated string in `utils/auth.ts`. So these
rows are not "untested in the Worker" — they are unimplemented, and the manifest's job is to make
sure the Stripe handler is built against this list rather than from scratch.

**Finding 5 — the Worker's one webhook idempotency mechanism is check-then-act, and untested.**
Established by working the two duplicate-event rows (`:183`, `:199`) against the Clerk handler,
which is the only webhook the Worker has and therefore the template the Stripe handler will copy.
`handleClerkWebhook` (`clerk/webhook-handler.ts`) runs three unrelated statements with no
transaction:

```
isNewClerkWebhookEvent(sql, svixId)   -> SELECT ... WHERE id = $1   (true)
processClerkWebhookEvent(sql, event)  -> all side effects
markClerkWebhookEventProcessed(...)   -> INSERT ... ON CONFLICT (id) DO NOTHING
```

Two concurrent deliveries of the same Svix event both observe `isNew === true`, because the marker
is written *after* processing. The `ON CONFLICT` deduplicates the marker row, not the work. Svix
delivers at-least-once and retries on timeout or 5xx, so concurrent redelivery is the expected case,
not a pathological one.

It is also not harmless. `ensureTrialSubscription` (`clerk/clerk-persistence.ts:97-102`) is itself a
check-then-insert — `if (existing.length > 0) return;` followed by an `INSERT INTO
subscription_tiers` with **no** `ON CONFLICT` — so a doubled `organization.created` delivery can
write two `trialing` rows for one organization.

A repo-wide search for `isNewClerkWebhookEvent`, `markClerkWebhookEventProcessed` and
`clerk_webhook_events` across `workers/src/**/*.test.ts` returns **nothing**. The mechanism has no
coverage at all. Recorded as a Phase 3.1 defect in its own right, separate from the Stripe work,
and as a constraint on it: the Stripe handler must not copy this shape.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| subscription.created for a non-existent organization throws NotFoundError and creates no tier | webhook.edge-cases.test.ts:63 | webhook-security | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — the Worker has no Stripe webhook handler, so there is no code to have a test for; bootstrap-handler.node.test.ts covers Clerk organization provisioning, which is the inverse direction (create the org) rather than rejecting an unknown one | worker-shaped-rewrite | Genuine and important: it asserts both the throw **and** that no `subscriptionTier` row was written for the unknown organization. The second half is the security-relevant one — a webhook naming an arbitrary organizationId must not cause a row to be created for it, which is how a forged or stale customer record would otherwise seed state | PROPOSED: worker-shaped-rewrite — net-new with the Stripe handler (task 3.1); preserve the no-row-written assertion, not just the throw |
| subscription.updated for a non-existent organization throws NotFoundError rather than crashing | webhook.edge-cases.test.ts:97 | webhook-security | none found (searched: workers/src/clerk/webhook-handler.ts callers in workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/error-handling.test.ts) — error-handling.test.ts is 19/28 tautological and covers generic response shaping, not webhook-specific unknown-tenant handling | worker-shaped-rewrite | The update-path twin of `:63`. Worth keeping distinct because create and update reach the organization lookup by different routes, and #466 is the standing reminder that sibling handlers can disagree about what they validate. Note this row asserts only the rejection; the Worker rewrite should also assert no row is written, matching `:63` | PROPOSED: worker-shaped-rewrite — net-new; strengthen over the Express original by asserting the absence of side effects, as :63 does |
| Customer metadata with no organizationId is rejected with a specific error | webhook.edge-cases.test.ts:127 | webhook-security | none found (searched: workers/src/clerk/webhook-signature.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/utils/auth.test.ts, workers/src/minimal-api-routes.test.ts) — webhook-signature.test.ts covers Svix HMAC verification only; no Worker code reads a tenant identifier out of webhook payload metadata | worker-shaped-rewrite | This is the tenant-resolution point of the whole Stripe surface: the organization is taken from `customer.metadata.organizationId`, which is the one place a webhook determines *which tenant it acts on*. Rejecting a payload that omits it is what stops an event applying to an arbitrary or default organization. The Clerk handler resolves tenancy differently (by `clerk_organization_id`), so there is no shared implementation to inherit | PROPOSED: worker-shaped-rewrite — net-new; this is the Stripe surface's tenant-resolution guard and should be treated as a tenant-isolation-adjacent gate, not just an input-validation one |
| Null organizationId in customer metadata is rejected with the same error | webhook.edge-cases.test.ts:154 | webhook-security | none found (searched: workers/src/clerk/webhook-signature.test.ts, workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/utils/auth.test.ts) — no Worker code path reads this field | worker-shaped-rewrite | The null variant of `:127`, and not redundant with it: absent-key and present-but-null take different branches through a truthiness check, and only one of them is exercised by `:127`. The equivalent Worker distinction is the empty-string case already covered for JWTs at utils/auth.test.ts:165 | PROPOSED: worker-shaped-rewrite — migrate together with :127; keep both the absent-key and null-value cases, plus an empty-string case by analogy with utils/auth.test.ts:165 |
| An already-processed event id is detected and not reprocessed | webhook.edge-cases.test.ts:183 | scheduled-job-idempotency | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/database.conformance.node.test.ts, workers/src/minimal-api-routes.test.ts) — the Worker **does** implement this shape for Clerk (`isNewClerkWebhookEvent` + `markClerkWebhookEventProcessed` over `clerk_webhook_events`, with `ON CONFLICT (id) DO NOTHING`), but a repo-wide search for those two symbols and that table name across workers/src/**/*.test.ts returns no match: the mechanism is entirely uncovered | worker-shaped-rewrite | The Worker has the right *shape* — a processed-events table keyed on the provider's event id — so this is a template rather than a blank slate. It is not an equivalent: it is a different table for a different provider, and per Finding 5 it has no test. Two rewrites are owed, one for the Stripe handler and one closing the existing Clerk gap | PROPOSED: worker-shaped-rewrite — Stripe is net-new; **additionally** file the untested Clerk idempotency path (Finding 5) as a Phase 3.1 defect, since it ships today with zero coverage |
| Three concurrent attempts to mark one event processed create exactly one row | webhook.edge-cases.test.ts:199 | scheduled-job-idempotency | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/database.conformance.node.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — catalogue-import-upsert.node.test.ts covers idempotent resume of an import job, a different mechanism; nothing covers webhook event deduplication | worker-shaped-rewrite | **The row that produced Finding 5.** Its assertion — exactly one row after three concurrent marks — is satisfied in the Worker at the SQL level by `ON CONFLICT (id) DO NOTHING`, so a naive audit would call this covered. Working it found that the guarantee does not reach the *processing*: `handleClerkWebhook` marks the event only after `processClerkWebhookEvent` returns, so concurrent deliveries both pass the `isNew` check and both perform the side effects. `ensureTrialSubscription` (clerk-persistence.ts:97-102) is itself check-then-insert with no `ON CONFLICT`, so the doubling can write two trialing subscription rows for one organization. The Express property is stronger than the Worker's actual behaviour, not merely untested there | PROPOSED: worker-shaped-rewrite — preserve "exactly one" as the assertion, and require it of the *processing*, not just the marker row. Phase 3.1 must close the equivalent Clerk gap (mark-before-process, a transaction, or a conditional insert as the claim) rather than copy the current shape into the Stripe handler |
| subscription.created writes tier, usage and audit-log rows atomically | webhook.edge-cases.test.ts:219 | webhook-security | none found (searched: workers/src/clerk/clerk-persistence.ts callers in workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/database.conformance.node.test.ts, workers/src/minimal-api-routes.test.ts) — bootstrap-handler.node.test.ts asserts organization and user provisioning rows against real SQL but covers no Stripe-driven tier/usage/audit triple | worker-shaped-rewrite | Genuine multi-table assertion: tier exists, usage exists, audit row with action `subscription_created` exists, and the tier level and `maxSkus` match. This is the row that makes the whole Stripe handler testable end-to-end. Two migration notes: Neon has no `$transaction` equivalent, so Worker atomicity needs a different construction; and the expected `maxSkus` of 50000 comes from the backend's professional-tier table, which the Worker's `LAUNCH_TIER_LIMITS` happens to agree with (index-minimal.ts:3983) — verify rather than assume when migrating | PROPOSED: worker-shaped-rewrite — net-new; the all-or-nothing property needs a Neon-shaped construction, and the atomicity must be asserted by failure injection rather than by a happy-path row count |
| A late checkout.session.completed restores the paid tier after a trial-expiry downgrade | webhook.edge-cases.test.ts:269 | webhook-security | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/org-status-integration.test.ts, workers/src/minimal-api-routes.test.ts) — subscription-validation.test.ts is 7/14 tautological and asserts tier-to-feature mapping; org-status-integration.test.ts is 16/19 tautological; neither covers out-of-order event reconciliation | worker-shaped-rewrite | The most valuable single row in the file and the one least likely to be reinvented. It encodes a real production race — the trial-expiry job downgrades an organization and locks creation, then the checkout webhook arrives late — and asserts full reconciliation: tier back to professional, status active, `trialEndDate` cleared, `isCreationLocked` false, and `maxSkus` raised to 50000. A Stripe handler written from the Stripe docs alone would not produce this, because the interaction is with *this system's* trial job, not with Stripe | PROPOSED: worker-shaped-rewrite — net-new, and flag as required reading for whoever implements 3.1; the unlock and trialEndDate-clearing halves are the parts most likely to be missed |
| subscription.updated arriving before subscription.created bootstraps the records | webhook.edge-cases.test.ts:362 | webhook-security | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker code handles Stripe event ordering | worker-shaped-rewrite | The second out-of-order case: `handleSubscriptionUpdated` must create rather than fail when no tier or usage row exists yet, since Stripe does not guarantee delivery order. Asserts the tier is created at the right level and usage is bootstrapped with the right cap. Pairs with `:97`, which asserts the same handler *rejects* an unknown organization — together they pin the distinction between "unknown tenant" (reject) and "known tenant, unseen subscription" (bootstrap), which is exactly the kind of sibling asymmetry #466 turned on | PROPOSED: worker-shaped-rewrite — net-new; migrate together with :97 so the reject-vs-bootstrap distinction is asserted rather than left to the implementer |
| subscription.deleted for a subscription absent from the database does not throw | webhook.edge-cases.test.ts:404 | negative/error | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — error-handling.test.ts is 19/28 tautological and covers generic error envelopes | worker-shaped-rewrite | Delivery-robustness: a cancellation for a subscription this system never recorded must be a no-op, not a 500, or Stripe retries it indefinitely. The assertion is only `resolves.not.toThrow()`, which is weak — the Worker rewrite should also assert the response is 200 and that no unrelated row was modified, since a handler that swallows everything would also pass | PROPOSED: worker-shaped-rewrite — net-new; strengthen beyond `not.toThrow()` to assert a 200 and the absence of collateral writes |
| Missing tier in price metadata defaults to the free tier rather than failing | webhook.edge-cases.test.ts:445 | webhook-security | none found (searched: workers/src/__tests__/subscription-validation.test.ts, workers/src/utils/feature-gates.test.ts, workers/src/minimal-api-routes.test.ts) — `normalizeLaunchTier` (index-minimal.ts:4000) performs the analogous defaulting for tier strings read from the database, but it is not reached from any webhook path and no test covers the webhook-metadata case | worker-shaped-rewrite | A fail-safe-direction assertion worth preserving precisely: absent tier metadata yields **free**, the least-privileged tier. The dangerous inversion — defaulting to the tier the caller last had, or to professional — would be a privilege-escalation path driven by a malformed Stripe price. The Worker has the right primitive in `normalizeLaunchTier`, which also defaults conservatively, so the rewrite should route webhook tier resolution through it rather than reimplementing | PROPOSED: worker-shaped-rewrite — net-new; assert the default is specifically `free` (least privilege), and reuse normalizeLaunchTier rather than adding a second defaulting rule |
| A deleted Stripe customer is rejected with NotFoundError | webhook.edge-cases.test.ts:491 | webhook-security | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/clerk/webhook-signature.test.ts, workers/src/__tests__/error-handling.test.ts) — no Worker code retrieves a Stripe customer | worker-shaped-rewrite | A `DeletedCustomer` object carries no `metadata`, so without this branch the tenant lookup would read `undefined` and take whatever path the missing-metadata check takes. Related to `:127` but distinct in origin — here the metadata is absent because Stripe deleted the record, not because it was never set — and it is the case most likely to be missed, since the deleted-customer shape is easy to overlook against the SDK types | PROPOSED: worker-shaped-rewrite — net-new; keep as a separate case from :127, since the deleted-customer object shape is a distinct branch rather than a variant of missing metadata |
| An invoice with no linked subscription is handled without throwing | webhook.edge-cases.test.ts:520 | negative/error | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/error-handling.test.ts, workers/src/minimal-api-routes.test.ts) — the Worker has no invoice handling | worker-shaped-rewrite | One-off invoices carry `subscription: null`, and the payment-failure notification must still go out — dropping it would silently lose a dunning signal. The assertion is `resolves.not.toThrow()` only, so it does not verify the notification actually happens; the rewrite should assert the notification side effect, which is the property that matters | PROPOSED: worker-shaped-rewrite — net-new; strengthen to assert the payment-failure notification is emitted, not merely that the handler survives |
| A trial_will_end event whose trial already ended is still handled | webhook.edge-cases.test.ts:558 | negative/error | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/__tests__/org-status-integration.test.ts) — the Worker has no trial-lifecycle webhook handling | worker-shaped-rewrite | Clock-skew and delayed-delivery robustness: a past `trial_end` must not produce a crash or a negative-day computation that escapes into a message. Currently asserts only that it does not throw, and the comment at `:620` concedes the handler emits a reminder with negative days — worth deciding rather than porting | PROPOSED: worker-shaped-rewrite — net-new; decide the intended behaviour for an already-elapsed trial (suppress vs send) instead of inheriting the current not-throw semantics |
| A trial_will_end event with a null trial_end is handled | webhook.edge-cases.test.ts:591 | negative/error | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/__tests__/subscription-validation.test.ts, workers/src/minimal-api-routes.test.ts) — no Worker trial handling exists | worker-shaped-rewrite | The null variant of `:558`. The test's own comment records the current behaviour as *"defaults trial_end to 0 (results in negative days) ... Handler will still send email"* — an acknowledged rough edge rather than a designed outcome. Migrate the input case, not the outcome | PROPOSED: worker-shaped-rewrite — net-new; carry the input case but re-decide the outcome, which the Express comment flags as unintended |
| Concurrent created and updated events for one subscription both settle without corruption | webhook.edge-cases.test.ts:627 | scheduled-job-idempotency | none found (searched: workers/src/clerk/bootstrap-handler.node.test.ts, workers/src/database.conformance.node.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/minimal-api-routes.test.ts) — nothing in the Worker suite exercises concurrent webhook delivery, and per Finding 5 the Clerk handler would not survive it | worker-shaped-rewrite | The property is right and the assertion is weak: `succeeded.length` greater than 0 plus "a tier row exists" is satisfied by outcomes that include a duplicate tier row, which is exactly the corruption Finding 5 describes in the Clerk path. Migrate the scenario, tighten the assertion to **exactly one** tier row for the subscription, and verify by mutation. This row and `:199` together are the source for the Stripe half of the idempotency gate | PROPOSED: worker-shaped-rewrite — net-new; assert exactly one tier row rather than at-least-one-success, since the current assertion passes on the duplicate-row outcome it is meant to exclude |

**Nothing is retired from this file.** All sixteen rows carry forward, which is unique in Part 4 so
far and reflects that the file drives real handlers through real database writes. The practical
output is a specification for the net-new Stripe handler at task 3.1 — tenant resolution from
customer metadata, least-privilege tier defaulting, both out-of-order reconciliation cases, the
deleted-customer branch, and exactly-once processing — plus one defect against existing shipped
code: the Clerk webhook's check-then-act idempotency (Finding 5), which has no test today and must
not be the template the Stripe handler is copied from.

---

### services/csv-injection.test.ts

Part of the backend's `penetration` gate, and the tightest file in Part 4: twelve tests, no mocks, no
database, driving `validateProductRowStrictly` from `csv-parser.service` directly. Every one is
genuine. The rule under test is the standard OWASP mitigation — a cell whose first character is
`=`, `+`, `-` or `@` (after stripping a leading tab or carriage return) is prefixed with an
apostrophe, so a spreadsheet renders it as text instead of evaluating it.

**The Worker does not implement this.** Checked directly: a search across all of `workers/src` for
any formula-escaping construct returns nothing, and the Worker's own parser handles the same three
fields the backend sanitizes —

```
workers/src/upload/catalogue-parser.ts:93-95
  const sku     = (row[columnIndexes.sku] || '').trim();
  const name    = (row[columnIndexes.name] || '').trim();
  const barcode = (row[columnIndexes.barcode] || '').trim();
```

`.trim()` is the whole of it. The values go on to `products` and `master_catalogue_entries` raw.

**Framed precisely, because the distinction matters for a deletion gate.** This is not a live
exploit in the Worker: the Worker has no CSV *export* route, and its only download path serves a
JSON error report (`index-minimal.ts:3843`). The finding is that **cutting over to the Worker
removes a control that exists today.** Sanitization belongs at ingestion exactly because the export
that weaponizes the payload need not live in the same service — the Express product-export path and
the frontend both read these columns, and a stored `=HYPERLINK("http://evil.com","Click here")` in a
product name is inert in the database and dangerous the moment anything writes it to a spreadsheet.
Retiring these rows on "no Worker export route exists" would be reasoning from the absence of one
half of a two-part vulnerability.

**Twelve rows for one rule.** Tests `:74` through `:107` are payload variations on the same
leading-character check — command strings, DDE, hyperlinks, extended characters — rather than
distinct behaviours. They are recorded individually because the manifest's contract is one row per
test, but the Evidence cells say which are variations, so Phase 3.2 reproduces one parametrised gate
with a representative corpus rather than twelve separate tests.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Leading `=`, `+`, `-`, `@`, tab-`=` and CR-`=` are each escaped with an apostrophe | csv-injection.test.ts:43 | penetration | none found (searched: workers/src/upload/csv-parser.test.ts, workers/src/upload/catalogue-parser.test.ts, workers/src/upload/expiry-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — csv-parser.test.ts and catalogue-parser.test.ts cover delimiter/quote handling and header-alias resolution; no Worker test submits a formula-leading cell, because no Worker code escapes one | worker-shaped-rewrite | **The core rule, and the row the other eleven are variations of.** An `it.each` over all six dangerous prefixes, including the tab- and CR-prefixed forms that defeat a naive `startsWith('=')` check. The Worker's catalogue-parser.ts:93-95 applies `.trim()` to sku, name and barcode and nothing else, so all six payloads persist verbatim. Note `.trim()` actually *strips* the leading tab and CR, which turns the two evasion variants into the plain `=A1` case — so the Worker would store a bare formula where the backend stores an escaped one | PROPOSED: worker-shaped-rewrite — requires Phase 3.1 to add ingestion-time sanitization to workers/src/upload/catalogue-parser.ts; this is a control that exists in Express today and would be lost at cutover, not a new hardening request |
| Safe product values pass through unmodified | csv-injection.test.ts:56 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — the Worker preserves values unmodified because it applies no transformation at all, which satisfies this property vacuously rather than by design | worker-shaped-rewrite | The false-positive half of the gate, and the one that stops an over-broad fix: escaping must not touch `Normal Product Name`, `SKU-12345`, `987654321`, or `Product (with parentheses)`. Vacuously true in the Worker today, which is precisely why it must be migrated **alongside** the escaping rule — added later it would pass trivially and prove nothing | PROPOSED: worker-shaped-rewrite — migrate as a pair with :43; asserted against the Worker as it stands this row passes vacuously, so it is only meaningful once sanitization exists |
| Dangerous characters in non-leading positions are not modified | csv-injection.test.ts:65 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-sync-upload-retail.node.test.ts) — no Worker test distinguishes leading from interior characters, since no Worker code inspects position | worker-shaped-rewrite | The precision half: `Price: $10.99 = value`, `Total: 5+3`, `Range: 10-20` and `Email: user@example.com` must survive intact. Without this, a plausible fix that escapes any cell *containing* `=` or `@` would corrupt every email address and every price in the catalogue. This is the row that constrains the fix rather than demanding it | PROPOSED: worker-shaped-rewrite — migrate with :43; this row is what prevents an over-broad implementation from being accepted, so it is not optional |
| Only the first dangerous character is escaped | csv-injection.test.ts:74 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — no Worker code escapes any character | worker-shaped-rewrite | `=SUM(A1)=5` yields `'=SUM(A1)=5`, one apostrophe. A variation on `:43` pinning that the transform is a single prefix rather than a per-character escape, which matters because a repeated-escape implementation would mangle the stored value and be hard to reverse | PROPOSED: worker-shaped-rewrite — fold into the :43 parametrised gate as an idempotency-of-prefix case |
| A formula referencing multiple cells is escaped | csv-injection.test.ts:78 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — no Worker code inspects cell content for formulas | worker-shaped-rewrite | Payload variation on `:43` (`=A1+B2-C3`). Adds no rule beyond the leading-`=` check | PROPOSED: worker-shaped-rewrite — payload variation; include in the :43 corpus rather than as a separate test |
| Command-injection payloads are escaped across all four prefixes | csv-injection.test.ts:82 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/upload/upload-handlers.test.ts) — upload-handlers.test.ts covers R2 key construction and multipart handling, not cell content | worker-shaped-rewrite | Payload variation covering `=cmd\|"/c calc"` and the `-`, `+`, `@` forms. The value it adds over `:43` is realism — these are the payloads that actually appear in exploit corpora, and they exercise all four prefixes with a live command string rather than an arithmetic one | PROPOSED: worker-shaped-rewrite — payload variation; keep these four strings in the :43 corpus as the realistic-payload cases |
| DDE injection payloads are escaped | csv-injection.test.ts:89 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — no Worker code escapes cell content | worker-shaped-rewrite | Payload variation: `=cmd\|"/c powershell"!A1` and `+DDE("cmd";"/c calc";"!A0")`. DDE is the highest-severity form of this class — it reaches process execution rather than data disclosure — so it is worth keeping explicitly in the corpus even though it exercises no new rule | PROPOSED: worker-shaped-rewrite — payload variation; retain explicitly in the :43 corpus given DDE's severity |
| Hyperlink injection payloads are escaped | csv-injection.test.ts:96 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-sync-upload-retail.node.test.ts) — no Worker code escapes cell content | worker-shaped-rewrite | Payload variation: `=HYPERLINK("http://evil.com","Click here")`. The phishing/exfiltration form, and the one most likely to survive a reviewer's glance at a catalogue because it renders as innocuous link text | PROPOSED: worker-shaped-rewrite — payload variation; retain in the :43 corpus |
| Cells with extended characters are handled correctly | csv-injection.test.ts:102 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts) — no Worker test covers non-ASCII cell content in any parser | worker-shaped-rewrite | Two halves: a non-ASCII product name passes through unchanged, and `=JP_TEST` is still escaped. The genuinely useful part is that the escaping rule is byte-position-based rather than confused by multi-byte content — a real risk in a Worker, where `TextDecoder` handling differs from Node's Buffer path. Worth migrating as a Worker-specific encoding case, not just a payload variation | PROPOSED: worker-shaped-rewrite — keep as a distinct case rather than folding in; the Worker's TextDecoder-based CSV path makes multi-byte handling a real risk the backend's Buffer path does not have |
| Cells containing commas and quotes are preserved | csv-injection.test.ts:107 | penetration | workers/src/upload/csv-parser.test.ts — covers quoted-field and embedded-delimiter parsing for the Worker's CSV reader; workers/src/upload/catalogue-parser.test.ts — covers header-alias resolution and row extraction over the same parser | worker-equivalent-exists | This row is about CSV *parsing* rather than injection: `Product, Description` and `Product "Name"` must survive the reader intact. That property is the Worker's own csv-parser's job and is covered by its tests. It sits in this file only because the helper routes through `validateProductRowStrictly`; the sanitization rule is not what it exercises | PROPOSED: worker-equivalent-exists — delimiter and quote preservation is a parser property covered by workers/src/upload/csv-parser.test.ts and catalogue-parser.test.ts; unrelated to the injection rule despite living in this file |
| A batch of ten injection payloads is each escaped and otherwise preserved | csv-injection.test.ts:114 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/upload/csv-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/__tests__/catalogue-import-loadtest.node.test.ts) — catalogue-import-loadtest.node.test.ts exercises batch import volume against real SQL but asserts nothing about cell content | worker-shaped-rewrite | The consolidated corpus — all ten payloads asserted both to start with an apostrophe **and** to equal the exact expected string. The second assertion is what makes it more than a smoke test: it catches an implementation that escapes but also mutates the payload. This is the row the Phase 3.2 parametrised gate should be built from, with the other payload rows folded in | PROPOSED: worker-shaped-rewrite — designate this the source for the parametrised gate; preserve both assertions (prefix present AND value otherwise unchanged) |
| Sanitization is applied to the sku, name and barcode fields together | csv-injection.test.ts:137 | penetration | none found (searched: workers/src/upload/catalogue-parser.test.ts, workers/src/__tests__/catalogue-import-upsert.node.test.ts, workers/src/__tests__/catalogue-sync-upload-retail.node.test.ts) — catalogue-import-upsert.node.test.ts asserts real-SQL upsert behaviour for these three columns but with benign values, so it would pass unchanged against unsanitized input | worker-shaped-rewrite | **The coverage row, and the most important one after `:43`.** A single row carrying a different formula in each of the three fields, asserting all three are escaped. Field-level coverage is where this class of fix habitually rots: sanitizing `name` and forgetting `sku` is the exact sibling-asymmetry shape that produced #466. The Worker touches precisely these three fields at catalogue-parser.ts:93-95, so the mapping is one-to-one and the rewrite should assert all three in one row for the same reason | PROPOSED: worker-shaped-rewrite — migrate as a single multi-field assertion, not three; per #466, per-field asymmetry is the failure mode this row exists to catch |

**What this file establishes for the deletion gate.** Express sanitizes spreadsheet-formula payloads
at ingestion for `sku`, `name` and `barcode`; the Worker does not, for the same three fields, on the
same kind of upload. Eleven rows carry forward as one parametrised gate plus the multi-field
coverage row, and one (`:107`) resolves to existing Worker parser coverage. The Phase 3.1 dependency
should be recorded as **restoring a control**, not adding one: this is the only place in Part 4 so
far where a working, tested Express defence has no Worker counterpart at all, and where the cutover
itself is the regression.

---

## A note that governs every `contract/` row below

The five files in `backend/src/tests/contract/` are 167 lines in total and are the thinnest in Part
4. Before any of their rows can be read, one fact about how they run:

```
backend/src/tests/setup-env.ts:8
  process.env.TEST_AUTH_BYPASS = process.env.TEST_AUTH_BYPASS || 'true';
```

That file is the suite-wide setup. `TEST_AUTH_BYPASS` therefore defaults to `'true'` for **every**
backend test unless a test overrides it, and `auth.middleware.ts:129` short-circuits to a synthetic
`default-org` Manager whenever it is set under `NODE_ENV=test`. So a contract test that issues an
unauthenticated request and asserts `200` is asserting **route shape only** — it cannot and does not
constrain authorization. `contract/auth.test.ts` is the sole file here that flips the flag
deliberately, which is why it is the only one with an authorization row.

**Finding 7 — the bypass helper and the bypass guard disagree.** Adjacent to the above, and worth
recording because it is the shape that produced #462 and #466:

| Location | Predicate |
|---|---|
| `middleware/auth.middleware.ts:129` | `NODE_ENV === 'test'` **and** `TEST_AUTH_BYPASS === 'true'` |
| `middleware/clerk-auth.middleware.ts:79` | `NODE_ENV === 'test'` **and** `TEST_AUTH_BYPASS === 'true'` |
| `utils/auth-bypass.ts:11` (`isTestEnvironment`) | `NODE_ENV === 'test'` **or** `TEST_AUTH_BYPASS === 'true'` |

Both middlewares use a conjunction; the helper that shares their vocabulary uses a disjunction. The
helper backs `getOrganizationId(organizationId?)` (`auth-bypass.ts:18-28`), which returns the literal
`'default-org'` rather than throwing whenever no organization id is supplied *and* the predicate
holds. Eight services take their entire tenant scope from it — `product.service.ts:101`,
`inventory.service.ts:83`, `csv-parser.service.ts:226`, `store-area.service.ts:29`,
`credit-claim.service.ts:132`, `markdown-config.service.ts:62`, `supplier-credit.service.ts:174`,
and `service-provider.ts:46` — and `ServiceProvider.withClients(prisma)` (`:64`) reaches it with
`organizationId` undefined by design.

Severity is bounded and stated honestly: reaching it requires `TEST_AUTH_BYPASS=true` under a
non-`test` `NODE_ENV`, which is a deployment mistake rather than a request an attacker can make, and
Express is not the deployed system. It is recorded because it is a silent cross-tenant *fallback*
where the two sibling guards fail closed, and because it means **no row in this file may cite a
service constructor as evidence that tenant scoping is enforced** unless the test passes an explicit
`organizationId`. The Worker has no counterpart: `index-minimal.ts:3352` is the sole assignment of
`organizationId` and it reads the verified JWT, with no fallback branch.

---

### contract/auth.test.ts

Two tests against `POST /auth/logout`, and the only contract file that manipulates
`TEST_AUTH_BYPASS` rather than inheriting it. The second test is genuine: it sets the flag to
`'false'` and asserts an unauthenticated request is rejected.

The Worker has no logout route. Its route table (`index-minimal.ts:240-300`) contains
`/api/auth/login` and `/api/auth/register` and no logout; sessions are Clerk JWTs verified per
request, so there is no server-side session to destroy and sign-out is a client-side Clerk call.
The *property* in the second test — an unauthenticated request to an authenticated route is
rejected — is real and does have Worker coverage, just not on this route.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Logout returns 200 with a fixed message when the test bypass is enabled | contract/auth.test.ts:11 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/utils/auth.test.ts, the `MINIMAL_ROUTES` table at workers/src/index-minimal.ts:240-300) — no `/auth/logout` or `/api/auth/logout` route exists in the Worker | retire | The assertion is `{ message: 'Logged out successfully' }` from a handler that does nothing but return it — there is no session store, token revocation, or cookie clear behind it in Express either. The route is absent by construction in the Worker: Clerk JWTs are stateless and verified per request | PROPOSED: retire — route absent by construction; the test asserts a constant string from a no-op handler |
| An authenticated route rejects a request with no Authorization header (401) | contract/auth.test.ts:20 | authorization-precedence | workers/src/minimal-api-routes.test.ts — `authenticateApiRequest` is the single entry gate for every `MINIMAL_ROUTES` handler and its rejection path returns a `Response` that short-circuits before any db call; the unauthenticated-rejection path is asserted there rather than per-route | retire | The property is real, and this is the only contract test that proves anything, because it is the only one that turns `TEST_AUTH_BYPASS` off. But it proves it about a route that does not survive the cutover. The general property is enforced in the Worker at a single choke point rather than forty-five times, so it does not need a per-route port | PROPOSED: retire — property preserved at the Worker's single `authenticateApiRequest` choke point; no per-route equivalent needed. Cited here so the `authorization-precedence` gate is not later reported as losing a source |

---

### contract/dashboard.test.ts

One test, thirteen lines: `GET /dashboard` returns 200 with `expiringSoon`, `markdownItems`, and
`recentActivity` keys present. Per the note above it runs under the default bypass, so it asserts
response shape and nothing else. `toHaveProperty` does not inspect the values.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Dashboard responds 200 with expiringSoon, markdownItems and recentActivity keys | contract/dashboard.test.ts:5 | — | workers/src/database.dashboard.pglite.node.test.ts — drives the real dashboard query against pglite and asserts `OTHER_ORG` rows are excluded from the requested organization's counts (`:110`), which subsumes the shape assertion and adds the tenant predicate this test never had | worker-equivalent-exists | The Worker route is `/api/dashboard`. The Express test is strictly weaker than the existing Worker coverage on both axes: it asserts key presence where the pglite test asserts values, and it asserts nothing about tenancy where the pglite test seeds foreign rows that would surface if the predicate were dropped | PROPOSED: retire in favour of workers/src/database.dashboard.pglite.node.test.ts — strictly weaker on both shape and tenancy |

---

### contract/inventory.test.ts

The one contract file that seeds real rows: it mocks `auth.middleware` inline (`:2-12`) rather than
relying on the env flag, upserts `default-org` and user 1, creates a product and a store area, then
asserts `POST /inventory-items` returns 201 with an `id` and `status: 'Normal'`.

The status default is the substantive part — a newly created inventory item is `Normal` rather than
inheriting an expiry-derived status at insert time.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Creating an inventory item returns 201 with an id and status "Normal" | contract/inventory.test.ts:74 | — | partial — workers/src/minimal-api-routes.test.ts covers `POST /api/inventory-items` against a mocked `db.sql` and asserts the created-row shape, but no Worker test asserts the `status` default | worker-shaped-rewrite | Two properties are tangled and separate cleanly. The 201-with-id half is covered. The `status: 'Normal'` default is a domain rule the Worker computes independently, and no Worker test pins it — a change to the default would be caught in Express today and by nothing after deletion. Small, and exactly the kind of row that is lost silently if the file is retired wholesale | PROPOSED: worker-shaped-rewrite — port only the `status: 'Normal'` default assertion; the 201/id half is already covered |

---

### contract/organization-invites-clerk-only.test.ts

One test, thirteen lines, asserting `POST /api/organizations/invites` returns **404** — a
negative-space test recording that invites are Clerk-managed and the app exposes no custom invite
endpoint.

This is the one row in Part 4 where "the Worker has no such route" is a *correct* retirement
argument rather than a mistake, and the distinction is worth stating because it is the inverse of
the csv-injection call above. There, absence of a route removed one half of a two-part
vulnerability and the defence still had to be restored. Here the absence **is** the property: the
test asserts that nothing is there, so a Worker in which nothing is there satisfies it directly.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| No custom organization-invite endpoint is exposed (404) | contract/organization-invites-clerk-only.test.ts:5 | authorization-precedence | satisfied by construction — the `MINIMAL_ROUTES` table (`index-minimal.ts:240-300`) contains no `/api/organizations/invites` entry, and unmatched `/api/` paths fall through to `errorResponse('Not Found', 404, env)` at `index-minimal.ts:431`; the route table itself is enumerated and asserted in workers/src/minimal-api-routes.test.ts:179 | worker-equivalent-exists | The property is "this route does not exist", and it is enforced by the Worker's closed route table rather than by a handler. A ported test would assert 404 against a route list that has no entry — true, but true of every string. The durable guard is the enumerated route table | PROPOSED: retire — property holds by construction against a closed, asserted route table. Note the *reason* differs from csv-injection, where route absence was explicitly not a sufficient argument |

---

### contract/reports.test.ts

Two tests, both under the default bypass, both asserting only a status code and a coarse body type.
Both comments in the file (`:9`, `:19`) record that the expectations were *relaxed to match the
implementation* — `monthly-markdown` returns JSON rather than the PDF the route name implies, and
`usage` returns a bare array rather than the documented `{ usage_data }` envelope. Tests amended to
match behaviour rather than the other way round are worth flagging in a migration manifest, because
they carry no design intent forward.

Neither route exists in the Worker. `/api/reports/*` there is a different, larger surface —
`daily-usage`, `expiry`, `expiry-details`, `expiry-entries`, `expiry-overall`, `items-by-date`,
`items-by-user`, `loss-by-department`, `loss-by-sku`, `sell-through`, `store-walk-audit` — with no
`monthly-markdown` and no bare `usage`. `/api/reports/daily-usage` is a per-day activity report, not
this route's payload; the closest counterpart to `/reports/usage` is `/api/organization/usage`,
which returns the quota envelope and is the endpoint Finding 4 point 3 shows reporting
`total_skus: 0` for every organization.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Monthly markdown report responds 200 with a JSON content-type | contract/reports.test.ts:5 | — | none found (searched: workers/src/minimal-api-routes.test.ts, the `MINIMAL_ROUTES` table at workers/src/index-minimal.ts:240-300) — no `monthly-markdown` route exists in the Worker; the eleven `/api/reports/*` routes are a different report set | retire | The assertion is `status === 200` plus `content-type` contains `application/json`, and the in-file comment at `:9` records it was weakened from a PDF expectation to match what the handler happened to return. It constrains no report content. If the monthly markdown report is a product requirement, that is a Phase 3 feature question rather than a test-migration one | PROPOSED: retire — route absent from the Worker and the assertion carries no content constraint. Flagged separately for product: confirm `monthly-markdown` is genuinely not needed rather than silently dropped |
| Usage report responds 200 with a bare array body | contract/reports.test.ts:14 | — | partial / different route — `GET /api/organization/usage` (`handleGetOrganizationUsage`, index-minimal.ts:3084) returns the quota envelope and `GET /api/reports/daily-usage` returns per-day activity; neither returns this route's payload, and no Worker test asserts either body shape | retire | `Array.isArray(response.body)` is the entire assertion, and the comment at `:19` records that the expected `{ usage_data }` envelope was dropped to match the implementation. Retiring it costs nothing. Recorded because the nearest Worker route, `/api/organization/usage`, is the one Finding 4 shows serving a permanently-zero `total_skus` — so the surface this test gestures at does have a real defect, just not one this test would catch | PROPOSED: retire — assertion is a type check on a body the test does not otherwise constrain; the related `/api/organization/usage` defect is already tracked in Phase 3.1 (Finding 4) |

---

### controllers/credit-claim.controller.test.ts

Six tests. The pattern is clean and worth noting because the rest of `controllers/` uses it too:
the test builds a throwaway Express app (`:7-33`), injects a `Partial<CreditClaimService>` of
`vi.fn()`s through the controller's factory parameter, and stubs auth with a two-line middleware
that sets `req.organizationId = 'org-123'` and `req.userId = 7` (`:12-16`). Nothing touches a
database. These are controller-layer tests in the strict sense — they constrain request parsing,
argument marshalling, status codes, and error mapping, and deliberately constrain nothing below.

That makes them cheap to evaluate: the question for each row is only whether the Worker's handler
does the same marshalling, and whether anything asserts it.

**Finding 6 — the Worker implements two of the eight credit-claim endpoints the frontend calls.**
This came out of working the `buildClaim` and `addPhoto` rows, and it is not a test gap. The
Worker's route table (`index-minimal.ts:240-300`, plus the five `RE_SUPPLIER_CREDIT_*` patterns at
`:186-190`) contains exactly two claim routes:

| Frontend call (`frontend/src/services/supplierCreditService.ts`) | Worker route |
|---|---|
| `GET /supplier-credits/claims?view=…` (`:140`) | `handleListCreditClaims` (`index-minimal.ts:2198`) |
| `GET /supplier-credits/recovery-report` | `handleGetRecoveryReport` |
| `GET /supplier-credits/claims/:id` (`:144`) | **none** |
| `POST /supplier-credits/claims` (`:150`) | **none** |
| `POST /supplier-credits/claims/:id/send` (`:153`) | **none** |
| `POST /supplier-credits/claims/:id/follow-up` (`:156`) | **none** |
| `POST /supplier-credits/claims/:id/outcome` (`:165`) | **none** |
| `POST /supplier-credits/claims/:id/lines/:lineId/photos` (`:183`) | **none** |

Checked three ways, not inferred: no regex in `:186-190` matches a path under
`/api/supplier-credits/claims/`; a search of `workers/src` for `INSERT INTO credit_claim` returns
only `database.credit-claim.conformance.node.test.ts` (test seed data), so no production Worker code
creates a claim, a claim line, a photo, or an event; and unmatched `/api/` paths fall through to
`errorResponse('Not Found', 404, env)` at `index-minimal.ts:431`. The `credit_claim_photos` table is
read at `database.ts:2075` and written nowhere.

The frontend resolves every call against a single origin (`frontend/src/lib/api.service.ts:1-4`),
so the supplier-credit workflow is read-only against whichever backend that origin points at when it
is the Worker: claims can be listed and the recovery report rendered, but not created, sent, chased,
or resolved. **Whether that is a live outage today or a latent one depends on which origin
production is configured with, which this audit does not establish and should not assert.** Either
way it is a Phase 4 deletion blocker of a kind Findings 1–5 are not: not a defect in what the Worker
does, but six routes that do not exist. Recorded in `tasks.md` under 3.1.

The rows below are therefore split. Marshalling behaviour on the two routes that exist is assessed
normally. The four rows whose route has no Worker counterpart are `worker-shaped-rewrite` blocked on
the route existing — the same treatment the `feature-limits` rows get for Finding 4, and for the
same reason: there is nothing to test until it is built.

One drift risk found alongside, small and cheap to fix. The Worker re-declares the claim status
partition as local literals:

```
workers/src/index-minimal.ts:192-193
  const OPEN_CREDIT_CLAIM_STATUSES = ['DRAFT', 'SENDING', 'SENT', 'ACKNOWLEDGED'];
  const SETTLED_CREDIT_CLAIM_STATUSES = ['CREDITED', 'PARTIALLY_CREDITED', 'REJECTED', 'CANCELLED'];
```

`shared/domain/credit-claim.ts:25` already exports `SETTLED_CLAIM_STATUSES` with identical members,
and that module's header states it exists precisely so "both backends must agree on the claim status
vocabulary" (golden rule 5). The two lists match today and nothing enforces that they keep matching.
There is no `OPEN_*` export in shared at all — the nearest, `CHASEABLE_CLAIM_STATUSES` (`:33`), is a
*different* partition (`SENT`, `ACKNOWLEDGED` only), so a future reader reaching for it would
silently narrow the open view. This is the same shape as the store-walk-audit rollup (#350) and has
the same remedy: hoist the partition into `shared/domain/credit-claim.ts` and pin it with a
conformance test.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| `?view=open` maps to the four open statuses | credit-claim.controller.test.ts:36 | — | workers/src/minimal-api-routes.test.ts:649 — drives `GET /api/supplier-credits/claims?view=open` and asserts `listCreditClaims` was called with `('org_123', ['DRAFT','SENDING','SENT','ACKNOWLEDGED'])`, so it pins the status list *and* the organization scoping the Express test never checks | worker-equivalent-exists | A genuine, non-tautological assertion on the same mapping, and strictly stronger: the Express stub middleware hardcodes `req.organizationId` and the test never asserts it reaches the service, whereas the Worker test asserts the org id is the first argument | PROPOSED: retire in favour of workers/src/minimal-api-routes.test.ts:649 — same mapping asserted, plus the tenant argument |
| `?view=settled` maps to the four settled statuses | credit-claim.controller.test.ts:44 | — | none found (searched: workers/src/minimal-api-routes.test.ts, workers/src/database.credit-claim.conformance.node.test.ts) — minimal-api-routes.test.ts exercises only the `view=open` branch at `:639-649`; the `settled` and default branches of the ternary at index-minimal.ts:2204-2208 are unreached | worker-shaped-rewrite | The sibling branch of a mapping whose `open` half *is* covered — the asymmetry is the tell. The Worker's settled list is a local literal at index-minimal.ts:193 duplicating `SETTLED_CLAIM_STATUSES` in shared/domain/credit-claim.ts:25, so this row should be migrated as a conformance assertion against the shared export rather than a second copy of the four strings | PROPOSED: worker-shaped-rewrite — cover the `settled` branch, asserting against `SETTLED_CLAIM_STATUSES` from shared rather than re-listing literals |
| No `view` parameter lists all claims (statuses undefined) | credit-claim.controller.test.ts:57 | — | none found (searched: workers/src/minimal-api-routes.test.ts) — the default branch of index-minimal.ts:2208 passes `undefined` and is unreached by any test | worker-shaped-rewrite | The third branch of the same ternary, unreached for the same reason. Cheap to add alongside the `settled` row and it guards a real regression: a mapping that fell through to `[]` instead of `undefined` would silently return no claims rather than all of them | PROPOSED: worker-shaped-rewrite — cover the no-`view` default branch in the same test as `:44` |
| The authenticated userId is passed into `buildClaim` | credit-claim.controller.test.ts:65 | — | none — no `POST /api/supplier-credits/claims` route exists in the Worker (Finding 6) | worker-shaped-rewrite | The property is real and security-adjacent: the claim's creator is taken from the verified session rather than the request body, which the Express test asserts by checking `buildClaim` received `7` as its second argument alongside the unmodified payload. It cannot be ported because the route does not exist. When the route is built, this assertion is the one to build with it | PROPOSED: worker-shaped-rewrite — blocked on Phase 3.1 implementing `POST /api/supplier-credits/claims`. Property recorded: creator identity comes from the verified JWT, never the body |
| A photo upload with no file is rejected 400 before the service is called | credit-claim.controller.test.ts:79 | negative/error | none — no photo route exists in the Worker (Finding 6); `credit_claim_photos` is read at database.ts:2075 and written nowhere | worker-shaped-rewrite | Two halves, and the second is the valuable one: the test asserts `addPhoto` was *not* called, not merely that the status was 400. That ordering property — reject before doing work — is what the Worker's `handleUploadDirect` already gets right for the other upload path (`index-minimal.ts:3600-3611` checks key ownership before parsing formData), so there is a house pattern to build against | PROPOSED: worker-shaped-rewrite — blocked on Phase 3.1 implementing the claim-photo route. Property recorded: validate and reject before invoking the service |
| The recovery report body is returned unmodified | credit-claim.controller.test.ts:87 | — | partial — workers/src/minimal-api-routes.test.ts:633 drives `GET /api/supplier-credits/recovery-report` and asserts status 200 plus `getRecoveryReport` called with `'org_123'`, but does not assert the response body | worker-shaped-rewrite | The route, the tenant argument, and the status are covered; the body is not. The Express test asserts `res.body` deep-equals the service return, which is what catches a handler that wraps, renames, or drops a field on the way out. Narrow gap, one assertion to close | PROPOSED: worker-shaped-rewrite — add a body-equality assertion to the existing Worker coverage at minimal-api-routes.test.ts:633; do not port the file |

---

### controllers/upload.controller.test.ts

Fourteen tests across `POST /api/upload/initiate`, `/complete`, and `/direct`, using the same
injected-mock-service pattern as the credit-claim controller. Unlike that file, the routes here all
exist in the Worker — `handleUploadInitiate` (`index-minimal.ts:3435`), `handleUploadComplete`
(`:3687`), and `handleUploadDirect` (`:3550`) — so every row is a straight coverage comparison.

The Worker's existing upload coverage is real but sits one layer down. `upload/upload-handlers.test.ts`
tests the extracted helpers (`userOwnsUploadKey`, `queueCompletedCatalogueUpload`,
`processStoredUpload`, `processCompletedUploadSync`) and `upload/upload-router.test.ts` tests
dispatch and key decoding. Neither drives a handler end to end, so the request-parsing and
response-shaping behaviour these Express tests cover is largely untested in the Worker.

Two behaviour differences the migrated tests must encode rather than port:

1. **Key ownership is scoped per user, not per organization.** Express raises "Upload key does not
   belong to this organization"; the Worker checks `key.startsWith('uploads/user-${userId}/')`
   (`upload/upload-handlers.ts:52-54`) and says "…this user". That is *tighter*, not weaker — a user
   belongs to one organization, so user scoping implies org scoping — and the trailing slash
   correctly prevents `uploads/user-70/` matching user 7. Migrated tests must assert the Worker's
   predicate, not the Express message.
2. **The Worker normalises `importType` on initiate.** `index-minimal.ts:3486-3488` builds the
   direct-upload URL with `importType=` set to `expiry-list` if the body said so and
   `product-catalog` otherwise — an unknown value is silently rewritten rather than passed through
   or rejected. Express forwards whatever it was given. No test covers the normalisation.

| Behaviour | Test file:line | Named gate | Existing Worker equivalent? | Target | Evidence | Decision |
|---|---|---|---|---|---|---|
| Initiate returns the upload strategy chosen by the service | upload.controller.test.ts:69 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — no test drives `handleUploadInitiate` | worker-shaped-rewrite | The Worker makes this decision itself rather than delegating: `fileSize > DIRECT_UPLOAD_THRESHOLD_BYTES` selects `strategy: 'presigned'` (index-minimal.ts:3471) over `strategy: 'direct'` (`:3483`). That threshold branch is a real behaviour with no coverage at all, and it is the row that decides whether a large file gets a presigned URL or is pushed through the Worker | PROPOSED: worker-shaped-rewrite — cover both sides of the `DIRECT_UPLOAD_THRESHOLD_BYTES` branch, asserting the strategy the Worker picks rather than one a mock returned |
| Initiate forwards importType when provided | upload.controller.test.ts:87 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) | worker-shaped-rewrite | Behaviour difference 2 above. The Worker does not forward `importType` — it normalises it into the returned upload URL, mapping anything that is not `expiry-list` to `product-catalog` (index-minimal.ts:3487). Porting the Express assertion would encode the wrong contract. The rewrite should pin the normalisation, including what happens to an unrecognised value | PROPOSED: worker-shaped-rewrite — assert the Worker's normalisation (`expiry-list` preserved, everything else → `product-catalog`), not Express's pass-through |
| Initiate returns 400 for a request missing required fields | upload.controller.test.ts:112 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) | worker-shaped-rewrite | The Worker has the equivalent guard at index-minimal.ts:3453 — `!body.filename \|\| typeof body.fileSize !== 'number' \|\| !body.contentType` — with no test on any of its three disjuncts. The `typeof … !== 'number'` clause is the one worth an explicit case, since a string `fileSize` would otherwise reach the size comparison at `:3460` | PROPOSED: worker-shaped-rewrite — cover the three-disjunct 400 guard, including a non-numeric `fileSize` |
| Complete calls the completion path for a valid key | upload.controller.test.ts:120 | — | workers/src/upload/upload-router.test.ts:13 — asserts `/api/upload/complete` dispatches to `handleUploadComplete` with the request, env and db | worker-equivalent-exists | Dispatch is covered. Note the Worker test asserts routing only, with the handler mocked, which is the same scope the Express test has (its service is mocked too) — so this is a like-for-like equivalence rather than a generous one | PROPOSED: retire in favour of workers/src/upload/upload-router.test.ts:13 — equivalent scope, both mock the layer below |
| Complete forwards importType to the completion path | upload.controller.test.ts:132 | — | workers/src/upload/upload-handlers.test.ts:103 — drives `processStoredUpload` with `'expiry-list'` and proves the routing by the discriminating error text ("No expiry rows found" vs the catalogue path's "No product rows found") | worker-equivalent-exists | A genuine test that could fail, and a better one than the Express original: it proves the importType reached a different *parser*, where the Express test only proves the string was passed along as an argument | PROPOSED: retire in favour of workers/src/upload/upload-handlers.test.ts:103 — proves the dispatch by observable effect, not by argument spy |
| Complete returns the expiry-import summary payload | upload.controller.test.ts:147 | — | partial — workers/src/upload/upload-handlers.test.ts:103 asserts `summary.errors` only, for the header-only failure case; no Worker test asserts the success-path summary shape | worker-shaped-rewrite | The Express test asserts the full expiry summary envelope. The Worker covers one field of one failure case. The gap is the success payload — counts of rows imported, updated, skipped — which is what the client renders after an expiry upload | PROPOSED: worker-shaped-rewrite — assert the success-path expiry summary shape, not just the error field |
| Complete preserves the product-catalog response shape | upload.controller.test.ts:194 | — | partial — workers/src/upload/upload-handlers.test.ts:61 asserts `processStoredUpload` writes processing-summary metadata back to R2 and `:23` covers the queued path's R2 metadata, but neither asserts the HTTP response body returned to the caller | worker-shaped-rewrite | The Worker has two distinct completion paths — queued (`queueCompletedCatalogueUpload`, taken when `CATALOGUE_QUEUE_ENABLED === 'true'`) and synchronous (`processCompletedUploadSync`) — and they return different envelopes. The Express test constrains one shape; the Worker rewrite must constrain both, and pin that the client-visible fields agree across them | PROPOSED: worker-shaped-rewrite — assert the response envelope on both the queued and synchronous completion paths, including that they agree on client-visible fields |
| Complete returns 400 when `key` is missing | upload.controller.test.ts:214 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) — upload-router.test.ts:33 covers a *malformed* key (invalid percent-encoding → 400) on the status route, which is a different guard | worker-shaped-rewrite | The Worker's guard is at index-minimal.ts:3698 and is untested. Distinct from the malformed-key case already covered: absent versus undecodable. Both should hold; only one does today | PROPOSED: worker-shaped-rewrite — cover the missing-`key` 400 on complete; the malformed-key case is separately covered at upload-router.test.ts:33 |
| Complete rejects an upload key belonging to another tenant (403) | upload.controller.test.ts:220 | tenant-isolation | workers/src/upload/upload-handlers.test.ts:18 — asserts `userOwnsUploadKey('uploads/user-7/products.csv', 7)` is true and `('uploads/user-8/products.csv', 7)` is false | worker-equivalent-exists | Worth stating why this resolves `exists` when the Express test is hollow. The Express test **mocks the service to reject** and asserts only that the controller maps that error message to 403 — it proves string matching in an error handler, not key ownership. The Worker test exercises the predicate itself, with a foreign key that would pass if the check were dropped, and the predicate is tighter than the Express one (per user, not per organization). Caveat recorded rather than hidden: the Worker test covers the helper, and the *wiring* of that helper into `handleUploadComplete` (index-minimal.ts:3702) and `handleUploadDirect` (`:3602`) is asserted nowhere | PROPOSED: worker-equivalent-exists — the predicate is genuinely tested and is tighter than Express's. Follow-up recorded: no test asserts the helper is actually called by the two handlers that depend on it |
| Direct upload invokes the direct-upload path | upload.controller.test.ts:237 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts, workers/src/minimal-api-routes.test.ts) — no test drives `handleUploadDirect` | worker-shaped-rewrite | `handleUploadDirect` (index-minimal.ts:3550) is the largest untested handler in the upload surface: it re-authenticates, re-checks key ownership, parses multipart, re-validates size against the tier limit, validates content type, and branches on the queue flag. None of it is covered | PROPOSED: worker-shaped-rewrite — this handler needs coverage regardless of what the Express file did; treat the Express rows as a floor, not a spec |
| Direct upload reads importType from the multipart body | upload.controller.test.ts:261 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) | worker-shaped-rewrite | Behaviour difference the port must not flatten: the Worker reads `importType` from **either** the query string or the form field, query taking precedence (index-minimal.ts:3607), where Express reads the multipart body only. The precedence is the assertion worth writing — a conflicting pair is the case that distinguishes the two implementations | PROPOSED: worker-shaped-rewrite — assert query-over-form precedence for `importType`, not the Express body-only behaviour |
| Direct upload returns the expiry summary payload | upload.controller.test.ts:282 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) | worker-shaped-rewrite | Same gap as the `/complete` expiry-summary row, on the other route. Both call into the same processing helpers, so one rewrite can cover the shared summary shape and assert the two routes agree on it | PROPOSED: worker-shaped-rewrite — fold into the expiry-summary rewrite for `:147`, asserting `/complete` and `/direct` return the same envelope |
| Direct upload preserves the product-catalog response shape | upload.controller.test.ts:315 | — | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) | worker-shaped-rewrite | Same gap as `:194` on the other route, with the same queued/synchronous split behind it | PROPOSED: worker-shaped-rewrite — fold into the response-envelope rewrite for `:194` |
| Direct upload returns 400 when no file is attached | upload.controller.test.ts:345 | negative/error | none found (searched: workers/src/upload/upload-handlers.test.ts, workers/src/upload/upload-router.test.ts) | worker-shaped-rewrite | The Worker's guard is `!(fileValue instanceof File)` at index-minimal.ts:3610, returning "No file uploaded". Untested, and the `instanceof` form is worth an explicit case because a form field submitted as a plain string rather than a file lands on the same branch and must not be treated as an empty upload | PROPOSED: worker-shaped-rewrite — cover the missing-file 400, including a non-file form value on the `file` field |

---
